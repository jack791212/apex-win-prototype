# ApexWin 技術債 / 打磨佇列（DEBT）

**這是「打磨既有表面」的執行清單** —— 相對於 [BACKLOG.md](../BACKLOG.md)（開新功能）。
由 skill `apexwin-consolidate` 產出與消化；建造 vs 打磨的比重由 [CONTROL.md](CONTROL.md) 的 `mode` 控制。

狀態：`⬜待批准` · `🟦已批准待做` · `🏗️進行中` · `✅完成`
每張卡：嚴重度 🔴高/🟡中/⚪低 · 工作量 S/M/L · 證據(file:line) · 完成判準。

> 種子來自 2026-07-10 一次對抗性審查工作流（4 維度 × verify × synthesize），下列證據皆為 CONFIRMED 或已修正 scope。

---

## 🧱 模板化 / 元件化（componentization）

- `✅完成` 🔴 **T1 建立 `HL.ui` 元件層（keystone）** — L　·　2026-07-10：7 個 primitive 全落地於 `core/ui.js`（`gameCard`/`carousel`/`promoCard`/`segmented`/`tabs`/`kv`/`resultBlock`）。遷移：lobby/casino(卡+輪播)、arena+demo-tools(seg 別名)、arena+casino(tabs)、arena×2/bounty×2/slot/vsslot(resultBlock 6 處)、arena/bounty/app-shell/global-prize(kv)。preview 驗證：10 個 HL.ui 匯出、各 view 渲染零 error、seg 單選/tabs 重繪/resultBlock 結構皆正確。
  - **尾巴（T1-tail，⚪ 低）**：✅ 2026-07-10（consolidate 首輪自主實作）標準型 core kv 已遷移 —— cashback×4 / challenges / faucet / fair `row()` helper → `HL.ui.kv`（零視覺變化，preview 各 modal 正常）。**保留**：非標準變體（span 無 `ax-muted`、值為節點：cashback TIER 列、happyhour、fair 部分）盲改會改變 DOM，故不動；app-shell 錢包分頁 in-place toggle（非重繪型 tabs）亦保留。
  - 現況：`HL.dom` 只有 el/clear/money（`prototype/src/core/dom.js:47`）、`HL.ui` 只有 toast/modal/comingSoon（`prototype/src/core/ui.js:68`）。每個 view 各自手刻卡片/格線/tab/結算塊。
  - 先抽：`gameCard`（`lobby.js:146` vs `casino.js:69`，已 drift）、`carousel`（`lobby.js:100-132` vs `casino.js:132-158`，逐字元相同）、`resultBlock`（arena/slot/bounty/vsslot 共 6 處重刻）、`tabs`、`segmented`（`arena.js:23` vs `demo-tools.js:30`）、`kv`。
  - 先例：`HL.arenaUI.roomCard`（`arena.js:677`）已被 `lobby.js:141` 重用 → 低風險、有前例。
  - 判準：lobby.js 與 casino.js 都呼叫 `HL.ui.gameCard`/`HL.ui.carousel`，重複本體刪除；改卡片緞帶或輪播節奏只需改一處。

- `✅完成` 🟡 **T2 `HL.ui.closeAll()/closeTop()` 收掉 8 處複製的關 modal** — S　·　2026-07-10：新增 `HL.ui.closeAll/closeTop`；8 處(main×2/arena/app-shell/bounty/liveroom/slot/progress)改呼叫（原「移全部」→closeAll、「移頂層」→closeTop，語意保留）。順帶修 U1 modal keydown 洩漏：force-close 走 mask.__axClose（移 Escape 監聽+還原焦點）。preview：2 疊→closeTop→1→closeAll→0；force-close 後 Escape 無作用/無錯；導覽自動關。
  - 證據：`.ax-modal-mask` 移除片段散在 main.js:45/111、arena.js:459、app-shell.js:310、bounty.js:47、liveroom.js:92、slot.js:419、progress.js:309；且行為不一致（liveroom/progress 只移頂層、其餘移全部）。

- `✅完成` 🟡 **T3 if/else router 換成 view registry** — M　·　2026-07-10（commit b5e6d18）：`main.js` if/else 路由 → 表驅動 `VIEWS`（render/backTo/isGame），`GAME_VIEWS`/`GAME_BACK` 兩份手動同步表併入單一真相。（狀態於 2026-07-13 consolidate 稽核時補正——本卡實作後遺留 `⬜待批准` 標記過期，見底部「已完成」line 84。）
  - 證據：`main.js:63-74` 10 分支 if/else + 兩份手動同步的 `GAME_VIEWS`(main.js:37)/`GAME_BACK`(main.js:39)；動態遊戲路徑已乾淨，兩套心智模型。

- `🏗️進行中` 🟡 **T4 把 walletModal/battleForm 的資料+驗證抽成純函式** — M　·　2026-07-10：先移除死 config `PAY_METHODS`（app-shell.js，從未使用）。**尾巴**：walletModal(200 行)/createBattleForm/bountyForm 的驗證+config 抽成 `core/` 純函式(可單測)風險較高，建議當獨立小任務逐一驗證，不做盲抽。
  - 證據：`app-shell.js:104-306` 把 config 陣列(74-88)、驗證(212-213)、會員分支(124-136)、5 個 render 閉包全塞一個 scope；`arena.js:563-625`/`465-530` 同型。`PAY_METHODS`(app-shell.js:74) 是死 config。

- `✅完成` ⚪ **T5 抽 `HL.ui.sectionTitle`（h2＋右側附件）** — S　·　2026-07-18（consolidate 自主實作）：新增 `HL.ui.sectionTitle(title, {extras, cls})`（core/ui.js）——h2＋extras 原樣 append（null/false 略過＝el() 既有語意）、`cls` 收 `--sort` 修飾。**10 處全遷**：lobby 局部 helper 改薄轉接（linkable 連結保留於呼叫端）、casino `section()`＋authors/providers/`--sort` 結果牆（muted 計數+sortControl 經 extras）、arena（＋開房按鈕）、global-prize（＋demo-tag）、tournament（＋LIVE badge）、heat 熱牆（＋muted 說明）、jackpot 彈窗（純標題）。multiline 複掃無漏網（U8 教訓）；標題字串照舊過 t()＝i18n 零變化。sw.js CACHE bump v30。preview 驗證：primitive 單元測（預設/extras null 略過/cls）三過；lobby(4：含無連結變體、linkable role=link 保留)/casino(12：熱牆 H2+SPAN、9 區 H2+A、authors/providers 純 H2)/`--sort`(H2+muted+LABEL 排序)/arena(H2+BUTTON)/globe(H2+demo-tag)/tournament(H2+LIVE)/jackpot 彈窗 逐處 DOM 結構正確、零 console error。
  - 原證據：`el("div",{class:"ax-section-title"},[el("h2",…),…])` 跨 8 檔 10 處手刻——lobby.js:24（局部 helper）、casino.js:90/102/110/134（局部 `section()`＋3 處 inline、含 `--sort` 變體）、arena.js:646、global-prize.js:191、tournament.js:93（LIVE badge）、heat.js:105、jackpot.js:142。判準：10 處呼叫 `HL.ui.sectionTitle`、DOM 結構逐處零變化。✅ 全數通過。

- `✅完成` ⚪ **T6 抽 `HL.ui.progress(pct, opts)` 進度條** — S　·　2026-07-18（consolidate 自主實作）：core/ui.js 新增 `HL.ui.progress(pct, {style})`——DOM 完全複刻 `div.ax-progress > i[width:%]`、0–100 clamp 統一入 primitive（原兩份局部 helper 有、3 處 inline 無 → 收斂為皆有；現值皆比例計算不逾界＝零視覺）。5 處全遷：challenges.js/progress.js 局部 `bar()` 改薄轉接（呼叫端零改動）、global-prize.js:60/220（220 的 inline `margin:6px 0 12px` 經 opts.style 保留）、lobby.js:62。複掃 `ax-progress` 全 src 僅剩 CSS 定義+primitive 本體＝零殘留。sw.js CACHE bump v33。preview 驗證：primitive 單元測三過（預設結構 DIV.ax-progress>I、clamp -20→0%/150→100%、style opt 透傳）；實地四畫面——lobby hero 78%、globe 主進度 68% + rail 68%（margin style 原樣）、每日任務彈窗 3 條、獎金錢包 ledger 1 條——結構全正確、零 console error。原卡：`.ax-progress` 手刻 5 處——challenges.js:20 與 progress.js:14 有**逐字相同**的局部 `bar(pct)` helper（含 0–100 clamp）、global-prize.js:60/220（220 帶 inline `margin:6px 0 12px`）+ lobby.js:62 三處 inline **無 clamp**。修法＝core/ui.js 新增 `HL.ui.progress(pct, {style})`（DOM 完全複刻 `div.ax-progress > i[width:%]`、clamp 統一入 primitive），5 處遷移、兩個局部 helper 改薄轉接或刪除。inline 三處補 clamp 屬行為安全化（現值皆為比例計算、實際不逾 100，零視覺）。**不強收**其他 bar 類（ax-player__bar/ax-heat__bar/ax-base__bar/ax-casino__bar…）——DOM/class 各異屬元件特定。判準：5 處呼叫 `HL.ui.progress`、DOM 逐處零變化。同輪複查：`ax-section-title` 手刻零殘留（T5 乾淨）、modal mask 建構已集中 ui.js 單點（T2 乾淨）、空狀態訊息皆單行 `el(tag,{class:"ax-muted"})` 無抽象價值——無其他新債。

- `✅完成` ⚪ **T7 抽 `HL.ui.stat(label, node, cls)` 統計小卡** — S　·　2026-07-19（consolidate 自主實作）：core/ui.js 新增 `HL.ui.stat(label, node, cls)`（DOM 完全複刻 `div.<cls> > small.ax-muted + node`）→ **7 處局部 helper 全改薄轉接**（呼叫端零改動）：instant-crash-mines/hilo/keno/towers `stat()`、picks `slipStat()`（皆 ax-mines__stat）＋ instant-games dice/limbo 兩份 `card()`（ax-dice__card）。CSS 兩 class 依原卡**不合併**（padding 差異＝視覺變化）。順手項一併落地：instant.js `chip()` 同檔逐字相同 ×2 hoist 至模組層（betPanel/amountField 共用，6 個呼叫點不動）。手刻複掃：`ax-mines__stat|ax-dice__card` 全 src 僅剩 CSS 定義＋薄轉接＝零殘留。sw.js CACHE bump v36。preview 驗證：primitive 單元測（DIV.ax-mines__stat > SMALL.ax-muted + B、ax-dice__card 變體）過；七款遊戲實地掛載 stat 小卡結構全數正確（dice/limbo/mines/hilo/keno/towers 各 3、picks 2）；chip hoist 後 ½/2×/Max 功能正常（100→2×→200→½→100、mines amountField 同列 chips 齊）；零 console error。原卡（2026-07-18 模板化淺審計）：「label 上、值下」統計小卡局部 helper 跨 6 檔 7 處手刻——**逐字相同** `function stat(l, n) { return el("div", { class: "ax-mines__stat" }, [el("small", { class: "ax-muted", text: l }), n]); }` ×5（instant-crash-mines.js:172、instant-hilo.js:123、instant-keno.js:146、instant-picks.js:159〔名 slipStat〕、instant-towers.js:129），同型 `card()` ×2 同檔重複（instant-games.js:77/124，class 為 `ax-dice__card`）。CSS 兩 class 定義近乎相同（components.css:1083 vs 1131，僅 padding 差 var(--ax-space-2)/7px）——**CSS 不合併**（改 padding＝視覺變化），只收 JS。修法＝core/ui.js 新增 `HL.ui.stat(label, node, cls)`（DOM 完全複刻 `div.<cls> > small.ax-muted + node`，cls 沿用各呼叫端既有 class），7 處局部 helper 改薄轉接或刪除。順手項：instant.js:45/156 `chip()` 同檔逐字相同 ×2，可 hoist 至模組層（同檔零風險）。**不強收** arena.js:575 `ax-bfoot__stat`（結構倒序 b→small，不同 DOM）。判準：7 處呼叫 `HL.ui.stat`、DOM 逐處零變化。

- `✅完成` ⚪ **T8 kv 局部 helper 殘餘 3 處收束（T1-tail/T7 同族收尾）** — S　·　2026-07-19（consolidate 自主實作）：三處手刻 kv 全改薄轉接（呼叫端零改動）——liveroom → `HL.ui.kv(k,v,{valCls:cls})`（同 global-prize 前例）、raffle/live-stats → `HL.ui.stat(k, el("b",…), "<原class>")`（T7 primitive 復用）。CSS 元件特定不動。sw.js CACHE bump v38。preview 驗證：primitive 單元測三結構逐字複刻原手刻 DOM（ax-kv>span+b / ax-raffle__kv>small+b / ax-lstat__kv>small+b）；實地——raffle 彈窗 4 kv（我的抽獎券 ax-gold ✓）、live-stats 浮窗 6 kv（投注數 ✓）、liveroom 確認跟注彈窗 3 kv（風險提示 ax-red ✓，沙箱登入 gate 依 §9 暫 stub HL.auth.user）——結構全正確、零 console error。原卡（2026-07-19 模板化淺審計開卡）：`function kv(` 全 src 恰 5 筆（複驗命令 `grep -rn "function kv(" prototype/src`）——ui.js:270 primitive、global-prize.js:25 既有薄轉接 ✓、**手刻 3 處**：① liveroom.js:16 `div.ax-kv > span.ax-muted + b` 與 `HL.ui.kv` 預設輸出**逐字同構** → 改 `HL.ui.kv(k,v,{valCls:cls})` 薄轉接（同 global-prize 前例）；② raffle.js:115 `div.ax-raffle__kv > small.ax-muted + b`、③ live-stats.js:55 `div.ax-lstat__kv > small.ax-muted + b`——兩者正是 T7 `HL.ui.stat(label,node,cls)` 的結構（`div.<cls> > small.ax-muted + node`）→ 改 `HL.ui.stat(k, el("b",{class:cls||"",text:v}), "<原class>")`。三處皆 DOM 逐字節點零變化；CSS（components.css:2026/2411）元件特定不動；載序安全（ui.js line47 先於三檔）。**不強收**：arena.js:514 `row`（ax-tool-row，label 元素）、chat/challenges/game-frame `row`、heat `badge/cell`——DOM 各異屬元件特定。判準：3 處改薄轉接、`grep "el(\"div\"" 於三 helper` 零殘留、DOM 逐處零變化。

- `✅完成` ⚪ **T9 倒數/計時格式化 helper 收束（pad ×6 逐字重複，T7/T8 同族收尾）** — S　·　2026-07-19（consolidate 自主實作）：core/dom.js 新增 `HL.dom.pad/mmss/dhms`（輸出與原各處手刻**逐字相同**）——① `function pad(` 逐字複製 ×6 檔（複驗命令 `grep -rn "function pad(" prototype/src`：arena.js:18/lobby.js:13/global-prize.js:15/instant-duel.js:19/tournament.js:10/raffle.js:109）→ 改 `var pad = HL.dom.pad` 薄別名（instant-duel 分數/tournament fmt/raffle fmtDHMS 呼叫端零改動）或整個移除（lobby/global-prize 遷移後 pad 無殘餘呼叫）；② 「mm:ss」式 4 處 → `HL.dom.mmss`：arena fmtLeft 分鐘分支、global-prize.js:169/173 inline（觀眾倒數）＋ **lobby fmtMS 經 grep 證實為死碼（僅定義零呼叫）直接刪除**；③ 「d天 hh:mm:ss」（pad 補零日）逐字相同 2 處 → `HL.dom.dhms`：global-prize fmtDHMS 改薄別名、lobby hero draw 改一行。**保留原地（語意各異判非債）**：tournament fmt（ms 入參）、raffle fmtDHMS（ms 入參、d 不補零、天過 t()）、arena fmtLeft「N時MM分」分支、cashback/faucet/happyhour/reload/shop/onboarding/app-shell/progress/notify 的 d·h／時·分／相對時間變體。載序安全（dom.js 為 index.html 首個 core script，六消費檔皆在後）。sw.js CACHE bump v42。preview 驗證：primitive 單元測 11 個邊界樣本（0/59/60/3599/3600/86399/86400/90061/999999…）與舊式逐字等值；實地——lobby hero「02天 14:32:18」、globe 主倒數 dhms＋觀眾 mmss「00:46/00:30」、arena 房卡 mm:ss ×6、tournament「02:58:09」、raffle 彈窗「5天 04:03:15」（d 不補零原樣）、duel 對戰一局分數兩位補零——格式全數正確、零 console error。判準：`function pad(` 全站恰 1 筆（primitive 本體）✓、各畫面倒數輸出逐字不變 ✓。

- `✅完成` 🟡 **T10 `makeDraggable` 拖曳 helper 跨 2 檔近逐字重複（已 drift）→ 抽 `HL.dom.makeDraggable`** — S　·　2026-07-20（consolidate 淺審計 workflow 產出，adversarial verify 通過；本輪自主實作）：`grep -rn "function makeDraggable" prototype/src` = **2 筆**（core/live-stats.js:82、views/game-frame.js:191）。兩份 ~20 行指標拖曳 helper（dragging/sx/sy/ox/oy＋pointerdown 按鈕守衛＋setPointerCapture try/catch＋pointermove clamp `maxX=global.innerWidth-host.offsetWidth`、`Math.max(0,…)`/`Math.max(8,…)`＋end 掛 pointerup/pointercancel）**唯一功能差**＝live-stats.js:88 多一行 `host.style.width=r.width+"px"`（手機版 left+right 佈局鎖寬），其餘僅註解/空白差；兩檔 IIFE 皆 `(function(global){…})(window)`＝`global===window`。**已 drift**（width-lock＋註解分歧）為真債非僅重複。呼叫點：live-stats.js:125 `makeDraggable(panel,head)`、game-frame.js:133 `makeDraggable(pipHost,head)`。修法（零行為變化）：core/dom.js 新增 `HL.dom.makeDraggable(host,handle,opts)`＝game-frame 版本＋pointerdown 內 `if(opts&&opts.lockWidth)host.style.width=r.width+"px"`（用 dom.js 自身 `global`＝window）；live-stats 傳 `{lockWidth:true}` 保手機鎖寬、game-frame 預設；刪兩份 local 函式。載序安全（dom.js 為 index.html 首個 core script、兩呼叫點皆 runtime 開啟時才跑）。判準：`function makeDraggable(` 全站 0 筆（僅剩 HL.dom primitive）、PiP＋live-stats 拖曳/clamp/鎖寬逐項不變、零 console error。**✅ 落地**：dom.js 加 primitive＋export（line 122）、live-stats.js:103 傳 `{lockWidth:true}`、game-frame.js:133 預設、刪兩份 local；`grep "function makeDraggable"` 全 src 恰 1 筆（primitive）。sw.js CACHE bump **v50**。preview 驗證（本 session 自起伺服器、DOM eval）：primitive 合成測——locked（lockWidth）pointerdown 後 width 鎖為 200px、unlocked 後 width 保持未設空字串；兩態 clamp 皆 leftMin=0px/topMin=8px/leftMax=maxX(1080)/topMax=maxY(640)、pointerup 後凍結（frozenAfterEnd）；**實地** HL.liveStats.open() 面板掛載、head 拖曳 widthLockedOnDown=true＋clamp 0/8 正確；零 console error。

- `🟦已批准待做` ⚪ **T11 `rnd()` 可驗證公平 float 後援 wrapper 跨 5 instant 遊戲近逐字重複 → 抽 `HL.fair.floatOr`** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rnP 'function rnd\(\).*HL\.fair\.float' prototype/src/views` = **5 筆**（instant-duel.js:17「dice-duel」/hilo.js:18「hilo」/keno.js:18「keno」/picks.js:25「picks」/towers.js:16「towers」），body 近逐字相同、僅遊戲 key 字面不同（`(HL.fair && HL.fair.float) ? HL.fair.float(KEY) : Math.random()`）。另有**較弱守衛**同型 inline 5 處 `(HL.fair ? HL.fair.float(KEY) : Math.random())`（instant-games.js:58/108/194＝dice/limbo/plinko、instant-crash-mines.js:68/154＝crash-x/mines）＝守衛 shape 已 drift。修法（零 RNG 語意變化）：core/fair.js `HL.fair` export 加 `floatOr(game){ return (HL.fair&&HL.fair.float)?float(game):Math.random(); }`；5 個 local `rnd()` 改別名或直接 `HL.fair.floatOr("KEY")`；可順手把 5 個 cousin inline 收斂到同出口統一守衛。float() 與各遊戲 key 逐字保留＝nonce 序列不變。判準：`floatOr` 單一出口、各遊戲每局抽樣/nonce 不變、`function rnd(` 於 views 收斂。**非零功能風險提醒**：後援永不觸發（fair.js 先於 views 載入），維護性收斂為主。

- `🟦已批准待做` ⚪ **T12 `dayNum()`/`weekNum()` epoch-bucket 日期 helper 跨 9 檔重複 → 抽 `HL.dom.dayNum/weekNum`（T9 同族收尾）** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rn "function dayNum" prototype/src/core` = **6**（challenges.js:19/luckyspin.js:24/progress.js:13/reload.js:18/rewards.js:27/shop.js:21，皆 `Math.floor(now/86400000)`）；`function weekNum` = **3**（cashback.js:20/reload.js:19/shop.js:22，皆 `/604800000`）；語意逐字相同、目前無 drift。＋1 個 uncounted inline day-bucket happyhour.js:100。challenges.js:6 註解自承「沿用 tasks 的 dayNum 模式」。**T9 直接前例**（已收 pad×6/mmss/dhms 同類一行時間 helper 入 HL.dom）。修法（零視覺零行為）：dom.js 加 `dayNum(){return Math.floor(Date.now()/86400000);}`＋`weekNum(){return Math.floor(Date.now()/604800000);}`（或 `epochBucket(ms)` 加兩薄 wrapper）並掛 HL.dom：96；刪 9 處 local、呼叫點改 `HL.dom.dayNum()`（reload.js PERIODS 用 fn ref `num:HL.dom.dayNum`）；各檔 `msToReset` 的乘回式（`(dayNum()+1)*DAY-…`）留原地讀共用 index；reload.js monthNum 不重複、不動。判準：daily（challenges/tasks/luckyspin/rewards）＋weekly（cashback/reload/shop）重置邊界逐項不變。

- `🟦已批准待做` ⚪ **T13 arena `statTile()` local helper 複刻 `HL.ui.stat` 輸出（T7 grep `function stat(`／T8 grep `function kv(` 皆以名漏掉）→ 改薄轉接** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：arena.js:221-222 `statTile` 產 `div.ax-astats__tile > small.ax-muted{label} + b.<cls>{val}`＝`HL.ui.stat`（ui.js:373）的 `div.<cls> > small.ax-muted + node` **逐字同構、small-first**；被 statTiles() 呼叫 6×（arena.js:227-232、call sites 243/265＝非死碼）。`function [Tt]ile\(` 全 src 恰 1 筆（arena.js:221）＝T7/T8 依名 grep 皆 0 命中而漏。與 T8 收 raffle/live-stats 同一 primitive-bypass 類。修法（零視覺、零呼叫點改動）：`function statTile(label,val,cls){ return HL.ui.stat(label, el("b",{class:cls||"",text:val}), "ax-astats__tile"); }`；DOM byte-identical、CSS（components.css:1757-1759/2143）不動。判準：arena 統計面板（:243）＋歷史彈窗（:265）render 6 tiles 標籤/值/色 class（ax-green/red/gold）不變。

- `🟦已批准待做` ⚪ **T14 inline `ax-kv` 列繞過 `HL.ui.kv`（13 處，T1-tail/T8 依名 grep 漏）→ 遷移** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：13 處 inline `el("div",{class:"ax-kv"},[el("span",{class:"ax-muted",text:k}),el("b",{[class],text:v})])` 與 `HL.ui.kv`（ui.js:270）輸出 byte-structure-identical；T1-tail 收 `row()` helper、T8 收 `function kv(` helper，兩掃皆針對**具名 helper**、從未掃 inline el() 建構點 → 這 13 處逃過所有 kv 卡。站點：progress.js:86/100/186/189/199/200/250/251/252/312、tournament.js:92/93、meta.js:100（複驗：全檔 `ax-kv` 24 筆＝13 標準＋10 非標準排除＋1 primitive；HL.ui.kv 呼叫 17 處＝primitive 廣泛採用）。drift 已現：獎金錢包列在 challenges.js:98 走 primitive、progress.js:312 卻 inline。修法（零視覺）：13 處改 `HL.ui.kv(k,v[,{valCls}])`；4 個 b-無-class 站點（86/186/189/252）得 inert `class=""`（視覺同）。**排除 10 非標準**（span 無 ax-muted：progress.js:178/185/243、cashback.js:67、jackpot.js:133、happyhour.js:62；值為 node：progress.js:87、fair.js:138/146、happyhour.js:56）。判準：5 彈窗（領取中心/VIP/返水/本期結算/黃金之城）render node-identical、零 console error。

- `🟦已批准待做` ⚪ **T15 牌桌下注面板（chipRail/totalEl/ctrls 外殼）百家樂＋輪盤逐字手刻 → 抽 `HL.table.panel`** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rn 'class: "ax-inst__panel ax-tbl__panel"' prototype/src/views` = **2**，5-node 區塊 table-baccarat.js:196-200／table-roulette.js:148-152 byte-identical（`ax-tbl__chiprow[籌碼+chipRail] + ax-tbl__totalrow[本局總注+totalEl] + ctrls.node`）。core/table.js:99-108 的 `HL.table.betArea` 已產 chipRail/totalEl/controls 但未組裝；`HL.table={betArea,CHIPS}` 無 panel helper。修法（零視覺）：core/table.js 加 `HL.table.panel(area,ctrls)` 回傳同一棵樹（比照 `HL.instant.betPanel` instant.js:184 前例）；兩 view 5 行區塊改 `HL.table.panel(area,ctrls)`。判準：百家樂＋輪盤面板 DOM/class/label 逐字不變。

- `🟦已批准待做` ⚪ **T16 規則項目清單（`ul.ax-rules > li`）4 檔 inline 建構 → 抽 `HL.ui.rules`** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rn 'class: "ax-rules"' prototype/src/views` = **4**（chicken.js:317/global-prize.js:86/liveroom.js:42/tournament.js:16），各 `el("ul",{class:"ax-rules"},[el("li",{text:…}),…])` 於 modal body、li 子皆純 {text}、僅字串不同；`HL.ui.rules` 未定義；CSS components.css:1254-1255 共用不變。修法（零視覺）：ui.js 加一行 primitive `HL.ui.rules=function(items){return el("ul",{class:"ax-rules"},items.map(function(t){return el("li",{text:t});}));}`；4 站點改 `HL.ui.rules([…字串…])`。dev-kit/styles.css 的 ax-rules 為獨立 kit 副本、out of scope。判準：4 規則彈窗 DOM byte-identical。
  - **附註（非債／已他卡覆蓋）**：本輪 workflow 另驗出 bounty.js:283＋liveroom.js:79 押注/跟注 `ax-stake` 手刻繞過 `HL.ui.segmented` ——**已被並行 firing 開的 U17**（手刻頁籤/單選群繞過 tabs/segmented，站點含此二處）涵蓋，遷移 segmented 同時解 a11y＋模板化債，故不另開 T 卡。

- `🟦已批准待做` ⚪ **T17 死碼 JS：`endRoom()`（bounty.js 完整「賞金局結束」modal builder，零呼叫）** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rnw "endRoom" prototype/src prototype/index.html` = **1**（定義 bounty.js:38，零 call-site）；HL.views.bounty 僅暴露 {render}＝無動態派發可達。live 結束路徑改由 inline 處理（flipResultCard:99-101／finishFlip:186-188／renderMine:242 各自 render「本場已結束」）。13 行（38-50）refund＋kv 面板＋導覽。修法：刪 bounty.js:38-50（零行為/零視覺）。**breadcrumb**：其中 host-refund 結算分支（`room.host.name==="你"&&!isMember()?room.prizePool:0`）live flow 從未接線＝接上屬新功能（evolve 領域），刪死 builder 時於 DEBT/BACKLOG 留一行註記。判準：grep 0、clean load。

- `🟦已批准待做` ⚪ **T18 死碼 JS：國戰 mock provider（factions/initWarCells）＝R5 已刪 war CSS 的 JS 孿生 → 刪除（補完 R5）** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過）：`grep -rnwE "initWarCells|factions" prototype/src prototype/index.html` = **4**，全在 mock-data.js（def :196/:200＋HL.mock export :358/:359），**零 consumer**（另驗 bracket/string access `["'factions|initWarCells` exit 1、`Object.keys/values/entries(HL.mock)` 迭代 exit 1＝無任何存取路徑）。components.css:1309 tombstone 明言「國戰（.ax-war/.ax-warmap/.ax-cell/.ax-faction-*）已移除…R5 清理 2026-07-10」＝R5 刪 CSS 半、留下 JS 半。修法：刪 mock-data.js:195-204（含註解＋factions＋initWarCells）＋export 行 358-359。判準：grep 0 consumer、clean console load。

- `🟦已批准待做` ⚪ **T19 死碼 CSS 大掃除：被取代面板 + 孤兒單例選擇器（~90 行，components.css，各 0 JS/HTML 引用）** — S　·　2026-07-20（consolidate workflow 產出＋verify 通過，5 群集各自 adversarial 反向 grep = 0；R5「刪死 ax-warmap」直接前例）。**每群集皆附 LIVE 守衛，逐值反向 grep 見 workflow wf_487a53b9-0f9 journal V14/V16/V17/V18/V19**：
  - (a) 大廳 快速入口卡 `.ax-quick*`（275-320）＋ 热门对押池 `.ax-pool*`（322-395，含交錯 `.ax-odds*`/`.ax-oddsbar` 352-377）＋ @media 2063/2075。**守衛：`.ax-btn-join`（:396）LIVE（arena/global-prize 7 refs）——刪 pool 塊必止於 :395。**
  - (b) 對押 versus/sides `.ax-sides`/`.ax-side*`/`.ax-vs-mid`（577-594）＋ @media 2080-2081。**守衛：`ax-side-item`/`ax-sidebar`/`ax-vs__side`/`ax-vs__mid`(vsslot)為 LIVE distinct token，勿誤刪。**
  - (c) 對戰橫幅 `.ax-duel__board`（564-569）＋ `.ax-duelbanner*`/`.ax-dbteam*`/`.ax-dbmid`（1386-1400）＋ @media 2104。**守衛：`ax-duel__back/__top/__title/__stats` LIVE，僅 `__board` 死。**
  - (d) ai-concierge 舊欄位 chrome `.ax-ai`(wrapper)/`__head`/`__head-actions`/`__close`/`__status`（408-423，**止於 :424 `.ax-ai__hero` LIVE 前**）＋ `.ax-shell.ai-closed` 塊（865-875）＋ @media 2062。**守衛：`ax-ai__hero/__avatar/__quick/__qbtn`＋`ax-ai-fab` LIVE（已被 `.ax-float` overlay 取代，panels.js makePanel）。**
  - (e) 8 孤兒單例（各一條 rule、0 emitter）：`.ax-countdown`:632、`.ax-casino__promo`:1315、`.ax-flip-grid`:2105、`.ax-mm__count`:1745、`.ax-wallet__coin`:100、`.ax-practice__bal`:1892、`.ax-hero__pct`:248（LIVE 兄弟 `__pctline`:1003）＋`.ax-select`:1441（**通用 form primitive、可能刻意留用——刪前確認保留意圖；其餘 7 個無條件刪**）。
  - 修法（零視覺、無 JS/DOM 觸碰）：單次謹慎刪除、**由高行號往低刪避免行位移**，bump sw。判準：每刪 token 反向 grep 0 emitter、所有 GUARD token 仍 LIVE、preview smoke（casino/lobby/arena/wallet/hero/countdown）零視覺差＋零 console error。

## 📱 自適應 / responsive

- `✅完成` 🔴 **R1 補手機主導覽（最嚴重功能缺口）** — M　·　2026-07-10：header 加漢堡鈕（≤720 才顯示）→ 左側抽屜 `.ax-drawer`，由 `SIDE` 渲染 大廳/全球獎/競技場/娛樂城/更多 + DEMO，經 `HL.router.go` 導覽、點選即關、Escape 關、遮罩點擊關、active 狀態同側欄，含 safe-area。preview 驗證：桌機隱藏/側欄顯示；375px 漢堡顯示/側欄隱藏、抽屜滑入 left:0、娛樂城→casino 且關閉、零 console error。
  - 證據：≤720px 側欄（唯一主導覽）`display:none`（`components.css:2066`），header 的 `.ax-nav` 是從未被 JS 渲染的死 CSS → 手機無法切換 大廳/競技場/娛樂城/全球獎。
  - 判準：≤720px 由 `SIDE`(app-shell.js:15) 渲染固定底部 tab / 抽屜，經 `HL.router.go` 導覽，與側欄同步。

- `✅完成` 🔴 **R2 別再藏鈴鐺/返水入口** — S　·　2026-07-10：≤480 原本 `.ax-icon-btn{display:none}` 藏掉全部圖示鈕（含 🔔通知/💧返水），改為只收起品牌文字 `.ax-brand__name`（保留 A 標記）騰出空間。preview 375px：🔔+💧+漢堡皆可見、header 無水平溢出。
  - 證據：`components.css:2127` 在 ≤480px 對 `.ax-icon-btn{display:none}`，連通知鈴 🔔(app-shell.js:428) 與返水領取 💧(app-shell.js:380) 一起藏掉。

- `✅完成` 🟡 **R3 `100vh → 100dvh` + `env(safe-area-inset)`** — S　·　2026-07-10：8 處 shell/main/float/game/splash/auth 的 `100vh` → `100dvh`（standalone 保留 `100vh` fallback 行；calc 內直接 dvh），行動瀏覽器動態視口正確。
  - **R3-tail ✅ 2026-07-18（consolidate 自主實作）**：底部安全列 safe-area 落地——`--ax-bottombar-h` token 本身改為 `calc(64px + env(safe-area-inset-bottom, 0px))`（tokens.css），grid 列高 + 全部「避開底列」的 calc 消費點（.ax-float×2/.ax-pip/.ax-onb-pill 的 bottom、4 處 100dvh 扣高）**自動繼承、免逐處改**；`.ax-bottombar` 加 `padding-bottom: env(safe-area-inset-bottom, 0px)` 讓 15 個項目留在上方 64px 內容區、inset 區留列背景（不被 home indicator 蓋）。無瀏海裝置 env→0px＝與原 64px 數學等值＝零視覺回歸。token 無 JS 消費者（grep 0 筆）；drawer 既有 safe-area 不動。sw.js CACHE bump v31。preview 驗證（沙箱窗格零寬、寬度量測作廢，依 §9 小抄用 DOM eval）：env=0 基準 token=calc(64px+0px)/列高 64/padding 0＝零變化；等值 override 模擬 inset=34 → 列高 98、grid 末列 98px、項目中心仍在上方 64px 區(28px)、浮動消費點 bottom=114px(64+34+16)、還原 64；零 console error。
  - 證據：全站 100vh（components.css:16/176/879/907/1536/1852/1854…），`viewport-fit=cover` 卻零 safe-area → 瀏海機底部安全列被 home indicator 蓋。

- `✅完成` 🟡 **R4 斷點收斂 + 刪死 token** — L　·　2026-07-10（逐斷點審視）：死 token `--ax-bp-*`（CSS 變數無法用於 @media）移除 → 改 tokens.css 文件化斷點階梯（480/560/720/1024/1280）。9→7 distinct：`520→560`、`880→1024`（皆「更早收欄＝更多空間」數學安全；preview 驗證 astats@950px=3 欄、540/950 無破版）。**保留** 760（Slots Battle 盤面）/860（直播間）為元件特定刻意例外並加註（強收 canonical 會在平板過度堆疊成單欄）。
  - 證據：9 個雜亂 max-width（480/520/560/720/760/860/880/1024/1280）、相鄰重複(1280×2、720×3)；`--ax-bp-*` 是死碼(CSS var 不能用於 @media)；斷點第三份真相硬寫在 `panels.js:55`。

- `✅完成` ⚪ **R5 修被 `overflow-x:hidden` 遮蓋的水平溢出** — M　·　2026-07-10（逐元件審視）：實測結論＝**無使用者可見的水平溢出**。輪盤 `.ax-rou__board` 已有 `overflow-x:auto`（375px 內部捲動、不撐頁）、底欄 ≤720 亦 `overflow-x:auto`；`.ax-warmap`（+整組國戰 `.ax-war/.ax-cell/.ax-faction-*`）為**死碼**（零 JS）已移除。殘餘 htmlScrollWidth 微量差（~1px）來自輪播捲動容器內容，`window.scrollX` 恆 0（不可捲、被 body overflow-x:hidden 遮蓋），非版面破壞。**更正（R4 逐斷點審視補測）**：底欄原本 `overflow-x:auto` 只在 ≤720，故在 **~721–1400px** 桌機窄寬，14 項底欄(scrollWidth≈1334)會真的撐出整頁水平溢出且**可捲動**（375px 測不出來）→ 已把 `overflow-x:auto` 提到 `.ax-bottombar` 基底（全寬皆內部捲動），950px 溢出 394→0、1400px 貼右不強制捲。
  - 證據：`base.css:28` body overflow-x:hidden 遮蓋 14 項底欄、11 欄 `.ax-warmap`(2096 未減欄)、520px `.ax-rou`(2377/2387) 的真實溢出。

- `✅完成` 🟡 **R6 PiP 溢出（更正 scope 後落地）** — S　·　2026-07-18（consolidate 自主實作）：**原卡核心宣稱經 grep 複驗為假**——「全 CSS 無任何 `.ax-pip` 小屏 @media」不成立：`@media (max-width:480px){ .ax-pip{width:calc(100vw-16px); right:8px} .ax-pip--big{width:calc(100vw-16px)} }`（components.css:2040-2042）自 GameFrame 首發 commit 53ebe71 即存在 → 375px 兩型皆 359px 在視口內，「小屏開 PiP 關不掉」不成立。**真實殘餘缺口僅一條**：481–591px 視口帶 `.ax-pip--big` 固定 576px 未被該 media 覆蓋（需 592px 才裝得下），且拖曳後 clamp `maxX=innerWidth-offsetWidth`（game-frame.js:196）轉負 → left 強制 0、右側 ⤢/▾/× 被裁。修＝一行 `.ax-pip--big{width:min(576px, calc(100vw - 32px))}`：≥608px 恆為 576＝桌機零視覺；≤480 的 media 規則在檔案後方、同特異度仍勝出＝手機零變化；offsetWidth 全域 ≤ 視口 → maxX 恆 ≥0，game-frame.js 免改。sw.js CACHE bump v34。preview 驗證（真 PiP 實測 dice ＋ 合成量測；沙箱登入 gate 依 §9 暫 stub `HL.auth.user`）：1280px pip=384/big=576/right edge=1264 與改前逐值相同；520px 真 PiP big=488 全在視口、× 鈕 elementFromPoint 可點、關閉正常回原框、dragMaxX=32；375px 兩型 359px 與改前相同；零 console error。**教訓開 E5**（負向宣稱開卡前必反向複驗）。原卡全文（存證勿刪）：`.ax-pip{width:384px; right:16px}`（components.css:1910）＝400px 佔位 > 375px 視口；`.ax-pip--big{width:576px}`（:1920）更甚。拖曳 clamp `maxX = innerWidth - offsetWidth`（game-frame.js:196）在 offsetWidth>innerWidth 時為負 → left 強制 0、右側溢出被 body overflow-x:hidden 裁掉——PiP 頭部**右側的 ⤢/▾/× 鈕恰在被裁區**，小屏使用者開了 PiP 後可能無法收合/關閉（功能性缺口，非僅視覺）。全 CSS 無任何 `.ax-pip` 小屏 @media。修法（桌機零視覺）：`.ax-pip` 加 `max-width: calc(100vw - 32px)`、`.ax-pip--big` 改 `width: min(576px, calc(100vw - 32px))`（或 ≤480 @media 收 `calc(100vw - 32px)`，斷點守 R4 階梯）；桌機 100vw-32 遠大於 384/576＝計算值不變。⚠ 注意 `.ax-pip__body .ax-gframe__stage` 的 `scale(0.42)+width:238%` 係對固定 384px 調校，收窄後可視窗變窄（內容多裁少縮）——可接受，若要等比另收後續卡。判準：375px 開 PiP（含 --big）整體在視口內、×/▾ 鈕可點；桌機 getBoundingClientRect 寬度不變。同輪複查：輪盤 `min-width:520px` 已由 R5 內部捲動吸收 ✓、live-stats 浮窗 ≤480 有收窄處理（components.css:2031）✓、chicken 過馬路 ≤720 收單欄 ✓——無其他新債。

- `✅完成` 🟡 **R7 modal 家族 vh→dvh 漏網（R3 同族收尾）** — S　·　2026-07-19（consolidate 自主實作）：6 處全落地——components.css `.ax-modal` 86dvh／`.ax-notif-list` 60dvh／`.ax-bgrid` 50dvh+38dvh（各保留 vh 前宣告為舊瀏覽器 fallback，沿檔內 :16/:1849/:1851 慣例）＋ arena.js:176/266 內聯改雙宣告字串。sw.js CACHE bump v39。preview 驗證（沙箱窗格零尺寸、像素量測作廢，依 §9 用 CSSOM/DOM eval）：CSSOM 四條規則 maxHeight 全解析為 dvh（後宣告勝出、來源保留 vh fallback）、`HL.dom.el` style 字串路徑雙宣告 resolved=50dvh、`HL.ui.modal` 開闔正常、零 console error；桌機零視覺＝數學恆等（無動態 chrome 時 dvh==vh、數值未動）。原卡（2026-07-19 自適應淺審計開卡）：R3 只換了 8 處 `100vh`，**非 100 的 vh 值全數漏掉**。分類後真債＝「fixed 遮罩內被困型」與「modal 內捲動區上限」共 6 處：① `.ax-modal { max-height:86vh }`（components.css:693）——mask 為 `position:fixed; inset:0; display:grid; place-items:center; padding:16px` 且 **overflow 未設**（複驗：`grep "ax-modal-mask" styles` 區塊零 overflow、`grep "max-height|overflow" core/ui.js` 0 筆＝無內聯覆蓋）→ 行動端 URL bar 顯示時 `86vh（大視口）> 100dvh − 32px`（iPhone SE 直向 574>521、橫向 322>293）成立，modal 置中溢出遮罩、上下同時裁切且**頁面捲動無效**（fixed）＝標題列 × 鈕觸不到（Escape/點遮罩仍可關，但頂/底內容不可讀）；② 同族 modal 內捲動區 cap 用大視口 vh：`.ax-notif-list` 60vh（2272）、`.ax-bgrid` 50vh/38vh（1827/1843）、arena.js 內聯 50vh/46vh（176/266）。修法＝沿用檔內既有 fallback 慣例（components.css:16/1849/1851 的 `100vh; 100dvh` 雙宣告）：各處補 `max-height:NNdvh` 第二宣告（內聯 style 同理，後宣告勝出、舊瀏覽器忽略 dvh 用 vh）。桌機/無動態 chrome 環境 dvh==vh＝**零視覺**；行動端 modal 縮進可視區＝修復。**非債勿收**：streamer cam `min(214px,42vh)`（min 防護+頁面流）、liveroom cam 56/46vh、theater `min-height:70vh`（頁面流可捲）、confetti `102vh` keyframe（裝飾性飛出）。判準：6 處皆有 dvh 宣告、桌機 computed maxHeight 逐值不變、零 console error。

- `✅完成` 🟡 **R8 iOS 輸入聚焦強制縮放（全站輸入控件字級 <16px）** — S　·　2026-07-19（consolidate 自主實作）：base.css 新增 `@media (pointer: coarse) { input, select, textarea { font-size: max(16px, 1em) !important } }`（!important 比照檔內 :focus-visible 慣例蓋過各元件散落 font-size；viewport 刻意不設 maximum-scale＝保縮放 a11y；桌機滑鼠環境不匹配此 media＝零視覺保證）。sw.js CACHE bump v43。**E5 反向複驗**：`grep -ci "input\|textarea\|select" prototype/src/styles/base.css` = **0**（reset 只蓋 `button { font: inherit }`、input 家族零規則）；全 styles 反向 grep「input/select/textarea 規則 font-size ≥16px」= **0 筆**；有 font-size 規則者全 13–15px（`.ax-search input`/`.ax-auth__in`/`.ax-redeem .ax-input` 15px、`.ax-fair__in`/`.ax-bsearch__in`/`.ax-sort` 13px），**核心下注欄 `.ax-inst__bet/__num/__target`（五款 instant＋autobet 5 欄）與 `.ax-chat__input input`/`.ax-gset__sel` 完全無 font-size 規則**＝瀏覽器預設 13.33px；`index.html` viewport 無 `maximum-scale` → iOS Safari 聚焦任一 <16px 控件即整頁強制放大且 blur 後不縮回——行動端核心下注動線每次輸入金額就跳縮放。preview 驗證（沙箱 pointer:fine＝規則 inert，依 §9 用 CSSOM/DOM eval；登入 gate 走 demo）：CSSOM 規則解析正確（conditionText `(pointer: coarse)`、宣告 `max(16px, 1em) !important`）、`matchMedia` 沙箱 fine；dice 下注欄桌機 computed **13.3333px 不變**＝桌機零視覺；inline 模擬 coarse 宣告 → **16px**、88px 固定寬不變、還原正常；casino 搜尋欄 15px→模擬 16px；零 console error。**已知限制**：沙箱無 coarse 裝置，真機 iOS 行為依文件化規格推定（<16px 聚焦觸發縮放）。判準：coarse 裝置全輸入控件 computed ≥16px、桌機逐值不變 ✓。

- `✅完成` 🟡 **R9 💧返水下拉窄視口左溢出被裁（幣別下拉同族漏網）** — S　·　2026-07-20（consolidate 自主實作）：≤480 @media（守 R4 階梯）補一行 `.ax-rb-dropdown { position: fixed; width: auto; left: 12px; right: 12px; top: calc(var(--ax-header-h) + 8px) }`（比照 `.ax-float` 左右緣定位慣例）；桌機不匹配 media＝零觸碰；JS 免改（外部點擊關閉用 `wrap.contains(e.target)`，fixed 後 dd 仍為 wrap 的 DOM 子節點＝行為不變）。**481–520 帶實測後判免擴斷點**：vw=481（未修帶最窄處）dd left=6 仍全在視口內、vw=500 left=19——原卡「殘餘輕微左溢出」疑慮不成立，斷點維持 480。sw.js CACHE bump v48。preview 驗證（清 SW+caches；resize_window 實視口量測）：vw=389 dd 由 left=-76 → `{left:12, right:377}` 全在視口、claim 鈕左右緣 elementFromPoint 皆可點；vw=375 同過（left=12、claim 可點）；vw=1280 桌機 `position:absolute; width:250; right 貼齊鈕緣(813=813)` 逐值不變；Escape 關閉＋aria-expanded 同步（U12 迴歸）✓；零 console error。驗證中發現既有分層缺陷開 **R10**（header 下拉被浮動面板蓋住——header z=30 stacking context 封頂，非本修引入：夥伴面板開啟時 elementFromPoint 命中 `.ax-float--partner` 而非 claim 鈕，關面板即正常）。原卡（存證）：`.ax-rb-dropdown`（components.css:810）固定 `width: 250px; position: absolute; right: 0` 錨定 header 💧 鈕，鈕在窄視口位於 header 中段 → 下拉向左展開超出畫面左緣。**preview 實測**（vw=389px，手機常見寬）：`#ax-rb-btn` right=174 → 下拉 `getBoundingClientRect()` = `{left: -76, right: 174}`＝**左側 76px 被裁**（body overflow-x:hidden 且 `window.scrollX` 恆 0＝被裁內容不可及），返水金額/標籤/滿版 claim 鈕左段全在被裁區——功能性缺口非僅視覺。**對照組**：同 header 的幣別下拉 `.ax-cur-dropdown`（290px）在 ≤480 @media 已有處理（components.css:2106 `max-width: calc(100vw - 24px); right: 0; left: auto`），僅 rb 漏。**E5 複驗**：`grep -n "ax-rb-dropdown" components.css base.css` 恰 4 筆（810 本體/823 .open/832-833 內部鈕）＝**全 CSS 無任何 `.ax-rb-dropdown` 小屏 @media**；cur 的 :2106 修法（max-width 收縮）對 rb **不適用**——rb 溢出方向是「右錨點太靠左」的左溢出，收 max-width 不動 right:0 錨點無效。修法（桌機零視覺）：≤480 @media（守 R4 階梯）改 `position: fixed; left: 12px; right: 12px; width: auto; top: calc(var(--ax-header-h) + 8px)`（比照 `.ax-float` ≤720 的左右緣定位慣例 components.css:2091）；桌機不匹配 media＝零觸碰。⚠ 481–~520px 帶殘餘輕微左溢出（鈕位隨品牌字收合浮動）實作時實測再決定斷點是否用 560。判準：375px 開 💧 下拉整體在視口內、claim 鈕全寬可點、桌機 getBoundingClientRect 逐值不變。同輪複查：`.ax-cur-dropdown` ≤480 已修 ✓、`.ax-gfcur__menu`（min-width 168px、right:0 錨 GameFrame 工具列右端）向左 168px 不出界 ✓、`.ax-lstat`（280px）≤480 有收窄 ✓、`.ax-pip` R6 已修 ✓——固定寬絕對定位 popover 全集掃畢僅 rb 一處。

- `✅完成` ⚪ **R10 header 下拉被浮動面板蓋住（stacking context 封頂，z-index 架構缺陷）** — S-M　·　2026-07-20（前景實作，commit 831d338）：採候選 (b)「下拉開啟時提升 header」但以**純 CSS `:has()`** 實現（優於原卡 JS toggle 版＝零 JS/零狀態耦合）：`.ax-header:has(.ax-cur-dropdown.open), .ax-header:has(.ax-rb-dropdown.open){ z-index:56 }`——下拉開啟時 header 暫升至浮窗(55)之上，其內 z:70 下拉隨 context 浮上；`.open` 不存在時恆 30＝drawer(50) 正常覆蓋、桌機零視覺；56<modal 遮罩(60)/toast(80)＝上層迴歸安全。避開候選 (a) 常態升 56 需連動 drawer 的副作用、避開候選 (c) 動 DOM 的重接成本。sw.js CACHE v49。preview 驗證（389px＋member stub）：cur/rb 下拉 open→header 56/close→30；lstat(z55) 疊加開啟時 elementFromPoint 命中 `.ax-cur-row`（下拉在浮窗之上）＝原 bug 修復；無下拉恆 30；桌機 idle 30/open 56；零 console error。原卡（存證，2026-07-20 R9 preview 驗證中實測發現）：`.ax-header { z-index: 30 }`（components.css:34）建立 stacking context，其內的幣別下拉（:789 `z-index:70`）與 💧返水下拉（:821 `z-index:70`）實際分層被封頂在 30 —— 低於 `.ax-float` 浮動面板/`.ax-lstat` 的 55（:903/:2018，該處註解明言設計意圖「z-index 55…低於 modal 遮罩(60)」＝下拉 70 本意應在浮窗之上）。**preview 實測**（vw=389、夥伴面板開啟＋💧下拉開啟）：claim 鈕中段 `elementFromPoint` 命中 `.ax-float--partner` 內元素而非 claim 鈕；且點擊被蓋區＝點在 float 上＝觸發 `onRbDocClick` 外部點擊關閉——使用者看到下拉「閃開」而非領到返水。觸發條件：窄視口（≤720 float 改左右緣定位後與下拉水平重疊）＋任一浮窗開啟（夥伴/聊天/live-stats）＋開 header 下拉；桌機兩者水平不重疊（dd right=813 vs float left≈884）不觸發。可恢復（關浮窗即正常）故 ⚪。**修法需決策（皆有側效，實作時擇一並全面驗證分層）**：(a) header z 30→56（>float 55）——但 drawer 遮罩/抽屜 z=50（:753）會反被 header 蓋，需連動調；(b) 下拉開啟時 JS 暫時提升 header z（或 body 掛 class 切換）——零常態影響但加狀態耦合；(c) 下拉改掛 body（脫離 header context）——動 DOM 結構、外部點擊/aria 錨定要重接。判準：窄視口浮窗開啟時開 💧/幣別下拉，下拉在浮窗之上、claim/幣別列可點；桌機/drawer/modal/PiP 分層迴歸全過。**E5 佐證**：z-index 全掃 components.css——header 30、drawer 50、float/lstat 55、modal 遮罩 60、下拉 70×2、toast 80、PiP 250、fullscreen 300、jpwin 4000；`grep -n "z-index" prototype/src/styles/base.css` 無 header 相關提升；分層錯序為 stacking context 語意（子 z 不能穿透父 context）非數值筆誤。

## 🎨 UI/UX 一致性 & a11y

- `✅完成` 🔴 **U1 全域 `:focus-visible` + modal role/Escape/focus-trap + toast aria-live** — M　·　2026-07-10：base.css 全域 focus-visible 環；ui.js modal role=dialog/aria-modal/aria-label、Escape 關閉、焦點鎖+還原、× aria-label；toast role=status/aria-live。（preview 驗證通過）
  - 證據：全站無 `:focus-visible`（6 處裸 outline:none：components.css:479/1317/1829/1861/2311/2434）；`ui.js:29-56` modal 無 role=dialog/aria-modal/Escape/focus 管理、× 無 aria-label；`ui.js:12-26` toast 無 role=status。

- `✅完成` 🟡 **U2 金色冒牌 token 收斂** — S　·　2026-07-10：33 個 `var(--ax-gold/purple-2, #冒牌hex)` 死 fallback 全數正規化為乾淨 `var(--token)`（fallback 永不渲染→零視覺變化，preview 驗證 gold 仍渲染 #ffb524）；`--ax-gold` 成單一真相。保留 2 條刻意的 `#ca8a04→#f59e0b` 進度條漸層（`.ax-player__fill`/`.ax-base__fill`）。
  - **2026-07-13 re-audit（UI-UX 維度淺審計）**：全 prototype/ 掃 `#ca8a04/#ffd76a/#ffb524` 剩 18 處，逐一核為**皆合法、非 token 繞過**——components.css 5 處全為多段漸層藝術（`.ax-player__fill`/`.ax-base__fill`/`.ax-slot-loading__track`/`.ax-rb__track`/`.ax-game__btn.is-real` 的漸層末端 stop）；`reveal.js:88` 為 8 色粒子調色盤；`lobby.js:155` `#ffb524` 位於 bwMeta 4 色分類調色盤（對戰`#36a6ff`/賞金`#ffb524`/originals`#2fd17a`/slot`#9d80ff`）內，兄弟色無對應 token，單獨轉 var 反使調色盤不一致。**結論：U2 主體已徹底完成，無新增裸 gold 繞過債，後續勿再追。**
  - 證據：`#ca8a04`×17、`#ffd76a`×14（散在 components.css:1023-2615），真 `--ax-gold`(#ffb524) 只 3 次；68 個 var() 帶不符的 hex fallback。

- `🏗️進行中` 🟡 **U3 font-size / 顏色 / 圓角 回歸 token** — L　·　2026-07-10：(1) 110 處 `font-size:12/13/15/18/24/34px` → `var(--ax-font-*)`；(2) 新增 `--ax-font-2xs:11px`/`--ax-font-3xl:40px` 並遷移 52 處 11/40px。全零視覺變化(preview computed 不變)。**尾巴**：零星尺寸(22/20/10/9/90px)、fluid clamp 標題、圓角 76 處硬寫、`transition:all`→具體屬性 皆需逐一判斷，另收。
  - **2026-07-14（token 稽核 workflow · 5 維度×對抗性驗證）**：圓角 exact-match 全數遷移（8px→--ax-radius-sm×18、14px→--ax-radius-md×2、999px→--ax-radius-pill×5，含 shorthand，零視覺，commit d882921）；並修 pill fallback 筆誤（`var(--ax-radius-pill, 10px)`→999px, f8b0dce）。font-size 90 處硬寫經查**全數離階**（無一等於 token 值）→ 零視覺遷移空間為 0，改需設計決策是否補級距（詳見下方 U7）。
  - 證據：225 硬寫 font-size vs 138 token（62% 繞過）、505 裸色碼、76 硬寫圓角。scale 缺 display / sub-12px 級距與 fluid clamp 標題。

- `✅完成` 🟡 **U4 調亮 `--ax-text-dim`（過 WCAG AA）** — S　·　2026-07-10：`#5d6a8a` → `#7d8aae`（全站 dim 文字一次提升，過 AA 4.5:1）。preview 驗證 token 已生效。
  - 證據：`tokens.css:53` #5d6a8a 在卡面 ~2.8:1，未達 4.5:1；用於 auth/空位/preview 等處。

- `🏗️進行中` ⚪ **U5 動效時間收斂到 `--ax-dur` + 加 `prefers-reduced-motion`** — M　·　2026-07-10：已加全域 `@media (prefers-reduced-motion: reduce)` 關閉非必要動畫/轉場（a11y）。**2026-07-13：`transition:all` 反模式全站清零** —— components.css 10 處 `transition:all` → 具體屬性（各選擇器只 tween 其 hover/state 實際變動的屬性；如 `.ax-pool`→transform,border-color、`.ax-icon-btn`→border-color、`.ax-stake`→border-color,color,opacity），preview 逐一以 getComputedStyle 驗 transitionProperty 正確、零 `transition:all` 殘留、零 console error（此即 07-10 首輪遺留、跨多輪心跳未提交的孤兒，本輪正式收編提交）。**尾巴**：20+ 種裸 duration 收斂到 `--ax-dur` 留待另收。
  - 證據：單一 --ax-dur/--ax-ease vs 20+ 種裸 duration；無 reduced-motion 區塊。`.ax-pool`(319) 用 `transition:all` 反模式。
  - **2026-07-14（token 稽核 workflow）**：字面「完全等於 0.18s」的 5 處已 → `var(--ax-dur)`（ax-fade-in / axDiceBounce / axPillIn×2 / slot-loading width，零視覺，commit f8b0dce）。其餘 ~40 處為離階快/中層（.08/.1/.12/.15/.2/.25/.3/.35s），收斂鄰近值＝非零視覺，需決策（見 U7）。長動畫/JS 動態時長屬刻意保留。reduced-motion kill-switch(2656/2658) 絕不 token 化。

- `✅完成` 🟡 **U6 保留焦點/捲動位置（全量重繪的 UX 傷害）** — M　·　2026-07-10：`HL.app.refresh` 改為包一層 —— 重繪前存 `#ax-main-content` scrollTop + 焦點元素 id，重繪後還原（有 id 才 re-focus）；導覽(enterView)不套用故換頁仍歸頂。preview：casino 捲到 300 → refresh 保持 300、導覽 lobby → 歸 0。
  - 證據：`main.js` renderApp() 每次清空 #app 全量重繪 → 焦點與捲動全丟。與 T1/T3 相關，元件層/registry 落地後較好處理。

- `✅完成` 🟡 **U8 可點擊 div 鍵盤不可及 + 新 primitive 缺 ARIA 狀態** — S-M　·　2026-07-18（consolidate 自主實作）：新增 `HL.dom.pressable(node)`（role="button"+tabindex="0"+Enter/Space→click；`e.target!==node` 防護＝內部按鈕鍵盤操作冒泡不誤觸整卡）→ 原證據 9 處全遷：bottombar 工廠(×15 項)/錢包設定列/arena `roomCard` 統一出口(bounty+battle 卡，lobby 重用同享)/battleForm 選遊戲卡/jackpot 三級卡/idol 頭像/mines 25 格/towers 格。**補漏第 10 處**：`HL.ui.gameCard`（`onClick: opts.onClick` 跨多行、逃過原單行 grep）——有 onClick 才包 pressable，全站卡片（casino 83+lobby 53 張實測）一次覆蓋。`HL.ui.segmented` 補 `aria-pressed`（初始/切換/onPick 回 false 取消 三態同步，S7 遷移的 towers/plinko/chicken 自動獲益）；`HL.ui.tabs` 補 `role=tablist/tab`+`aria-selected`。CSS 全站零 `[role]`/`[tabindex]` 選擇器＝零視覺回歸保證。sw.js CACHE bump v28。preview 驗證：判準全過——Tab 聚焦 bottombar 項+Enter 開任務彈窗、內部元素 keydown 不誤觸、towers 難度 chips aria-pressed="true"、casino/arena/globe/mines/towers/錢包設定 逐點 role+tabindex 正確、segmented/tabs 單元測三態正確、零 console error。
  - 證據①：`el("div",{…onClick})` 可點擊 div 共 9 處 6 檔——app-shell.js×2（**含 bottombar item 工廠＝一處產 15 個項目**，另 mobileDrawer 項）、arena.js×3、global-prize.js×1、instant-crash-mines.js×1、instant-towers.js×1、jackpot.js×1——皆無 `role="button"`/`tabindex="0"`/keydown，鍵盤與讀屏完全不可及（U1 只覆蓋 modal/focus-visible，未及非 button 可點元素）。
  - 證據②：新 primitive `HL.ui.segmented`（ui.js:231）按鈕無 `aria-pressed`、`HL.ui.tabs`（ui.js:250）無 `role=tablist/tab`+`aria-selected`——S5/S7 遷移後全站難度/頁籤選中態對讀屏不可見。
  - 修法（零視覺）：div→補 `role="button" tabindex="0"` + Enter/Space keydown（或低風險處直接換 `<button>`＋外觀 reset）；segmented/tabs 在切換處同步 set `aria-pressed`/`aria-selected`。
  - 判準：Tab 可聚焦 bottombar 各項並以 Enter 開啟；segmented 選中鈕 `aria-pressed="true"`；零視覺回歸。✅ 全數通過。
  - **尾巴（U8-tail，✅完成 2026-07-18 consolidate 自主實作）**：`el("a", {onClick})` 無 href 假連結 ×9 鍵盤不可及——新增 `HL.dom.linkable(node)`（role="link"+tabindex="0"+**僅 Enter**→click；Space 保留原生捲動＝與 pressable 的 button 語意區分；已有 role 不覆蓋）→ 9 處全遷：bounty.js:293/304、vsslot.js:54/104/251（‹返回競技場/取消）、casino.js:92（查看全部→setFilter）、liveroom.js:179（玩法說明）、lobby.js:26（section 連結）、tournament.js:91（‹返回大廳）。multiline grep 複掃無漏網（U8 gameCard 教訓）；CSS 零 `[tabindex]`/`a[tabindex]` 選擇器＝零視覺。sw.js CACHE bump v29。preview 驗證：unit（Enter 觸發/Space 不觸發/preset role 保留）、casino 9 連結 Enter→分區換濾鏡、tournament Enter→返回大廳、bounty/vsslot 房間結束分支+liveroom 屬性正確、零 console error。

- `✅完成` ⚪ **U9 幣別下拉 `ax-cur-row` 鍵盤不可及（U8 第 11 處漏網）** — S　·　2026-07-18（consolidate 自主實作）：`app-shell.js:44` 幣別列包 `HL.dom.pressable`（一行修法，既有 primitive）；sw.js CACHE bump v32。preview 驗證：10 幣別列全 `role="button" tabindex="0"`、列上 Enter 切換幣別（TWD→USD）、內部 span Enter 不誤觸（冒泡防護生效）、零 console error；CSS 零 `[role]`/`[tabindex]` 選擇器＝零視覺（U8 已驗證）。原卡（2026-07-18 UI-UX-a11y 淺審計）：`app-shell.js:44` walletWidget 幣別切換列 `el("div",{class:"ax-cur-row",onClick})` 未包 `HL.dom.pressable`——同一 dropdown 內 line 57 錢包設定列已包、僅幣別列漏（U8 當時 multiline grep 已掃，此處 onClick 在物件第 3 行、`el("div", {` 與 onClick 間隔 class 行，落在 `[^}]*` 貪婪匹配邊界外）。鍵盤使用者可開 dropdown（pill 是 button）卻無法選幣別。修法＝一行包 pressable（既有 primitive，冒泡防護內建）+ sw bump；零視覺（CSS 零 `[role]`/`[tabindex]` 選擇器，U8 已驗證）。判準：Tab 進 dropdown 各幣別列、Enter 切換幣別。同輪複查：arena roomCard 統一出口已包 ✓、select×2 皆有 label 關聯 ✓、img 皆有 alt ✓。

- `✅完成` 🟡 **U10 核心下注 input 無 accessible name（×5）** — S　·　2026-07-18（consolidate 自主實作）：五處 input 掛中文 `aria-label`（instant.js betPanel/amountField「下注金額」、chicken「投注額」、limbo 目標「目標倍數」、fair 種子欄「客戶端種子」）。**修法較原卡更正**：原卡「創建時 t() 生效」不成立——`HL.i18n.t` 是 passthrough（永遠回 def），真翻譯在 DOM walker → 改為 **walker 擴充支援 `aria-label`**（i18n.js 三處：attributeFilter/tAttrs/querySelector），全站 aria-label 從此走「畫面中文為 key」同一機制、**切語系即時翻不待重繪**；既有 4 處 aria-label（主選單×2/modal 標題/關閉鈕）一併受益。字典補「投注額/主選單」EN+Hans（chicken 可見 `<small>` 投注額與漢堡 title 原本漏譯、順勢修復）。sw.js CACHE bump v35。preview 驗證（清 SW+caches 後）：5 處 zh-Hant aria-label 逐一正確、切 EN → Bet amount/Target multiplier/Main menu/Close 全翻、切回 zh-Hant 還原、chicken 可見標籤同步翻譯、零 console error；styles/ 零 `[aria-label]` 選擇器＝零視覺。原卡：`instant.js:40`（betPanel 主注欄＝Dice/Limbo/Plinko 全 instant 家族）、`instant.js:153`（amountField＝Crash/Mines 重用）、`chicken.js:337`（投注額）、`instant-games.js:93`（Limbo 目標倍數）、`fair.js:135`（clientSeed 改種子欄）——兄弟 `<small>` 說明文字未以 `<label>` 包裹或 for 關聯（對照組：autobet 5 欄 `instant.js:101 field()` 有 label 包裹 ✓），讀屏對「下注金額」這種核心欄位唸不出名字。**負向宣稱複驗（E5 鐵律）**：`grep -rn "aria-label" prototype/src` 全 src 僅 4 筆（app-shell.js:422/513、ui.js:69/73，皆 chrome/modal）＝上列 5 檔 0 筆；`grep -n "aria-label" core/i18n.js` 0 筆（walker 只譯 title/placeholder，aria-label 非其範疇）；`grep -rn "aria-label" prototype/src/styles` 0 筆（零 CSS attribute 選擇器＝零視覺保證）。修法＝比照既有前例 `app-shell.js:422`（`"aria-label": t("nav.menu","主選單")`）建構時附 `"aria-label": t("下注金額")` 等——語系切換走全量重繪、創建時 t() 生效無虞；不動 DOM 結構。判準：5 處 input 皆有 aria-label、切 EN 後重繪為英文、零視覺。**非債註記（勿再追）**：chat/liveroom/casino 搜尋/redeem/auth 等 input 有 placeholder（accname 規範以 placeholder 為 fallback）屬可接受；instant-crash-mines.js:29 autoIn 有 title ✓。

- `✅完成` ⚪ **U11 進度條無 progressbar 語意 + 漢堡鈕無 aria-expanded** — S　·　2026-07-19（consolidate 自主實作，commit 30ba2dd）：① `HL.ui.progress`（core/ui.js:361，T6 剛收斂的全站唯一進度條出口）純 `div>i` 無 `role="progressbar"`/`aria-valuemin/max/now`——lobby 流水進度、globe 主進度+rail、每日任務、獎金錢包 ledger 5 處消費點對讀屏完全隱形（只聽得到相鄰文字，聽不到「進度條、百分比」）；② 漢堡鈕 `ax-nav-toggle`（app-shell.js:422）有 aria-label ✓ 但無 `aria-expanded`——抽屜開闔狀態對讀屏不可知（U8 補了 pressed/selected，expanded 漏網）。**負向宣稱複驗（E5 鐵律）**：`git grep -rn -E "progressbar|aria-valuenow|aria-expanded" -- prototype/src` → **0 筆**（全站零 progressbar 語意、零 aria-expanded）；`git grep -n -E "\[role|\[aria-" -- prototype/src/styles` → **0 筆**（CSS 零 role/aria 屬性選擇器＝零視覺保證）。修法：progress() 加 `role="progressbar" aria-valuemin=0 aria-valuemax=100 aria-valuenow=<clamped>`（width 計算式不動＝像素零變化；全站全量重繪架構下 valuenow 隨重繪同步）；漢堡鈕建構時 `aria-expanded:"false"`、openDrawer/closeDrawer 各一行同步 true/false。判準：5 處進度條皆有 progressbar 三件套、漢堡開抽屜 aria-expanded="true" 關回 "false"、零視覺回歸。**非債註記**：輪播（ui.js carousel）無按鈕（拖曳+自動輪播）無可及名缺口；抽屜本體 nav aria-label/Escape/焦點管理 R1 已齊 ✓。
  - ✅ 全數落地 · 2026-07-19（consolidate 自主實作）：如上修法逐字實施（valuenow 取 Math.round、width 沿用原 clamp 式）；sw.js CACHE bump v37。preview 驗證：lobby/globe×2/任務彈窗×3/獎金錢包 ledger 進度條 role+min/max/now 正確且 width 與改前逐值相同、漢堡 openDrawer→"true"/closeDrawer→"false"、零 console error。

- `✅完成` ⚪ **U12 揭示型下拉缺 aria-expanded ＋ Escape 關閉（U11 同族收尾）** — S　·　2026-07-19（consolidate 自主實作）：三鈕建構時 `"aria-expanded":"false"`＋open/close 同步 setAttribute（app-shell 比照 setNavExpanded 加 setCurExpanded/setRbExpanded；gfcur 收斂為單一 setOpen(on) 出口，選項點擊/toggle 皆走它）；三下拉開啟時掛 document keydown Escape→close（capture、比照 onDrawerKey 慣例，關閉時移除）。sw.js CACHE bump v41。preview 驗證（沙箱登入 gate 依 §9 暫 stub HL.auth.user）：錢包 pill／💧返水 init=false→開=true/true→Escape=false/false、外部點擊路徑（setTimeout 掛 doc click 為既有設計、非同步重測）同步 false/false；dice GameFrame 💱 init→開→Escape→選 USD 關閉四態全過（label 換 USD、還原 TWD）；零 console error。原卡（2026-07-19 UI-UX-a11y 淺審計開卡）：U11 只補了漢堡鈕，全站其餘揭示型（disclosure）下拉切換鈕全漏——① 錢包幣別 pill `#ax-wallet-pill`（app-shell.js:59，open/close 於 68-70）、② 💧 返水鈕 `#ax-rb-btn`（app-shell.js:379，open/close 於 405-416）、③ 遊戲外框 💱 幣別鈕（game-frame.js:29，`menu.classList.toggle("open")` 行內）——讀屏使用者按下後不知道選單開了沒。且三個下拉皆**無 Escape 關閉**（app-shell 兩者只能點外部關、gfcur 選單連點外部關都沒有，鍵盤使用者開了只能選一項或找到原鈕再按一次）——抽屜已有 Escape 前例（app-shell.js:507 `onDrawerKey`）。**負向宣稱複驗（E5）**：`grep -rn "aria-expanded" prototype/src` 恰 2 筆（app-shell.js:422/508 皆漢堡）；`grep -rn -E 'classList\.(toggle|add)\("open"\)' prototype/src` 恰 3 筆＝揭示元件全集如上；`grep -rn -E '\[aria-|\[role' prototype/src/styles` 0 筆＝CSS 零屬性選擇器、零視覺保證。修法（零視覺）：三鈕建構時 `"aria-expanded":"false"`，open/close 各同步 setAttribute（比照 setNavExpanded）；三下拉開啟時掛 document keydown Escape→close（比照 onDrawerKey capture 慣例）。判準：三鈕開合 aria-expanded 正確翻轉、Escape 關閉三下拉、零視覺回歸、零 console error。**非債註記**：通知 🔔/簽到/錦標賽走 modal（Escape 已有）；遊戲設定 ⚙ 彈窗原生 checkbox+label 語意健全。
- `✅完成` ⚪ **U13 input/select/textarea 缺 `font: inherit` reset（全站輸入框渲染系統字體）** — S　·　2026-07-19（consolidate 自主實作）：base.css 比照 button 慣例補 `input, select, textarea { font: inherit; }`（置於 button reset 後、R8 coarse 區塊前）。**E5 實作前複驗**：base.css input 家族僅出現於 R8 coarse 區塊內＝reset 全漏成立 ✓；全 styles 掃 input 家族 font-family 規則僅 `.ax-fair__in`（刻意 monospace）——class 特異度勝 element selector，各元件自帶 font-size/family/weight 全不受 shorthand 覆蓋。sw.js CACHE bump v44。preview 驗證（沙箱視口零尺寸、頁面級量測作廢，依 §9 CSSOM/computed 相對比較＋stub auth.user 過登入 gate）：CSSOM 規則解析 `font: inherit` ✓；dice 下注欄 `.ax-inst__bet` Arial 13.33px→**Segoe UI 15px**、weight 700 保留、88px 固定寬不變，且與 ½/2×/Max 鈕（button 早有 font:inherit）**同高 40.5px 同 top 完美對齊**＝改前 input 較矮本就不齊、此為對齊修復；autobet `.ax-inst__num` 15px ✓；chat input 15px ✓；gset select 15px（checkbox 18×18 保留）✓；casino 搜尋欄 family 修正、自帶 15px 不變 ✓；fair 種子欄 monospace 13px 原樣保留 ✓；R8 coarse 疊加正常（1em=15px→max(16px,1em)=16px）✓；零 console error。**已知限制**：line-height 繼承 1.5 使無自帶規則的輸入框高 +~7px（與相鄰 button 對齊＝一致性方向）；沙箱零視口未能實測 375px 真機版面，線上驗收時留意小屏下注列。原卡判準：全站 input computed font-family = app 堆疊 ✓、無版面破壞（受影響輸入框逐一驗）✓。原卡（R8 輪開）：base.css reset 只蓋 `button { font: inherit }`（:36），input 家族全漏 → 無自帶 font-size 規則的輸入框（chat/下注欄/gset select/concierge/liveroom…）渲染瀏覽器預設 Arial 13.33px 而非 app 字體堆疊，與相鄰文字明顯 drift。⚠ 非零視覺（13.3→15px、Arial→Segoe UI＝修復一致性的刻意變化）。
- `✅完成` ⚪ **U14 對話送出鈕 `➤` 零 accessible name ×3 ＋ 收藏切換鈕缺 aria-pressed（U8–U12 同族收尾）** — S · 2026-07-19（commit 487fc8c；狀態於 2026-07-20 consolidate 稽核補正——本卡實作後遺留 🟦已批准待做 標記過期，同 T3 前例；實作全文見下方 ✅ 子項與底部「已完成」清單）：① 三個聊天送出鈕 `el("button",{class:"ax-chat__send",text:"➤"})` 完全無 title/aria-label——ai-concierge.js:68、chat.js:74、liveroom.js:185——emoji-only 按鈕對讀屏唸不出用途（同列 🔔/🌐/💧/⚙ 等 icon 鈕皆有 title＝既有慣例，僅此三處漏）；② `HL.fav.button`（core/fav.js:26，全站遊戲卡收藏鈕**單一出口**）是切換鈕（♥/♡＋is-faved class）卻無 `aria-pressed`——收藏與否對讀屏不可知（U8 補了 segmented/tabs、U11/U12 補了 expanded，pressed 出口漏此一處）。**負向宣稱複驗（E5）**：`grep -rn "ax-chat__send" prototype/src` 恰 4 筆（3 JS 建構＋1 CSS）＝三鈕建構行如上、無任何 name 屬性；`grep -c "aria-pressed" prototype/src/core/fav.js` = **0**；全站 aria-pressed 恰 3 筆皆 ui.js segmented；`grep -rnE '\[aria-|\[role' prototype/src/styles` = **0 筆**＝CSS 零屬性選擇器、零視覺保證。修法（零視覺）：三鈕補 `title:"送出"`（比照兄弟 icon 鈕慣例；字典既有「送出」→Send，walker 譯 title ✓）；fav.button 建構時 `"aria-pressed": has(id)?"true":"false"`＋onClick 內 setAttribute 同步；順勢補 dict key「我的最愛」（fav title 現況無此 key＝EN 漏譯）。判準：三鈕有 title 且切 EN 為 Send、收藏鈕 aria-pressed 隨切換翻轉、零視覺回歸。**非債註記**：PiP 收合頁籤鈕（game-frame.js:128）openPip 時 textContent="🎮 遊戲名" 有 accname ✓；chicken/instant 的 ½/2×/Max 為文字內容可唸 ✓；reveal 🫧 泡泡屬遊戲面 N 顆裝飾互動不逐顆命名。
  - `✅完成` 2026-07-19（consolidate 自主實作）：如上修法逐字實施——三鈕補 `title:"送出"`（walker 譯 title，U10 機制）、fav.button 建構 `"aria-pressed"` ＋ onClick setAttribute 同步、i18n 字典補「我的最愛」EN/Hans（fav title 原漏譯順勢修復）。sw.js CACHE bump v45。preview 驗證：收藏鈕三態（init false→click true+is-faved+♥→click false+♡）、全站 51 顆皆有合法 aria-pressed、送出鈕 ×3（chat/AI 面板＋liveroom）title=送出、切 EN→Send ×3＋fav title=My Favorites、切回 zh-Hant 視圖內元件全還原、零 console error。驗證中發現持久浮動面板語系往返不還原＝既有引擎限制（對照組：面板既有 × 鈕/內文同樣停留英文），非本改動引入 → 開 U15。

- `✅完成` 🟡 **U15 持久浮動元件切語系往返不還原（i18n 引擎缺原文還原機制）** — M　·　2026-07-19（consolidate 自主實作，採修法 a 中央修）：walker 三翻譯路徑（精確/PREFIX/SUFFIX）翻譯前把原文存 expando `node.__i18nOrig`（首存不覆蓋）、屬性翻譯存 `elm.__i18nOrigA[attr]`；`setLang` 於 stopObserver 後、refresh 前先走 `restore()` pass（TreeWalker 全 body 還原 text node ＋ `[title|placeholder|aria-label]` 元素還原屬性、清空存檔）→ 持久元件與 views 一律回 zh-Hant 原文，renderApp 尾端既有 apply() 再翻目標語。**動態內容防 stale**：observer characterData 分支先清 `__i18nOrig` 再譯（app 重寫＝新原文）、attributes 分支依 `m.attributeName` 清對應存檔；還原屬性時 `getAttribute(a) != null` 防護（app 已移除的屬性不回填）。sw.js CACHE bump v46。preview 驗證（先清 SW+caches+cache-buster 重載；沙箱登入 gate 依 §9 stub HL.auth.user）：**核心判準過**——夥伴/聊天面板＋live-stats 浮窗開啟後 en→zh-Hant 往返：送出 title ×2、你的專屬夥伴、聊天室、關閉 全數還原、零殘留英文；三語 en→zh-Hans→zh-Hant：面板正確變「你的专属伙伴」（證明還原先於翻譯生效，否則英文卡死）再全還原；characterData 動態改寫（關閉→Close→app 改「兌現」→Cash out→還原=**兌現**非舊「關閉」）；PREFIX（房主 Alice→Host Alice→還原）/SUFFIX（3 分前→3m ago→還原）分支同過；modal（Risk notice/lose it all）＋toast（Redeemed）observer 動態翻譯迴歸正常；零 console error。驗證中發現既有 i18n 覆蓋缺口開 U16。原卡（存證）：`setLang` 的還原策略＝「`HL.app.refresh()` 全量重繪回 zh-Hant 原文 → `apply()` 再翻目標語」（i18n.js:504-506），但**掛 body、建一次快取重用的持久元件不在重繪範圍**——切 EN 再切回 zh-Hant 後永遠停留英文。preview 實測重現：聊天/AI 面板的送出鈕 title、× 鈕 "Close"、內文 "Your buddy" 往返後全數不還原。受影響持久元件（建構快取判斷佐證）：panels.js:35-52（partnerEl/chatEl 快取）、game-frame.js:123-124（pipHost `if (pipHost) return`）、live-stats.js:105（`if (panel) return panel`）、streamer.js:25/122（panelEl 快取，惟 close 會 removeChild＝重開自癒）、faucet.js:111 / onboarding.js:138（pillEl）。**負向宣稱複驗（E5）**：`grep -c "__orig|origText|dataset\.i18n|restore" prototype/src/core/i18n.js` = **0**＝walker 翻譯時不保存原文、無任何還原機制；apply() 對已是英文的內容 dict lookup 失敗＝維持英文。修法候選（實作時擇一）：(a) **中央修**——walker 翻譯 text node/屬性時把原文存 `node.__i18nOrig`（WeakMap 或 expando），setLang 先走還原 pass 再 apply（一次修好全部持久 DOM、免逐元件改，但動 i18n 引擎核心需完整迴歸：modal/toast/動態 DOM/MutationObserver 路徑）；(b) 逐元件修——setLang 廣播事件，各持久元件銷毀快取重建（記開啟狀態）。判準：EN→zh-Hant 往返後 面板送出鈕 title/× 鈕/內文、PiP 工具鈕 title、live-stats 浮窗全數還原中文；切語系既有路徑（視圖/modal/toast/動態訊息）零迴歸；零 console error。⚠ 改 i18n.js 後 preview 驗證需先清 SW/caches（CLAUDE.md §4）。

- `✅完成` ⚪ **U16 live-stats 浮窗六標籤 i18n 漏譯（i18n 覆蓋）** — S　·　2026-07-20（consolidate 自主實作）：**實作前複掃發現缺口比原卡更大——live-stats 全檔零字典覆蓋**（不只六標籤：盈虧/📈 實時統計標題/🔗 分享戰績/重置統計/toast 實時統計已重置/🔒 伺服器結算資料/Demo 客端資料/本瀏覽器工作階段 全漏）→ DICT.en 一次補 **14 鍵**、zh-Hans 補 14 鍵（與繁體同形者依慣例不列）。順帶掃其他持久元件（原卡指定）：faucet pill/onboarding pill 覆蓋健全 ✓；**streamer PiP 靜態標籤 3 缺**（虛擬主播/跟注 ▶/已跟注 ✓ 取消，`grep -c` 各 = 0）→ EN 補 3 鍵、zh-Hans 補「虚拟主播」（跟注兩鍵簡繁同形不列）。分享戰績的 Web Share 組字串（「我在 ApexWin 玩…」）走系統分享面板非 DOM＝walker 不觸及，判非債。sw.js CACHE bump v47。preview 驗證（清 SW+caches 後）：live-stats 浮窗 EN 全譯（title/盈虧→Profit/六標籤 Bets…Recent game/雙 foot 鈕/note tag/關閉→Close）、zh-Hans 全譯（实时统计/投注数…）、zh-Hant 往返**全數還原**（U15 restore 機制與新鍵相容）；streamer 面板 虛擬主播→Virtual host、跟注 ▶→Follow ▶ 往返還原；toast「實時統計已重置」EN 動態譯 Live stats reset（observer 路徑）；零 console error。原卡（2026-07-19 U15 preview 驗證中實測發現）：live-stats 浮窗（core/live-stats.js:69-74）六個 kv 標籤「投注數/中獎數/總投注/總贏分/最大單筆/最近遊戲」字典無 key → EN/zh-Hans 下仍顯示繁中。**E5 複驗**：`for k in 投注數 中獎數 總投注 總贏分 最大單筆 最近遊戲; do grep -c "$k" core/i18n.js; done` = **全 0**（實作前複驗同結果 ✓）。判準：EN 下六標籤全譯 ✓、zh-Hant 往返還原 ✓、零視覺 ✓。

- `🏗️進行中` 🟡 **U7 設計 token 中間階 / 語意 token 決策（自 2026-07-14 全量稽核 workflow）** — M：token 一致性稽核（radius/duration/font/color/spacing 5 維度 × 對抗性驗證）找出大量「離階/無對應 token」侵蝕；此類**無法零視覺遷移**（新增 token＝零視覺收斂；四捨五入到鄰階＝微視覺變化），需人為設計決策。exact-match 零視覺部分已完成（radius/duration/color，見 U3/U5 與 commit d882921/f8b0dce）。**2026-07-14 使用者決策＝「新增中間階 token（零視覺）」**，已據此完成間距/圓角xs/font-3xs（見下），commit e555e30。清單（✅=已做 / ⏳=待決策）：
  - **間距**：✅ exact-match（4/8/12/16→`--ax-space-*`）211 處（66f2add）＋ ✅ 離階（6/10/14/18→新增 `--ax-space-1_5/2_5/3_5/4_5`）225 宣告（e555e30），皆零視覺、含 shorthand component-wise。⏳ 僅剩零星奇數微值(2/3/5/7/9/11px、20/22 等)保留為一次性。
  - **圓角**：✅ 6px×10 → 新增 `--ax-radius-xs`(6px)（e555e30）。⏳ **10px×12、12px×14 保留**：兩值同處 sm(8)↔md(14) 縫隙，加兩個半階 token 過度granular；宜挑單一 canonical（或接受 ±1~2px 收斂）而非硬塞兩 token，待決策。9px×4 疑為 8px 誤植（改＝微視覺變化）。
  - **font-size**：✅ 10px×11 → 新增 `--ax-font-3xs`(10px)（e555e30）。⏳ 16px×13、20px×14 經稽核多為裝飾字符/圖示容器，維持硬寫；fluid clamp 標題×6 無 fluid token（待議是否建 `--ax-font-fluid-*`）。
  - **duration**：⏳ 是否新增 `--ax-dur-fast`/`--ax-dur-slow`——注意僅「值完全等於所選 canonical」者可零視覺遷移（如 fast=0.15s 只收 12 處，其餘 .08/.1/.12/.2/.25/.3s 需接受收斂＝微視覺變化）。建議另立專案逐段拍板 canonical 值。
  - **語意色**：#fff 白字×40、金底深棕字 #2a1b00×6、亮底深字 #10131c×3 → 是否新增 `--ax-white`/`--ax-on-gold`/`--ax-ink`。**幽靈 token bug**：`var(--ax-muted)`（未定義）於 components.css 2454/2461/2586 靜默回退 #9aa4b2（≠`--ax-text-muted` #8895b3）需修（改 token＝微視覺變化）。stale `var(--token, #舊值)` fallback ×12（低優先可讀性 hygiene）。
    - **2026-07-14 consolidate 幽靈 token 全掃補正**（回歸稽核當日大量 token 遷移 e555e30/66f2add/f8b0dce/d882921 — 交叉比對 57 個 `var(--ax-*)` 引用 vs tokens.css 定義，**確認當日遷移零新增破損引用**；下列二筆皆為**遷移前既有**的未定義 token，非本日引入）：① **`--ax-muted` 漏記第 4 處 2572 `.ax-ddu__vs`＝`color: var(--ax-muted)` 無 fallback** → 未定義且無退路，實際渲染成**繼承色**（比 2454/2461/2586 的回退 #9aa4b2 更糟＝真・錯色），修＝改為 `--ax-text-muted` 屬微視覺變化，併入本語意色決策；② **`--ax-bg-soft`（未定義，U7 原未記）** 於 2424 `.ax-redeem .ax-input`＝`background: var(--ax-bg-soft, rgba(255,255,255,0.04))`，渲染 fallback（無可見 bug），修＝定義為共用 surface token 或內聯，屬設計意圖決策。**二筆皆非零視覺。**
    - **✅ 2026-07-14 consolidate 落地（幽靈 `--ax-muted` 全數收斂）**：4 處 `var(--ax-muted…)` → 既有 `var(--ax-text-muted)`（#8895b3）——2454/2461/2586 由 fallback #9aa4b2→#8895b3（微視覺、向真 token 靠攏）、**2572 `.ax-ddu__vs` 修正真·錯色 bug**（原無 fallback→繼承亮色，現為預期 muted 灰）。幽靈 token `--ax-muted` 自 prototype/ **完全消失**（grep 0 筆）。**此項與「語意色新增 token 決策」脫鉤**：收斂幽靈 token→既有 token 無設計選擇（`--ax-muted` 即 `--ax-text-muted` 的孤兒別名），非新增 `--ax-white/--ax-on-gold` 那類決策。preview 驗證：token resolve #8895b3、CSS 零 parse error、零 console error。**剩 `--ax-bg-soft`（2424）仍待決策**（是否建共用 surface token）。
  - **完整性批評（尚未量化）**：z-index（magic number 無 token）、border-width（1/2px）、line-height（1.5/1.45/1.8）、font-weight（600~900 大量硬寫）亦無 token 化。

- `🟦已批准待做` 🟡 **U17 手刻頁籤/單選群繞過 `HL.ui.tabs`/`segmented` — 缺 role=tab/tablist/aria-selected 與 aria-pressed（T1/S7/U8 收斂長尾）** — M-L（2026-07-20 UI-UX-a11y 淺審計 · consolidate workflow 4 鏡頭×對抗性驗證；合併 keyboard-aria＋ux-behavior 兩鏡頭 3 筆 CONFIRMED）：U8 只把 aria 補進 `HL.ui.tabs`/`segmented` 兩個 primitive 本體、S7 只遷 3 個遊戲難度選擇器；**大量 call site 從未改走 primitive、仍各自手刻「清兄弟 class＋啟用自身」迴圈且零 aria 選中態**＝當前分頁/選項對螢幕閱讀器隱形。**頁籤類（應走 `HL.ui.tabs`，缺 role=tablist/tab+aria-selected）**：`core/instant.js:57-58` 手動/自動下注分頁（`ax-inst__tab is-active`，setMode 僅 classList.toggle、:136 入 `.ax-inst__tabs`）＝Dice/Limbo/Crash/Mines/Plinko 五款共用引擎每處都有；`views/auth-view.js:17-18` 登入/註冊（`ax-auth__tab is-on`，:66 入 `.ax-auth__tabs`）；`layout/app-shell.js:240` 錢包 via 分頁、`:294-296` walletModal 儲值/提款/紀錄分頁（`.ax-tab`，classList.toggle 手刻）。**單選值群（應走 `HL.ui.segmented`，缺 aria-pressed/aria-checked）**：`core/table.js:78-80` 籌碼列（`ax-tbl__chip`，百家樂/輪盤共用）、`views/instant-crash-mines.js:129` 地雷數 1/3/5/10/24（`ax-inst__chip`）、`views/bounty.js:283` 押注額＋`views/liveroom.js:79` 跟注額（`ax-stake`，兩處逐字同構）、`layout/app-shell.js:198` 付款方式 fiat/crypto。**E5 反向複驗**：`grep -rUn 'Array.prototype.forEach.call(.*children.*classList.remove' prototype/src --include=*.js | grep -v ui.js` 恰命中 app-shell:198/:240、bounty:283、instant-crash-mines:130、liveroom:79（5 筆手刻迴圈；ui.js:242 primitive 對照排除）；`grep -c 'aria-pressed\|aria-selected'` 於 instant.js/auth-view.js/table.js/bounty.js/liveroom.js/instant-crash-mines.js 皆 0（app-shell 僅 aria-expanded 供下拉）；對照 `HL.ui.tabs`(ui.js:253) 具 role=tablist/tab+aria-selected、`HL.ui.segmented`(ui.js:232) 具 aria-pressed。**dedup**：T1=建 primitive、S7=遷 3 遊戲難度、U8=為 primitive 補 aria＋遷列舉的 pressable/linkable——皆不含這批未遷移手刻群（instant-crash-mines:129 地雷「數量選擇器」≠U8 的「mines 25 格棋盤」；U8 的「bounty/liveroom 連結」指 linkable 返回鈕/玩法說明、非 stake chips）。**修法**（零視覺）：頁籤群改走 `HL.ui.tabs`、單選群改走 `HL.ui.segmented`（傳 cls/btnCls/activeCls 保留 ax-tbl__chip/ax-stake/ax-inst__chip/ax-paym__opt/ax-tab 外觀，比照 S7 慣例），刪手刻 sibling-clear 迴圈；確有特殊邏輯難遷處退而補 aria-pressed/aria-selected＋建構同步。判準：上列站點皆有 role=tab/tablist+aria-selected 或 aria-pressed/aria-checked、切換同步、選中態對 SR 曝光；preview 各控制視覺不變、aria 正確翻轉、零 console error。⚠ 跨 6 檔多站點建議分批：先 instant.js（五遊戲共用最高曝光）+table.js → app-shell 錢包群 → auth → bounty/liveroom。

- `🟦已批准待做` 🟡 **U18 可點元素鍵盤不可及 ×2（U8 pressable 漏網）** — S（2026-07-20 UI-UX-a11y 淺審計 · keyboard-aria 鏡頭 2 筆 CONFIRMED）：① **賞金翻牌局 `.ax-fcard` 為可點 `<div>` 未包 pressable ＝鍵盤完全玩不了此遊戲**（🟡 功能性）：`views/bounty.js:60` `el("div",{class:"ax-fcard"})`、`:66` active 時 `node.addEventListener("click",…pickCard)`，無 role/tabindex/keydown（同檔 :223 地雷格用原生 `<button>`＝同檔即不一致）；② **大廳 World Event 英雄橫幅 `.ax-hero` 為可點 `<section>` 未包 pressable 且無內層 CTA 鈕**（⚪）：`views/lobby.js:36-58` `el("section",{class:"ax-hero",onClick:開 modal})` 子節點全 div/small/h1/span，對照同檔 newGamePromo(:73)/tournamentBanner(:88) 皆有內層 `<button>`+stopPropagation 提供鍵盤路徑、唯 hero 沒有。**E5 反向複驗**：`grep -n 'pressable\|linkable\|role\|tabindex' views/bounty.js` 僅回 :293/:304（linkable 返回鈕）＝ax-fcard 零鍵盤處理；`grep -n 'ax-hero\|pressable\|role\|tabindex' views/lobby.js` 僅回 :37 class 字串。**dedup**：U8 列舉清單（mines 25 格＝instant 版棋盤、bounty 連結＝linkable 返回鈕）不含 bounty 翻牌卡 `.ax-fcard` 與 lobby `.ax-hero`。**修法**（零視覺，比照 U8）：active `.ax-fcard` 包 `HL.dom.pressable`（role=button+tabindex+Enter/Space→pickCard）、`.ax-hero` 包 `HL.dom.pressable`（或內置 CTA 鈕）。判準：Tab+Enter 可翻牌並結算、可開 World Event modal；CSS 零 `[role]/[tabindex]` 選擇器＝零視覺（U8 已驗）。

- `🟦已批准待做` 🟡 **U19 留存/金流面板 i18n 覆蓋補完（U16 同族延伸，EN 顯中文）** — M（2026-07-20 UI-UX-a11y 淺審計 · i18n-coverage 鏡頭 4 筆 CONFIRMED，皆逐字串 `grep -c` 於 core/i18n.js = 0 佐證）：U16 只補 live-stats 全檔＋streamer；其餘留存/金流高曝光面板仍大量未進 DICT.en＝EN 模式顯示中文。**I-1 VIP 俱樂部＋Rakeback 詳情彈窗**（core/progress.js）：:199「💧 返水率（本級）」、:200「可領取返水」、:188「已達最高等級 💎」、:250「目前返水率」、:255「尚無可領取返水」、:253 說明長句、:259 demo tag「綁 VIP 等級係數 · 每日桶逾期作廢 · Demo」（同彈窗「目前等級/累積有效押注/今日可領返水」已譯＝部分覆蓋）。**I-2 每日任務＋多倍數挑戰彈窗**（challenges.js:96-101 標題「🎯 多倍數挑戰」/kv「獎金錢包」/說明/demo tag；progress.js:312/314 tasksOpen kv+demo tag、:294 裸「去簽到」＝字典僅有「去簽到 →」帶箭號不匹配）。**I-3 通知中心**（notify.js:19-21 三則種子通知標題+內文、:69 demo tag；rain.js:50-51「聊天室紅包雨」）＋ **ago() SUFFIX 缺「分鐘前」「天前」**（i18n.js:264 SUFFIX.en 僅 秒前/分前/小時前 → notify.js:49「N 分鐘前」/:51「N 天前」EN 未譯、同函式「小時前」正常＝行為不一致）。**I-4 VIP 段位名 青銅/白銀/黃金/白金/鑽石 全站零字典覆蓋**（progress.js:117-123 RANKS，以「icon+空格+name」單節點渲染於 header 迷你等級＋VIP/Rakeback/Cashback 段位列；grep 段位名為獨立鍵=0 筆、黃金 6 筆全為「黃金之城」meta 非段位）。**dedup**：S4=gameInfoBar、S7=難度詞彙、S12/S13=錦標賽/簽到功能、U10=input aria-label、U16=live-stats+streamer——皆不涵蓋 progress/challenges/notify/rain 面板 body。**修法**：DICT.en/zh-Hans 補上列鍵（比照 U16）；SUFFIX.en 補「分鐘前/天前」；VIP 段位名因「icon+name 單節點」需拆兩節點後補 5 鍵（或加 emoji-前綴 PREFIX 條目）故 M。判準：EN 開 VIP/rakeback/任務/挑戰/通知彈窗＋header 段位全英文、時間戳「Nm/Nd ago」、zh-Hant 往返還原（U15 相容）、零 console error。⚠ 改 i18n.js 後 preview 需清 SW/caches（§4）。

- `🟦已批准待做` ⚪ **U20 會員錢包紀錄面板缺 async 錯誤態（違反自身 .catch 慣例）** — S（2026-07-20 UI-UX-a11y 淺審計 · ux-behavior 鏡頭 CONFIRMED）：`layout/app-shell.js:285` `HL.api.walletHistory(20).then(rows=>show(…))` 只接 resolve、無 `.catch`／error 分支；`core/api.js:87-89` walletHistory 亦裸 `.then(res=>res.data||[])` 無 catch（對照 api.js:65/75/105 的 rpc/sbAuth 皆有 catch＝錯誤處理本是慣例、此處為例外）。載入態（:277「載入中…」）與空態（:281「尚無交易紀錄。」）已存在、唯錯誤態缺：網路層拒絕拋 unhandled promise rejection，且視 postgrest 版本可能降級為「尚無交易紀錄」的**誤導性空態**（明明是失敗）。⚪（僅會員模式、Demo 不顯）。**E5 反向複驗**：`grep -n '\.catch(' layout/app-shell.js` 零輸出（全檔 4 個 .then 於 128/260/285/363 皆無 catch）；`grep -n '\.catch' core/api.js` 僅 65/75/105。**dedup**：R1-R10/U/T 卡皆未觸 async 錯誤態；lobby feedWins/global-prize feedLeaderboard 雖同無 catch 但有 Demo seed baseline 優雅降級＝非卡死點，wallet-hist 會員面板內容全靠此 fetch 故為獨有受害點。**修法**（零視覺，Demo 零影響）：`HL.api.walletHistory` 補 `.catch(→[])` 或呼叫端補 `.catch→show()` 加錯誤列「載入失敗，請稍後再試」（字典補 en/zh-Hans）。判準：member 模式斷網開錢包紀錄見錯誤提示而非卡死/誤導、無 unhandled rejection。

## 🎰 Stake 對標打磨（S 系列 · 2026-07-17 前景深研 workflow 產出）

> 來源：船長指定的 Stake 深度拆解（UX/功能/遊戲 三維 × ApexWin 基準盤點，詳見 `intel/platforms/stake.md` 🔬 節）。verify agents 撞 session 上限，核心宣稱已由前景 grep 逐項驗證（標 ✓）。全部為「打磨既有表面到 Stake 級慣例」，符合 mode:polish。

- `✅完成` 🔴 **S1 共用遊戲設定齒輪 ⚙️（keystone）** — M　·　2026-07-17（前景實作，commit f26cd0d）：core/game-settings.js HL.gset（fast/anim/hotkeys，localStorage 跨遊戲）；極速=animate 歸零+autobet 470→110；動效 off=.ax-anim-off kill-switch；GameFrame ⚙ 彈窗 + PiP ⚙ 接通；無音效層故無音效 flag。原規格：GameFrame 工具列加 ⚙️ → 極速模式(跳過結果動畫)/動效開關/音效開關/熱鍵開關 四 flag，localStorage 持久化、跨遊戲生效；動效 off 同步尊重 `prefers-reduced-motion`。✓ 證據：instant.js 零 settings/sound/animation；game-frame.js:40-42 工具列僅 ⛶/📈。Stake 慣例：齒輪集中 Instant Bet/Animations/Hotkeys/音效、設定持久化。判準：任一 instant 遊戲改設定，切到另一款仍生效。
- `✅完成` 🟡 **S2 instant 家族統一熱鍵** — S　·　2026-07-17（前景實作，commit f26cd0d）：Space/S/A/D 已上（D=最小注 1），gset gate+輸入框/彈窗雙防護，preview 全路徑驗證過。原規格：Space=下注、S=加倍、A=減半、D=歸零（W=cashout 留 Crash），由 S1 齒輪 gate、預設關、開啟時 toast 提示。✓ 證據：instant.js 零 keydown。掛 HL.instant 一處=五款遊戲全獲益，鍵盤 a11y 加分。
- `✅完成` 🔴 **S3 Crash/Mines 補接 HL.fair + 公平入口一致** — M　·　2026-07-17（前景實作）：crash crashAt 與 mines 佈雷（每雷一抽、皆入 nonce 序列）→ HL.fair.float；火花粒子/隨機選格（視覺/玩家代理）刻意保留 Math.random；PF 白名單 +crash-x/mines。preview：float 域內、nonce 遞增、🔒 兩遊戲顯示、未接遊戲不顯示。原規格：instant-crash-mines.js 4 處 `Math.random` → `HL.fair.float`（比照 dice），GameFrame/betPanel 固定位置公平標記白名單加入兩款。✓ 證據：grep instant-crash-mines.js：Math.random×4、HL.fair×0；instant-games.js（dice）fair×3 為既有範本。CLAUDE.md §4 既列此差距。
- `✅完成` 🟡 **S4 統一遊戲資訊列（RTP/最大賠付/公平標記）** — S　·　2026-07-17（consolidate 自主實作）：新增 `HL.ui.gameInfoBar({fair, edge, rtp, max, demo, note})`（core/ui.js），固定段序 🔒可驗證公平(可點開驗證彈窗) → 莊家優勢 → RTP → 最高倍率 → Demo → 玩法一句話；沿用 `.ax-demo-tag` 外觀（`.ax-gameinfo` 加 wrap + 公平鈕虛線底線樣式，base.css）。**12 處遷移**：dice/limbo/plinko（原本沒標可驗證公平，現補上🔒）、crash-x/mines（S3 剛接 fair，同步補🔒）、hilo/keno/picks/towers/duel（保留 nonce 粒度說明）、chicken（RTP 97% + 最高 5000×）、slot 賠付表（RTP 96.13%）、bounty 房（RTP 100%）。i18n：全句 key 拆為逐段 key（EN+zh-Hans 各 ~20 條，dice/limbo/plinko/crash/mines 五款玩法注記原本無翻譯，本輪一併補上=i18n 覆蓋+5）；死掉的 5 條全句 key 已移除。sw.js CACHE bump v24。preview 驗證：11 遊戲實地渲染段序正確、公平鈕開 fairnessModal、EN/zh-Hans 逐段翻譯正確、零 console error（slot 賠付表因沙箱資源載入逾時未實地開啟，改動與 chicken 同型+primitive 單元測通過）。原規格：抽 `HL.ui.gameInfoBar`，RTP/莊家優勢+最大倍率固定位置呈現。✓ 證據：RTP 文案散落 ≥10 檔（arena/bounty/chicken/instant-*…）格式各異。Stake 慣例：全目錄 RTP 98-99% 固定標示。
- `✅完成` 🟡 **S5 近期結果歷史列統一元件** — M　·　2026-07-17（consolidate 自主實作）：新增 `HL.ui.histBar({cls, itemCls, max, newestFirst, fair})`（core/ui.js）→ **8 處遷移**（審計實測比原證據多 2 處）：dice/limbo/plinko/crash/hilo/duel 6 處 `fair:true`（膠囊升級為可點 button → 開可驗證公平彈窗，`.ax-histbar__b` 只重置原生 button 外觀，視覺仍由各遊戲既有膠囊 class 主導）＋ 百家樂/輪盤 2 處純 span（未接 fair，註記補接後改 fair:true）。完全複刻各處既有 class/上限(10~18)/方向（hilo 新在尾、其餘新在頭），重複的 insertBefore+裁切邏輯刪除。sw.js CACHE bump v25。preview 驗證：primitive 單元測試（裁切/方向/button-fair/span-plain/clear）全過；**flex 容器內 span→button 像素級零差**（getBoundingClientRect dw=0 dh=0、11 項 computed style 全同）；8 款遊戲實地各跑一局（dice 92.16 / limbo 1.30× / plinko 0.4× / crash 1.20× / hilo 8♥ / duel 38:23 / 輪盤 13 黑 / 百家樂 P）膠囊 tag/class/文字全正確；點 dice 膠囊開「🔒 可驗證公平 · 當前種子」彈窗；零 console error。原規格：抽共用「贏綠輸紅膠囊歷史列」，已接 fair 的遊戲點擊可開驗證。✓ 證據：crash-mines/duel/games/hilo ≥4 檔各自手刻 hist。與 DEBT 模板化方向同軸。
- `✅已存在勿做` 🟡 **S6 進階自動下注補 On Win/On Loss %** — S（2026-07-17 前景查證：**gap agent 宣稱錯誤**——instant.js:87 早有「贏後+%/輸後+%」欄位且語意=Stake reset/increase，CLAUDE.md §10 記載 ✅2026-06-22。僅剩 Crash/Mines Auto 佈局一致性歸入 S3/G-P7 檢查）。原規格：instant.js 自動下注已有 次數/止盈/止損，補「贏後 reset/+X%、輸後 reset/+X%」兩欄（Martingale 類標配）。✓ 證據：instant.js 無 onWin/onLoss。Crash/Mines 的 Auto 佈局一致性一併檢查（原 G-P7 併入）。
- `✅完成` 🟡 **S7 難度選擇器收斂** — S　·　2026-07-17（consolidate 自主實作）：`HL.ui.segmented` 加 opts `{cls, btnCls, activeCls}`（預設不變＝arena/demo-tools 零影響）＋ `onPick 回傳 false 取消切換`（局中鎖難度）→ **3 處手刻選擇器遷移**：towers 難度（沿用 `ax-inst__chip is-active`）、plinko 排數/風險（chipSel 改薄轉接）、chicken 難度（沿用 `ax-chx__diff is-on`）。**詞彙統一** 簡單/普通/困難/專家（Easy/Medium/Hard/Expert）：chicken「中等→普通、地獄→專家」（key easy/mid/hard/hell 不動＝RPC 相容、💀🐣 icon 保留）。i18n 覆蓋順勢補齊：chicken 難度鈕（emoji+詞全字 key）先前 EN/zh-Hans **完全未譯**，本輪 EN 5 鍵 + zh-Hans 8 鍵（含 Towers 區塊漏譯的 難度/下一層/簡單/困難）。sw.js CACHE bump v26。preview 驗證：primitive 單元測（預設外觀不變/自訂 class/取消切換）、towers 局中點他檔不切且塔仍 3 格、chicken 四檔切換正確、plinko 兩列正常、EN/zh-Hans 全譯、零 console error。原規格：Easy/Medium/Hard/Expert 統一詞彙 + `HL.ui.segmented` 復用（Stake 新作已把四檔難度變跨遊戲文法）。
- `⬜待批准` ⚪ **S8 大廳策展微調** — S：搜尋上移至最頂、排序控制常駐（Stake：搜尋→Trending→Originals→促銷→分類）。
- `⬜待批准` ⚪ **S9 遊戲卡「N 人在玩」徽章** — S：偽隨機模擬即時人數（與 HL.heat 整合，假活躍度敘事與虛擬主播同軸）。
- `✅完成` 🟡 **S10 display-in-fiat 金額顯示幣別** — M　·　2026-07-17（前景實作）：⚙ 遊戲設定第 4 列「金額顯示幣別」select（HL.gset.fiatView 持久化）→ HL.dom.money 全站出口以 mock 示意匯率換算顯示（rate 欄新增於 mock currencies；原生 NT$ 輸出零變化、未知代碼防護、crypto 小數自適應）。切換即 HL.app.refresh 全站生效。**scope 澄清**：money() 本就是單一出口；app-shell fmtBal 是「各幣別各自錢包餘額」不同語意、刻意不動。支撐長期目標 4 多幣別。原規格：純顯示層法幣換算（標註指示性），money() 統一出口順勢收斂。⚠ 需先細查：money.js 零 fiat 碼，多幣別散在 app-shell，scope 待實作前確認。
- `⬜待批准` 🟡 **S11 VIP 福利矩陣面板** — S-M：一眼看「下一級解鎖/放大什麼」（Stake 福利矩陣慣例）。
- `✅完成` 🟡 **S12 錦標賽付獎曲線陡頭長尾 + 榜深 + 入口常駐** — S　·　2026-07-19（consolidate 自主實作）：① **付獎曲線 8→30 名陡頭長尾**（core/tournament.js SPLIT）：頭部 10 名 25/14/9/6.5/5/4/3.2/2.6/2.2/1.9% 陡減（73.4%）＋ 11–20 名各 1.5%、21–30 名各 1.16% 長尾，合計恰 100%（preview 驗證 30 名逐級 prizeFor 加總＝恰 1,000,000、第 31 名 0）；② **榜深對齊付獎深**：bot 11→49（榜 12→50 人，20 個 fakeNames 循環+雙位尾碼）、賽事頁榜單 slice(0,12)→前 30 名全列＋我在圈外時補「⋯＋我的列」不失焦（既有 4 欄 row 結構、零新 CSS）；③ **入口常駐**：bottombar 於每日簽到後新增 🏆 限時錦標賽項（S13 同型 pattern：`#ax-bb-tourney` 動態徽章 圈外「衝榜分獎池」⇄ 圈內「第 N 名 · 領獎圈內」，refreshChrome 即時翻新）→ `HL.router.go("tournament")`——原本僅 lobby banner＋促銷卡兩入口皆在大廳。文案同步：規則彈窗第 3 條、myGap「衝進前 N 名」改 SPLIT.length 動態；i18n 死 key「前 8 名分得獎池…」EN/Hans 移除、新 key（規則新文、限時錦標賽、衝榜分獎池）補齊，混數字徽章與 15 兄弟項同慣例不譯。sw.js CACHE bump v40。**已知限制**：既有 localStorage 舊期（11 bot）沿用至該期結束才換 50 人榜（一期最長 3h，懶結算冪等路徑 preview 實測正常）。preview 驗證：SPLIT 30 級加總/邊界、舊期結算→新期 50 人、榜 32 列（圈外）/30 列（圈內、is-you 首列 250,000、第 30 列 11,600）、規則彈窗 zh/EN（MutationObserver 非同步、延遲後斷言通過）、bottombar 項 role=button（pressable 工廠自動繼承）＋徽章 record(500000)→refreshChrome 翻「第 1 名 · 領獎圈內」、EN「Timed Race」切換往返、零 console error。原規格：對齊 Daily Race「付獎深、零報名自動入榜」（零報名自動入榜既有：liveStats 中央掛鉤 ✓）。
- `✅完成` 🟡 **S13 簽到常駐入口 + 連登徽章** — S　·　2026-07-18（consolidate 自主實作）：底部功能列（app-shell.js bottombar）於「每日任務」後新增 📆 每日簽到項 → `HL.rewards.open()`（原本只能從新手引導/任務清單觸達，無常駐入口）；連登徽章＝`#ax-bb-checkin` 動態 sub：可簽→「今日可簽」（行動呼籲）、已簽→「連登 N天 ✓」，`refreshChrome` 掛 id 更新（claim() 既有呼叫 refreshChrome ＝ 領完即時翻新，沿用 ax-bb-hh 同型 pattern）。i18n：「每日簽到」「今日可簽」EN+Hans 補齊；混數字徽章字串與 15 個兄弟項同慣例（walker 全節點比對不拆）。sw.js CACHE bump v27。preview 驗證：項目渲染於第 2 位、點開簽到彈窗、領取後徽章 今日可簽→連登 1天 ✓、EN/Hans 標題翻譯、375px 底欄內部捲動無頁面溢出、零 console error。原規格：底部功能列加簽到項。
- `⬜待批准` ⚪ **S14 桌面側欄收合 icon-rail** — M：收合而非消失（R1 抽屜已解手機，此為桌面加分項）。

## ⚙️ 引擎可靠度（元循環自身）

- `✅完成` 🔴 **E1 落地 `build_lock` + 逐檔 add 鐵律** — S　·　2026-07-17（前景實作）：radar/investigate 兩 SKILL 補上「進場檢查 build_lock→讓路/stale heal(>2h)→上鎖→收尾解鎖」（evolve/consolidate 原已合規）；並把兩者的 `git add intel/` 整目錄反模式改為**逐檔 add 鐵律**（07-09 掃走他人未提交工作的根因）。船長指定引擎可靠度實作授權
  - 證據：CONTROL.md 記錄多輪「觸發卻未收尾」孤兒（#26/#31/#32）、並行寫入 counter 漂移；已加 `build_lock` 旗標，需 evolve/investigate/radar/consolidate 進場檢查+設定、收尾清回。**必須先於任何 counter-based 比例閘。**

- `✅完成` 🟡 **E2 no-op 靜默退出 + 日誌搬出 CONTROL** — S　·　2026-07-17：CONTROL.md **150,780 → ~4.3KB（-97%）**——103 筆例行心跳（2026-06-28 起）原文全數搬到新檔 `intel/loop-journal.md`（最新在上、含遷移說明 header）；CONTROL.md 只留設定區 + 船長指令對話 + 一筆遷移指標。**慣例固化**：CONTROL.md 指引文字 + 4 個 SKILL.md（radar/investigate/evolve/consolidate 的船長指令步驟）皆加註「例行心跳（無待處理指令）寫 loop-journal.md 最上方、一輪一則 1–3 行精簡，不 append CONTROL」；已回應區今後只放對待處理指令的真回覆。**自主實作依據**：本卡雖原標 ⬜待批准，但 07-17 惡化已跨過「功能性破壞」線——150KB 超過 routine 單次 Read token 上限，任何 Routine 已無法完整讀取 CONTROL（連船長指令歷史都讀不完），非單純成本問題；SKILL 鐵律「全自動模式下不需等對話批准」+ auto_implement:true + 引擎可靠度本屬四維審計輪替之一，故納入本輪唯一實作卡。零 prototype/ 觸碰、零視覺風險、git 可回滾。
  - 證據：CONTROL.md ~117KB、43 筆重複 heartbeat（「本輪 0 筆到期」）；investigate SKILL 已說無變更不 commit 卻仍附段落。建議日誌搬到 `intel/loop-journal.md`。
  - **2026-07-14 re-measure（引擎可靠度維度審計）**：CONTROL.md 已達 **145,691 bytes（~146KB）**，較 card 記錄的 ~117KB **+24%**；07-13→07-14 心跳續 append ~28KB。**E2 debt 正加速惡化**，且惡化源就是每輪（含 consolidate 自身）的 verbose 心跳 append——是最應優先批准落地的引擎債。BACKLOG.md 126,669 bytes（~124KB）與 E3 card 記錄相符、暫穩。

- `✅完成` 🟡 **E4 no-op 心跳去 commit（0 筆到期時靜默退出）** — S　·　2026-07-18（consolidate 自主實作）：investigate SKILL 第 1 步 no-op 分支改為「當日首輪照舊 心跳+STATE+commit（保引擎活著證據）；同日後續輪 不推游標、不寫 journal、不 commit、清鎖靜默退出」；到期日行為不變。判準＝連續 0 到期期間每日 `intel(scan)` commit ≤1 筆（07-24 watchlist 到期前即可從 git log 驗證斷崖）。原卡（2026-07-18 引擎可靠度淺審計開卡）：investigate 每小時觸發，watchlist 全數 next_due=07-24 → 每輪「0 筆到期」仍推進 r 游標 + journal append 一行同文 + commit。實測 07-17→07-18 已 20 筆 `intel(scan)` 心跳 commit（r43–r56 全同文），07-24 前依現頻率將再堆 **~140 筆零資訊 commit**；loop-journal.md 已 162KB（append 屬設計、但同文重複行無追溯價值，且 162KB 已超過 E2 當時判定 CONTROL「功能性破壞」的 150KB 線——差別僅在 journal 不需整檔讀，尚非破壞）。修法（E2 精神延伸）：investigate SKILL 加「0 筆到期 → **當日首輪**照舊寫一則心跳+commit（保『引擎活著』證據），**同日後續 no-op 輪**不推游標、不寫 journal、不 commit、靜默退出」；判準＝連續 0 到期日每日 `intel(scan)` commit ≤1 筆、git log 心跳雜訊斷崖式下降、到期日行為不變。
  - 證據：`git log --since=2026-07-17 | grep -c "intel(scan)"` = 20；journal r43–r56 逐行同文（僅 r 號遞增）；`intel/loop-journal.md` 162,675 bytes。

- `✅完成` ⚪ **E5 淺審計「負向宣稱」開卡前必反向複驗** — S　·　2026-07-18（consolidate 自主實作）：consolidate SKILL.md 第 1 步新增鐵律——開卡含負向宣稱（「全站無 X」「零處理」「從未 Y」）必附可重現反向 grep 命令＋0 筆輸出為證，否則不得入列；實作人動手前必先複驗原卡宣稱再改 code（R6/U8/U9 三前例入註）。判準「SKILL.md 含該鐵律」✓；本輪新開的 U10 已依此鐵律附三組反向 grep 證據＝立即生效。原卡：R6（07-18 自適應審計開）核心宣稱「全 CSS 無任何 `.ax-pip` 小屏 @media」為假——該 media 規則自 53ebe71 首發即存在，一句 `grep -n "ax-pip" components.css` 即可證偽，卻以 🟦已批准待做 入列、差點讓本輪把「375px 溢出關不掉」當事實實作（實測 375px 早已在視口內）。同族失誤已三犯：U8 gameCard（多行 el() 逃過單行 grep）、U9 幣別列（onClick 隔行逃過貪婪匹配）、R6（負向宣稱未複驗）。修法＝consolidate SKILL 第 1 步加一行鐵律：「開卡若含**負向宣稱**（『全站無 X』『零處理』『從未 Y』），必須附可重現的反向 grep 命令與其 0 筆輸出為證；實作人動手前先複驗原卡宣稱再改 code」。判準：SKILL.md 含該鐵律；此後新開卡的負向宣稱皆附複驗證據。

- `✅完成` 🟡 **E6 stale-heal 判準被 E4 副作用擊穿（誤判搶鎖並行寫入風險）** — S　·　2026-07-19（consolidate 自主實作）：radar/investigate 兩 SKILL 的 build_lock stale-heal 判準由「journal 最新心跳距今 >2h」改為「**`intel/CONTROL.md` 檔案 mtime 距今 >2h**」（上鎖動作本身必寫 CONTROL ＝ mtime 為上鎖時刻下界；船長手改 CONTROL 只會延後 heal＝安全方向）；evolve/consolidate 補上同判準 stale-heal（原本完全無 heal，反向 grep `stale` 兩檔皆 0 筆——若崩潰輪留下 lock=true，兩者只會永遠讓路）。**順手項**：evolve/consolidate SKILL 驗證步驟引用的過時 preview 工具名（`preview_console_logs`/`preview_screenshot`/`preview_snapshot`/`preview_eval`/`preview_resize`，皆非現行工具）更正為現行 `read_console_messages`/`read_page`/`javascript_tool`/`computer` 截圖/`resize_window`。零 prototype/ 觸碰＝免 preview。原卡（2026-07-19 引擎可靠度淺審計，依 E5 鐵律附證據）：E4 讓 investigate 在 0 到期靜默期「同日後續輪不寫 journal、不 commit」→ journal 最新心跳可合法地 >2h 舊；此時若 consolidate/evolve 正持鎖工作中（打磨輪含 preview 驗證常 >40 分鐘），radar（08:15）或 investigate（整點）進場見 lock=true、查 journal >2h → **把合法持有的鎖誤判為 stale、清鎖進場並行寫入**＝E1 單一寫入鎖失效、重演「觸發卻未收尾」孤兒根因。證據：radar/investigate SKILL 各 line 12 原文「若 `intel/loop-journal.md` 最新心跳距今 >2 小時，視為前一輪崩潰未清鎖，可清回 false」；investigate SKILL line 24（E4 靜默分支「不寫 journal」）；反向 grep：`grep -c stale apexwin-consolidate/SKILL.md` = 0、`grep -c stale apexwin-evolve/SKILL.md` = 0（兩者無任何 heal）。判準：四 SKILL 的 stale 判準皆以 CONTROL.md mtime 為據、不再引用 journal 心跳；evolve/consolidate 具備 heal 能力。✅ 全數落地。

- `✅完成` ⚪ **E3 BACKLOG 日誌歸檔** — S　·　2026-07-17（前景實作）：分析師日誌 44 則中 41 則歸檔至 `BACKLOG-archive.md`（BACKLOG 69k→38k chars，-46%）、BACKLOG 留最新 3 則+輪替規則、evolve SKILL 固化「超過 3 則移最舊到 archive」。任務佇列原地保留（evolve 去重來源、行為零風險）；佇列本體壓縮列為可選未來項。船長指定引擎可靠度實作授權
  - 證據：BACKLOG.md ~126KB 從不修剪；evolve 每 2h 須整檔 Read+去重，成本無上限成長。歸檔到 `BACKLOG-archive.md`、留精簡索引。

- `🟦已批准待做` 🟡 **E7 build_lock stale-heal 非原子 → 雙 firing 並行進場（E1/E6 單一寫入鎖被 TOCTOU 擊穿）** — M（2026-07-20 本輪 consolidate 執行中實測發現 · 活體證據）：E6 把 stale-heal 判準改為「CONTROL.md mtime >2h」修好了「E4 靜默期誤判」，但 **heal 本身是「讀→判 stale→清 false→設 true(claim)」的非原子 read-modify-write**：兩個 firing 若同時見到同一把真·stale 鎖（mtime >2h），會**各自 heal 並各自 claim、雙雙進場**＝E1 單一寫入鎖形同虛設，重演「並行寫壞 prototype/」風險。**本輪活體重現**：04:27 有一 firing 崩潰留下 build_lock=true（進場時 mtime 7.7h＝真 stale）；隨後**本 firing 與另一 consolidate firing 幾乎同時進場**、兩者都通過 >2h heal 閘門並開始寫——build_lock 在數分鐘內 true↔false 反覆（雙方交替 heal/claim）、components.css 一度出現又消失、對方最終先 commit R10（831d338）並在 commit 訊息留言「DEBT R10 ✅ 標記待並行 firing 釋出 DEBT.md 後補」＝**對方偵測到本 firing 存在、手動用 commit 訊息協調交接**（f210f56 甚至誤判本 firing 已崩潰而「收編」本 firing 的 U14 稽核＋heal 孤兒鎖）。這次靠雙方良好 §7 紀律（只 commit 自己的檔、commit 訊息交接、逐檔 add）沒釀成災難，**但那是運氣不是設計**。**E5 佐證**：consolidate/radar/investigate/evolve 四 SKILL 的 stale-heal（如 `apexwin-consolidate/SKILL.md:14`）皆為「mtime >2h → 清 false 後照常進行」的非原子序列；`grep -c 'token\|原子\|atomic\|re-read\|重讀確認' <四 SKILL>` = 0（無任何佔位／再讀確認機制）。**修法候選**（實作時擇一、四 SKILL 同步）：(a) **claim-token 再讀確認**（建議）——heal 後寫入 `build_lock: <firing 唯一 token>`（非裸 true；token=進場時間戳＋亂數），停頓數秒後**重讀 CONTROL**，token 仍是自己的才進場、否則讓路（打破 TOCTOU 平手）；(b) 用 git 當互斥——以 `git commit` 原子性競標鎖（首個 commit build_lock 的 firing 勝、其餘 pull 後見鎖讓路），代價多一個 lock-only commit；(c) 進場前額外檢查「近 N 分鐘是否有他人 commit／prototype 檔剛被寫」的活體訊號，有則讓路（heuristic 非嚴謹）。判準：兩 firing 同時對同一 stale 鎖進場時只有一個成功 claim、另一個讓路；四 SKILL 皆具再讀確認；模擬雙進場測試通過。**⚠ 此為引擎自身可靠度債（非 prototype/ 表面）；本輪僅開卡不實作（實作＝改四 SKILL 屬引擎維護，另輪徹底做）。**

---

## ✅ 已完成
- **T1**（模板化 keystone）：`HL.ui` 元件層 7 primitive 全落地並遷移主要重複點（gameCard/carousel/promoCard/segmented/tabs/kv/resultBlock）— 2026-07-10。（尾巴 T1-tail 見上，低優先）
- **T3**（router registry）：`main.js` if/else 路由 → 表驅動 `VIEWS`（render/backTo/isGame）— 2026-07-10（commit b5e6d18）。
- **U2**（金色/紫色 token fallback 正規化）— 2026-07-10。
- **U3 部分**（font-size 精確對應回歸 token，110 處，138→217 token 使用）— 2026-07-10。
- **R1**（手機主導覽）：header 漢堡 → 左側抽屜，≤720 補上 大廳/全球獎/競技場/娛樂城 導覽 — 2026-07-10。
- **R2**（≤480 保留 🔔/💧 圖示鈕，改收品牌文字）— 2026-07-10。
- **R3 部分**（8 處 100vh→100dvh；safe-area 底列留 R3-tail）— 2026-07-10。
- **U4**（--ax-text-dim 提亮過 WCAG AA）— 2026-07-10。
- **U5 部分**（加 prefers-reduced-motion；duration 收斂留尾巴）— 2026-07-10。
- **U5 追加**（`transition:all` 反模式全站清零：components.css 10 處 → 具體屬性；收編 07-10 首輪孤兒）— 2026-07-13。
- **T2**（HL.ui.closeAll/closeTop 收 8 處關 modal + 修 keydown 洩漏）— 2026-07-10。
- **U6**（refresh 保留主捲動位置 + 焦點）— 2026-07-10。
- **T4 部分**（移除死 config PAY_METHODS；深度純函式抽取留尾巴）— 2026-07-10。
- **U3 追加**（新增 --ax-font-2xs/3xl 並遷移 52 處 11/40px）— 2026-07-10。
- **T1-tail 部分**（標準型 core/* kv 遷移；非標準變體保留）— 2026-07-10（**consolidate 首輪自主實作**，實證 polish 迴圈閉環）。
- **R5**（逐元件審視：輪盤內部捲動、國戰死碼移除；底欄桌機窄寬溢出補修為全寬 overflow-x:auto）— 2026-07-10。
- **R4**（斷點 9→7 + 刪死 --ax-bp token + 文件化階梯；760/860 保留為註記例外）— 2026-07-10。
- **U1**（a11y：focus-visible + modal 對話框語意/Escape/焦點管理 + toast aria-live）— 2026-07-10。
- **附帶**（templating `ticker-leak-on-refresh`）：`main.js` renderApp 統一 `ticker.clearAll()`，修 refresh 路徑（i18n 切語系/改資料/存檔）ticker 重複註冊洩漏 — 2026-07-10。
- **E2**（引擎可靠度：日誌搬出 CONTROL）：CONTROL.md 150KB→4.3KB，103 筆心跳遷至 `intel/loop-journal.md`；CONTROL 指引 + 4 SKILL 固化「心跳寫 journal、已回應只回指令」慣例 — 2026-07-17。E1（build_lock 收尾保證）/E3（BACKLOG 歸檔）仍 ⬜ 待批准。
- **S5**（近期結果歷史列統一元件）：`HL.ui.histBar` 落地，8 處手刻 hist 遷移（6 fair 遊戲膠囊可點開驗證 + 百家樂/輪盤純 span）；flex 容器內像素級零視覺差 — 2026-07-17（consolidate 自主實作）。
- **S13**（簽到常駐入口 + 連登徽章）：bottombar 📆 每日簽到項 + `#ax-bb-checkin` 動態徽章（今日可簽 ⇄ 連登 N天 ✓，refreshChrome 即時更新）；「每日簽到/今日可簽」i18n 補齊 — 2026-07-18（consolidate 自主實作）。同輪 UI-UX-a11y 淺審計開 U8（可點 div 鍵盤不可及 ×9 + segmented/tabs 缺 ARIA 狀態）。
- **U8**（a11y：可點擊 div 鍵盤可及 + segmented/tabs ARIA 狀態）：`HL.dom.pressable` 落地（role/tabindex/Enter-Space，冒泡防護），原證據 9 處＋補漏 `HL.ui.gameCard`（全站卡片一次覆蓋）共 10 處遷移；`segmented` aria-pressed 三態、`tabs` role=tablist/aria-selected — 2026-07-18（consolidate 自主實作）。同輪修復 S13 插卡誤傷的 U7 標題行（`🏗️進行中` U7 標題與 U8 判準黏合，自 git 歷史還原）。尾巴：`<a onClick>` 假連結 ×9 另收（U8-tail，已批准待做）。
- **U8-tail**（a11y：`<a onClick>` 假連結鍵盤可及）：`HL.dom.linkable` 落地（role="link"+tabindex+僅 Enter，與 pressable 的 button 語意區分），9 處 6 檔全遷（bounty/vsslot 返回鈕、casino/lobby 查看全部、liveroom 玩法說明、tournament 返回大廳）；multiline 複掃無漏網、零視覺 — 2026-07-18（consolidate 自主實作）。同輪模板化淺審計開 T5（ax-section-title 跨 8 檔 10 處手刻 → 抽 `HL.ui.sectionTitle`）。
- **R3-tail**（底部安全列 safe-area）：`--ax-bottombar-h` → `calc(64px + env(safe-area-inset-bottom, 0px))`＋`.ax-bottombar` padding-bottom inset，grid 列高與全部 calc 消費點自動繼承；無 inset 裝置零視覺 — 2026-07-18（consolidate 自主實作，R3 全卡收束 ✅）。同輪引擎可靠度淺審計：**開新債 E4**（no-op 心跳每小時 commit 同文雜訊，0 到期期間 ~20 筆/2 日，🟦已批准待做）。
- **T5**（抽 `HL.ui.sectionTitle`）：h2＋右側附件 primitive 落地，8 檔 10 處手刻區段標題全遷（extras 原樣 append 收攏 連結/badge/按鈕/排序控制 變體、cls 收 `--sort`），DOM 逐處零變化 — 2026-07-18（consolidate 自主實作）。同輪自適應淺審計：@media 全數守 R4 階梯（480/560/720/1024/1280＋註記例外 760/860）、100vh 殘留皆為 dvh 覆蓋前刻意 fallback、100vw 皆有 calc/min 防護——無新債。
- **E4**（引擎可靠度：no-op 心跳去 commit）：investigate SKILL no-op 分支改「當日首輪保活著證據、同日後續輪靜默退出（不推游標/不寫 journal/不 commit）」，到期日行為不變 — 2026-07-18（consolidate 自主實作）。同輪 UI-UX-a11y 淺審計：**開新債 U9**（幣別下拉 `ax-cur-row` 未包 pressable＝U8 第 11 處漏網，🟦已批准待做）；select×2 有 label、img 皆 alt、roomCard 統一出口已包——無其他新債。
- **U9**（幣別下拉 `ax-cur-row` 鍵盤可及）：一行包 `HL.dom.pressable`，U8 系列第 11 處收束——dropdown 內兩列（幣別+錢包設定）鍵盤語意一致 — 2026-07-18（consolidate 自主實作）。同輪模板化淺審計：**開新債 T6**（`.ax-progress` 手刻 5 處、含兩份逐字相同局部 helper → 抽 `HL.ui.progress`，🟦已批准待做）；sectionTitle/modal 收斂皆無殘留。
- **T6**（抽 `HL.ui.progress`）：進度條 primitive 落地，5 處手刻遷移（兩份逐字相同局部 helper 改薄轉接、3 處 inline 補 clamp）、`ax-progress` 手刻零殘留 — 2026-07-18（consolidate 自主實作）。同輪自適應淺審計：**開新債 R6**（PiP 固定寬 384/576px 手機溢出、關閉鈕可能被裁不可及，🟦已批准待做）；輪盤/live-stats/chicken 小屏處理皆已到位——無其他新債。
- **R6**（PiP `--big` 481–591px 帶收進視口）：原卡宣稱經複驗大半不成立（≤480 media 自首發即存在、375px 無溢出），真實殘餘僅 `--big` 一條，一行 `min(576px, calc(100vw-32px))` 修畢、拖曳 clamp 連帶恆 ≥0 — 2026-07-18（consolidate 自主實作）。同輪引擎可靠度淺審計：**E4 已生效**（accaa0d 落地後 `intel(scan)` 心跳 commit 歸零、同日後續輪靜默退出，判準提前應驗）；**開新債 E5**（負向宣稱開卡前必反向複驗——R6 誤開即案例，🟦已批准待做）。
- **E5**（引擎可靠度：負向宣稱鐵律）：consolidate SKILL 第 1 步入鐵律——負向宣稱開卡必附反向 grep 命令＋0 筆輸出、實作前必複驗原卡；R6/U8/U9 三前例入註 — 2026-07-18（consolidate 自主實作）。同輪 UI-UX-a11y 淺審計：**開新債 U10**（核心下注 input ×5 無 accessible name，🟦已批准待做，已依 E5 鐵律附三組反向 grep 證據）；pressable/linkable 覆蓋 multiline 複掃乾淨（arena roomCard 統一出口 ✓、gameCard 有 onClick 才包 ✓、`el("a",onClick)` 9 處全包 ✓）、autobet 欄位 label 包裹 ✓、placeholder-fallback 類判非債。
- **U10**（核心下注 input accessible name ×5）：i18n walker 擴充支援 `aria-label` 屬性（與 title/placeholder 同機制、切語系即時翻）＋五處 input 掛中文 aria-label；字典補「投注額/主選單」順勢修復 chicken 可見標籤與漢堡 title 漏譯 — 2026-07-18（consolidate 自主實作）。同輪模板化淺審計：**開新債 T7**（統計小卡局部 helper 跨 6 檔 7 處手刻、5 份逐字相同 → 抽 `HL.ui.stat`，🟦已批准待做）；sectionTitle/progress/histBar/kv 收斂皆零殘留。
- **T7**（抽 `HL.ui.stat` 統計小卡）：primitive 落地，6 檔 7 處局部 helper 改薄轉接（5 份逐字相同 stat/slipStat + dice/limbo 兩份 card）、順手 hoist instant.js 重複 chip()，手刻零殘留 — 2026-07-19（consolidate 自主實作）。同輪自適應淺審計：375px 實測 16 view/遊戲 頁面級溢出全 ≤1px（R5 已知輪播 artifact、不可捲）、6 款彈窗＋錢包 modal 皆在視口內零內溢、CSS 固定欄格線（lobby/globe/liveroom/room-detail/battlecreate/chicken/tny）小屏收欄全數已有——**無新債**。
- **E6**（引擎可靠度：stale-heal 判準修正）：四 SKILL 的 build_lock stale-heal 統一改以 `CONTROL.md` mtime 判鎖齡（radar/investigate 原用 journal 心跳——E4 靜默期會把合法持鎖誤判 stale 而搶鎖並行寫入；evolve/consolidate 原完全無 heal）；順手更正 evolve/consolidate 過時 preview 工具名 — 2026-07-19（consolidate 自主實作）。同輪引擎可靠度淺審計：E4 生效（07-19 僅 1 筆首輪心跳）、watchlist 32 筆排程健康（最早 07-24）、工作樹無孤兒——除 E6 外無其他新債。
- **U11**（進度條 progressbar 語意 + 漢堡 aria-expanded）：`HL.ui.progress` 單點加 role/valuemin/max/now（width 式不動＝像素零變化、5 消費點一次覆蓋）、漢堡鈕 aria-expanded 建構+開闔同步 — 2026-07-19（consolidate 自主實作，commit 30ba2dd）。同輪 UI-UX-a11y 淺審計：輪播無按鈕（拖曳+自動輪）、抽屜 nav 語意 R1 已齊——除 U11 外無新債。
- **T8**（kv 局部 helper 殘餘收束）：liveroom/raffle/live-stats 3 處手刻 kv 改薄轉接（HL.ui.kv／HL.ui.stat 復用、DOM 逐字零變化），`function kv(` 全站僅剩 primitive+薄轉接 — 2026-07-19（consolidate 自主實作）。同輪模板化淺審計：row/badge/cell 類局部 helper 皆 DOM 各異屬元件特定判非債——除 T8 外無新債。
- **R7**（modal 家族 vh→dvh 漏網收尾）：`.ax-modal` 86vh 等 6 處補 dvh 雙宣告（fixed 遮罩內被困型＋modal 內捲動區 cap），行動端 URL bar 顯示時 × 鈕被裁修復、桌機零視覺 — 2026-07-19（consolidate 自主實作）。同輪自適應淺審計：hover-only 互動零功能性依賴（`.ax-bcard__chk` 走 `.is-on`）、抽屜 overflow-y:auto 橫向短視口可捲、streamer/liveroom cam 與 theater 屬頁面流可捲判非債——除 R7 外無新債。
- **S12**（錦標賽付獎曲線陡頭長尾＋榜深＋入口常駐）：SPLIT 8→30 名（頭部 73.4% 陡減＋長尾各 1.5%/1.16%、合計恰 100%）、榜 12→50 人＋前 30 名全列＋圈外「⋯＋我的列」、bottombar 🏆 常駐入口（動態名次徽章、S13 同型）；規則/i18n 同步、死 key 移除 — 2026-07-19（consolidate 自主實作）。同輪引擎可靠度淺審計：E4/E6 已生效（07-19 僅 1 筆首輪心跳）、watchlist 32 筆排程健康（最早 07-24、2027 到期為已停運站年檢屬刻意）、STATE 一致、工作樹僅使用者 Game assets 雜訊——無新債。
- **U12**（揭示型下拉 aria-expanded + Escape 關閉）：錢包幣別 pill／💧返水／GameFrame 💱 三揭示鈕補 aria-expanded 建構+開闔同步、三下拉補 Escape 關閉（比照抽屜 onDrawerKey capture 慣例）——U11 同族收尾，全站揭示型控制項 ARIA 覆蓋完備 — 2026-07-19（consolidate 自主實作）。同輪 UI-UX-a11y 淺審計：遊戲設定 ⚙ 原生 checkbox+label 語意健全、通知/簽到/錦標賽走 modal（Escape 已有）判非債——除 U12 外無新債。
- **T9**（倒數格式化 helper 收束）：`HL.dom.pad/mmss/dhms` 落地——pad 逐字複製 ×6 檔歸零（全站僅剩 primitive 本體）、mm:ss ×4 與「d天 hh:mm:ss」×2 收斂、lobby 死碼 fmtMS 移除；語意各異的 d·h/時分/相對時間變體保留原地判非債 — 2026-07-19（consolidate 自主實作）。同輪模板化淺審計：`ax-modal__actions` 建構 6 處內容/按鈕各異、僅 global-prize 一處局部 helper 判非債；row/badge/cell 類前輪已判——除 T9 外無新債。
- **R8**（iOS 輸入聚焦強制縮放）：`@media (pointer: coarse)` 全輸入控件字級鎖 `max(16px,1em)`——全站輸入框皆 <16px（核心下注欄等多數連 font-size 規則都沒有＝13.33px 預設）且 viewport 無 maximum-scale，iOS 聚焦即整頁強制放大；桌機不匹配 media＝零視覺 — 2026-07-19（consolidate 自主實作）。同輪自適應淺審計：**開新債 U13**（input 家族缺 `font: inherit` reset、全站輸入框渲染 Arial 系統字體與 app 字體 drift，🟦已批准待做、非零視覺留 UI-UX 輪驗證）。
- **U13**（input 家族 `font: inherit` reset）：base.css 補 `input, select, textarea { font: inherit }`——全站無自帶 font 規則的輸入框 Arial 13.33px→app 堆疊 15px、下注列 input 與 chips 鈕同高對齊；各元件自帶樣式（fair monospace、搜尋 15px、bet weight 700）class 特異度勝出全保留 — 2026-07-19（consolidate 自主實作）。同輪引擎可靠度淺審計：E4/E6 持續生效（07-18 起 scan 心跳僅 1 筆）、watchlist 32 筆健康（最早 07-24）、STATE 一致、journal append-only 判非債沿用、工作樹無孤兒——無新債。
- **S7**（難度選擇器收斂）：`HL.ui.segmented` 支援自訂 class + 可取消 onPick，towers/plinko/chicken 3 處手刻選擇器遷移（各自外觀零變化）；詞彙統一 簡單/普通/困難/專家 + chicken 難度鈕 i18n 從零補齊 — 2026-07-17（consolidate 自主實作）。同輪自適應淺審計：斷點守住 R4 階梯（480/560/720/1024/1280＋註記例外 760/860）、JS 零 media query、100vh 殘留皆為刻意 fallback——無新債。
- **U15**（i18n 引擎原文還原機制）：walker 翻譯前存原文 expando（text `__i18nOrig`／屬性 `__i18nOrigA`）、setLang 先 restore() 全 body 還原再重繪翻譯——掛 body 的持久浮動元件（panels/pip/live-stats/faucet pill…）切語系往返終於還原；observer characterData/attributes 分支清 stale 存檔防動態內容還原成舊文 — 2026-07-19（consolidate 自主實作，修法 a 中央修）。同輪模板化淺審計：T5–T9 收斂後複掃 `item/opt/cell/tile/pill/list` 類局部 helper 無新逐字重複——無新模板化債；**開新債 U16**（live-stats 浮窗六標籤字典零 key＝i18n 覆蓋缺口，U15 驗證中實測發現，🟦已批准待做）。
- **U14**（送出鈕 accessible name ×3 ＋ 收藏鈕 aria-pressed）：三個 `➤` 聊天送出鈕補 title（chat/AI 面板＋liveroom，walker 譯 title）、`HL.fav.button` 單一出口補 aria-pressed 建構＋切換同步；字典補「我的最愛」修復 fav title EN 漏譯 — 2026-07-19（consolidate 自主實作）。同輪 UI-UX-a11y 淺審計：PiP 頁籤鈕有動態 accname、½/2×/Max 文字可唸、reveal 🫧 遊戲面判非債；**開新債 U15**（持久浮動元件切語系往返不還原＝i18n 引擎缺原文還原機制，preview 驗證中實測發現，🟦已批准待做）。
- **U16**（live-stats 浮窗 i18n 覆蓋）：實作前複掃升級 scope——live-stats **全檔零字典覆蓋**，EN/zh-Hans 各補 14 鍵（六標籤+盈虧+標題+雙鈕+toast+資料來源 tag）；順帶補 streamer PiP 靜態標籤 3 鍵（虛擬主播/跟注 ▶/已跟注 ✓ 取消）；faucet/onboarding pill 覆蓋健全判無缺；EN/zh-Hans/zh-Hant 往返全過（U15 restore 相容）— 2026-07-20（consolidate 自主實作）。同輪自適應淺審計：固定寬絕對定位 popover 全集掃描——**開新債 R9**（💧返水下拉 250px right:0 錨定，389px 實測左溢出 -76px 被裁＝claim 鈕左段不可及；幣別下拉 ≤480 已修僅 rb 漏，🟦已批准待做）；cur/gfcur/lstat/pip 皆已有處理判非債。
- **R9**（💧返水下拉窄視口左溢出）：≤480 改 `position:fixed` 左右緣定位（比照 .ax-float 慣例、一行 CSS），389px 由 left=-76 → 全在視口、claim 鈕全寬可點；481–520 帶實測 left≥6 免擴斷點、桌機逐值不變、U12 Escape/aria 迴歸過 — 2026-07-20（consolidate 自主實作）。同輪引擎可靠度淺審計：E4/E6 持續生效（07-19 起 `intel(scan)` 心跳僅 1 筆＝當日首輪）、radar 依排程（02:34 檢查時尚未到 08:15 檔期）、STATE 一致、工作樹僅使用者 Game assets 雜訊無孤兒——引擎無新債；**驗證中實測發現開新債 R10**（header stacking context z=30 封頂兩個 z=70 下拉、被 z=55 浮動面板蓋住＝窄視口浮窗開啟時 claim 鈕不可點且誤觸關閉，🟦已批准待做）。
- **T10**（`makeDraggable` 拖曳 helper 抽 `HL.dom.makeDraggable`）：live-stats／game-frame PiP 兩份近逐字重複（已 drift：唯一差＝live-stats 鎖寬）收斂為單一出口、鎖寬改 `opts.lockWidth` 開關；`function makeDraggable` 全站歸 1（primitive）— 2026-07-20（consolidate 自主實作，sw v50）。**同輪模板化淺審計＝4-finder × adversarial-verify workflow（wf_487a53b9-0f9）**：19 候選 → **15 CONFIRMED**，開新債 **T10–T19**（helper 去重 makeDraggable/rnd→floatOr/dayNum·weekNum/statTile/inline-kv/table-panel/rules；死碼 endRoom/國戰 mock/CSS 大掃除 ~90 行）；4 筆正確反駁（rint 已有 HL.mock.rint／fmtMult 誤 scope 且瑣碎／duel 返回鍵 U8-tail 已刻意不抽／STAGE_BG 開關實由 CSS class 接線）；stake-picker（bounty/liveroom `ax-stake`）**由並行 firing 的 U17 涵蓋**不另開卡。並行協調：審計期間 firing 4c776dd 落地（U17–U20/E7），依 §7 重讀磁碟 DEBT.md 後才寫入避免覆蓋。
