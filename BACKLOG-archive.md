# ApexWin 分析師日誌歸檔（BACKLOG-archive）


- **2026-08-31（平台軌 · **20:00 窗** · 建置輪＝台帳輪替審「**後台**」8 模組＋修掉**量測工具自己的篩子**（註解／字串裡的提及被算成呼叫點）＋立常駐鎖 `platform/registry-sites-code-only`＋回填 4 筆被污染的讀數＋開卡 #153·claim `p-201015-c3f8`·純 `tests/`＋`intel/`＋`BACKLOG`·**零首屏位元組**·`sw` 不 bump）**
    - **① 進場**：`build_lock` 乾淨 `false`（遊戲軌 16:00 窗 `g-160830-b2e7` 已釋放，commit `91b4808`）→ claim `p-201015-c3f8` 並**當下單檔 commit**（`db3cc98`，落實 08-20 那次「只存在工作區的 claim 等於沒 claim」的教訓）→ 停頓做非寫入讀取後重讀 CONTROL，token 仍在＝claim 成功。dark **5.6h**（`last_platform_run_at` 08-31T14:36）<24h ⇒ 非 catchup；`lead_track=games` 下本可讓路，但 14:00 窗已明文把本窗指向台帳下一格、且本輪為零首屏成本工作 ⇒ **做而不讓路**。`ledger-card-sweep.js` 進場＋收尾**雙向皆 0 筆告警**。
    - **② 輪替格＝後台**（14:00 窗預告「前端UI/UX 或 後台」，按逐模組新鮮度取唯一最舊者：`後台` 08-28、`前端UI/UX` 已於 08-30 審過）。調研 LIVE overdue **0/35**（最近到期 09-02 thrill／mega-dice）⇒ 走 `idle_escalation` ①，而**本輪的真產出不是來自取材、是來自量測自審**，據實記下、不虛報調研成果。
    - **③ ⭐ 真產出＝我們用來取代手量的那把尺，犯的是與手量同一類的錯**。`tests/registry-probe.js` 的檔頭**從建檔第一天（08-26）就寫著**「口徑沿用專案硬規則**只認呼叫、不認提及**」，而**實作從來沒有做到**：它直接對整份原始碼跑 `HL.<ns>.register(` 正則 ⇒ 註解與字串裡的提及一律被算成呼叫點。實測污染 **10 個命名空間、共 17 筆**非程式碼命中，其中三筆直接讓台帳讀數錯，而且**全都是 08-31 14:00 窗（也就是前一輪的我）剛抄進台帳與 CONTROL 船長區的**：
      · `src/i18n/en.js:47`（**註解**）⇒ `HL.econCfg` 外部註冊者被報成 **15**，真值 **14**；
      · `core/reports.js:726`（**字串**）⇒ `HL.achievements` 多出一個**從未註冊過任何成就**的註冊者檔（**5→3**）；
      · `data/games-loader.js:4`（**註解**）⇒ `HL.games` 同上（它讀 `registry.json` 注入各 `game.js`、註冊是遊戲檔自己做的，**它本人一次都沒呼叫**，正是 §5 方案 B 的設計）（**26→24 個呼叫點／21 個檔**）。
    - **④ ⭐ 更嚴重的那一半＝分類邊界由一句註解決定（不只是數字醜）**。`sites.length > 0` 是 ①「有呼叫點」／②「檔內登記簿」的分水嶺，而 `edge`／`guild`／`progressSrc`／`selftest` **四支的唯一命中都是自己檔頭的用法示範** ⇒ 它們被歸進①「壞掉會在行為上現形」，真實 code 呼叫點是 **0**。而 `unproven` 棘輪**只算 ① 的成員** ⇒ **把一句檔頭註解整理掉（維護軌每輪都在做的事），就等於把一支擴充點移出棘輪射程，而所有既有斷言仍然全綠。** ⇒ CLAUDE.md §4「修一半而看不出來」在**量測層**的一例，與 14:00 窗〔功能／中央掛鉤〕那份「數字對、成員錯」的名冊同型（那次是兩個相反方向的錯互相抵銷）。
    - **⑤ 本輪最重要的取捨＝一版順手的修法被自己的實測推翻後撤回**。修完篩子後 ①／② 由 **14／10 變 10／14**，四支移到②就離開了 `unproven` 射程；直覺的修法是「把 unproven 的射程擴到②」，我做了、然後量了——**三支當場被判 unproven（`shop`／`sla`／`rg`），而三支的真因都是列舉器詞彙表 `ENUMERATORS`（ids/list/all/count/keys）收不到它們的列舉器**（`shop.catalog`／`sla.dims`／`sla.caps`／`rg.pauseOptions`）；同時 `dock` 被判「不是擴充點」，只因 `layout/` 不進 vm 沙箱而它明明有真實外部註冊者。⇒ **一邊三個誤報、一邊一個漏報**，硬擴射程只會製造假警報疲勞（同 08-30 20:00 窗「刻意不鎖正向」的判斷）。⇒ **撤回該版**、把取捨與撤回理由寫成 `registry-probe.js` 內的註解、開卡 **#153**（判準從清單式換成行為式，S·純 `tests/`·不卡 #118），並由新鎖的第 ⑤ 條先守住那四支「仍然證明得到」。
    - **⑥ 立鎖 `platform/registry-sites-code-only`**：主斷言**打在 fixture 上而不是真實檔案上**——「沒有非程式碼命中被算進去」在篩子修好後是**由構造成立**的，拿真實檔案斷言它等於恆真（本專案反覆記過的空心鎖）。fixture 同時放**三種提及**（行註解／區塊註解／字串）＋**一個真呼叫**，要求恰好回 1 筆 code；三種提及是**三個獨立失敗模式**（只擋行註解的半修版會被字串那筆抓到，反之亦然）。四道反向錨：篩子要答得出「有」也答得出「沒有」／全庫 `docMentions` 總數 ≥15（今天 17，改回原正則即歸零轉紅）／①②分類的結構一致性／四支移動過去的擴充點必須仍 node 或沙箱可驗。
    - **⑦ 既有鎖改守形狀、不放寬**（§10.1）：`platform/registry-extension-fail-closed` 原本鎖 `registries ≥ 14`／`internalOnly ≥ 10`，而**這兩個分項數字都是篩子搬得動的**；改鎖**總數 ≥24**（同一批擴充點只是換邊站，篩子搬不動總數）＋兩個分項各自保留下限防某一邊被掃成 0。
    - **⑧ 台帳審「後台」8 模組**（接續 14:00 的『擴充性』，全部回填 `last_audited=08-31`）：儀表板 `partial`（寫入面 `onClick|onInput|onChange` **仍恰 2 處**＝連九輪逐位相同；`HL.econCfg` 佔用 **13**＝連六輪）／CMS `partial`（`mock-data.js` 模組級 var **24**＝連五輪）／GameList `partial`（MANIFEST **20**＝連四輪；`registry.json` HEAD 仍 **1** 筆）／會員管理 `absent`（admin 類 view **0**＝連九輪·維持不開卡）／Bonus Builder `partial`（promoCal 註冊者 **7**＝連四輪）／經濟設定 `partial`（`game-rtp.ids()` **18**＝連四輪；#103 (c) 雙向不變量仍成立）／金流模式 `partial`（`jurisdiction` 外部讀者 **0**＝連八輪）／營運自動化 `absent`（**0 命中**＝連三輪·**維持不開卡**，理由本輪再次坐實：三個設定面都還沒有寫入端 ⇒ 先做規則引擎會做出一個沒有 action 可執行的容器）。
      · ⭐ **順帶釘正兩格的量測口徑**（同 08-28『台帳記數字必須連運算式一起記』，這次是反向：運算式在、但它本身會誤報）：(a) `grep -rl "HL.promoCal.register(" src | wc -l` 今天得 **8**，多出的那筆是 owner 檔自己的註解——舊 evidence 靠「（排除定義處）」這句**人工排除**擋掉，換一個無記憶的 session 用裸 grep 就會讀成漂移 ⇒ 權威口徑改為引用 `registry-gaps.js` 的 `external`；(b) 經濟設定那格的 row 數（08-28 記「60 row／35 strict」**沒附運算式、復現不了**）改為 vm 沙箱實跑 `HL.econCfg.all()` 逐表 `describe()` ＝ **14 表／64 row／43 strict（67%）**。
    - **⑨ 驗證**：node **315→316 全綠**；**負向擾動 7/7 CAUGHT**（P1 篩子整條還原成原正則／P2 只擋行註解的半修／P3 只擋字串的半修／P4 恆假〔真呼叫也判成提及〕／P5 `docMentions` 歸零／P6 guild 沙箱不再可驗／P7 篩子掃不到任何檔）。**P5／P6 僅本鎖轉紅＝鑑別力**；P1/P2/P3/P4/P7 另連帶 `registry-extension-fail-closed`（兩鎖共讀同一支探針、壞篩子會搬動①②計數 ⇒ **覆蓋重疊、非誤判**，據實記下）。擾動前先證基線非零退出能被正確解析（08-31 遊戲軌記過的坑：`run.js` 失敗會 exit 非零 ⇒ `execSync` throw，須捕 `e.stdout`）。還原後 316 全綠。
    - **⑩ [P-FS]／`sw`／counters**：首屏 **1638373／1638400 bytes、90 支 script＝與進場逐位相同**（餘裕仍 **27B**；本輪一支首屏 src 都沒碰，全部落在 `prototype/tests/`＋`intel/`＋`BACKLOG`）＝**連續第九個「逐位未動」數據點**。**`sw.js` 不 bump**（08-26 判準：本輪沒有任何檔會被送到使用者的瀏覽器）。`platform_cards_opened` **+1**；`platforms_researched` **不加**（未深挖平台檔）；`platform_cards_implemented` **不加**（落地物是鎖與工具修正，非佇列上的卡，不虛報）；`consecutive_idle_rounds` 維持 **0**。磁碟既存 `prototype/games/registry.json`(M)＋`src/styles/tokens.css`(M)＋`games/slot-engine/`(??)＋兩個 `Game assets/`(??) ＝session 起始快照即在（mtime 08-03／08-30），依 §7 **一位元組未碰**（待船長 [ENG-1]）。
    - **⑪ 下輪平台軌 09-01 08:00**：台帳輪替下一格＝**金流**（6 模組·08-29 最舊）；`platforms.json` 09-02 有兩站到期，可留給 09-01 20:00 或 09-02 窗；實作候選＝**#153**（本輪開的，S·純 `tests/`·零首屏）。**#118 未解前一律不動首屏 src。**

- **2026-08-31（平台軌 · **14:00 窗** · 建置輪＝台帳輪替審「擴充性」7 模組＋新增全庫首見模組（盲點第 7 例）＋立常駐鎖 `platform/central-hook-tolerates-absent-downstream`＋**更正一份連續四輪被讀成「零漂移」的名冊**＋開卡 #152 · claim `p-141215-a9c4` · 純 `tests/`＋`intel/`＋`BACKLOG` · **零首屏位元組** · `sw` 不 bump）**
    - **① 進場**：`build_lock` 乾淨 `false`（維護軌 12:00 窗 `m-120910-4f2a` 已釋放）→ claim `p-141215-a9c4` 並**當下單檔 commit**（`6f77f45`）→ 停頓做非寫入讀取後重讀 CONTROL，token 仍在＝claim 成立。dark **5.1h**（`last_platform_run_at` 08-31T09:05）<24h ⇒ 非 catchup；`lead_track=games` 下本可讓路，但本輪為零首屏成本工作 ⇒ 做而不讓路。船長待處理 [P-FS]／[ENG-1] 續掛，無新指令。台帳自我查核 `ledger-card-sweep.js` 進場＋收尾**雙向皆 0 筆告警**。
    - **② 輪替格更正**：前手（08-31 08:00）在 journal 預告「下輪＝前端UI/UX」，但按 SKILL 的輪替序（…資安→資料→**擴充性**）與逐模組新鮮度（`擴充性`／`後台` 同為 **08-28** 最舊；`前端UI/UX` 已於 08-30 審過）**下一格應為擴充性** ⇒ 本輪審擴充性，據實記下這處更正（不是跳格）。調研：`platforms.json` LIVE overdue **0/36**（最近到期 09-02）⇒ 依 SKILL 第 5 步 escape① 走「加深/擴大取材」，角度對準本窗格「擴充性」（2026 平台側材料一致把「模組化服務＋分階段推出＋地理圍籬旗標」列為標配；**這一輪取材沒有生出新缺口**，真產出來自台帳自審＋機械普查，據實不虛報）。
    - **③ ⭐ 真產出（一）＝台帳盲點第 7 例：流量最高的擴充點不是登錄表**。`grep "liveStats.register|liveStats.on("` ＝ **0 命中**；`registry-gaps.js` 列出全庫 **21 個** `HL.<ns>.register` 擴充點（games 26 個外部註冊者、econCfg 15、support 11…），**中央結算掛鉤不在其中**——22 個下游全部硬寫在 `core/live-stats.js:22–58`（第 47 行一行 11 支）。為什麼台帳先前問不到：`[功能]` 那一格問「匯流點存不存在」（present，兩條鎖在守），`[擴充性]` 七格沒有一格問「**這個擴充點本身是不是資料驅動的**」。⇒ 新增模組「中央結算掛鉤的擴充形制（訂閱者登記簿）」(absent)、開卡 **#152**。**為什麼是現在**：[P-FS] 餘裕 **27B**，而一行 `if (HL.x) HL.x.record(bet);` 約 30–40B ⇒ 這個擴充機制在 #118 解鎖前**實質關閉**（08-15 evidence 寫的「後來的卡自己掛上來、不必改架構」在位元組層面已不成立）。
    - **④ ⭐ 真產出（二）＝更正一份「數字對、成員錯」的名冊，而它連續四輪被讀成零漂移**。台帳〔功能／中央掛鉤〕自 08-15 起記「下游訂閱者 22」，08-21 還把尺寫下來（掃**全檔** `HL.<ns>` 扣掉 dom/ui/base/auth 四個「基礎設施」）、08-29 據此覆核仍為 22 並記為「逐位相同」。本輪機械複量：`record()` 函式體（去註解）內的 `HL.<ns>` 也是 **22 個，但成員不同**——舊尺 (a) 把 `base`（＝`core/meta.js` 基地層，第 47 行真的餵它 `HL.base.record(bet)`）當基礎設施**扣掉**；(b) 把 `share`（第 116 行、面板分享鈕，**與結算無關**）**算進來**。**兩個錯誤剛好互相抵銷** ⇒ 數字凍在 22，而「零漂移」被當成尺可信的證據。⇒ §4「修一半而看不出來」在**量測層**的一例（數字對、成員錯，而數字是唯一被看的東西）。**修法不是再量一次**：`tests/checks-platform.js` 的 `FANOUT_ROSTER`（08-27 起，實跑真檔觀測誰被呼叫）**早就是對的**（含 `base`、不含 `share`），只是台帳從沒向它求值 ⇒ 本欄此後一律引用那條鎖的名冊（同 08-31 08:00 立的紀律：**向鎖求值、不抄數字**）。
    - **⑤ 真產出（三）＝新常駐鎖 `platform/central-hook-tolerates-absent-downstream`（互補面，非雙胞胎）**：既有 `central-hook-fanout-roster` 只跑 `runLiveStats({})`＝**所有下游都在**的那一種世界 ⇒ 「每個用法都與存在性守衛同行」這條**只由 22 個呼叫累積出來的慣例**在機械上完全沒有網。少寫一次守衛不是「那一格失效」，是 `record()` 當場 throw ⇒ **它之後的所有下游整局失守**（帳本記了 bet、注單沒落地、任務沒推進），而各遊戲 view 沒有 try/catch 會一起斷；demo 站模組全在 ⇒ 永遠測不出來。新鎖以**同一支 harness**（不另造第二把尺）逐支 **one-out omit 22 支**實跑，驗三件事：缺席那支**不得**再被呼叫（omit 真的生效）、其餘各支**必須**全到（不得連坐）、`record()` **不得** throw；並把 `omit` 射程補到先前被無條件提供的 `edge`（pull 型，守衛與別人一樣脆）。
    - **⑥ 驗證／負向擾動**：node **313→314 全綠**。**負向擾動 6/6 CAUGHT**：P1 拿掉 `heat` 的存在性守衛（→ 缺席即 throw）／P2 harness 對 `edge` 的 omit 失效（→ 驗到假世界）／P3 清空 `progressSrc` 退化路徑例外清單／P4 `omit` 整個變 no-op（→ 反恆真錨③ 抓到「同一個世界跑 22 次」）／P5 `FANOUT_MODS` 縮水一支（→ 反恆真錨② 抓到射程縮小）／P6 `betlog` 的守衛守錯命名空間（→ 連坐）。**P1／P2／P3／P6 僅本鎖轉紅；P4／P5 另連帶 `platform/central-hook-fanout-roster`**（兩例破壞的是共用 harness ⇒ 覆蓋重疊、非誤判，據實記下不粉飾）。還原後 314 全綠、`live-stats.js` 逐位回到 HEAD。
    - **⑦ 台帳審「擴充性」7 模組（輪替接續 08-31 08:00 的『資料』）**：Dockable Layout 維持 `partial`（`HL.dock.register` **3 處/2 檔**、註冊者仍 partner/chat/growth 且仍惰性 ⇒ 開站 `ids()` 回 `[]`）；設計系統 維持 `present`（`HL.ui` 出口 **19** ＝與 08-15 基準同）；自動化測試 維持 `partial`（313→**314**；本輪新形制見 ⑤）；首屏分割 維持 `present`（可回收 **0.0KB**、bound 3／shared-dep-blocked 3／boot-registration-blocked 0 ＝零漂移）；上架排程 維持 `present`（`AUDIENCES` 仍單一來源、四消費端皆走 `HL.release`）；Feature Flags 維持 `absent` **且維持刻意不開卡**（`featureFlag|feature_flag|HL.flags` 仍 **0 命中**）；大廳版位登記簿 維持 `absent`（`views/casino.js:150–170` 仍逐行硬寫，#139 一字未動）。**＋新增第 8 個模組**（見 ③）⇒ 台帳 66→**67** 模組。
    - **⑧ [P-FS]／sw／counters／未碰他人工作**：本輪只動 `prototype/tests/checks-platform.js`（**不出貨**）＋`intel/`＋`BACKLOG*` ⇒ 首屏**一支 src 檔都沒碰**；**`sw.js` 不 bump**（照 08-26 判準：本輪沒有任何檔會被送到使用者的瀏覽器）。`platform_cards_opened` **+1**；`platforms_researched` 不加（主題式加深取材、未深挖平台檔）；`platform_cards_implemented` 不加（本輪未實作卡，落地的是鎖）；`consecutive_idle_rounds` 維持 **0**。磁碟既存 `prototype/games/registry.json`(M)＋`src/styles/tokens.css`(M)＋`games/slot-engine/`(??)＋兩個 `Game assets/` ＝ session 起始快照即在，依 §7 一位元組未碰（待船長 [ENG-1] 裁決）。**下輪平台軌 08-31 20:00**：台帳輪替下一格＝**前端UI/UX**（`後台` 同為 08-28 最舊，可視 `next_due` 到期情況調整）。

- **2026-08-30（平台軌 · **20:00 窗** · 建置輪＝把台帳自我查核工具補成**雙向**（新增反向錨）＋立常駐鎖 **`platform/ledger-card-anchor-bidirectional`**＋回填它當場掃出的 3 筆斷點＋台帳輪替審「**資安**」8 模組＋開卡 #150 · claim `p-201230-e7a9`）**
    - **① 進場**：`build_lock` 乾淨 `false`（遊戲軌 08-30 16:00 窗 `g-160538-e468` 已釋放，commit `c308015`）→ claim `p-201230-e7a9` 並**當下單檔 commit**（`4d3ed27`）→ 停頓做非寫入讀取後重讀 CONTROL，token 仍在＝claim 成立。dark **5.6h**（`last_platform_run_at` 08-30T14:35）< 24h ⇒ 非 catchup；`lead_track=games` 下本可讓路，但前輪 14:00 窗已明文把本窗指向「`ledger-card-sweep.js` 反向錨」⇒ 做而不讓路。船長待處理 [P-FS]／[P-I18N3]／[ENG-1] 續掛，無新指令。
    - **② 調研（LIVE overdue **0/36**，最近到期 09-02）⇒ 依 SKILL 第 5 步 escape① 走「加深/擴大取材」而非到期刷**，取材角度對準本窗台帳輪替格「**資安**」（比照 14:00 窗對準「活動」）。三則淨新，**全部用來補強既有卡、一張新卡都不因它而開**：(a) Crypto.com 官方 Help Center 明載**新加白名單的提款地址 24 小時內停提** ⇒ #137 的「新增後冷卻」**有一手實作可對照**，非我方推想；(b) 2026 平台側材料把 **Device History＋玩家自助處置**列為標準安全功能 ⇒ #125 首批四項的形制**取得第二個獨立來源**（此前僅 Stake 一家）；(c) Stake Help Center 直接讀到 `Ghost Mode` 定義（「不出現在公開動態與遊戲預覽」）⇒ #127 的形制**從二手評測升為一手口徑**。
      · **一則據實不追的判定**：MGA 2026 主題審查點名「自我排除跨品牌失效」，本庫 `responsible.js:35` 明載限額走 `HL.dom.lsGet/lsSet`＝**demo/live 各自一份**，形狀相同。但**有害方向不成立**：真站的排除存在真站命名空間，玩家要玩真錢就得切回真站、切回去就讀得到那把鎖；切去假站只能玩假錢。⇒ **記為「形狀相似但不構成缺口」，不開卡**。
    - **③ ⭐ 本輪真產出（一）＝把一支只有單向的工具補成雙向，而缺的那一向是它結構上永遠掃不到的**：`intel/tools/ledger-card-sweep.js` 原本只走 **evidence → 卡**（evidence 寫了 `#N` ⇒ 去看 #N 的狀態）。這個方向對兩種情形**結構免疫**：**evidence 沒寫**、與 **evidence 寫錯卡號**。前者本輪首跑當場掃出 **3 筆**（#90 後台·已✅／#106 功能·已✅／#137 資安·⬜——三張卡都明寫自己來自某台帳分類，而**該分類沒有任何模組記得它們**）；後者正是 08-30 08:00 窗真的發生過的那筆（evidence 寫「⇒ 開卡 #147」、實開 #148，當下兩張都 ⬜ ⇒ **零告警**，而等 #147 先落地時工具會叫我們把主題那條線標成「已關閉」，**它卻一個字都沒接**）。新增 `reverse()`：**卡自己宣告**來自台帳某分類時，該分類至少要有一個模組 evidence 提到 `#N`；沒指名分類的（13 張）**無錨可比、不告警**（寧漏報不誤報）。
    - **④ 真產出（二）＝那把尺 + 三筆回填**：立常駐鎖 **`platform/ledger-card-anchor-bidirectional`**（主斷言＝未回指筆數 0；**四道反向錨**防靜默轉綠：卡表解析 ≥50 張／分類 ≥8 個／可比對宣告卡 ≥20 張／比對子必須答得出「沒有」——且用 **`#999`**（恆真壞法）與 **`#14`**（**前綴**壞法：它是 #140/#144/#148 的前綴，少了 `(?!\d)` 就會誤中）**兩個哨兵**分別打兩種壞法）。三筆回填據實寫成兩類：#137 是**未來會高報**（卡未落地、evidence 從未寫下出口）；#90／#106 是**傳承斷點**（缺口早被自己的卡關掉、台帳只記形狀不記出口，**不改判定、不動 `last_audited`**）。
    - **⑤ 驗證**：node **308→309 全綠**；**負向擾動 7/7 CAUGHT，7 例全部僅本鎖轉紅**（P1 撤掉 #137 回填／P2 比對子恆真／P3 拿掉 `(?!\d)` 前綴誤匹配／P4 卡首行解析歸零／P5 分類讀不到／P6「宣告來自台帳」判準退化成空集合／P7 反向掃描 rows 恆空）。⚠️ **刻意不鎖正向**：正向的「已回填？」是關鍵詞啟發式、誤報率不為零（工具檔頭記過首跑 3 筆中 1 筆誤報），鎖它會製造假警報疲勞；反向錨是精確比對（有沒有 `#N` 這個字串），才適合當常駐鎖。
    - **⑥ 台帳審「資安」8 模組（輪替接續 14:00 窗的『活動』）**：五個模組的權威樣式**逐位可重現**（真/假站軸 83處/36檔＝連兩輪；反詐欺 0 命中＝連十輪；KYC 4 命中＝連五輪；rg 閘 27/21、自我排除 41/4 皆與 08-27 相同）。**釘正一個會逐年失真的量測法**：`is_ops_admin()` 裸 grep 今天得 **4**，而 08-22 以來記的「2 個呼叫點」指的是**呼叫**——多出的兩筆是函式定義與 `grant execute` ⇒ 往後量呼叫點用 `not public.is_ops_admin()` 這個形狀（與 08-27「收斂讓 `isLive()` 讀數下降」是同一類代理指標病、方向相反）。
    - **⑦ 開卡 #150（守 `max_cards_per_run`，只開 1 張）**：責任博弈**面板是完整的**（BC.GAME 2026 整套工具集逐項全備、對照零新缺口），缺的是所有工具共用的那條「**範圍**」軸——Stake 把 `Casino Exclusion`／`Poker Exclusion` 列為與帳戶級排除**並列**的獨立工具，而本庫 `HL.rg.check()` 的 **25 個真實呼叫點全部只傳一個數字**、簽名裡**根本沒有遊戲身分** ⇒ 「只關掉一類遊戲」架構上做不出來。與 #149 是同族的另一半（那張是「傳了沒看」、這張是「從來沒傳」），**修法與回歸面不同級故刻意分卡**。
    - **⑧ [P-FS]／sw／counters**：本輪只動 `prototype/tests/checks-platform.js`（不出貨）＋`intel/`＋`BACKLOG*` ⇒ 首屏實測 **1638373／1638400 bytes、90 支 script，與進場逐位相同**（第 N 個「逐位未動」數據點·餘裕仍 27B）；**`sw.js` 不 bump**（照 08-26 08:00 窗判準：本輪沒有任何檔會被送到使用者的瀏覽器）。`platforms_researched` **不加**（本輪未深挖平台檔、走的是主題式加深取材）、`platform_cards_opened` 130→**131**、`platform_cards_implemented` **不加**（落地的是常駐鎖與工具，無卡轉 ✅）、`consecutive_idle_rounds` 維持 **0**、yield/stalled 不增。孤兒 `prototype/games/registry.json`(M)＋`games/slot-engine/`(??)＋兩個 `Game assets/`(??)＋`tokens.css`(M·別軌) 依 §7 **一位元組未碰**。
    - **下輪（08-31 08:00）**：LIVE overdue 仍 0（最近 09-02 mega-dice／thrill）⇒ 續走加深取材；台帳輪替下一格＝**資料**（4 模組·08-27 最舊）或**擴充性**（08-28）；實作候選①續走零首屏成本類（`tests/`＋`intel/`）②**#118 未解前一律不動首屏 src**（餘裕 27B）。
- **2026-08-30（平台軌 · **14:00 窗** · 建置輪＝立常駐鎖 **`platform/central-hook-game-arg-consumed`**＋台帳輪替審「**活動**」7 模組＋釘正一個錯的卡號引用＋開卡 #149 · claim `p-141520-c7e2`·帶心跳 14:15→14:31·進場鎖乾淨 `false`·未奪鎖·**零首屏位元組**·`sw` 不 bump）**
    - **① 進場**：`build_lock` 乾淨 `false`（維護軌 08-30 12:00 窗 `m-120910-bf2c` 已釋放，commit `150f22d`）→ claim `p-141520-c7e2` 並**當下單檔 commit**（`275e316`，落實 CLAUDE.md §10「前景/排程 claim 必須當下 commit」）。dark **~3.9h**（`last_platform_run_at` 08-30T10:12）< 24h ⇒ 非 catchup；`lead_track=games` 下本可讓路，但 gamdom／duelbits **同日到期**且為前輪 08:00 窗明文點名留給本窗者 ⇒ 做而不讓路。船長待處理 [P-FS]／[P-I18N3]／[ENG-1] 續掛，無新指令。
    - **② 調研（守 `max_platforms_per_run: 2`）＝gamdom／duelbits 到期當日刷**，`last_investigated=08-30`／`next_due=09-29`；刷完 **LIVE overdue 0/36**。取材角度刻意**對準本窗台帳輪替格「活動」**（比照 08-30 08:00 窗對準「前端UI/UX」），只挖活動/促銷層，不重抄 07-31 已挖到零開卡的留存/rakeback 線。
      · **一則據實不追的判定**：gamdom 的 King of the Hill 對外口徑本輪為月 $1M＋日 $10k（07-31 記月 $500k／日上限 $30k）⇒ 判為**行銷數字的季度調整而非機制變化**，本檔只追「日/月雙節奏」這個機制。同理 duelbits 的「Ace Rewards 上限 60%」與 07-31 的「四桶各 up to 12.5%」是不同口徑、不衝突、不追。**這是為了避免每輪重抄一個會變動的數字、把它累積成假訊號。**
    - **③ ⭐ 本輪查獲＝§4「修一半而看不出來」家族第 ⑧ 例，而這一例兩端都在、線也接好了**：中央點 `live-stats.js` 的 `record(game,bet,win)` 把**裸 `game`** 當頂層引數交給 **8 支**下游，其中 **7 支真的讀它**，**只有 `challenges.record` 是 0**——`core/challenges.js` 全檔 `game` 僅 2 次命中（檔頭註解＋簽名），**函式體內零次**。⇒「這個挑戰只在 slot 算」「本週指定遊戲命中 25×」這一整類形制**架構上做不出來**，而**簽名看起來是完整的**、每個既有驗證面都是綠的（扇出名冊 ✅／引數值鎖 ✅／record-once ✅／console 零錯誤／挑戰照常達成照常發獎）。
      · **前例佐證這不是假想風險**：#89 修的是**同一條線的另一半**——當年 `HL.bonus.onWager` 是這掛鉤上「最後一個**收不到** `game` 的大消費端」。#89 接上之後，下一種形狀就是**收到了卻不讀**，且更難看見。
      · **五站共識支撐**（非單站新聞）：Duelbits（monthly challenges **for selected games**）／Gamdom（**Game of the Week**）／Shuffle（multiplier-based Challenges，一次大命中決勝）／Gambulls（18 條週挑戰，across the most popular titles in the lobby）／CryptoGames（Weekend Challenge Multigame）。
    - **④ 產出＝那把尺（純 `tests/`·零首屏位元組）**：常駐鎖 **`platform/central-hook-game-arg-consumed`**。射程是**掃出來的地面真相**不是硬寫檔名：從 `record()` 本體（去註解去字串）掃出誰收到裸 `game` → 由 `HL.<ns> =` 解定義檔 → 由 `HL.<ns> = { <fn>: <內部名> }` 解**匯出別名**（`onWager: bOnWager`／`accrue: rbAccrue` 兩支就是靠這步才找得到實作）→ 取同位置形參數讀取次數。與宣告名冊做**雙向等式**（多一個孤兒＝紅；#149 落地卻沒把 `challenges.record` 移出免罪名單＝也紅）。**解析失敗一律當場轉紅**（不得靜默縮小射程，08-17 教訓）。尺自身七條反向錨：不讀/有讀兩面、**註解不算讀**、**字串不算讀**、**`gameId`／`games`／`nextGame`／`o.game` 不得誤匹配**、別名解析恆真/恆假兩面。node **306→307 全綠**。
    - **⑤ 負向擾動 7/7 CAUGHT，每例僅本鎖轉紅**：P1 修好 challenges 但沒改名單／P2 新孤兒（heat 改成不讀 game）／P3 讀取偵測恆真／P4 恆假／P5 拿掉字界防撞（子字串誤匹配）／P6 別名解析失效（射程被靜默縮小）／P7 中央點不再把 `game` 傳給 challenges。**P1 順帶產出一個實測數據點**：只往 `record()` 插**一行** `if (game === "__never__") return;`，`platform/first-screen-budget` **當場轉紅** ⇒ #149 的首屏阻塞是**被證明的、不是估的**（見 ⑦）。
    - **⑥ 台帳（活動 7 模組全審）＋一個台帳自我查核的真產出**：季票／世界活動／抽獎-rain／推薦／新手引導／遊戲內活動掛件 六個維持原判、依 08-17 自訂紀律**一行帶過**（世界活動兩道真站閘行號 `lobby.js:30`／`:187` **本輪零位移**＝08-27 那條「用行號當錨的代價」註記的第一次複驗；推薦本輪起改記 **bytes**〔22,263／17,794〕以免每輪重抄一個會四捨五入的 KB 數）。促銷框架維持 partial（`registry-gaps.js` 重量外部註冊者 **7**，零漂移），**本輪的缺口據實記在「功能/挑戰與任務」側而非本模組**——因為缺的不是排程容器（`promoCal` 已在位），是**挑戰引擎的遊戲維度資格閘**。
      · ⭐ **釘正一個錯的卡號引用**：08-30 08:00 窗在「外觀/主題模式」模組的 evidence 寫「⇒ 開卡 **#147**」，但實際開出的是 **#148**（#147 是 08-29 20:00 窗開的登錄表語意卡）。**`ledger-card-sweep.js` 對這種錯免疫**——它只做**單向**比對（evidence 的「開卡 #N」→ BACKLOG 第 N 張狀態），而兩張卡當下**都是 ⬜待批准** ⇒ 解析得出、狀態合理、**零告警**。後果不是今天的：等 #147 落地 ✅ 而 #148 沒落地時，工具會指示「本模組缺口已被 #147 關閉、請回填」——**而主題那條線其實一個字都沒接**。⇒ **缺的是反向錨**（卡的「來源」欄要指得回同一個模組：#148 寫「來源：台帳『前端UI/UX』」對得上，#147 寫「來源：#135 落地當…」對不上）。已釘正 evidence，**工具的反向錨列為下一輪候選**（本輪守 `max_cards_per_run` 與「一次只徹底做完一張」，不順手多開）。
    - **⑦ [P-FS]·第十四個「逐位未動」數據點**：`git status` 對 `prototype/index.html`／`prototype/src/`／`prototype/sw.js` **三者皆零改動**（負向擾動全數還原並複跑驗證 307/307）⇒ 首屏實測 **1638373／1638400＝餘裕 27 bytes**，與進場逐位相同。`sw.js` **刻意不 bump**（本輪動到的 `tests/`／`intel/`／`BACKLOG.md` 都不會送到玩家瀏覽器——照 08-26 08:00 窗訂下的判準）。
    - **⑧ counters／收尾**：`platforms_researched` 112→**114**、`platform_cards_opened` 129→**130**；**`platform_cards_implemented` 不加**（本輪落地的是常駐鎖，沒有任何 BACKLOG 卡轉 ✅完成）；`consecutive_idle_rounds` 維持 **0**（真產出）。`yield/stalled` 不增。孤兒 `prototype/games/registry.json`(M)＋`games/slot-engine/`(??)＋兩個 `Game assets/` 目錄 mtime 08-03 依 §7 **一位元組未碰**（待船長 [ENG-1] 裁決）；`prototype/src/styles/tokens.css` 工作區有 **CRLF-only** 差異（`git diff` 淨空、內容逐位等於 HEAD＝08-30 12:00 窗維護軌那筆 autocrlf 殘留），**不入本輪 commit**。
    - **⑨ 下輪平台軌 08-30 20:00**：`platforms.json` LIVE overdue **0/36**（最近到期 09-04）⇒ 調研可走「加深/擴大取材」而非到期刷；台帳輪替下一格＝**資安**或**資料**（皆 08-27，最舊）；實作候選＝① `ledger-card-sweep.js` 的反向錨（見 ⑥，純 `intel/`、零首屏）② 續走零首屏成本類。**#118 未解前一律不動首屏 src。**
- **2026-08-29（平台軌 · **20:00 窗** · 建置輪＝**#135 落地**：把 #145 的 vm 沙箱一般化成**第三個「證明得到」的環境**，全庫 `unproven` 歸零＋立常駐鎖 `platform/guild-registry-provable` · 台帳審「**功能下半**」8 模組（**全數零漂移**）· 開卡 **#147** · claim `p-201230-e7a9` · 帶心跳 20:12→21:05 · 進場鎖乾淨 false · 未奪鎖 · **首屏逐位未動（餘裕 27 bytes）** · `sw.js` 不 bump）**
    - **① 進場**：`build_lock` 乾淨 `false`（遊戲軌 08-29 16:00 窗 commit `020d932` 釋放）→ claim `p-201230-e7a9` → **當下即單檔 commit `32feea7`**（§10 紀律）→ 重讀確認 token 仍在。node 進場基線 **301/301 全綠**。`ledger-card-sweep` **⚠️ 0 筆**。dark **~5.5h**（`last_platform_run_at` 08-29T14:40）<24h ⇒ 非 catchup；`lead_track=games` **本可讓路**，但前手 14:00 窗明文指派三項（台帳輪替下一格＝**功能下半 8 模組**／到期平台若為 0 則走加深取材／實作候選走零或負首屏成本類）⇒ **做而不讓路＝跨輪指派連續第二十輪**。船長待處理區無新指令（[P-FS]／[ENG-1] 續掛）。
    - **② ⭐ 本輪的發現不是新缺口，是一條**過期的阻塞事實**——而且它過期的方向是「成本被**高估**」**：`HL.guild.register` 自 08-26 起被記為「全庫唯一兩個環境都無法證明」，脫離之道寫成「加 `module.exports`」＝首屏位元組 ⇒ #135 標「卡在 #118」，**連續四輪台帳審照抄這個結論**。
      但那個成本模型在**同一天 14:00 窗**就失效了：#145 為了驗「同 id 再註冊」建了 **vm 沙箱**，而沙箱裡的 `window` 是我們自己造的物件 ⇒ 「`guild.js:295` 頂層取用 window」不再是障礙，`module.exports` 那一行**根本不需要**。⇒ #135 的真實首屏成本＝**0 bytes**，同窗落地。
      ⭐ **紀律（本輪真正的產物）**：08-24 記過「零首屏成本 ≠ 不加 script，註解也是位元組」＝成本被**低估**；本輪是**同一條軸的反方向**——**卡片與台帳上的「阻塞事實」跟 evidence 一樣會過期，而高估同樣有代價**（一張 S 卡被誤判受阻四輪）。⇒ **新工具落地時要回頭問「它讓哪些卡的前置條件失效了」**，已寫進 `registry-probe.js` 的 `boot()` 檔頭與本卡。
    - **③ 落地內容（全部落在不出貨的 `tests/` 與 `intel/`）**：`tests/registry-probe.js` 新增 `boot(files)`（通用 vm 沙箱·最小 shim）／`sandbox()`（唯讀共用快取）／`freshSandbox()`（給會寫入的測項，避免污染快取）／`coreScripts()`；**首屏 script 清單的單一真相＝`index.html` 本身**（不抄第二份，否則新增 script 時沙箱會靜默漏掉而斷言仍全綠）。**實測首屏核心 76 支全載入、0 失敗、一次 ~80ms**。分類由兩格擴為三格（+`sandboxVerifiable`），`UNPROVEN_BASELINE` `["guild"]` → **`[]`** ⇒ 既有零成長棘輪**變嚴不是放寬**。`intel/tools/registry-gaps.js` 多印「沙箱可驗」欄與沙箱載入摘要。
    - **④ 立鎖與反向錨（CLAUDE.md §4 三個自問）**：`platform/guild-registry-provable`＝(a) 規模自保：種子須恰為 **6** 公會（08-18 brace-matching 權威值，本輪機械複驗成立）(b) 外部註冊一筆 → `ids()`／`status().count`／`totalGuilds` 三出口同步 6→7 (c) **消費端錨＝走玩家路徑**：`join()` → `status().guild.name` 解析到新註冊那一筆＋結構錨釘住 `guildBrowser()` 走的就是同一個 `GUILDS`（**只斷言 `ids()` 變長擋不住「`ids()` 是一份裝飾性平行清單」**）(d) 反向錨①壞 spec 5 種全拒 (e) 反向錨②**釘住「先到先得」語意** (f) 棘輪錨＝guild 不得再被加回免罪名單。另在既有鎖補**沙箱自保**三條——否則沙箱靜默壞掉時 `sandboxVerifiable` 整排變 false，錯誤訊息會指向「新的無法證明擴充點」而**把人帶錯方向**。
    - **⑤ 驗證**：node **301→302 全綠**；**負向擾動 7/7 CAUGHT**（P1 移除 `|| byId[spec.id]` 先到先得守衛／P2 register 不再要求 `spec.id`／P3 `ids()` 改回硬寫平行清單／P4 `guildBrowser` 改抄快照／P5 把 guild 塞回免罪名單／P6 首屏有一支在沙箱裡會拋／P7 `join()` 不再解析 byId）；擾動後 `guild.js`／`edge.js` 以 `git status` 驗零殘留，收尾 `git diff` 僅本輪三支檔。
    - **⑥ 台帳「功能下半」8 模組＝全數零漂移，據實一行帶過（不把「穩定未變」寫成每輪新發現）**：會員/身分 present（`HL.auth.` 20 檔／73 次·四種登入路徑全命中）／PWA present（`sw.js` v244）／通知 present（`notify.add(` 23 檔／37 點·逐位相同）／支援中心 present（203 行·外部註冊者 10 檔／11 次·逐位相同）／社群共決 absent（#116 續卡 #118）／通知分類軸 partial（`notifyPref|通知偏好|registerCat` 仍 0 命中）／收集套組 absent（`HL.collection|圖鑑` 0 命中）／公會 partial（**本輪唯一實質更新，見 ②–④**）。
    - **⑦ ⭐ 三個尺陷阱（同 08-29 14:00 窗「兩個數字不合時，錯的往往是尺」家族）**：① `guild.js`／`support.js` 用 `split("
").length` 得 296／204，看似各 +1，實為**結尾換行**造成——`wc -l` 得 295／203，與舊記載逐位相同 ⇒ **行數一律以 `wc -l` 為準**。② `grep -rln "HL.guild."` 得 **5 檔**看似成長，其中一檔是 `guild.js` **自我引用** ⇒ 真實消費端仍 **4**（與 08-29 14:00 窗 liveStats 自我引用同型）。③ 社群共決 `HL.poll|提案|投票` 得 **1 命中**（08-26 記 0），逐筆確認為 `demo-tools.js:3` 的**註解**「提案展示用」＝誤報，status 不動。
    - **⑧ 開卡 #147（⬜待批准·純 `tests/`·零首屏）**：沙箱一般化後順手對全部 21 個 `HL.<ns>.register` 做機械普查 ⇒ **三種互斥的「同 id 再註冊」語意並存**：**A 先到先得 7 支**（guild／achievements／shop／reports／gameAxes／rbac／reload）、**B 整筆取代 13 支**（support〔外部註冊者最多的 11〕／econCfg／promoCal／progressSrc／scoreAxis／bonusTtl／wagerScope／rg／sla／selftest／content／i18n／edge）、**C 疊上本次宣告的欄位 1 支**（games，**08-29 才變成 C**）。**B 族有出貨紀錄**——#145 就是 B 的實例（洗掉開發者暱稱、Leo 整個人從大廳消失）。兩族失效方向**相反且都靜默**：A＝該生效的更新沒生效（且**沒有任何呼叫端在檢查回傳值**）、B＝不該消失的欄位消失了。**卡刻意不主張統一**（三種各有正當用途，5 支在註解裡明寫過是刻意的）——問題在**沒有宣告、也沒有測項守著**，任一輪「順手統一」的重構都能靜默翻轉其中一支。
    - **⑨ 調研＝0 個平台到期，據實不虛報**：`platforms.json` LIVE 逾期 **0/36**（wow-vegas／shuffle／gamdom／duelbits **08-30** 到期＝明日）。本輪**無 dossier 深挖**，`platforms_researched` **不加**。未走 idle_escalation ①「加深取材」——因為本輪有真建置產出（非閒置），`ban_busywork_heartbeat` 的適用前提不成立。
    - **⑩ [P-FS]／sw／counters／未碰他人工作**：收尾首屏 **1638373／1638400＝餘裕 27 bytes、90 支 script，與進場逐位相同**（做法＝**連一支首屏 src 檔都沒碰**，第六個「逐位未動」數據點）。`sw.js` **不 bump**：機械複查 `grep -c "tests/" prototype/sw.js` ＝ **0**、PRECACHE 只有 `./`／`index.html`／`manifest.webmanifest`／`icon.svg` ⇒ 本輪動的檔**永遠不會送到使用者的瀏覽器**（08-26 判準）。`platform_cards_opened` **+1**（#147）／`platform_cards_implemented` **+1**（#135）／`platforms_researched` **不加**／`consecutive_idle_rounds` 維持 **0**。⚠️ **未碰他人工作**（§7）：`prototype/games/registry.json`(M)、未追蹤的 `Game assets/001_Shadow Ritual`／`002_小雞過馬路`、`prototype/games/slot-engine/`(??)——一律一個位元組未碰。
    - **⑪ 下輪（08-30 08:00）**：wow-vegas／shuffle／gamdom／duelbits **四站同日到期** ⇒ 取 `max_platforms_per_run: 2` 深挖兩站（建議 priority 較高者優先），並**用新增的第 8 條取材維度**（創作者/工作室做為瀏覽軸與目的地）第一次真正對照對手形制，回填 #146；台帳輪替下一格＝**活動**；首屏餘裕 **27B** 仍吃緊，續走零／負成本類（#147 本身即零首屏，可直接接）。

- **2026-08-29（平台軌 · **08:00 窗** · 建置輪＝**#144 落地**：查獲並修掉一個**活的**送幣成本歸屬缺陷＋立常駐鎖 `platform/bonus-add-source-attribution` · 台帳審「**金流**」6 模組 · toshi.bet 到期深挖（取得 Vault 第二來源）· claim `p-081010-5e2b` · 進場鎖乾淨 false · 未奪鎖 · **首屏淨 −26B**）**
    - **① 進場**：`build_lock` 乾淨 `false`（維護軌 08-29 00:00 窗 commit `ef44c53` 釋放）→ claim `p-081010-5e2b` → **當下即單檔 commit `a344922`**（§10 紀律：前景 claim 必須當下 commit）→ 重讀確認 token 仍在＝claim 成功。node 進場基線 **297/297 全綠**。dark ~11.1h（`last_platform_run_at` 08-28T21:05）<24h ⇒ 非 catchup；`lead_track=games` **本可讓路**，但前手 20:00 窗明文指派三項（toshi-bet 到期深挖／台帳輪替下一格＝**金流**／實作候選走零首屏成本類）⇒ **做而不讓路＝跨輪指派連續第十八輪**。船長待處理區無新指令（[P-FS]／[ENG-1] 續掛）。
    - **② ⭐ 本輪查獲的缺陷，形狀是「漏斗保證了總額，於是沒有人再問歸屬」**：紅利側記帳是**單一漏斗**——17 個發放模組全部呼叫 `HL.bonus.add()`，由 `progress.js` 的 `badd()` 統一 `HL.ledger.record("bonus", …)`。這個設計很好，**「有沒有記到帳」架構上不會漏**。
      但 `badd()` 記的是 `(opts && opts.source) || "其他紅利"` ⇒ **漏傳 `source` 的呼叫端，錢照樣進總額，只是被靜默併進雜項桶**。實測 `HL.bonus.add(` **19 呼叫點／17 檔：18 帶 source、1 沒有**＝`core/rewards.js:433` 連登里程碑大禮（額度 3,000／8,000／15,000／**50,000**，不是零頭），而同一 `claim()` 函式**往上兩行**才剛為日獎寫過 `{ source: "每日簽到" }`。**慣例是真的、這裡是 drift。**
    - **③ 為什麼它屬 §4「修一半而看不出來」的最乾淨形狀**：玩家拿到的錢正確、餘額正確、`🏅 連登里程碑` 通知正確、帳本**總額**正確、儀表板照樣渲染出一個**看起來合理的**「其他紅利」數字 ⇒ **畫面上沒有任何一處會變**。只有當有人問「連登里程碑到底花了我們多少」才會發現答不出來——而且會拿到一個**看起來像答案的錯答案**（把雜項桶讀成雜項）。且它一次錯三處消費端：`ledger.bySource` 分桶／`ops-dashboard` 的「🎁 送幣成本明細」／真站 `api.opsLog` 上雲。
    - **④ 立鎖與鑑別力（承 #142 的教訓）**：`platform/bonus-add-source-attribution`（純 `tests/`，不出貨）。**關鍵在判準要有鑑別力**——只檢查「有沒有 `source:` 這個字」是**沒有鑑別力**的，`{ source: "" }`／`{ source: undefined }`／`{ source: v }` 三種都會過。故判準要求**非空字串字面量**（三元式則兩臂都要），並在鎖內用 **8 個反例自證**分得出好壞；另加規模自我保護（≥18 點／≥15 檔，防掃錯目錄假綠）、反向錨（`ledger.js` 的 `|| "其他紅利"` 後備**必須留著**——拿掉它漏傳會以 `undefined` 當桶名＝**比現況更糟**）、消費端錨（ops-dashboard 消費 `bySource`、ledger 鏡射 `opsLog`）。
    - **⑤ 驗證**：node **297→298 全綠**；**負向擾動 9/9 CAUGHT、9 例全部僅本鎖轉紅**（P1 回種原缺陷／P2 兩條送幣路徑共用同桶／P3 空字串／P4 變數／P5 拿掉 ledger 後備桶／P6 拿掉 dashboard 的 bySource 消費／P7 拿掉 opsLog 鏡射／P8 另一模組 tournament 漏傳／P9 三元式一臂改變數）；擾動涉及的 5 支檔（rewards／ledger／ops-dashboard／tournament／challenges）全部以 `Buffer.equals` 驗**逐位還原**，`git diff` 收尾僅 `rewards.js` 一支。
    - **⑥ [P-FS] 處置：本卡是首屏正數卡，但收尾是淨負 −26B**：`core/rewards.js` 是首屏 eager（`index.html:70`），補 source 淨增 **+31B**，而進場餘裕只有 **10B** ⇒ 直接落地會讓 `platform/first-screen-budget` 轉紅、**三軌同時被擋**。處置＝**只回收本卡所在三個函式（`status()`／`claim()`／`open()` 頭）裡的「註解欄位對齊空白」共 9 行**（純 cosmetic，**一個字都沒刪**）⇒ 收尾 **1638364／1638400＝餘裕 36 bytes**（進場 10）。
      ⚠️ **與 08-26 明文拒絕的「砍註解換餘裕」不是同一件事**：那條拒絕的是刪掉「為什麼」的**文字**（用文件品質換 KB，且 08-24 超標正是註解造成的＝拆自己護欄）；本卡刪的是**欄位對齊的空白**，why 一字未動，而該寫的 why 寫進了**不出貨的 `tests/` 鎖檔頭**（零首屏成本）。
      🔗 **順帶給 08-28 12:00 窗『找淨負首屏機會償還 −53B』的指派補上另一半**：維護軌 08-29 00:00 窗已定論「**CSS 層無可回收位元組**」（死變數 0／死 class 0）。本輪證明 **JS 層的註解欄位對齊空白是可回收的**——光 `rewards.js` 一支就有 **219B**，本卡只動了其中 86B。⇒ 「淨負機會不存在」**只對 CSS 成立，對 JS 不成立**。
    - **⑦ 台帳審「金流」6 模組（輪替接續 08-28 20:00 的『後台』）**：`ledger-card-sweep` **⚠️ 0 筆**。六筆見證數**全數複現、status 逐筆不變**：收銀台 partial（`HL.payment|payMethod|payments.register|pspRegistry` **0** 命中，連七輪；FIAT 3／CRYPTO 3）／錢包帳本 present（`HL.ledger.record(` raw **10 行／7 檔**，扣 `reports.js:582` 字串述詞＝真插樁 **9／6**，該筆假上升第三次確認穩定）／提款佇列 absent（raw **4**、實質 **0**，續 avoid）／P2P partial（`canTrade(` **1**，連六輪）／獎金流水 partial（`wagerMult` 全 src 仍 **1** 且仍是註解）／Vault absent（`vault|保險庫|分倉` **0**）。
      ⭐ **本輪為『錢包/帳本』補上一條通則**：該模組的 present 過去**只由「插樁點還在不在」支撐＝只證覆蓋率、不證歸屬**。凡「**單一漏斗 + 可選 meta + `||` 後備**」這個形制，後備會把呼叫端的疏漏**吸收成一個看起來合理的數字**，覆蓋率指標對它**完全免疫** ⇒ 審記帳類模組要**同時問兩題：有沒有記到？記成了誰？**
    - **⑧ 調研＝toshi-bet 到期當日刷（`platforms_researched` +1）**：`platforms.json` LIVE 逾期本輪 **1/36**（toshi-bet 08-29 到期），已刷回 `next_due=2026-09-28`。**淨新一條有價值**：⭐ **Vault 帶 VIP 進度維度**——最高階 VIP 權益含 `increased vault size` 且存入資金**生息** ⇒ 給了 08-26 才入帳、當時**僅 Rainbet 單源**的模組一個**獨立第二來源**，且多一條軸：**Rainbet 版 Vault 是開關，toshi.bet 版是可成長的容器**。已寫進台帳 evidence 與既有卡 **#132**（含實作時的三條採納意見：倉容掛 `HL.vip` 段位＝不新增經濟旋鈕／生息屬送幣須帶 `source`＝受本輪 #144 新鎖管轄／生息率須進 `HL.econCfg`）。**不另開卡**（#132 已存在，依去重紀律以既有卡為主體吸收）。
      **兩條據實記為零缺口、不虛報**：(a) **XP 依產品線分速率**（casino 1×／sportsbook **3×**，週抽獎券同型）＝與 `HL.edge.weighted(game,bet)`（#50）**同一種形制**，且既有常駐鎖 `platform/central-hook-fanout-roster` 已守住這種設計最容易錯的地方（「兩把尺不互相冒充」）⇒ 判定既有能力；(b) **玩家保護**（存款限額／時段提醒／自我排除／即時追蹤）與 `HL.rg`（#67/#86/#96）逐項對照**無缺口**——這是該維度 08-16 加進清單後本平台**第一次**被問到，結果是 reconfirm，但**問了才知道沒缺**。
    - **⑨ sw／counters／未碰他人工作**：`core/rewards.js` **會送到玩家瀏覽器** ⇒ 依 08-26 訂下的判準 `sw.js` **v240→v241**（不是「有沒有動 prototype/」，是「這次動到的檔會不會送到使用者瀏覽器」）。`platform_cards_opened` +1（#144）／`platform_cards_implemented` +1（#144 同窗開+結）／`platforms_researched` **+1**（toshi-bet）／`consecutive_idle_rounds` 維持 **0**（有真產出）。`max_cards_per_run: 2` **未用滿＝刻意**（Vault 訊號歸併 #132 而非開雙胞胎）。⚠️ **未碰他人工作**（§7）：`prototype/games/registry.json`（工作區未提交）、未追蹤的 `Game assets/001_Shadow Ritual`／`002_小雞過馬路`、`prototype/games/slot-engine/`（船長待處理區明列的遊戲軌 POC）——一律一個位元組未碰，台帳一律以 HEAD 為準。
    - **下輪（08-29 14:00）**：`platforms.json` **wow-vegas／shuffle／gamdom／duelbits 08-30 連續到期**（明日），本輪後最早到期為 08-30 ⇒ 14:00 窗**可能無到期對象**，若無則走「加深取材」而非虛報；台帳輪替下一格＝**功能**（17 模組·全庫最大分類，建議分兩輪審）；實作候選首屏餘裕已回升至 **36B**，仍屬吃緊，續走零／負首屏成本類。

- **2026-08-28（平台軌 · **14:00 窗** · 建置輪＝**#140 落地**：立**常駐鎖 `platform/i18n-attr-surface-closed`**＋修掉 #122 漏修的 placeholder 契約分流 · 台帳審「**前端UI/UX**」5 模組（4 審 1 刻意降頻）＋**新增全庫首見模組「語言包完備度登記簿（執行期）」**＋開卡 #141 · claim `p-141115-8d4e` · 帶心跳 14:11→15:05 · 進場鎖乾淨 false · 未奪鎖 · **首屏逐位未動（餘裕 10 bytes）** · `sw.js` 不 bump）**
    - **① 進場**：`build_lock` 乾淨 `false`（維護軌 08-28 12:00 窗 commit `3ff60c7` 釋放）→ claim `p-141115-8d4e` → **當下即單檔 commit `070eec6`** → 重讀確認 token 仍在＝claim 成功。dark **~5.6h**（`last_platform_run_at` 08-28T08:35）<24h ⇒ 非 catchup；`lead_track: games` 下平台軌本可讓路，但本輪有真工作（台帳輪替到期＋查獲一條五面棘輪都沒問過的上游缺口）且**零首屏成本** ⇒ 做而不讓路。孤兒：`git status` 僅 `Game assets/`（跨軌雜訊·非我）＋`prototype/games/registry.json`(M)＋`games/slot-engine/`(??)＝遊戲軌 ENG-1 POC·船長裁決中，依 §7 一位元組未碰。台帳自我查核 `ledger-card-sweep.js` **28 筆引用、⚠️ 0 筆待確認**（乾淨，無需回填）。node 進場基線 **293/293**。
    - **② ⭐ 本輪查獲：五面 i18n 棘輪問的都是「寫下去的中文有沒有條目」，沒有一條問過上游那個更基本的問題——「引擎到底翻哪幾個屬性，而尺知不知道」。** `core/i18n.js` 把它硬寫**三次**（`:83 attributeFilter` 通知面／`:102 tAttrs` 動手翻的那份／`:151 walk()` 子孫走訪面），尺又各自宣告一次（#122／#129／#120 三處）＝**五份副本零關聯**。加第四個屬性（例 `alt`）漏改任一份，症狀全是「畫面看起來完全正常」：漏改 `:151` ⇒ 只有 root 被翻、**子孫原樣繁中**；漏改 `:83` ⇒ 首次翻到、之後改屬性不再翻；**漏改尺 ⇒ 該屬性永遠不進零容忍棘輪的分母、五面繼續寫「缺漏 0」**＝§4 名冊變體第三例。
    - **③ 同輪修掉一條真的「修一半」**：#122 論證的是**契約差異**（`tAttrs` 只精確比對、無 PREFIX/SUFFIX 分支 ⇒ 被前綴表覆蓋的屬性值執行期翻不到），據此把 `title`／`aria-label` 改用 `coversExact`——**但 `placeholder` 是同一個 `tAttrs` 翻的、卻留在 DOM 面被寬鬆 `covers()` 判**（`measureDom` 舊碼一律 `covers`）＝三個屬性修了兩個而四面全綠。修法＝**逐形狀分流**＋外露決策點 `domCovers()`＋補 `strictDelta` 自我揭露。**落地當輪 strictDelta＝0**（9 條 placeholder 命中全是精確覆蓋：`fair.js:177/178`／`ai-concierge.js:78`／`app-shell.js:187/278/409`／`chat.js:64`／`auth-view.js:14`／`liveroom.js:107`）⇒ **零回歸，也沒有真實 witness** ⇒ 合成探針站崗。
    - **④ 驗證**：node **293→294 全綠**；**負向擾動 8/8 CAUGHT、7 例僅本鎖轉紅**（P1 引擎只改 `attributeFilter`／P2 只改 walk 選擇器／P3 引擎三處都加 `alt` 而尺沒跟上／P4 引擎撤掉 `placeholder` 而尺還在要求＝死鍵向／P5 分流改回一律 `covers`／P6 分流退化成全部從嚴／P7 `engineAttrs` 回空集合＝空≡空恆真的空掃陷阱／P8 `attrShapeSet` 恆回空）。**P3 另觸發 `platform/first-screen-budget`＝[P-FS] 第二個實證**（往首屏檔插三處 `, "alt"` 就吃掉全部 10 bytes）。擾動後兩支檔**逐位還原**（`Buffer.equals` 驗證）、FINAL 294/0。
    - **⑤ 台帳審「前端UI/UX」5 模組（08-25/08-26 最舊·輪替接續 08-28 08:00 的『擴充性』）**：`導覽殼層` present（#93 缺口**第五次複驗逐位未動**：`SIDE` 5 頂層項、無 `enabled`/`order`、`registerNav`/`navRegistry`/`HL.nav` 仍 0/0/0；**08-25 在此欄查獲的第五面風險已由 #129 關閉**）／`遊戲客戶端框架` present（`title:` **6 字面量／7 出現**——本欄自此明記兩個數以免後手誤判漂移；`isPF` 去註解 2 處）／`多語系前端 i18n` **partial（理由少一條）**——五面全機械零容忍，08-25 記的 (i)「第五面未守」已由 #129 關閉，仍 partial 因 #126 批次三＋營運面無語言包／`大廳分群軸` partial（`gameAxes.register` 2 處、`volatility` 仍無資料欄＝遊戲軌權威不代填；`casino.js` 對五個軸名 0 命中＝#94 性質第五次證成）。**`大廳/遊戲牆` 刻意不回填 `last_audited`（維持 08-25）＝服從它自己上輪寫下的「降頻」建議**，並在欄內寫明理由（照抄結論≠今天量過）。⚠️ **口徑更正**：`ax-drawer` 用 `grep -ro prototype/src` 得 **22**、前幾輪記的 **9** 是只數 `app-shell.js` ⇒ 兩數都對，錯的是放同欄比。
    - **⑥ 配對取材＋新模組（全庫首見）＝「語言包完備度登記簿（執行期）× 語言選單狀態」（absent）⇒ 開卡 #141**：`platforms.json` LIVE 逾期 **0/36**、最早 `next_due` **08-29 toshi-bet** ⇒ 本輪無深挖對象，走配對取材（2026 本地化實務：passively supported 語言的英文回退是**須被追蹤的暫時狀態**／workflow 須自動 flag 未翻內容／terminology 一致性）。**機械對照**：`LANGS` 只有 code/name/flag/src，`coverage|完備度|翻譯進度` 全 src **僅 1 命中且是字典值**，五面棘輪全在 `tests/`（不出貨）⇒ **執行期零讀數**。**盲點成因＝「開發期尺 vs 執行期讀數」的交界**：六面尺全在問「寫下去的中文有沒有條目」，「玩家此刻這個語言整體翻到幾成」不屬於任何一面 ⇒ **台帳盲點第 6 例**。
    - **⑦ [P-FS]／sw／counters**：本輪只動 `prototype/tests/checks-platform.js`＋`prototype/tests/i18n-key-scan.js`（皆不在 PRECACHE、不出貨），`prototype/index.html`／首屏 `src/`／`sw.js` **三者逐位未動**（`git status` 實證）＝**第十三個「逐位未動」數據點**。**採維護軌 12:00 窗交接的新基線並複核成立：1638390／1638400 bytes＝餘裕 10 bytes、90 支 script**（該窗 T45 守衛 +53B 令餘裕 63→10）。`sw.js` **刻意不 bump**（本輪無任何會送到玩家瀏覽器的檔改動·維持 v238）。`platform_cards_opened` 121→**123**（#140／#141）；`platform_cards_implemented` 82→**83**（#140 同窗開+結）；`platforms_researched` **+0**（無深挖對象，不虛報）；`consecutive_idle_rounds` 維持 **0**。#118 閘上積壓 **17→18 件**（+#141）。**下輪（08-28 20:00）**：台帳輪替下一格＝**後台**（8 模組·全庫最大分類）；`platforms.json` **08-29 起 toshi-bet／wow-vegas／shuffle／gamdom／duelbits 連續到期 ⇒ 下輪起恢復有深挖對象**；實作候選仍受 [P-FS] **10 bytes** 夾住，續走零首屏成本類（棘輪／掃描器／常駐鎖／情報）。

- **2026-08-28（平台軌 · **08:00 窗** · 建置輪＝立**常駐鎖 `platform/audience-consumer-roster-closed`** · 台帳審「**擴充性**」6 模組＋**新增全庫首見模組「大廳版位登記簿 × 分眾大廳」** · **據實作廢一條沿用 17 天的過時缺口敘述** · 開卡 **#139** · **首屏零位元組** · claim `p-081100-4c9a` · `sw.js` 不 bump）**
    - **① 本輪查獲的東西不是產品缺陷，是網子自己的邊界在縮**：`platform/audience-single-vocabulary` 的**消費端名冊是硬寫的兩支**（`promo-cal.js`／`redeem.js`——寫下它時剛好只有兩支），而受眾消費端實測已經長成 **4 支**（+`core/content.js` #61、+`core/shop.js` #123）。**兩支新的都不在那條鎖的射程內，而它一個字也沒說。** ⇒ 第五個消費端落地時，就算自己讀 `HL.vip.status().level` 判資格（＝第二份受眾述詞，門檻會與詞彙表悄悄分岔），**現有每一條鎖仍然全綠**。這是 §4「修一半而看不出來」家族的**名冊變體**：層①（每支自己的鎖）都活著、層②（名冊）不隨程式碼成長 ⇒ **覆蓋率單向下降，而下降過程沒有任何讀數**。
    - **② 立鎖（比照 08-27 `central-hook-fanout-roster` 的雙向等式）**：`platform/audience-consumer-roster-closed` — 射程不是硬寫檔名，是**掃出來的地面真相**（誰解參 `.audience`）≡ 宣告名冊；逐支驗「必須委派 `HL.release.matches`／不得自建 `AUDIENCES`／不得自刻玩家維度（VIP 等級・活躍度・帳齡・季票階）」；宣告端 kind 必須在詞彙表內（**孤兒 kind ⇒ `matches()` 恆 false ⇒ 那則內容/獎品永遠隱藏、不報錯、console 全乾淨**）；witness 下限（消費端 ≥4／宣告 ≥5，防「掃描器壞掉時完美通過」的同形陷阱）；**尺自身的反向錨 3 條**（fail-open 假消費端須被同一段程式抓到；名冊多一支／少一支時 `extra`／`missing` 必須真的非空）。**node 290→291 全綠·負向擾動 6/6 CAUGHT**。
    - **③ 據實講兩件事**：(a) **我沒有發現它現在是壞的**——四支消費端今天全部合規（`.audience` 解參者恰 4 支、全部委派、全庫零第二份受眾表、零孤兒 kind）。這條鎖買的是「**以後長第五支時會被抓到**」。(b) 負向擾動 6 例中 **5 例僅本鎖轉紅**；**P2 另外觸發了 `platform/first-screen-budget`——那不是誤傷，是 [P-FS] 的實證**：P2 只在首屏檔 `content.js` 插入**一行**（~72 bytes）就吃掉了全部 63 bytes 餘裕。⇒ 「首屏餘裕」現在的量級是**一行程式碼**，不是一個功能。
    - **④ 台帳審「擴充性」6 模組，一筆據實作廢值得後手知道**：`內容/遊戲上架排程` 欄 08-06／08-11 兩度記「受眾述詞表尚未被第二個消費者複用（消費端仍為 `views/casino.js` 3 處）」——**實測早已不成立**：現有 **4 個消費端**（content／promo-cal／redeem／shop）＋ **2 個宣告端**（onboarding newcomer／activity active）。那條缺口其實在 #107 落地當天就關了，**台帳晚了 17 天**＝「台帳承諾與現況不符」家族又一例。另修一條口徑陷阱：`first-screen-deps.js` **不帶 `--scope all`** 時會把 `boot-registration-blocked` 印成「（無）」，那是射程差異不是那 24 支消失了（本輪差點自己誤讀，已寫進該欄）。
    - **⑤ 新模組（全庫首見）＝「大廳版位登記簿 × 分眾大廳」（absent）⇒ 開卡 #139**：本庫其餘大表面**全都有登記簿**（content／reports／dock／promoCal／achievements／shop／games），**唯獨流量最高的大廳沒有**——`views/casino.js:154–166` 把區塊清單、順序、標題、對誰出現四件事全部硬寫。**盲點成因是「分類交界」的變體**：`大廳/遊戲牆` 欄問的是「遊戲牆好不好用」（present，判得沒錯）、`Dockable Layout` 欄問的是「側浮窗容器」（partial，也沒錯），**「大廳自身的組成是不是資料驅動」不屬於任何欄位**。⇒ 台帳盲點第 5 例（前四例：玩家保護／帳戶安全／Feature Flags／營運自動化・出金安全鎖）。
    - **⑥ [P-FS]／sw／counters**：本輪只動 `prototype/tests/checks-platform.js`（不在 PRECACHE、不出貨），`prototype/index.html`／首屏 `src/`／`sw.js` **三者逐位未動** ⇒ 首屏**結構上不可能**改變（**第十二個「逐位未動」數據點**·餘裕維持 **63 bytes**／91 支 script）。`sw.js` **刻意不 bump**（本輪無任何會送到玩家瀏覽器的檔改動，bump 只會讓所有人白白重抓一次）。`platform_cards_opened` +1（#139）；`platforms_researched` **+0**（本輪無深挖對象——`platforms.json` LIVE 逾期 **0/36**，最早 `next_due` 08-29 toshi-bet ⇒ 不虛報）；`platform_cards_implemented` **+0**（產出是常駐鎖與情報，非卡落地）；`consecutive_idle_rounds` 維持 **0**。#118 閘上積壓 **16→17 件**。

- **2026-08-27（平台軌 · **14:00 窗** · 建置輪＝立**常駐鎖 `platform/site-ns-storage-allowlist`** · 台帳審「**資安**」7 模組＋**新增全庫首見模組「出金安全鎖」** · **作廢並改寫 08-24 立下的一條回歸基準** · 開卡 **#137** · **首屏零位元組**（`prototype/src` 逐位未動）· claim `p-141015-9f2a`）**
    - **① 本輪最重要的一件事：一條回歸基準今天讀出了「掉了 6 個閘」，而真相是一個閘都沒少——量測法本身不是重構不變量。** 台帳「真/假站軸」08-24 立的基準是 `isLive()` 實測 **73 處／35 檔**，本輪量到 **67／32**。根因機械鎖定到單一 commit：**T43（`6371fba`，維護軌 08-27 00:00 窗）** 把 6 個各自重刻的站別三元式收斂成委派 `HL.site.mode()`——站別語意逐位不變，只是**文字上少了 6 次 `isLive()`**。
      ⇒ **這條基準對「把重複的判斷收斂到單一真相」這件我們一直在鼓勵的事，讀數是下降的**；而 08-05 訂的判讀規則只寫「此數不會增加即為訊號」（**只測非增、對下降沒有規定**）⇒ 後手最可能的反應是把基準直接改小，於是這個方向從此不再有訊號。**這是「台帳承諾與現況不符」家族的新變體：不是記載過期，是量測法不是 refactor-invariant。**
      **處置**：改用 refactor-invariant 樣式 `isLive(|liveOn(|site.mode(`，以 `git grep` 對 T43 前後兩個 ref 實測皆為 **83 處／36 檔＝逐位相同** ⇒ 新基準 83／36；裸 `isLive()` 數降級為參考值。
    - **② 而這個模組真正沒有人在守的那一面，本輪補上了常駐鎖（§4「修一半」第 ⑥ 例·新漏法＝每個檔各自守自己）**：平行宇宙隔離的機械前提是「**所有玩家存檔都經 `HL.dom.lsGet/lsSet` 這唯一出口套 `HL.site.ns()` 前綴**」，而全庫對「不得直接碰 `localStorage`」的防守**實測只有 4 筆、且全是單檔自守**（`rakeboost.js:351` 自己掃自己＋tests 對 `rbac.js`／`release.js`／`referral-core.js` 的逐檔斷言）——**沒有任何一條在問「全庫現在有幾支在繞過這個出口」**。
      ⇒ 明天任何一支新經濟模組寫一行 `localStorage.setItem`，真站（營運健檢）的帳目就與假站那份慷慨的假資料**共用同一份存檔**，而**兩站畫面都完全正常、node 也全綠**（單一站別的使用者結構上看不到異常，只有切站才會發現存檔跟著走）。
      **新鎖 `platform/site-ns-storage-allowlist`**：全庫掃描 ＝ 白名單 **10 支**的**雙向等式**（多一支＝有人繞過、少一支＝豁免過期），白名單成員全部是 §4 明列的跨站 UI 偏好或出口本身；外加三組反向錨——偵測器自身的咬合測試（防「掃不到＝完美通過」）／`dom.js` 的 `nsKey` 必須真的向 `HL.site.ns()` 求前綴／**`site-mode.js` 以 stub global 實跑真檔**（驗 demo→`""`、live→`"r:"`、旗標以原生 key 寫入、MODE 讀一次後頁內固定不可即時變）。
      node **286→287 全綠**、**負向擾動 7/7 CAUGHT**（P1 非白名單檔直接碰／P2 `nsKey` 不套前綴／P3 真站前綴退化為空／P4 旗標改帶前綴寫入＝自我指涉陷阱／P5 MODE 變成頁內即時可變／P6 白名單成員豁免過期／P7 偵測器自身寫壞；**每例失敗恰 1 支且僅本鎖**，baseline 與還原皆 287/0）。
    - **③ 台帳審「資安」7 模組全審＋1 新增**：真/假站軸 present（基準改寫，見 ①②）／風控反詐欺 weak（連九輪零變化，`velocity|deviceId|antifraud|risk_score|riskScore` 仍 0 命中，`dock-growth.js` 的 `fingerprint()` grep 陷阱第七次複驗仍成立）／KYC absent（4 命中連四輪逐位相同、全是註解與風險提醒文案）／負責任博弈 present（`HL.rg.(allow|check|gate|canBet)` 27 處／21 檔逐位相同；**本期無新遊戲上線故持平屬預期**，依 08-24 訂的靈敏判準）／權限角色 partial（`is_ops_admin()` 仍只有一支 RPC 的兩版、`audit|稽核` 仍 0 命中）／帳戶安全自助中心 absent（依 08-24 判準直接看抽屜，仍是四個入口無安全分頁）／玩家可見度 absent（「隱身玩家」仍只是假玩家的 15% 機率裝飾欄位）。
    - **④ 新增全庫首見模組「出金安全鎖」（absent ★）⇒ 開卡 #137**。**盲點的成因是新的**：它落在**資安與金流兩個分類的交界**——資安 7 模組沒有一個問「錢往外走」，金流 6 模組的提款相關那格是**營運側**審核佇列 ⇒ 兩邊都以為對方會問。並且它很可能一直被 **`CONTROL.avoid` 的外溢**蓋住（第二例，前一例是 Vault）：avoid 明列「真金流串接／KYC」，而「提款目的地白名單」聽起來像金流，實際上**零 PSP、零合規面、完全站內**。
    - **⑤ 我在開卡前先做了反向搜尋，結果直接改寫了卡的範圍（08-27 08:00 窗新訂紀律的第一次實戰驗證）**：「**安全性放寬要等、收緊立即**」這條語意本庫**已經有單一真相**＝`core/responsible.js:220` 的 `planChange()`（`RAISE_DELAY_MS`）與 `planPause()`（單調不可縮短）。⇒ #137 卡上明文寫死「冷卻期一律向它求、不得自刻第二份延遲算術」。**若沒做這次反向搜尋，實作輪幾乎必然會為出金鎖新寫一份 24h 倒數**，然後兩套延遲語意各自漂移。
    - **⑥ [P-FS]／`sw.js`**：`git status` 對 `prototype/index.html`／`prototype/src/`／`prototype/sw.js` **三者皆零改動** ⇒ 首屏**結構上不可能改變**（**第十個「逐位未動」數據點**，餘裕維持 **63 bytes**）。本輪只動 `prototype/tests/`（不在 PRECACHE、不出貨）與 `intel/`／`BACKLOG.md`（不被前端服務）⇒ 依 08-26 08:00 窗訂的判準「**這次動到的檔會不會被送到使用者的瀏覽器**」，`sw.js` **刻意不 bump**（bump 只會讓所有人白重抓一次全站）。
    - **⑦ 對船長的一句**：這個閘（#118）上現在排了 **15 件事**——平台卡 **#125／#127／#128／#130／#131／#132／#133／#134／#135／#136／#137 共 11 張**（**#127 仍是已批准**）＋維護債 **T27／T29／T42／U34 共 4 條**。裁決題目與 08-23 完全相同（(A) 你在前景開一輪 preview／(B) 允許 headless 純搬移版），只有代價在變大。**本輪沒有空轉**：常駐鎖與台帳都是零首屏成本的真產出。

- **2026-08-27（平台軌 · **08:00 窗** · 建置輪＝立**常駐鎖 `platform/promo-optin-invariants`** · 台帳審「**活動**」6 模組＋**新增全庫首見模組「遊戲內活動掛件」** · 取材＝**本庫首次向「網路型促銷」取材** · 開卡 **#136** · **首屏零位元組**（`prototype/src` 逐位未動）· claim `p-080950-3c7d`）**
    - **① 本輪修的不是畫面，是一條沒有人在守的經濟不變量**。台帳審「活動」時查到 `core/promo-cal.js` 的 **#52 opt-in 狀態層落地至今零測項覆蓋**（`optIn|joinedToday|canJoin` 在 `tests/` 的唯一命中屬 `vsslot` 入座判斷、與本層無關），
      而那一層裡有一條**只由一行註解守著**的不變量：`leave()` 保留 `day` 記錄 ⇒「退出」不繞過 `optInDaily` 的每日一次限制。把它「簡化」成 `delete o[id]`，玩家就能 join→leave→join **無限續期** `progress-src.js` 的 #49 限時經驗加速（`1.5×`／`6h`）⇒ **賽季階梯（真獎勵）被刷穿**，
      而**日曆照常顯示、加成照常倒數、node 全綠、console 零錯誤**。⇒ CLAUDE.md §4「修一半而看不出來」家族的第 ⑤ 例，**新的漏法＝不變量寫在註解裡、且沒有第二個消費者去打它**。
    - **② 鎖的形狀（比照 #107 ④ 的 shim 實跑，不做源碼級代理）**：以最小 shim 把**真的** `promo-cal.js` 載進來跑 `join/leave/canJoin`，守五條紀律——未宣告 `optIn` 零行為／重複加入不得延長起算時間／**退出不得繞過每日一次**／跨日必須解除（限制不是永久封鎖）／TTL 逾時自動失效且到期不等於重置每日一次。
      時間流逝以「直接把已存記錄的起算時間往前推」模擬（確定性、零等待、不 mock `Date.now`）。**刻意不 skip**：shim 載不起來就 FAIL（skip 會讓「shim 過時」與「不變量壞掉」在輸出上同形）。
      內建**尺自身的反向錨**：同一段探針餵一個 `leave` 直接 `delete` 的假實作，必須被判「限制失效」——沒有這條，斷言寫壞時整條鎖會靜默全綠（`SELFTEST_ORDER_DEBT` 棘輪當年正是栽在這裡）。
      node **284→285 全綠**、**負向擾動 6/6 CAUGHT**（P1 leave 直接 delete／P2 join 拿掉 canJoin 守衛／P3 joinedAt 拿掉 TTL／P4 canJoin 拿掉 optIn 守衛／P5 joinedToday 恆真＝永久封鎖／P6 未宣告 TTL 套隱含預設期限；**每例失敗恰 1 支、皆僅本鎖轉紅**，baseline 與還原皆 285/0）。
    - **③ [P-FS] 第九個「逐位未動」數據點，而本輪是最強的一種**：`git status` 對 `prototype/index.html`／`prototype/src/`／`prototype/sw.js` **三者皆零改動** ⇒ 首屏**結構上不可能**改變；實測 **1638337／1638400 bytes、90 支 script**（餘裕 **63 bytes**，維護軌 T43 於 00:00 窗釋出的 60B 仍在）。
      `sw.js` **刻意不 bump**：本輪動到的 `tests/` 不在 PRECACHE、`intel/`／`BACKLOG.md` 不被前端服務 ⇒ 依 08-26 08:00 窗訂下的判準（「這次動到的檔會不會被送到使用者的瀏覽器」），bump 只會讓所有人白重抓一次全站、零收益。
    - **④ 台帳審「活動」6 模組，其中一筆是據實更正自己**：08-24 記「`HL.promoCal.register` 註冊點 **19 處／9 檔**（…／`responsible`）」——本輪以 08-26 20:00 窗訂下的權威來源 `node intel/tools/registry-gaps.js` 重量得**外部註冊者 7 檔**，
      加上本體 2 處自我種子註冊＝呼叫點 **9 處／8 檔**；**`core/responsible.js` 是誤列**（對 `promoCal` 的 2 次命中 `:12`／`:20` **全在註解**，零 register 呼叫）。⇒ **口徑家族第三例**（前有成就筆數 19/20/21/24、grace 家族 76/112），處置同前：明寫口徑 + 固定讀數來源。
      另更正一處行號漂移：`views/lobby.js` 的第二道真站假活動閘由 `:183` 位移到 **`:187`**（四輪重抄同一個行號的固有代價；本欄往後以語意 + `isLive()` 命中數為主，行號僅供導航）。
    - **⑤ 新模組（全庫首見）＝「遊戲內活動掛件 In-game Promotion Surface」（absent·★）⇒ 開卡 #136**。取材是本庫**第一次向「網路型促銷」取材**（Drops & Wins／Wazdan Network Promotions／ESA prize drop），
      而這類促銷的**定義性性質不是獎池，是露出位置**：玩家在**遊戲畫面裡**就看到剩餘份數、雙結束條件與自己的進度。ApexWin 可機械證明的 absent：`game-frame.js` 對活動詞彙命中 **0**；引用 `HL.promoCal` 的 view 只有 `lobby`／`casino`／`tournament`＝**零遊戲 view**；`mountView` 在遊戲頁只補返回列。
      ⇒ `promoCal` 的 **7 個外部註冊者沒有一個**能出現在遊戲畫面上。**據實界定不誇大成零**：底部列有兩格活動訊號（`app-shell.js:616` 簽到／`:617` 錦標賽），但那是**硬寫的固定入口**、不承載任何一則促銷的即時狀態。
    - **⑥ 這一輪我刻意沒開的那張卡，比開了的那張更值得記**：對手形制裡「預定義獎項池逐份被領走 + 剩餘份數即時可見 + 到時間或發完先到者為準」看起來像一個大缺口——**但它在本庫已經有了**：
      `core/challenge-slots.js`（`HL.chalSlots`，#57 純數學層）的 `state()` 就回 `{total,taken,remaining,takenBy,closed,open}`，`closed = remaining<=0 || expired` 正是雙結束條件。⇒ 若照關鍵詞開卡，會生出 `HL.chalSlots` 的雙胞胎。
      **已把這段寫進「抽獎/Raffle/Rain」模組的 evidence**，並在 #136 卡上明文寫死「份額一律向 `HL.chalSlots` 求，不得長出第二份算術」——這是 08-14 `#95`／`#72` 雙胞胎教訓的預防式落實（**比對來源模組 + 出口形狀，不是比對卡名**）。
    - **⑦ 對船長的一句**：這個閘（#118）上現在排了 **14 件事**——平台卡 **#125／#127／#128／#130／#131／#132／#133／#134／#135／#136 共 10 張**（**#127 仍是已批准**）＋維護債 **T27／T29／T42／U34 共 4 條**。裁決題目與 08-23 完全相同，只有代價在變大。
      本輪也再次證明「在你裁決前平台軌不會空轉」：`tests/` 側的建置是零首屏成本的，而它抓到的不是形式問題，是一條會被靜默刷穿的經濟不變量。

- **2026-08-26（平台軌 · **20:00 窗** · 建置輪＝立**常駐鎖 `platform/registry-extension-fail-closed`** + 新工具 `intel/tools/registry-gaps.js` · 台帳審「**功能**」16 模組＋**新增全庫首見模組「收集套組與圖鑑」** · 取材＝**本庫首次向「遊戲化引擎供應商」取材** · 開卡 **#134／#135** · claim `p-201015-e4a9`）**
    - **① 為什麼這一輪能建置（在 [P-FS] 3-byte 凍結下）**：本輪產出**一個位元組都不進首屏**——只動 `prototype/tests/`（不在 PRECACHE、永不送達使用者瀏覽器）與 `intel/`（不被前端服務）。
      落地後首屏實測 **1638397／1638400 bytes、餘裕 3 bytes、90 支 script**——與進場時**逐位相同**（連續第八個「逐位未動」數據點）；而且這次不只是量出來的：`git status` 對 `prototype/index.html`／`prototype/src/`／`prototype/sw.js` **三者皆零改動** ⇒ 首屏在**結構上不可能**改變。（script 支數以鎖自身口徑 `staticScripts()` 計＝**90**；歷輪 evidence 曾記 91，係另一種計法，此處一律以鎖的口徑記，避免第二把尺。）**`sw.js` 刻意不 bump**——依 08-26 08:00 窗訂下的判準「這次動到的檔會不會被送到使用者的瀏覽器」，答案是不會 ⇒ bump 只會讓所有人白重抓一次全站、零收益。
    - **② 本輪守的是什麼（先講結論：沒有發現缺陷，這條鎖是把健康狀態釘住）**：全站有 **14 個有外部呼叫點的 `HL.<ns>.register` 擴充點**＋**10 個只在檔內註冊的內部登記簿**。
      逐個行為探針（真的呼叫 `register()` 餵壞 spec 再看列舉器）：**8 支探得到，全部 fail-closed**（壞 spec 一律拒收、列舉器逐位不變）、**leaky 0**。
      ⇒ 登記簿層是健康的。**但在本輪之前沒有任何東西在守這個性質**——下一個新登記簿只要寫成「來者不拒」就會靜默上線（畫面完全正常，直到某天有人註冊了一筆壞 spec 才在渲染端炸）。
    - **③ 唯一的 unproven：`HL.guild`（開成 #135）**。14 個裡 13 個至少有一條可證明的路；`guild` 兩條都沒有（`guild.js:295` 頂層取 `window` ⇒ node 不可 require；外部註冊者 0）。
      它成為新鎖的**唯一基線例外**，且鎖帶**基線防腐**斷言：基線項一旦被證明得到就**必須**從清單移除（不許養過時的免罪名單）。
    - **④ 這條鎖刻意避開的坑**：2026-08-17 的 `SELFTEST_ORDER_DEBT` 棘輪栽在**用 grep 位置代理行為**（7 支誤報 4 支）。
      故本鎖 ①②③ 條全是行為斷言，只有第 ④ 條是覆蓋面清單；並內建**尺自身的反向錨**——拿一個「什麼都收」的假登記簿餵同一段檢測程式，必須被判 `failClosed:false`（否則整條鎖可能空心而全綠）。
      **負向擾動 5/5 CAUGHT**（P1 把 `support.register` 改成來者不拒／P2 拿掉 `progressSrc` 的列舉器／P3 清空 `BAD_SPECS` 把尺空心化／P4 把 `guild` 從基線移除＝模擬新增無法證明的擴充點／P5 把已證明的 `edge` 塞進基線＝基線腐化）——
      **P1／P3／P4／P5 皆僅本鎖轉紅**（`fail=1`）；**P2 另外連帶 4 條既有 `progress-src/*` 轉紅**，那是正確的（改掉公開列舉器名稱本來就該弄壞它自己的測項）。五例還原後皆回 **282／0**。
    - **⑤ 台帳審「功能」16 模組（該分類 `last_audited` 為全庫最舊）·一筆據實更正 + 一個整格缺席**：
      · **更正**：`成就徽章牆` 的筆數。逐行實測種子陣列 **19 筆**＋外部註冊者 **5 筆**（activity 1／challenges 2／reports 1／responsible 1）＝執行期 **24 筆**。08-24 記的「實測 20 筆」**未載口徑且與逐行實測不符**。
        ⇒ 這是本模組 evidence **第三次**因口徑不明給出不同數字（08-18 記 19、同輪 brace-matching 誤得 21、08-24 記 20）⇒ 往後本欄一律**同時寫種子數與外部註冊者數**，讀數來源固定為 `node intel/tools/registry-gaps.js`。
      · **零漂移覆核**：MANIFEST 20 檔／registry.json 2 筆／views 命中 `HL.fair` 25／`liveStats.record` 46 檔／`support.js` 203 行＋11 外部註冊者／`guild.js` 295 行／`notify.add` 23 檔 37 呼叫點——全部與 08-24/08-25 逐位相同。
      · **整格缺席 ⇒ 新模組「收集套組與圖鑑」（absent·全庫首見）+ 卡 #134**：見 ⑥。
    - **⑥ 本輪取材法與它撿到的東西**：延續 08-26 08:00（後台→B2B 平台供應商）與 14:00（金流→支付編排商）的「**配對取材**」——本輪台帳輪到**功能**，
      而功能的欄位級語意只有**賣遊戲化引擎的人**寫得出來 ⇒ 首次取材 **Smartico／Gamanza Engage**（全庫首次：`grep -rio smartico intel/` 此前僅 **2 次**、且都在已退役的 `reports/2026-07-13.md`）。
      · **正面結論（連續多輪沒機會確認的一條）**：以供應商的任務條件清單逐項對表，ApexWin 的**任務條件維度確實齊備**——次數型✅`challenges.js`／遊戲別型✅#64+`gameAxes`／期間型✅`promoCal`／名額型✅#57／存款動作型✅#65。
      · **缺的那一格**：「Achievement/**Collection** Systems（gem sets／card collections／map pieces、7 of 10 pieces 的成套驅動）」在 ApexWin **完全不存在**，而且是**可機械證明的 absent**：
        `achievements.js` 的判定只有 `stat >= goal` 與 `test(stats)`，兩者都對**純量**終身統計求值 ⇒ 沒有集合型狀態、沒有 piece 掉落來源、沒有成套判定。
        全 `src/` grep `收集/碎片/集滿/套組/collection/fragment/shard` 的命中**全部**落在 slot 內部玩法（pirots 網格收集／golden-toad／cases 開箱）＝**單局內**詞彙，與跨局蒐集進度無關。
      · **為什麼它不是徽章的皮膚**：徽章＝跨門檻的單點事件；蒐集＝集合狀態＋成套判定。**「差 3 片」這個狀態本身就是回站動機，而門檻式進度只有一個百分比、沒有「缺哪幾片」。**
      · **刻意不開卡的兩處**（避免雙胞胎與 avoid 外溢）：Marketplace「以外部 API 發紅利」＝兌現通道差距，落在 `CONTROL.avoid`（真金流）⇒ 只把 `點數商城` 的 partial 理由釘死，不開卡；
        tournaments 的 cross-vertical scoring ＝ #85 `HL.scoreAxis` 已是同一抽象，差的只是第二個 vertical（運彩在 avoid）⇒ 不開卡。
    - **⑦ 對船長的一句**：這個閘（#118）上現在排了 **13 件事**——平台卡 **#125／#127／#128／#130／#131／#132／#133／#134／#135 共 9 張**（後兩張本輪新開，其中 **#127 是已批准**）＋維護債 **T27／T29／T42／U34 共 4 條**。
      08-23 你收到這則時是 1 件、08-26 14:00 是 11 件。**裁決題目與 08-23 完全相同，只有代價在變大。**
    - **怎麼看**：本輪**沒有任何使用者可見的改動**（產出全在 `tests/` 與 `intel/`，兩者都不出貨）⇒ 線上站台與昨天逐位相同，不需要 Ctrl+F5。
      要看本輪成果請跑：`node prototype/tests/run.js`（**282 項全綠**，較上輪 +1＝本輪新鎖）與 `node intel/tools/registry-gaps.js`（三段報告，第 ③ 段是「該行動的清單」）。

- **2026-08-26（平台軌 · **14:00 窗** · **情報輪**＝金流台帳全審 5 模組 + **新增全庫首見模組「資金分倉／保險庫 Vault」** · rainbet 到期票複查 · 開卡 **#132／#133** · **三軸吸收進 #82 不開雙胞胎卡** · sweep 回填 2 筆 · claim `p-141020-c4a1`）**
    - **① 為什麼是情報輪而不是建置輪（據實記，非藉口）**：佇列裡 **7 張平台卡（#125/#127/#128/#130/#131/#132/#133）全數卡在 [P-FS] 3-byte 邊界**，其中 **#127 是已批准**。
      本輪先把「有沒有便宜的路可以繞過那 3 bytes」量到底，三個結論全是新的：(a) 可回收 **0.0KB**（`first-screen-deps.js`：三支 `shared-dep-blocked` 就是 #118 本體那 64.2KB）；
      (b) ⭐ **首屏 90 支 eager script 無一是死碼**——逐支掃它註冊的 `HL.<ns>` 有沒有外部消費者，**只有 2 支零消費者**（`core/faucet.js` 9.3KB／`data/games-loader.js` 2.4KB），
      而**兩支都是自掛載 boot 模組**（faucet 自管 interval 在餘額見底時浮藥丸、games-loader 開機讀 registry）⇒ **零消費者 ≠ 死碼**，它們是「沒人需要呼叫」不是「沒人需要」。
      ⇒ **不存在「先砍點肥肉買時間」這個選項**（此條先前無人量過，是本輪對船長裁決最有用的一格）；
      (c) **lazy→lazy 靜默退化風險 0**——我不只掃清單上的檔，是掃**全部 30 支非首屏檔** ⇒ 既有 `platform/lazy-no-unsatisfiable-shared-dep` 射程完整，
      **而它正是 #118 真的落地時用來抓「搬完之後誰靜默壞掉」的機械網**。⚠️ 也因此**本輪刻意不立新鎖**：想立的那條與它重複，據實不造雙胞胎鎖。
    - **② sweep 前置抓到當日失真**：`ledger-card-sweep.js` 告警 2 筆——`導覽殼層`／`多語系前端` 的 evidence 仍寫「⇒ 開卡 #129」，而 **#129 就在今天 08:00 窗落地**。
      這正是工具檔頭寫的「卡落地日在該模組 `last_audited` 當天或隔天 ⇒ 純看新鮮度指標對這種失真**完全免疫**」的**第二個實例**（第一次是 08-14 首跑）。
      回填後重新判定：`導覽殼層` **present 不變**（#129 從來不是它 present/partial 的理由，它真正的缺口仍是 #93）、`多語系前端` **partial 維持但理由由三個收斂為兩個**。sweep ⚠️ 歸 0。
    - **③ 台帳審金流 5 模組**（`last_audited` 08-23＝全 8 分類最舊）：五個 status 全數維持。其中 `提款審核佇列` 讓**本模組自己記下的通則第二次生效**——
      `審核|review_queue|pending_withdraw` 命中 **3→4**，逐筆讀過**四筆全部是在描述本功能不存在**（3 筆註解 ＋ `service-level.js:598` 一筆給玩家看的說明字串）
      ⇒ **命中數上升的增量來自把缺席寫得更清楚**，不是功能出現。回填後全庫最舊變 `功能`/`活動`/`資安`（皆 08-24），下一個輪替＝**`功能`**。
    - **④ ⭐ 真產出一＝台帳新增全庫首見模組「金流／資金分倉／保險庫 Vault」(absent) + 開卡 #132**。`vault|保險庫|分倉|金庫` 全 `src/` **0 命中**；
      `儲蓄|鎖倉|存起來|savings|reserve|不可下注` 命中 2 筆**皆為無關註解**。**開卡前先自己打自己**（防雙胞胎），三條邊界都成立：
      與 `HL.bonus` locked/unlocked **鎖的主體相反**（平台鎖玩家 vs 玩家鎖自己）／與 `HL.rg` 冷靜期·自我排除 **鎖的對象不同**（鎖行為·全有全無 vs 鎖金額·可繼續玩）／與提款不同（那需牌照，分倉完全站內）。
    - **⑤ 真產出二＝三個新軸吸收進 #82，不開雙胞胎卡**（SKILL 第 3 步去重規則的正向執行——「同一個模組被連續幾輪審到時最容易長出雙胞胎」，而收銀台正是**連六輪**被審）：
      (a) **可求值的 `recommend`**（#82 原有的 `order` 是靜態名次；Rainbet 把 SEPA 置頂標 recommended、GR8 寫的是依 player behavior 動態調整）——並在卡上釘死「不得長成第二份受眾表，復用 `HL.release.AUDIENCES`」；
      (b) **`networks[]`：幣別 ≠ 網路**（Rainbet 20+ 幣 × 15+ 鏈 vs `CRYPTO_COINS` 硬寫 1:1，USDT↔TRC20 唯一）；
      (c) **交易狀態詞彙**（`TXN_KINDS` 只有型別沒有狀態、`pending|處理中|declined` 於 `app-shell.js` **0 命中**）。
      ⭐ **(c) 的價值超出 #82**：台帳「提款審核佇列」absent 的**根就是它**——沒有 `pending` 這個態，審核佇列在資料上無處可掛；而**狀態詞彙本身純前端可做、不在 `avoid` 內**
      ⇒ 它是那個 absent 模組**唯一能先推的一半**（這條先前六輪審計都沒有連起來）。
    - **⑥ 真產出三＝開卡 #133**（儲值階段的紅利選擇）。GR8 Tech 官方部落格一手查證 "bonus selection at the deposit stage"；
      `doDeposit()`（`app-shell.js:146-160`）全鏈只碰 `HL.rg.checkDeposit`／`HL.api.walletTxn`／`HL.state.set`／`HL.rg.recordDeposit`／`HL.progressSrc.grantNotify`，
      **`HL.bonus` 與 `HL.promo*` 在該函式內 0 命中** ⇒ **儲值禮在架構上不可能存在**。屬「兩個都做好了但沒有接起來」型缺口（故 S–M 而非 M–L）。
    - **⑦ ⭐ 本輪最重要的方法論，而它不是關於首屏**：Vault 是**純前台**的東西、任何評測都會寫它，本庫卻**連六輪審金流都沒撿到**。
      追下去的原因不是取材對象錯，是 **`CONTROL.avoid` 的外溢**——avoid 明列「真金流串接／KYC」，歷輪因此把**所有**與資金相關的形制都當成「只記錄不推進」，
      而 Vault **完全站內、零金流、零合規面**，被那條禁令的陰影一起蓋掉了。⇒ **`avoid` 清單會投射出比它本身更大的盲區。**
      這與 08-16（玩家保護）、08-24（帳戶安全）、08-26 08:00（營運自動化）同屬一家族的**第七例**，但**漏法是新的**：
      前六例漏的是「**清單上沒有這個維度**」，本例漏的是「**清單上有，但被一條禁令的外溢語意關掉了**」。已提船長最小修法（在 avoid 那行加註「僅指需牌照的真金流」），**未擅自改旗標**。
    - **⑧ 驗證與紀律**：node **280/280 全綠**；`prototype/` **淨零改動** ⇒ **`sw.js` 不 bump**（照 08-26 08:00 窗訂下的判準：問的不是「有沒有動 `prototype/`」，是「這次動到的檔會不會被送到使用者的瀏覽器」）；
      首屏 **1638397／1638400 bytes 逐位未動**（連續第六個數據點）；platforms overdue **0/36**（rainbet 回填 `next_due` 09-09）。
      **刻意沒做的三件事**：不動 `BUDGET_KB`（放寬違反 §10.1）／不砍註解位元組換餘裕（08-24 那次超標**正是註解造成的**⇒ 這條路本質上是拆掉自己的護欄）／不自行把 #118 改成 headless 版（船長 08-23 明文問的就是這題，不代為決定）。

- **2026-08-25（平台軌 · **20:00 窗** · 建置輪＝實作 **#122**〔i18n 第四面：屬性面〕· 台帳審「**前端UI/UX**」5 模組 · 取材＝**新開維度「在地化的產品形制」** · 開卡 **#129／#130** · commit 見下）**
    - **① 實作 #122（屬性面棘輪）**：`core/i18n.js:102` 的 `tAttrs` 翻 **title／placeholder／aria-label 三個**屬性，而棘輪自 #120 起只守 `placeholder`。
      本輪把另外兩個納管：**353 宣告點／156 去重鍵**，首量缺 EN 3／缺 zh-Hans 6，**當輪補到 0**、零容忍、不留基線表。
      新鎖 `platform/i18n-attr-ratchet`（node **276 → 277**）。
    - **② 兩個設計決定，都是「契約差異」不是偏好**：
      (a) 覆蓋判定用新的 **`coversExact`**——`tAttrs`（`i18n.js:104`）只有 `if (d[k]==null) return;`，**沒有 PREFIX/SUFFIX 分支**；
      被前綴表『覆蓋』的屬性值執行期根本翻不到。⚠️ 落地當輪 `strictDelta === 0`＝這條嚴格性**在真實語料上沒有 witness**（拆掉它一樣全綠）
      ⇒ 由**合成探針**站崗（同 #120 健檢②-b 的教訓：沒有 witness 的性質等於沒被守住）。
      (b) `title` 一詞**三**義（HTML 屬性／測項標題 **131 條**／玩家面資料欄位）改用**逐宣告** `testSpecRegions()` 判別，
      取代 #126 批次二的**逐檔** `SPEC_HOSTS`——那 24 支檔的 172 條 title 裡有 **41 條是玩家面文案**（`content.js` 促銷標題／`activity.js`／`responsible.js` 成就名），
      逐檔排除會連它們一起藏掉。連帶退役 **4 筆殘骸**（battle-tempo／challenge-slots／ledger／selftest，`title` 移走後在資料面零命中），`title` 也自 `DATA_FIELDS` 撤出交由屬性面單一持有。
    - ⭐ **③ 本輪最重要的發現不在屬性面，在尺本身——`segmentIsConcat` 把真缺漏當成 N/A 吃掉了**：
      它原本**無條件往外走 5 層**找 `+`；第 3 層起往往已走出物件字面量、進到函式主體，而函式主體裡幾乎一定有某個深度 0 的 `+`（任一行 `var s = a + b;`）
      ⇒ 撿到**別的語句**的 `+` 就把本筆判成「補了也翻不到」而**靜默退出分母**。實測 **34 筆**被這樣吃掉，其中 **32 條是真缺漏**：
      `出發`（小雞過馬路主按鈕）／`點擊略過`／`開牌中…`／`史詩大獎 EPIC WIN`｜`超級大獎 MEGA WIN`｜`大獎 BIG WIN`（三檔大獎橫幅）／`伺服器忙線，請再試一次。`／`💣 踩到地雷，這局結束`…
      **而當時三段棘輪全部是綠的、全部寫著「零容忍」。**
      修法＝往外走每層先問「還在值語境嗎」（`isValueGroup`：物件字面量／引數列／陣列／括號運算式），走進 block 就停；
      但 block **本身仍要量**（`x.textContent = "甲" + n;` 的最近群組就是 block），差別在 block 要**多用 `;` 切段**，否則又撿到兄弟語句的 `+`。
      ⇒ **CLAUDE.md §4「修一半而看不出來」第六例**，形狀是新的：**N/A 口徑排除過度外擴，連它不該排的東西一起排掉**。
      立鎖自問從此多一條：**「這條口徑排除，會不會連它不該排的一起排掉？」**（原本四問只問了正向與反向，沒問**過度**）。
    - **④ 兩個缺陷是被自己寫的反向錨當場逼出來的，不是人工複查**：(i) `testSpecRegions` 首版只看 `run: function`，
      於是 `core/challenges.js:310` 的 `action: { label:"開啟挑戰面板", run: function(){…} }`（**說明中心的行動描述子**）被當成測項而整段免譯
      ——抓到它的是「屬性面認定託管測項的檔，檔案級 `hostsTestSpec` 也必須同意」這條**雙粒度一致性錨**；判準遂補上 `register(` 那半（`isRegisterArgBrace`）。
      (ii) `DATA_EXTRA`／`SPEC_HOSTS` 的 5 筆殘骸全由「明列的檔必須真的有命中」那條殘骸錨逐一逼紅。
    - **⑤ 驗證**：`node prototype/tests/run.js` **277/277 全綠**（+1 常駐鎖）。**負向擾動 13 例、13 CAUGHT**
      （P1 拿掉 block 硬邊界／P2 isValueGroup 恆真／P3 block 不用 `;` 切段／P4 coversExact 退化成 covers／P5+P13 testSpecRegions 丟掉 `register(` 那半／
      P6 localeDeclRegions 恆空／P7 不認 `"aria-label"` 引號鍵／P8 射程跳過 `src/core/`／P9 刪一條本輪補的字典條目／P10 title 偷加回 DATA_FIELDS／
      P11 isRegisterArgBrace 恆真／P12 呼叫名邊界放鬆成前綴比對；每例僅**對應的**那一條鎖轉紅，還原皆回 277）。
      ⚠️ **兩條量測誠實紀錄**：(a) P1 與 P12 **首輪都是打空**——P1 因為既有探針缺了「函式外面還有 `+`」那個形狀、P12 因為 `/register$/` 對 `registerPause` 根本不成立；
      依 08-25 已立的紀律「**擾動打空必須當成失敗處理**」，兩例都補了合成探針後重跑才 CAUGHT。
      (b) P5 首輪是**harness 自己的語法錯**（替換字串尾端加 `// P5`，把同行剩下的 `i++; continue; }` 一起註解掉）⇒ 無 summary 視同失敗、修好重跑。
    - **⑥ [P-FS] 逐位未動**：落地後首屏實測 **1638397／1638400 bytes、90 支 script**，與進場逐位相同（**連續第五個未動數據點**）。
      做法同前手：**一個首屏 src 檔都不碰**（只改 `tests/`〔不出貨〕／語言包〔按語言拆檔、`platform/i18n-packs-not-eager` 擋著〕／`sw.js`）。`sw.js` **v227→v228**（語言包經 SW 快取，不 bump 線上仍露繁中）。
    - **⑦ 台帳審「前端UI/UX」5 模組**（全數回填 `last_audited`）：大廳/遊戲牆 present（`casinoGames` 51 筆、分佈**第四輪逐位零漂移** ⇒ 建議降頻）；
      導覽殼層 present（抽屜三構件全在線）；遊戲客戶端框架 present（`title:` 6 條、`isPF` 2 處，T28 未回退）；多語系前端 **partial**（四面全零容忍，但仍有三個 partial 理由）；分群軸 partial（`volatility` 仍無資料來源，屬遊戲軌權威）。
      ⚠️ **一筆量測法更正**：分群軸這輪改用「grep 註冊呼叫點」——單檔 vm 載 `core/game-axes.js` 會回 `all()=0`（軸其實由 `data/game-traits.js` 註冊），
      **單檔 vm 會給出誤導值**，口徑已寫進台帳省後手一趟。
    - **⑧ 取材＝escape①「加深/擴大」，且刻意開新維度**：`platforms.json` 本日 **0 筆到期**（最近 08-26 leovegas），
      且 08-23／08-24 的負面紀錄已坐實「查平台榜只回聯盟頁」⇒ 照 SKILL「開新維度前先問這份清單漏了什麼」，本輪新開維度 **「在地化的產品形制」**
      （既有七維只有『UX/上手』，從未問過多語系/在地化怎麼做）。外部形制核心＝「**規則引擎輸出型別化狀態，不輸出寫好的句子**」
      ⇒ 直接對上本站一個從沒被命名的缺口：**220 條 `NA_CONCAT`＝結構性永遠翻不到的文字節點**（散 54 支檔），而既有機制 `HL.i18n.fmt`（U22）只被 8 個 view 用到 ⇒ **開卡 #130**。
      **不新增平台**（0 筆到期、`_queued_candidates` 兩筆 caliente/bet-br 仍待船長裁決清單上限）。
    - **⑨ 開卡兩張**：**#129**（🟦已批准待做·S·零首屏成本·今天缺漏就是 0 ⇒ 立棘輪成本為零）＝第五面：非中文 key 的 `t(key, 中文 fallback)`，
      42 呼叫點／37 key **同時逃出全部四面**（第一面要第一引數含 CJK、其餘三面要值是字面量），今天實際外洩 0 條（35/35 fallback 中文都在字典）；
      **#130**（⬜待批准·L·卡在 #118）＝把 220 條串接節點遷到 `HL.i18n.fmt` 模板。
    - **⑩ counters**：`platform_cards_opened` 110→**112**、`platform_cards_implemented` 80→**81**（#122 單張卡完整落地並關閉）；
      `platforms_researched` **不加**（本輪未新增/未深挖任何平台，取材產出落在維度與台帳上，據實不記成研究平台數）。`consecutive_idle_rounds` 維持 0、`yield/stalled` 不加。
    - **⑪ 下輪 08-26 08:00**：台帳輪替＝**`後台`**（7 模組，08-23 最舊；CMS 08-25 較新）。取材＝`leovegas` 本日到期（08-26）⇒ 有真到期可深挖，不必走 escape。
      實作候選＝**#129**（零首屏成本、基線已是 0、輸入資料〔42 呼叫點／37 key／兩支主要檔〕本輪已量好寫進卡，不必重新發現）。
      **#118／#125／#127／#128／#130／#93 仍全數卡在首屏 3 bytes 與 preview 結構性不可得。**
> E3（2026-07-17）：BACKLOG.md 的「分析師日誌」歷史條目歸檔於此（最新在上）。
> 現行規則：BACKLOG.md 只留最新 3 則；三軌（platform/games/maintain）追加新則時，把溢出的第 4 則移來本檔最上方。（2026-07-30 M2 更正：舊述「evolve/consolidate」為 2026-07-23 重構前的四軌 skill 名，已廢除。）



<!-- 2026-08-26 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入（08-24 20:00 窗一則） -->

- **2026-08-25（平台軌 · **14:00 窗** · 建置輪＝實作 **#126 批次二**〔i18n 資料面射程擴到 `src/core/`〕· 台帳審「**擴充性**」5 模組 + **新增 2 個模組** · 取材 crown-coins／1xbet · 開卡 **#128** · claim `p-141215-9c4e` · commit `93a7bc1`）**
    - **① 實作 #126 批次二**：射程 **48 → 92 支檔**（+`src/core/`；用**目錄**不用檔名清單＝新檔天生在射程內，理由同批次一）、**33 條缺漏（EN 33／zh-Hans 21）當輪補到 0**，零容忍不留基線表。`node` **275→275 全綠**（擴的是既有鎖的射程與錨、未新增註冊項故總數不變）、**負向擾動 10/10 CAUGHT**。
    - ⭐ **② 本輪最重要的產出不是補字典，是修一條「看起來在守、其實守錯對象」的不變量**——而且**卡上與掃描器檔頭寫的前置本身就是錯的**：
      批次一與 #126 卡都寫著「`title` 進 `core` 會撞 `selftest.register({title})`，而那只有 `core/selftest.js`／`core/challenge-slots.js` **兩支**」，並據此把 `responsible.js`／`activity.js`／`progress-src.js` 列為「可先做的安全子集」。**三支全都不安全。**
      本站的測項有**三種**註冊形制，舊錨④ 只認第一種：① `selftest.register({…})` 字面（僅 2 支）／② **注入式** `function registerTests(st){ st.register({…}) }`（`responsible.js:286` 起 12 筆即此形，**22 支 core 檔**）／③ **裸呼叫** `register({…})`（`core/selftest.js` 自己——它就是定義 `register` 的那支檔）。
      ② 型光 `title:` 欄就 **191 條測項標題**（`content.js` 25／`responsible.js` 18／`activity.js` 18／`service-level.js` 14／`progress-src.js` 13…）。照原前置直接把 core 併進射程，這 191 條會**當成玩家面缺漏灌進分母**，而**專為此而立的錨④ 會保持全綠**。
      ⇒ CLAUDE.md §4「修一半而看不出來」**第五例**，形狀是新的：**不變量只認了同一件事的其中一種寫法**。判準改為形制無關的 `hostsTestSpec()`（認 `register({ … run: function}`，不認呼叫者名字）。
      ⭐ **形制 ③ 不是人工複查發現的**——是本批新加的錨④-c（「SPEC_HOSTS 明列的檔必須**真的**託管測項」）當場把 `core/selftest.js` 逼紅逼出來的。**把「這條不變量有沒有反向」寫成測項，當輪就回本一次。**
    - **③ 營運受眾改為「逐宣告」（#126 範圍①/③ 說的那個真正的設計題）**：`HL.econCfg.register({label})` 的 **28 條**（`淨損 Cashback（#33）`／`餘額歸零救濟金（#39）`／`VIP 升級金／舊制流水（#29／#74）`…）唯一渲染端是 `views/ops-dashboard.js`（營運受眾）且文案帶**內部卡號** ⇒ 直譯就是把卡號外洩到英文介面。但它們**散落在玩家面的 core 檔裡**（`progress.js` 同一支檔還有 VIP 段位名 `青銅/白銀/黃金/白金/鑽石`）⇒ **檔案級 `OPS_ONLY` 切不開**，把那 10 支檔丟進 OPS_ONLY 會連玩家面真缺漏一起藏掉（方向正好是最危險的那個）。⇒ 逐宣告排除，且計入**看得見的** `naOps`（**靜默丟棄正是尺說謊的方式**）。括號配對沿用掃描器既有的正則字面量跳越規則，不重蹈 08-24「遮罩器不認正則」那次失準。
    - **④ 台帳審「擴充性」5 模組（輪替接續 08-25 08:00 的資料）＋ 本輪查獲第二把壞掉的尺**：Dockable Layout partial（`HL.dock.ids()` 仍 3）／設計系統 present／自動化測試 partial（275）／首屏架構 present（**1638397 bytes 逐位未動**）／上架排程 present。
      ⚠️ **`內容/遊戲上架排程` 的量測式已腐爛，方向是「永遠回報缺口」**：evidence 自 08-04 起沿用 `grep "earlyAccess|early_access|releaseAt|audience"`，今天 `earlyAccess|releaseAt` 兩詞全 `src` **0 命中**——但 **#54 早已落地**（`core/release.js` 397 行），實作用的是 **`earlyAt`／`startAt`**，從來不叫 `releaseAt`，`earlyAccess` 這字也從沒進過程式。**量測式點名了實作根本沒採用的欄位名** ⇒ 照它斷言就會把 present 永遠報成 absent。與 08-25 08:00 那筆「把量測結論寫進被量測範圍」（假**陽**性）互為**反方向的同族**（本例假**陰**性），共同點都是**尺本身壞了**。權威量法改記為「`HL.release` 的下游消費者數」＝實測 **6 模組 + 1 view**。
      順帶更正第三把小尺：`grep "HL.dock.register"` 今天只命中 1 支（evidence 記 2），因為 `panels.js` 是**鏈式**呼叫（`HL.dock` 換行後 `.register({`×2）⇒ 權威量法改用 `HL.dock.ids()` 長度。**含物件前綴的 grep 字串會被鏈式呼叫系統性低報。**
    - **⑤ 新增 2 個台帳模組（盲點第 4、5 例）**：
      · **[擴充性] Feature Flags 模組開關（absent）＝盲點第 4 例，形狀最刁鑽：缺口就寫在本檔自己的清單裡**——`extensibility_patterns` 的**第一條**就是 Feature Flags，但擴充性分類的 5 個模組**沒有任何一列在追蹤它** ⇒ patterns 列了就看起來有在管，實際沒有 status 欄位對應，歷次審計結構上不可能判它缺口。`grep "featureFlag|HL.flags"` ＝ **0**（`旗標` 的 12 筆命中逐筆確認**全是各模組自己的重入鎖/冪等布林**）。**⚠️ 刻意不開卡**：repo 內已有明文反向紀律（`core/game-settings.js` 檔頭「**避免死 flag**」，BACKLOG:431 實際引用它拒絕過新增旗標），而**沒有真實灰度需求就先建的 flag store，定義上就是死 flag 工廠**；且已有三條正交軸（`HL.site`／`HL.money`／`HL.rbac`）在做它的一部分工作。本列的作用是**讓這一格從此有欄位可問**，等第一個真實需求出現再由那張卡順勢落地。
      · **[功能] 通知中心：分類軸與玩家偏好（partial）⇒ 開卡 #128**（本輪唯一淨新，見下）。
    - **⑥ 取材 crown-coins(75)／1xbet(74)（`max_platforms_per_run:2`，兩筆皆今日到期）· 兩筆都刻意換維度而非重掃舊清單**：
      · **crown-coins**：換「**VIP 階級待遇**」維度。一處**本庫記載更正**——VIP **不是五階，是 6 階**（Entry/Bronze/Silver/Gold/Diamond/Emerald）。四項階級待遇逐項對照全數已有出口（更大日登獎＝`rewards.js` LADDER 里程碑 8/15/22/30／月獎＝`reload.js` 三週期／**更快兌獎＝`HL.sla` 且已被 `sla/monotone-service` 上鎖**／**商城高階解鎖＝#123 已落地**）⇒ **零淨新**，`saturation_watch` **1/2**（08-11 那輪產出過 #88，並非零增量）。
      · **1xbet**：換「**玩家保護/責任博弈**」+「**帳號安全**」兩維度。⭐ **結論與預期相反：限額面是 ApexWin 更完整**——1xBet 載明的存款限額／工作階段限額／現實檢查／自我排除四項 ApexWin 全有，且**淨多**「調降立即生效、調升須等 24h」的不對稱冷卻（`rg/raise-cooldown`）＋儲值日/週/月三週期。1xBet 淨多兩項，兩項都**刻意不開新卡**：① 鎖定特定遊戲品類（ApexWin 是全站粒度，但 `evaluate(…, axis)` 已有 axis 參數＋`HL.gameAxes` 已有分群軸 ⇒ 屬「加一筆 spec」非容器缺口）／② 裝置 session 清單 + 2FA ⇒ **已是 #125**（依 SKILL 判準比對**來源模組 + 出口形狀**而非卡名：同一模組、同一出口形狀、同一批首批註冊者 ⇒ 不開雙胞胎；本輪的貢獻是給 #125 補上**第一個非 Stake 的對手形制**）。
      ⚠️ **一條方法論結論（本輪自己先踩了才寫下）**：`saturation_watch` 的計數**只能由真的零增量輪推進**。本輪首版把 1xbet 從 1/2 遞增到 2/2，但本輪**不是**零增量輪（它產出了 #128）——只是增量落在**別的維度**上。已改回 1/2。⇒ **連兩輪在同一份維度清單上零增量，不等於這個平台身上沒有東西可學。**
    - **⑦ 開卡 #128（⬜待批准）通知的分類軸與玩家偏好**：`grep -c "notify.add("` ＝ **37 個呼叫點**，而 `add(n)` 收 `{ic,title,text}`——**零分類維度**；`grep "notifyPref|通知偏好|退訂|mute"` ＝ **0**。37 個生產者共用**一條扁平串流**，玩家關不掉促銷推播、無法只看帳務。**在對標平台這是被歸進玩家保護工具的**（1xBet 把速率告警門檻與現實檢查間隔都做成可設定），而 ApexWin 的 `HL.rg` 已很完整、**只有通知這一面空著**；且 `HL.notify.add` 是**中央掛鉤**（`liveStats.record` 家族）⇒ 缺分類軸＝所有下游都沒有選擇權。**與 #125／#127 卡在同一個閘上**（首屏 3 bytes）⇒ 標 ⬜待批准。
    - **⑧ [P-FS] 逐位未動**：落地後首屏 **1638397／1638400 bytes、91 支 script，與進場逐位相同**（只碰三類零首屏成本檔：`tests/`〔不出貨〕／語言包〔按語言拆檔、`platform/i18n-packs-not-eager` 擋著〕／`sw.js` **v225→v226**〔語言包走 SW runtime 快取，不 bump 則線上仍露繁中〕）。連續第四個「邊界未動」數據點。
    - **⑨ counters**：`platforms_researched` 105→**107**、`platform_cards_opened` 109→**110**；**`platform_cards_implemented` 刻意不加**（#126 是分批卡，批次二落地但卡未關、仍標 🏗️進行中——不把「做了兩批」記成「做完一張」）。`consecutive_idle_rounds` 維持 **0**、`yield/stalled` 不加。孤兒 `games/slot-engine/`＋`registry.json` mtime 仍 **08-03** ⇒ 依 §7 一位元組未碰、待船長裁決 ENG-1。
    - **下輪（平台 08-25 20:00）**：台帳輪替＝**`前端UI/UX`**（5 模組，08-23 最舊）。取材＝`platforms.json` 已 **0 筆到期**（最近到期 09-08）⇒ 走 escape①「加深/擴大調研」或依 `stale_days:7` 挑 `last_investigated` 最舊者重驗。實作候選＝**#126 批次三**＝把 **SPEC_HOSTS 那 22 支檔**收回射程，而那**正是 #122 的題目**（逐宣告判別「同一物件字面裡有 `run: function` 的 `title` 是測項標題」）⇒ **建議下輪直接做 #122**，本輪已把它的輸入資料（22 支檔 × 191 條 title）量好寫進掃描器檔頭，不必重新發現。**#118／#125／#127／#128／#93 仍全數卡在首屏 3 bytes 與 preview 結構性不可得。**

- **2026-08-24（平台軌 · **20:00 窗** · **先 rescue 本軌 14:00 窗死鎖孤兒 WIP** · 建置輪＝實作 **#121** i18n 資料面〔+1 常駐鎖·負向擾動 7/7〕· 台帳審「資安」5 模組複核 + **新增全庫首見模組** · 開卡 #125/#126）**
    - **第一件事＝rescue-commit 前手的孤兒 WIP**（`84f952f`／`bf17f46`）。本軌 14:00 窗 claim 鎖後凍結（心跳停在 14:40），遊戲軌 16:00 窗以 stale-heal 奪鎖並複驗全綠、依 §7 一位元組未碰、明文指定本窗接手。我先獨立複跑 `node` **272/272 全綠**＋`first-screen-deps.js --verify` 四項全過，**未重做任何一行**直接分段提交。
      ⭐ **那份 WIP 修掉的是一個很危險的東西，值得留在日誌裡**：首屏依賴分析器的 `maskSrc` **不認正則字面量**，`core/reports.js:69` 那行 `var NEEDS_QUOTE = /[",\n]/;` 讓遮罩自該行起整檔失準（最長一段吞掉 line 318→738）⇒ `HL.reports = {…}`（:714）**根本沒被看見** ⇒ 分析結果「① 全域：（無）」⇒ **「首屏零引用」在沒有任何全域可查的前提下自動成立** ⇒ 把**全庫可回收 KB 最大的一支（42.9KB）**判成 `safe-to-lazy`。當時 `--verify` 全過、既有兩條鎖全綠、node 269 全綠。
      ⇒ CLAUDE.md §4「修一半而看不出來」家族的一個新形狀：**最高信心的那一格，可能正是「什麼都沒看到」產生的**。「零引用」必須先證明「有東西可引用」，否則它只是**沒有證據**，不是**安全的證據**。
    - **實作 #121**（落地紀錄見卡片）：i18n 第三面＝中文以**資料值**存在於 `src/data/` 的物件裡，渲染端只寫 `text: p.title`（那一行一個漢字都沒有）⇒ #119 呼叫面與 #120 DOM 字面面**天生掃不到**。實測 60 鍵／**45 條缺漏當輪全補到 0**（`en.js` +25／`zh-Hans.js` +19），並在**同一支**掃描器加第三種抽取法（不得另立第二把尺）＋第三段棘輪 `platform/i18n-data-ratchet`（零容忍、**不留基線表**、七道反向錨）。`node` **272→273 全綠**、負向擾動 **7/7 CAUGHT**。
    - ⭐ **本輪最重要的一條：`title` 這個欄位在不同射程裡是不同的東西，而這件事可以立成鎖**。#120 當初把 `title:` 整批排除，是因為它與 `selftest.register({ title })` 一詞兩義（333 命中裡約 122 是測項標題）。但本面的射程是 8 支資料宣告檔，**`selftest.register` 在其中零命中** ⇒ `title` 在這裡是單一語意、可以安全納入。
      關鍵是我**沒有把這個結論當成一次性的人工判斷**：反向錨④ 每輪重掃射程內有無 `selftest.register`，有就轉紅並在訊息裡寫明處置（移出 `src/data/` 或把 `title` 撤出欄位閘）。⇒ **「這個前提現在成立」與「這個前提會一直成立」是兩件事，只有後者值得拿來放寬射程。**
    - ⚠️ **第二重要：一個「裸正則多算一條」的實例，差點在字典裡留下死鍵**。首版補字典時照裸正則量到 61 條，其中 `data/games.js:50` 的 `title: "我的拉霸"` 位於 dev-kit **註解範例**內；抽取器走狀態機（只認宣告、不認提及）正確排除之，實際是 60 條。那一條若留著，就是字典裡一條**沒有任何表面在消費**的鍵——而它會讓後手以為那個位置「已覆蓋」。已移除並寫進掃描器檔頭：**量本面的權威是 `scanDataValues`，不是 grep。**
    - **台帳審「資安」5 模組**（輪替接續 14:00 窗的活動）全數複核回填，狀態全維持（present／weak／absent／present／partial）；三組可重現樣式皆逐位重現（`isLive()` 73/35 持平·`velocity|deviceId|antifraud|…` 連八輪 0·`kyc|…` 連三輪 4 且全為註解）。
      ⭐ **撿到一筆正向的架構訊號**：`HL.rg.(allow|check|gate|canBet)` 的**檔數自 20 升為 21**，新增的是 `views/instant-moles.js`（Moles 打地鼠，遊戲軌 08-21 復刻）⇒ **另一條軌在沒有任何人交代的情況下，把新遊戲的押注路徑接上了責任博弈閘**＝限額型別註冊表這個形制已被當成「新遊戲的既定接線」。也讓判準更靈敏：**新遊戲上線而 rg 閘檔數不動＝該遊戲漏接閘**。
      ⚠️ **並對 08-22 自己寫下的一條量測法結論據實更正**：該輪宣告「`\brole\b` 命中數自本輪起不再全是 aria（`rbac.js` 內有真實 role 語意）⇒ 舊判法失效」——本輪機械複驗**不可重現**：`core/rbac.js` 的 `\brole\b` 命中數是 **0**（通篇用中文「角色」與 `grant`／`aud`／`ctxFlag` 詞彙），全 src 19 命中濾掉 aria 書寫形狀後**剩 0** ⇒ **舊判法仍然成立、並未失效**。
      ⇒ 「台帳承諾與現況不符」家族的新變體：**前一輪主動宣告一個仍然有效的量測法「已失效」**，比高報缺口更難察覺——後手會直接不再跑那條 grep，那個方向從此無人量測。教訓：**宣告一把尺失效，必須附上讓它失效的那筆具名命中**（本例只要當時把 `rbac.js` 的命中列出來，就會發現是 0）。
    - ⭐ **新增台帳模組「資安／帳戶安全自助中心」（absent，全庫首見）＝台帳盲點第 2 例**：資安原有 5 個模組分別是工程軸／營運看玩家／法遵／玩家管錢與時間／營運端授權，**沒有一個是「玩家管自己的帳號安全」** ⇒ 這一格從來沒有欄位會問到它。已開 **#125**（⬜待批准，因入口必落在首屏檔 `app-shell.js`、撞 [P-FS] 3-byte 邊界）。
      並記下「grep 命中 ≠ 功能出現」第 3 例、**首次由中文一詞兩義造成**：`2fa|authenticator|裝置管理|…` 全 src 79 命中**全是誤導**——絕大多數是可驗證公平的「🔎 **驗證器**」，與帳號安全的「**驗證器 App**」同詞不同義。
    - **[P-FS] 本輪逐位未動**：落地後首屏實測 **1638397／1638400 bytes、90 支 script，與進場時逐位相同**（語言包按語言拆檔不在 `index.html`、`tests/` 不出貨）。`sw.js` v221→v222 是必須的——語言包經 SW 走 runtime 快取，不 bump 會讓既有使用者拿到舊字典＝**改動全綠、線上仍露繁中**。
    - 🔧 **本輪機械確認了一件三軌一直只能推測的事**：`preview_start` 在排程 session 下**回傳明確拒絕**（「Dev servers can't be started from unattended sessions」）⇒ **#118 與 #93 需要的「可靠 preview 輪」在排程軌下是結構性不可能，不是運氣問題**。這替 [P-FS] 的裁決去掉一個變數：#118 只有「船長前景做」或「允許 headless 純搬移版」兩條路，沒有第三條「等某輪 preview 剛好能用」。
    - **下輪（平台 08-25 08:00）**：台帳輪替＝**`資料`**（3 模組，08-22 最舊）。取材：`platforms.json` 08-25 到期 4 筆（stake-us／roobet／crown-coins／1xbet）⇒ 依 `max_platforms_per_run: 2` 深挖前二。實作候選 **#126**（#121 的續攤·先定受眾口徑再分批·首屏零成本·headless 可驗）；**#125／#118／#93 仍卡在首屏 3 bytes 與 preview 不可得**。**未碰** `prototype/games/registry.json`＋`games/slot-engine/`（mtime 08-03·依 §7·待船長裁決 ENG-1）。

<!-- 2026-08-25 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入（08-24 08:00 窗一則） -->

- **2026-08-24（平台軌 · **08:00 窗**建置輪 · 實作 **#123** 商城目錄註冊表 + 受眾資格閘〔+1 常駐鎖·負向擾動 7/7〕· 深挖 bet365／rollbit · 台帳審「功能」14 模組 + **新增全庫首見模組** · 開卡 #123/#124）**
    - **實作 #123**（落地紀錄見卡片）：`HL.shop.register()` 註冊表（內建品項走同一道驗證門、`CATALOG.push` 單一寫入口）＋ 受眾閘（UI 與**派彩**兩側共用 `eligible()`）＋ `release.js` 受眾詞彙新增 `goal` 欄位。`node` **267→268 全綠**、負向擾動 **7/7 CAUGHT**（每例只紅本鎖、還原回 268）。
    - ⭐ **本輪最重要的一條：一條既有的常駐鎖差點被我違反，而它反過來讓設計變好**。#107 的 `platform/audience-promo-hidden-not-greyed` 立的是「**灰掉＝預告一個玩家拿不到的獎**」⇒ 不符受眾必須隱藏；但商城要的正好相反（bet365 刻意讓你看到還沒解鎖的 Cash Reward）。
      該鎖以檔案為射程（只掃 promo-cal.js）⇒ **我照商城形制寫也不會轉紅**＝可以靜靜在站上長出第二種風格而沒人說得清規則。真正的差別是**受眾維度可不可達成**（VIP 只升不降＝目標；newcomer 過了永遠回不去＝空頭承諾）⇒ 把它做成**詞彙自帶的 `goal` 欄位**，讓「該藏 vs 該鎖」由 `HL.release` 回答，並立鎖禁止 `shop.js` 列舉 kind。**個案風格 → 可機械檢查的規則。**
    - ⚠️ **第二重要：[P-FS] 預告的事本輪發生了，但來源不是新 script 而是註解**。首版落地即讓 `platform/first-screen-budget` 轉紅（**1601KB > 1600KB**）。處置＝只收緊**本輪自己寫的**註解（`shop.js` −746B／`release.js` −134B），**未動他人檔案、未放寬門檻**、未新增任何首屏 `<script>`。⇒ 給下一輪的教訓：**「零首屏成本」不等於「不加 script」，註解也是位元組**；餘裕已進入「一段話就能撞線」的量級。
    - **台帳審「功能」14 模組**（08-21/08-22 起最舊·輪替接續 08-23 的金流）全數回填 08-24，狀態全維持；**新增台帳模組「功能／點數商城與兌換目錄」（★，partial，全庫首次入帳）**。
      ⚠️ **這筆新增本身是一則台帳盲點紀錄**：`core/shop.js`＋`core/redeem.js` 自 07 月就在站上（#36/#42），但因為當初以「開卡」形式落地、**沒有對應的台帳模組**，於是**連續多輪的 `功能` 分類審計都沒有任何欄位會問到它** ⇒ 硬寫目錄這個擴充性缺口安靜存在了七週。與 08-16「玩家保護維度漏了 35 份 dossier」**同型**：**台帳沒有的欄位，審多少輪都審不到。**
    - **深挖 bet365（tier-1）**：終於拿到那個「有階梯的一半」的具體機制——Loyalty Club ＝ 5 Level × 每級內若干 **Step**（每填滿一 Step 就發 Coins，不必等升大階）→ Coins 進 **Store**（Cash Reward 僅 Platinum/Diamond）。
      ⇒ ① ApexWin 既有的雙層進度（RANKS 5 × SUBS 5 ＝ Lv 1..21，#29）**取得第二個獨立對手實證**（此前僅 Shuffle），連「小步就發獎」細節都一致 ⇒ 不需改動；② 形制差＝兌換目錄的等級門檻 ⇒ #123。
      ⚠️ **射程誠實邊界**：上述全出自 **poker 側**公開頁；**casino 側仍無公開階梯**（第 5 次 reconfirm）⇒ 不得當成 bet365 casino 的制度。**並因此留了一個裁決給下一輪**：本輪增量非零 ⇒ 依判準 saturation 計數歸零、**不判 saturated**；但若觀察單位縮成「bet365 casino」，它已連 5 輪零增量 ⇒ 建議把該筆的射程改寫成「集團跨產品線留存形制 + UX 教材」或降頻，本輪**不擅自改 tier/頻率**。
    - **深挖 rollbit（tier-2）**：淨新＝**分潤獎池**（獎池由平台每日利潤 20% 挹注）⇒ 開 **#124（⬜待批准）**，明確不做代幣/質押。返水 5%→10%（同業 15–20% ⇒ 我方假站慷慨度合理、真站保守）記為對照不改。
      ⚠️ **本站是玩家保護的負面樣本**（Trustpilot 2.4/5、Casino Guru **4.2/100**、出款/KYC 爭議）⇒ **機制可抄、治理不可抄**；且這直接決定 #124 的設計：分潤獎池靠「相信平台照帳付」撐著，我們的等價物是 `HL.ledger` 的**可查帳**，賣點應是「金額怎麼算出來的你點得進去看」。
    - **玩家保護維度**：bet365 五項工具（存款限額/reality check/時間提醒/自我排除/帳戶活動彙總）⇒ ApexWin **全備**（#67/#70/#86/#96、`rg/*` 11 條鎖）⇒ 淨新缺口 **0**，不開卡。
    - **台帳自我查核**：`intel/tools/ledger-card-sweep.js` 掃 30 筆卡號引用，**⚠️ 待人工確認 0 筆**（無「缺口已被自己的卡關閉卻沒回填」）。
    - **驗證誠實聲明（連續第二十四輪）**：排程輪工具明文禁止啟 dev server ⇒ **UNVERIFIED＝視覺**（鎖態卡樣式、`🔒 未解鎖` 按鈕、解鎖條件那行的排版未經目視）。邏輯、帳目、可見性、零回歸全部 headless 驗過（§9 配方）。

<!-- 2026-08-24 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入（08-23 14:00 窗一則） -->

- **2026-08-23（平台軌 · **20:00 窗**建置輪 · 實作 **#120** i18n 棘輪第二段〔**DOM 綁定面：267 條缺漏當輪補完、基線直接 0 且刻意不建基線表**〕· 台帳審「金流」5 模組 ＋ 回填「多語系前端」· 開卡 **#122** · **不取材**〔`platforms.json` 逾期 0/36、最近到期 08-24〕）** — 閘門：`loop_enabled/platform_track_enabled/auto_implement` 皆 true；進場 `build_lock` 乾淨 false（遊戲軌 16:00 窗 16:45 釋放），claim `p-201200-c9e2` **即刻單檔 commit** `64351bc` 後重讀確認 token 仍在＝claim 成功；dark **4.95h**（`last_platform_run_at` 08-23T15:12）<24h ⇒ 非 catchup；`lead_track=games` 准讓路，但前手 14:00 窗已把「台帳輪替＝`金流`」與「實作候選 #120（推薦·不吃首屏·headless 完全可驗）」指好、且本輪有真產出 ⇒ **做而不讓路（跨輪指派連續第二十輪）**。照 ENG-1 處方**分段提交**（補譯 `03a9eaa`／掃描器與鎖 `930604e`／intel）。
    - **實作 #120**（落地紀錄見卡片）：`scanDomBindings` 併進同一支掃描器；新鎖 `platform/i18n-dom-ratchet` 零容忍無基線表；`covers()` 改為與 `tText()` 同構。node **264→265 全綠**、**負向擾動 10/10 CAUGHT**、**首屏零影響**（1593.7KB／90 支，語言包非首屏）、sw v215→v216。
    - ⭐ **本輪最重要的一條：擋下錯誤的那個，不是鎖，是補譯腳本自己的自我檢查**。第二批補譯時腳本對「key 與 value 逐字相同」丟例外，才逼出 `changedCharSet` 的「等長換詞」污染（`重整→刷新` ⇒ 假映射 `整→新` ⇒ `🔄 重新整理` 被誤判需要 zh-Hans 條目）。這條污染**第一段棘輪（#119）從 08-23 08:00 起就一直帶著**，只是當時基線恰為 0、沒有 witness 所以不會露出。⇒ 補上第二道濾網（目標字若是繁簡共用字則整條映射作廢）後一擊命中全部 4 條假映射。**教訓：把「不該發生的事」寫成腳本裡的 throw，和寫成常駐鎖一樣值錢——它抓到的是鎖天生看不到的那一面。**
    - ⚠️ **第二重要：一條「擾動打空」的紀錄**。M8「拿掉 PREFIX 覆蓋判定」**沒讓鎖轉紅**——因為兩面都補到 0 之後，`covers()` 的前後綴分支已無任何真實 witness。**沒有 witness 的性質等於沒被守住**（與 08-23 08:00 窗那條「擾動打錯目標時紅不紅都證明不了鎖」互為表裡：一個是打錯目標，一個是目標不存在）⇒ 在鎖裡自造 witness 後 M8/M9/M10 全部 CAUGHT。
    - **台帳審「金流」5 模組**（08-21 起最舊·輪替接續 14:00 的後台）：支付/收銀台 partial（缺口形狀**連第七輪**逐位相同；`HL.payment|payments.register` 仍 0；`FIAT_METHODS`/`CRYPTO_COINS` 仍無 `enabled`/地區/幣別/限額）；錢包/帳本 present（真插樁點仍 **9 行／6 檔**，naive 10 含 `reports.js` 的 `src:` 述詞）；提款審核佇列 absent（raw 4 命中**全部在描述本功能不存在**）；P2P partial（3 處逐位同，連第四輪）；獎金/流水 partial（`wagerMult` 仍僅 1 命中且是註解）。`ledger-card-sweep` **0 筆告警**。
    - ⭐ **金流審計的兩個真訊號**：① **#82 的樣板換人了**——08-21 記「照抄 #109 `HL.reports`」，但今天 14:00 落地的 **#61 `HL.content` 更貼**：它同時有型別 fail-closed（同 #109）＋**窗口**（支付通道的維護/上下架本來就是時間性的）＋**受眾一律向 #54 `HL.release.matches` 求、本層不建第二張表**（地區/幣別/限額正是授權語意，照抄它才不會把法規閘寫進顯示層）。② **獎金/流水那一格第一次有了明確的觸發條件**：#107＋#61 落地後「這筆活動獎 3×」第一次**有地方寫**，但實測 `content.js` 的 payload 仍純顯示欄位（tag/title/sub/ic/c1/c2）、無送幣語意 ⇒ 需求仍是潛在 ⇒ **仍不開卡，但把觸發條件寫死進台帳**：第一個促銷 descriptor 的 payload 出現送幣語意時即轉真需求。
    - **不取材**：`platforms.json` 逾期 **0/36**，最近到期 08-24（bet365／rollbit）⇒ 無深挖對象；前手 14:00 窗六小時前才做過一次完整取材並把結論寫進 `_sourcing_negative_log`（「聯盟榜是最大噪音源」），六小時內重跑同一組來源屬 busywork ⇒ 本輪不取材、`platforms_researched` 不加。
    - **提報船長**：**[P-FS] 首屏預算仍是 1593.7KB／90 支（餘裕 6.3KB）**——本輪刻意選了**零首屏成本**的卡（語言包非首屏、tests/ 不出貨）；平台軌「#118 落地前不再新增首屏 script」的自我約束**本輪確實遵守**。counters：`platform_cards_opened` +1（#122）、`platform_cards_implemented` +1（#120）、`platforms_researched` **不加**、`consecutive_idle_rounds` 0。
    - **驗證誠實聲明（連續第二十三輪）**：排程輪工具明文禁止啟 dev server ⇒ **UNVERIFIED＝視覺**。本輪補的 264 條譯文裡有多條長段落（`core/fair.js` 的機制說明、`views/ops-dashboard.js` 的健檢文案、桌遊規則），**英文普遍比中文長 1.5–2 倍**，是否在窄面板/按鈕內斷行破版**未經目視**。機械面已驗到底：字典可 vm 實跑載入、鍵與程式碼字面 trim 後逐字相同、兩面缺漏皆 0、node 265 全綠。下個 preview 輪建議切 EN 逐一目視這三處。
    - **下輪（平台 08-24 08:00）**：台帳輪替＝**`功能`**（依 8 分類序，金流已審完）。**取材該做了**——bet365／rollbit 08-24 到期。實作候選 **#121**（走「補字典」那半·不吃首屏·headless 可驗）或 **#122**（本輪新開·同上·但要先解 `title` 一詞兩義）；**#118 仍須可靠 preview 輪**；**#93 仍需 preview 目視三個導覽表面**。**未碰** `prototype/games/registry.json`＋`games/slot-engine/`（mtime 08-03·依 §7·待船長裁決 ENG-1）。

- **2026-08-23（平台軌 · **14:00 窗**建置輪 · 實作 **#61** 內容資料層 `HL.content`〔**一張 banner 的三語第一次由「作者當下寫齊」而不是「有人記得去補字典」**〕· 台帳審「後台」7 模組（CMS **absent→partial**）· 開卡 **#121** · **不取材入庫**〔逾期 0/36，本輪取材結論寫進新設的 `_sourcing_negative_log`〕）** — 閘門：`loop_enabled/platform_track_enabled/auto_implement` 皆 true、進場 `build_lock` 乾淨 false（維護軌 12:00 窗已釋放）、claim `p-141540-b4d1` 即刻單檔 commit `e841850` 後重讀確認 token 仍在＝claim 成功；dark **5.6h**（08-23T08:34）<24h ⇒ 非 catchup；`lead_track=games` 下平台軌照做自身建置（真產出、非讓路）。
    - **實作 #61**（落地紀錄見卡片）：`core/content.js` 新檔＝內容註冊表（schema/窗口/受眾/三語/型別 fail-closed/熱替換保序），12 則促銷內容自 `mock-data.js` 遷入並**逐條刪除原陣列不留代理**，`views/lobby.js`／`casino.js` 改讀註冊表。node **260→264 全綠**（+4 常駐鎖）、**負向擾動 5/5 CAUGHT**、**遷移對等性 headless 12 項全 PASS**、sw v214→v215。
    - ⭐ **本輪最重要的一條：容器的價值有時不在容器，在它逼你看見的東西**。開卡理由是「換一張 banner 要改碼」（真的，但不痛）。真正的缺陷是遷移時逐條比對字典才發現的——**那 12 張卡的 36 段文案幾乎零 i18n，切 EN/简中時首屏最顯眼的輪播原樣顯示繁中**，而 node 全綠、console 零錯誤、繁中下完全正常。
      **而且它躲過了我們昨天才立的棘輪**：#119 掃 `t("中文")` 呼叫點、#120 要掃 `text:` 字面，而這裡的中文**兩者都不是**——它是**資料值**（渲染端寫 `text: p.title`）。⇒ 一個新的家族成員：**「中文躲在資料裡」**，已開卡 #121 並量出全貌（mock-data 60 條中缺 EN 45）。
    - **台帳審「後台」7 模組**（08-20 起最舊）：**CMS 內容管理 absent→partial**（#61 落地當輪；`mock-data` 模組級 `var` **26→24**＝該數字建檔以來首次下降；仍 partial 的三個理由：無編輯 UI／無公告頁面型內容／24 個硬寫陣列尚未遷移）；
      營運後台儀表板 **partial**（唯讀性 `onClick|onInput|onChange` 恰 **2** 處＝**連六輪零漂移**；新記一個已就緒接點＝`HL.content.counts()`）；GameList **partial**（lazy MANIFEST **19→20 檔／22→23 款**＝Moles 上架，容器該有的行為非漂移；registry HEAD 仍 1 筆純字串）；
      會員管理 **absent**（`admin|cms|backoffice` view 0 命中，連六輪）；活動/紅利設定 **partial**（promoCal 註冊者 **5→7**，新增 activity/onboarding；補記與 #61 的分工）；經濟/RTP **partial**（單值 `ids()` **17→18**＝Moles 同輪登記；#103 (c) 雙向不變量仍成立）；金流模式 **partial**（`jurisdiction` 外部讀者仍 **0**，連五輪）。
      `ledger-card-sweep` 本輪 **0 筆告警**（29 個卡號引用全數對得上）。
    - **取材：查了、刻意不收，並把這個結論做成出口**。逾期 0/36（最近到期 08-24 bet365/rollbit）⇒ 無深挖對象。兩次 WebSearch 回來的全是**聯盟行銷榜**（cryptonews/cryptomaniaks/webopedia…），排序與流量無因果；對其中量級最大的 **Thunderpick** 做第二層查證：全球排名 74.8k→**77.7k 走弱**、月流量 **−21.02%**、且是 rollbit 的 similar-site 第 4 名（＝弱於本庫已追蹤的 rollbit），主軸 esports 投注落在 `avoid` ⇒ 不收。
      ⭐ 但「查過且不值得收」以前**沒有出口**，於是每一輪的無記憶新 session 都會用同一組關鍵詞查到同一批站、得出同一個結論 ⇒ 本輪在 `db/platforms.json` 新設 **`_sourcing_negative_log`**（與 `_queued_candidates` 互補：後者是**值得但被計數上限擋住**，前者是**查過不值得**），並把可重用的教訓寫進去：**聯盟榜是取材的最大噪音源，下輪直接走流量/監管訊號**。
    - **提報船長（非阻塞，2 條）**：① **首屏預算實質耗盡**——本輪 1579→**1594KB**（門檻 1600、餘裕 **5.7KB**）⇒ 下一個首屏新增必須先等 **#118**；平台軌自我約束：在 #118 落地前不再新增首屏 script。② 資料型 i18n 缺口已成第三面（#121）。
    - **驗證誠實聲明（連續第二十二輪）**：排程輪不得啟動 dev server（工具明文拒絕）⇒ **UNVERIFIED＝視覺**（輪播卡在 EN 較長標題下的斷行/擠壓）；資料層、欄位對等、三語無殘留、窗口/受眾/熱替換皆已機械證明。
    - **未碰**：`prototype/games/registry.json`（M）與 `prototype/games/slot-engine/`（untracked）——mtime 皆 08-03（20 天前），非本輪工作，依 §7 一個位元組沒碰。

<!-- 2026-08-23 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入（08-22 20:00 窗一則） -->

- **2026-08-22（平台軌 · **20:00 窗**建置輪 · 實作 **#117** `HL.rbac` 營運身分述詞註冊表〔**受眾值域從「硬寫兩值＋一條把它釘死的測項」換成容器**〕· 台帳審「資料」3 模組 ＋ #117 同輪回填「資安/權限角色」＋ 首屏餘裕警示 · 開卡 **#119** · **不取材**〔platforms.json 逾期 0/36、最近到期 08-24〕）** — 閘門全過，進場 `build_lock` **false 乾淨**（遊戲軌 16:00 窗已釋放、未奪鎖）→ claim `p-201200-b7a1`（帶心跳 20:12→20:58，**claim 當下就 commit** `cdb2c05`）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.7h**（08-22T15:25 → 本輪 20:12）未達 catchup 門檻；`lead_track=games` 准讓路，但前手已把「台帳輪替＝`資料`」與「實作候選」指好、且本輪有真產出 ⇒ **做而不讓路**。照 ENG-1 處方**分段提交**（程式 `5fd28b7`／測項 `c768819`／intel）。
    - **實作 #117**（見上方卡片落地紀錄）：`core/rbac.js` 新檔、`reports.js` 移除 `AUDS`、node **247→254 全綠**、**負向擾動 8/8**。
    - ⭐ **本輪最重要的一條，而且它是「卡上要求的鎖不該立」的那種結論**：#117 卡末要求立「只數 `HL.rbac.register` 外部呼叫者」的鎖（防孤兒容器，前面已踩三次）。
      照著立會**逼出一個更糟的形狀**——唯一誠實的外部註冊者是 `demo-tools.js`（全站唯一產生 ops 上下文者），而它靜態排在 `reports.js` **之前**；
      把 `ops` 身分搬過去，`reports.js` 註冊 3 張 ops 報表時值域尚不存在 ⇒ `register()` **回 null 不拋錯＝靜默少三張報表、畫面完全正常**。
      ⇒ **「防孤兒」與「防靜默拒收」在這張卡上互相衝突**，而兩邊都是本專案自己踩出來的教訓。選了後者（載入序確定性）＋改用「消費端非孤兒 + 兩路徑同一謂詞 + 種子身分恰為兩種」守同一件事，
      並**先立好順序鎖**讓將來的外部註冊者無處可躲。**教訓：既有教訓之間會打架；照抄卡上的鎖不等於守住不變量，要先問「這條鎖會逼我把東西搬到哪裡」。**
    - ⭐ **第二條：這一輪是「後手在別的卡的實作輪，偶然發現前一張卡漏了一整層」**。#109 報表中心 08-21 落地時**整個 i18n 是空的**（13 個純片語鍵兩份字典皆 0 命中）——
      切成英文會原樣顯示繁中，而 node 全綠、console 零錯誤、中文下完全正常。P3 紀律至此第 7 次被記錄，但**前 6 次都靠落地者自己記得**，
      而既有 i18n 鎖全是逐表面特化（`support-title-i18n`／`auth-view-i18n`／`game-axes-title-i18n`…）⇒ **新表面天生不在任何鎖的射程內**。本輪補齊那 24+ 條翻譯並開卡 **#119**（棘輪型覆蓋閘：先量基線、新檔 0 容忍、兩條反向錨）。
    - ⭐ **第三條：§10.1 改錨這次同時發生在產品側與測項側，而測項側那一份是「抄本」。** `checks-platform.js` 對每個外部註冊者硬寫 `aud: "(player|ops)"` ——
      那是**測項自己抄了一份受眾值域**。值域搬進 rbac 後，這份抄本會在「新增第三種身分」時先變紅（而產品是對的）⇒ 改成**把 aud 的值抓出來、拿去問真授權表**。
      同理 `loadReports` shim 原本沒有 `rbac` ⇒ 補注入**真的 `rbac.js` 實例**而不是假物件（假一份等於在測項裡重建那把剛被移除的尺）。
    - **台帳審「資料」3 模組**（`資料` 與 `前端UI/UX` 同為最舊 08-19，取量最小者）：報表與匯出 present（**內建 6 張 + 外部 3 檔＝執行期 9 張**、事件 schema 4 筆、`new Blob` 非註解仍只 1 處；本輪實質變化是那個閘的權威來源）／資料分析 partial（`funnel|cohort|eventSchema` 全 src 僅命中 `reports.js` 自己的說明＝分析本體仍零，缺口敘述一字不改、仍不開卡）／玩家注單 present（277 行、`CAP=500`、**真實寫入點仍恰 1 處**——三筆 grep 命中中兩筆是測項斷言與 schema 字串，依 08-19 紀律只記可重算判準不記行號）。
    - **同輪回填兩筆**（回填紀律第五個正面實例）：① `資安/權限角色` 仍 partial，但**partial 的理由換了一半**——「前端無角色分級」那半已關閉，剩下「伺服器權威只覆蓋一支 RPC」＋「無稽核軌跡」；並**明文廢掉舊判準**（`\brole\b` 逐筆過濾 aria 後應為 0——`rbac.js` 之後這條永遠不再成立，繼續用它會開始高報缺口）。② `擴充性/首屏載入架構`：首屏 1550.9→**1576.9KB／90 支**，M6 餘裕 49KB→**23.1KB（1.4%）＝本季最窄** ⇒ 對下一個建置輪立下硬約束（先做 #118 或讓改動淨負）。
    - **不取材**：`db/platforms.json` 逾期 **0/36**（最近到期 bet365／rollbit **08-24**）⇒ 本輪無到期深挖對象；有真實作 + 真台帳產出 ⇒ 非閒置、`consecutive_idle_rounds` 維持 0。
    - **下輪（平台 08-23 08:00）**：台帳輪替＝**`前端UI/UX`（5 模組·其中 4 筆 08-19＝現存最舊）**。實作候選：**#119**（本輪新開·S–M·純工具+棘輪鎖·headless 完全可驗·**推薦**）或 **#93**（導覽入口註冊表·卡上明寫需 preview 逐一目視三個表面 ⇒ **仍不宜排程輪吞**）；**#118 必須排可靠 preview 輪**，但它現在多了一個理由——首屏餘裕只剩 23.1KB，成長軌再加一支首屏檔就會撞門檻。**#116 仍不建議**（票權尺無權威來源、需設計決策）。
    - **下一個可靠 preview 輪的必做事項**：① 開 ⚙ DEMO →「📊 報表中心（營運）」，確認 **9 張報表**分群正確、ops 報表下方**兩行**說明（原有的「不對玩家開放」＋新增的「權威在伺服器」）**沒有擠壓**；② 切 EN 與简中各一次，確認報表中心**不再殘留繁中**（本輪新補的 24+ 條）；③ 從注單頁「📊 報表中心」進入（玩家路徑），確認**看不到任何 ops 報表**且匯出 CSV 正常；④ 開 ⚙ →「🧪 自我檢測」確認總數 **254** 且全綠。

<!-- 2026-08-22 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入（08-21 兩則） -->

<!-- 2026-08-23 平台軌 14:00 窗：日誌保留最新 3 則，以下一則自 BACKLOG.md 移入 -->

- **2026-08-22（平台軌 · **14:00 窗**建置輪 · 實作 **#112 的一半** ＋ 台帳審「活動」5 模組 ＋ 開卡 **#118**〔**「首屏零依賴」不等於「可以搬」——延遲檔之間也有依賴，而它壞掉的時候每一個既有驗證面都是綠的**〕）**
    - **落地**：`casino`／`tournament`／`chicken` 三支 route 型 view 移入 `HL.lazyViews`＝**−43.9KB**（首屏 1593KB／91 支 → 1550.9KB／88 支，門檻餘裕 0.4% → 3.1%）。node **243 → 244 全綠**、負向擾動 **4/4**、執行期驗證 **22/22**（假 document 驅動真 `lazy-views.js` 走完 stub→注入→換手→重繪，含「玩家已離開 ⇒ 零重繪」反例）。
    - ⭐ **本輪最重要的一條（也是把 #112 只做一半的理由）**：#111 那支工具問的是「**首屏**那一次渲染有沒有同步碰到它」——它結構上答不出第二個問題：「這支檔掛出去的全域，有沒有**另一支同樣被延後**的檔在用？」`slot.js` 的 `HL.slotEngine` 正是如此：首屏零引用（判 safe-to-lazy 是對的），但 `vsslot.js:508` 用裸守衛消費它、而 vsslot 自己也在延遲清單上 ⇒ **每一場 Slots Battle 都會顯示「遊戲引擎未載入。」**。而當下：首屏 KB 真的降了、`--verify` 全過、兩條既有鎖全綠、console 零錯誤 —— **每一個既有驗證面都是綠的，只有玩家看得到功能壞了**。
    - ⭐ **第二條：讓 ③ 失效的是一個寫法，不是一個遺漏**。`fgboard.js:20` 的 `var E = HL.slotEngine;` 讓文字上永遠不出現 `HL.slotEngine.makeGrid`，`needs-methods` 偵測因此整段失效。**別名是偵測器的天然盲點** ⇒ 新的 ④ 節把「別名接走整個命名空間」直接列為不可滿足，不去猜它後面用了哪些成員。
    - ⭐ **第三條（台帳失真的新子型·委派型引用）**：`ledger-card-sweep` 只認「開卡 #N」，但台帳也會寫「刻意不開卡、**掛在 #107 底下追蹤**」。`活動/抽獎` 的「兌換碼缺領取資格述詞」就是這樣寄放的——**#107 與 #59 都已落地**（`redeem.js` 現有 `audience` 述詞閘兩碼、滾動窗口基礎設施由 0 檔變 3 檔），台帳卻連四輪照舊高報。⇒ 工具已擴充委派句式並在輸出區分 `開卡`／`委派`，本輪告警由 3 筆回填至 **0 筆**。**委派型引用是「不開卡」換來的代價：它把回填責任藏得比開卡型更深。**
    - **台帳「活動」5 模組**（`活動` 為現存最舊分類）：季票 present（config 驅動未變）／世界活動 present（兩道真站閘行號未移）／抽獎 present（**兩筆委派缺口回填**，僅剩「全站供給上限」需後端）／推薦 partial（#58 兩檔在位，兩個結構性理由未變）／新手引導 present（**把口徑不明的「76 命中」釘死＝行數口徑、本輪重跑仍 76 零漂移；出現次數口徑為 112**，兩者混用會製造假訊號；`onboarding.js` 步驟仍硬寫＝**連五輪**，若 #93 再延應升格獨立小卡）。
- **2026-08-22（平台軌 · **08:00 窗**建置輪 · 實作 **#115** 報表註冊出口〔**同一個容器上，第二次長出「孤兒註冊出口」——而這次是被上一輪剛學會的那把尺抓到的**〕· 台帳審「資安」5 模組 ＋ **sweep 回填成就徽章牆** · 開卡 **#117** · **不取材**〔platforms.json 逾期 0/36、最近到期 08-24〕）** — 閘門全過（loop／platform／auto_implement 皆 on），進場 `build_lock` 為 **false 乾淨**（維護軌 08-22 00:00 窗已釋放、未奪鎖）→ claim `p-081548-9d4c`（帶心跳 08:15→09:05，**claim 當下就 commit** `4f9cb84`）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.5h**（08-21T20:38 → 本輪 08:15）未達 `catchup_if_dark_hours`；`lead_track=games` 准讓路，但**前手已把 #115 的阻塞事實抄齊、且台帳輪替到期** ⇒ **做而不讓路**。照 ENG-1 處方**分段提交三段**（程式 `4c1a39e`／測項 `2bacc91`／intel）。
    - **實作 #115**：`HL.reports.register` 的非註解外部呼叫檔數 **0 → 3**（`release.js` 上架排程／`responsible.js` 自律設定／`arena.js` 對戰戰績），`core/reports.js` **一 byte 未改**。node **237 → 240 全綠**，**負向擾動 7/7**。詳見卡片。
    - ⭐ **本輪最重要的一條，而它是實作到一半才浮出來的**：新增的戰績報表是「對戰排名」的**第五個表面**。08-21 前景修的那個顯示 BUG，根因正是四個表面各自硬寫「總分越高越好」；
      如果報表也自己算一份，`crazy`（最低總分勝）與 `terminal`（比最後一輪增量）兩個模式的 CSV 就會與**同一列的「勝負」欄互相矛盾**。
      而且這裡有一個畫面沒有的加重因子：**匯出檔比畫面更難被發現說謊**——螢幕上至少有旁邊那個「勝/敗」可以立刻對照，CSV 打開來只剩一堆數字。
      ⇒ 排名量走 `metricOf`、欄名走 `displayMetricLabel`、模式與勝負條件走 `labelOf`／`winCondOf`，並立成第三條鎖。**教訓：加一個新表面時，先問「這個領域已經有幾個表面、它們的單一真相在哪」**，而不是等它變成第五個 bug。
    - ⭐ **第二條：把一條鎖拆開的時候，要順手證明「拆完的兩條真的不是同一把尺」**。#115 的主鎖只數 `register`／`defineEvent`，
      但它跟被保留的舊鎖（數 `open|download|register|defineEvent` 任一）**共用同一段掃描程式**——只要有人把正則「順手統一一下」，兩條就悄悄退化回同一把尺，而**兩條都還是綠的**。
      ⇒ 主鎖裡加了一條**反向錨**：必須存在「只消費不註冊」的檔（實測 `betlog.js`／`demo-tools.js`）。擾動 M5 把尺改寬回去，該錨當場變紅 ⇒ 這條退化路徑被封住了。
    - **台帳審「資安」5 模組（全審 · 該分類 `last_audited` 08-19）**：`真/假站軸` present（`isLive()` 閘 **54 → 73 處／35 檔**，08-05 立的回歸基準連四輪為正訊號、本輪增幅最大＝競技場重構帶進大量真站分歧；**下次基準 73/35**，並記下該基準的侷限：它只在『新增產生器卻沒加閘』時保持不動，數字持平不等於漏閘）；
      `風控/反詐欺` weak（`velocity|deviceId|antifraud|risk_score|riskScore` 仍 **0 命中**、連四輪）；`KYC` absent（4 命中逐筆全是說明本項為 absent 的註解，與 08-16 逐位可重現）；
      `負責任博弈` present（`自我排除|selfExclude` **41 命中／4 檔** ⇒ 08-14 收斂成單一項的那個缺口確由 #96 關閉；**本輪新增一維：資料可攜由零變一**——`rg-limits` 報表讓限額狀態第一次可以被玩家帶走）；
      `權限/角色 RBAC` partial（`\brole\b` 19 命中逐筆全為 aria 屬性、前端角色邏輯仍 0）。
    - ⭐ **第三條：一個連五輪寫著同一句抽象理由的 partial，可以只靠「做別的卡時順手 grep」就變成兩個可指的坐標**。RBAC 從 08-05 起五輪都寫「無完整角色分級/稽核」——抽象到無從動手。
      本輪查到：① 全站唯一產生營運授權上下文的**產品程式碼只有一行**（`core/demo-tools.js:69` 的 `{ ops: true }`）⇒ **前端的「你是營運」＝「你打開了 ⚙ DEMO 面板」**；
      ② 受眾值域是 `core/reports.js:49` 硬寫的 `AUDS = ["player","ops"]`，且有測項 `t.equal(AUDS.join(","), "player,ops")` 把它**釘成恰好兩值**。
      ⇒ 新增第三種營運身分要改的是**值域與測項**，不是加一筆註冊——這與全站「加一筆 spec」的形制背道而馳。已開卡 **#117**（`HL.rbac`，容器先行；**卡上明寫權威仍在伺服器、註冊表只決定「提供什麼」不決定「准不准」**，並先抄進「那條測項一定會變紅、依 §10.1 只能改成守新形狀下的同一組不變量、不得放寬成不檢查」）。
    - **`ledger-card-sweep` 抓到 1 筆並已回填**：`功能／成就徽章牆` 的 evidence 停在「外部註冊者＝0 ⇒ 開卡 #114」，而 #114 已於**同日 20:00 窗**落地（0 → 4 檔、19 → 23 枚）⇒ 台帳正在高報一個自己已關閉的缺口。**這是「卡落地日在該模組 `last_audited` 的當天或隔天」的第三例**——純看 `last_audited` 的新鮮度指標對這種失真完全免疫，工具是唯一出口。回填後 sweep ⚠️ 歸 0。
    - **驗證**：`node prototype/tests/run.js` **240/240 全綠**；**負向擾動 7/7**（拿掉任一/兩處註冊、載入序倒置、把註冊者加進 lazy 清單、把新鎖的尺改寬回去、拿掉一筆 `aud`、把排名量改成硬寫總分——每一項都被**對應的那一條**抓到，且每次先斷言 `mutated !== orig` 防「沒擾動與沒抓到同形」）。
      另跑**執行期驗證 24 項全過**（把三個檔裡真正那段註冊程式碼原文抽出來，餵給 `reports.js` 的真 `makeRegistry()` 執行）——靜態 grep 證不了「`register()` 有沒有靜默回 null」與「`cell()` 產不產得出字串」，這兩件正是本專案「修一半而看不出來」最愛躲的地方。sw **v204 → v205**。
    - ⚠️ **驗證誠實聲明（連續第十八輪）**：排程輪**不得啟動 dev server**（本輪實測 `preview_start` 直接回「unattended sessions 無人可核准」）⇒ 無 preview。**UNVERIFIED**：三張報表在報表中心頁的分群位置與版面（`account` 是本站首次出現的分群）、`arena-battles` 11 欄在窄螢幕的橫向捲動觀感。
    - **下輪（平台 08-22 14:00）**：台帳輪替＝**`活動`（6 模組·其中 5 筆 08-18＝現存最舊；本輪刻意依前手交棒先審 `資安`，據實記下 `活動` 才是嚴格最舊）**；實作候選 **#112**（首屏第三批遷移·#111 的工具與鎖已就位、照著跑即可）或 **#93**（導覽入口註冊表）；**#117 是 M 且會讓一條既有測項變紅**（需照 §10.1 改寫而非放寬），建議排在能專心的輪。**#116 仍不建議排程輪吞**（票權尺無權威來源、需設計決策）。
    - **未碰**：`prototype/games/registry.json`（modified）與 `prototype/games/slot-engine/`（untracked）——mtime 皆 2026-08-03，非本輪工作，依 §7 一個位元組沒碰。

- **2026-08-21（平台軌 · **20:00 窗**建置輪 · 實作 **#114** 成就牆註冊出口〔**一條綠著的「非孤兒」鎖可以完全不涵蓋它名字裡那件事**〕· 台帳審「擴充性」5 模組 ＋ **新增 1 模組** · 開卡 **#115／#116** · **取材：Jackpotter 複查**）** — 閘門全過（loop／platform／auto_implement 皆 on），進場 `build_lock` 為 **false 乾淨**（遊戲軌 16:00 窗已釋放、未奪鎖）→ claim `p-200950-c4a1`（帶心跳 20:09→20:27，**claim 當下就 commit** `efb0ee8`，不留只存在工作區的鎖）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.0h**（本日 16:10 → 本輪 20:09）未達 `catchup_if_dark_hours`；`lead_track=games` 准讓路，但**台帳輪替到期 ＋ 佇列頂端 #114 是 14:00 窗剛開、阻塞事實已抄齊的 S 卡** ⇒ **做而不讓路**。分段提交三段（程式`5ea694b`／測項`6ce4545`／intel），照 ENG-1 的處方走。
    - **實作 #114**：`HL.achievements.register` 的非註解外部呼叫檔數 **0 → 4**（`challenges/activity/responsible/reports` 各掛一枚，新分類「平台里程碑」，**一律 `reward: 0`**）。`core/achievements.js` **一 byte 未改**。node **231 → 234 全綠**，**負向擾動 13/13**。詳見卡片。
    - ⭐ **本輪最重要的一條，而它是在做 #114 的過程中**順手**查出來的**：把 #114 那把尺套到整個註冊表家族做全庫量測後，`HL.reports.register` 的外部呼叫檔數＝**0**。
      而 `platform/reports-not-orphan`（#109 立的鎖）**一直是綠的**——因為它數的是 `open|download|register|defineEvent` **任一**，實測命中的 `betlog.js`／`demo-tools.js` 用的是 `download`／`open`，是**消費端不是註冊者**。
      ⇒ **一條鎖的定義可以比它的名字寬，寬到把它要證明的那件事漏在外面**，而且它綠著會讓人以為那件事已被證明。這是「修一半而看不出來」家族的**新一種**（前四種是：登記簿自刪／不變量只擋一個方向／規則放在延遲載入模組／JS 與 CSS 各寫一套）。已開卡 **#115**（補外部註冊者 + 把鎖拆成兩條）。
    - ⚠️ **量測本身踩過一次坑，而它正是 16:00 遊戲軌剛記下的那條**：家族量測第一版正則寫 `HL\.x\.register\(`，把 `panels.js` 的 `HL.dock` ＋換行＋`.register(` 漏掉 ⇒ dock 算成 **1**，與台帳既載的 **2** 打架。
      **兩個數字不合時，錯的往往是尺**：放寬成 `HL\.x\s*\.\s*register\(` 後 dock=2、與既載一致 ⇒ **尺校準完才敢寫台帳**。（同理已註明這把尺量不到 `HL.selftest`——它走 `registerTests(HL.selftest)` 的間接形式，另計 23 檔提及。）
    - **取材（不是例行刷新，是回頭修一個取材漏洞）**：本輪新平台取材把 **Jackpotter 的 community-powered** 列為 2026 新站代表性創新——而本庫 `platforms/jackpotter.md` **08-06 的首次深挖對此一字未提**，儘管那是該站首頁講的第一句話、是它的招牌差異點。
      機械查證：`grep -lie "投票|vote|voting|共決|roadmap|提案" intel/platforms/*.md` 在 **36 份**中命中 3 份，逐筆讀完**全部是我們自己寫的 "ROADMAP LATER"** ⇒ **競品共決機制的真實命中數＝0**。
      ⇒ 這與 08-16 補「玩家保護」時的「35 份 0 命中」**逐字同構**：問題不在那一輪查得不認真，在**維度清單決定了眼睛看得到什麼**。已把「社群治理／共決」補成 `db/sourcing-methods.md` 的**第 6 條常規維度**，並明訂往後每份 dossier 必須明文回答（沒有就寫「無」）。開卡 **#116**、台帳新增模組 `功能／社群共決`（absent，全庫首筆）。
    - **台帳審「擴充性」5 模組**（該分類 `last_audited` 最新值 08-19＝全 8 分類最舊）⇒ 全數回填 08-21、狀態**全維持**：Dockable Layout **partial**（外部註冊 2、三缺口一字未動）／Design Tokens **present**（本輪 4 枚徽章**零新 CSS、零新 token、零裸值** ⇒ 又一次反向壓力測試通過）／Automated Tests **partial**（234 全綠；`new Function` 注入假 window 實跑被測模組本體現有 3 個先例，可視為底座既定形制；**仍無 CI、無瀏覽器端自動化** ⇒ 保真閘 10/11 的 UNVERIFIED 紀律仍然正確）／Code-splitting **present**／Release Scheduling **present**。
    - ⚠️ **審 Code-splitting 時發現一個尚未發生的張力，先記下來**：本輪 4 枚徽章刻意掛在**已在首屏的** core 檔上；但 #112 的候選清單裡有 `ops-dashboard`／`arena` 等 view。**哪天把一個「註冊過東西」的檔移出首屏，掛在它上面的註冊項會跟著消失，而且不報錯**（成就牆少一枚、報表少一張，畫面完全正常）⇒ **遷移任何檔之前先問「它有沒有註冊過什麼」**。這條與 §4 記載的「規則放在延遲載入的模組 ⇒ 取不到就靜默退回」是同一個家族的第三個實例。
    - **對船長指令的回應**：[ENG-1] 平台軌的部分早已在 08:00 窗辦完；本輪是**今日第三個正常收尾的平台窗**（08:00／14:00／20:00 全數收尾）⇒ 「firing 卻沒收尾」目前看來確實是 08-20 heavy build 期間的機器狀態問題、非結構性故障。你那條 **「孤兒 WIP 優先於讓路」** 的建議**仍未擅自寫進 SKILL**（等你點頭）。`games/slot-engine/` 與 `prototype/games/registry.json` 的未提交改動我一個位元組沒碰（mtime 08-03，非本輪工作）。

- **2026-08-21（平台軌 · **08:00 窗**接手輪 · **收尾 #109 報表/匯出註冊表**〔前手 08-20 14:00 窗做完卻凍結、WIP 在磁碟上躺了 18 小時〕· 台帳審「金流」5 模組 ＋ **回填 4 筆**〔2 筆 sweep 告警 ＋ 2 筆 #109 自己關掉的缺口〕· 不取材 · **不開卡**）** — 閘門全過（loop／platform／auto_implement 皆 on），進場 `build_lock` 為 **false 乾淨**（維護軌 00:00 窗已 stale-heal 清回）→ claim `p-081200-a4f7`（帶心跳 08:12→08:32）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **23.6h**（08-20T08:30 → 本輪 08:12）**未達** `catchup_if_dark_hours` 24h 門檻，但差 24 分鐘——**這個數字本身就是本輪最該記的事**（見下）。`lead_track=games` 准讓路，但船長 [ENG-1] 明文指派「平台軌今早 08:00 窗第一件事＝驗一下 #109 就直接 commit 收掉」⇒ **做而不讓路＝跨輪/船長指派連續第二十八輪**。
    - **收尾 #109**（commit `0ecc78e`·**未重做任何一行**）：磁碟上 `core/reports.js`（630 行新檔）＋`index.html`／`betlog.js`／`demo-tools.js`／`checks-platform.js`(+168)／`run.js` 五檔改動，mtime 全在 **08-20 14:21–14:24**。依 §7 鐵律先查 mtime 才判孤兒（18h 前寫入、非活躍工作）⇒ 正當接手。驗證：`node prototype/tests/run.js` **215/215 全通過**（含本卡 8 個 `reports/*` 契約測項 ＋ 4 條常駐鎖 `platform/reports-{load-order,single-export-out,not-orphan,gate-and-no-drift}`），與維護軌 00:00 窗的獨立複跑數字**逐位相同**。本輪唯一補做的一行＝`sw.js` CACHE **v188→v189**（`index.html` 在 PRECACHE 裡，不 bump 會被舊快取餵舊的 script 清單⇒**新報表中心在使用者機器上靜默不存在**）。執行期實測：註冊 **6 張報表**（玩家 3：注單／每日活躍〔#59 日桶〕／進度快照；營運 3：營運彙總 GGR-NGR-RTP／逐遊戲／事件 schema 清單）＋ **4 個事件 schema**。
    - ⭐ **本輪最重要的一條＝「未提交的 WIP」是本引擎目前唯一會讓「已完成的工作」歸零的失效模式，而它的偵測窗口比想像中窄很多**。#109 在磁碟上是**完成且全綠**的（維護軌與本輪兩次獨立複跑同為 215/215），它離永遠消失只差一件事：有人 `git add` 一整個目錄或還原污染檔。而**發現它的唯一機制是 `git status` + mtime**——`STATE.last_platform_run_at` 凍在 08-20T08:30 只說明「有一輪沒收尾」，不會說明「它其實做完了」。⇒ 兩條可操作的紀律：① **heavy build 應分段 commit**（STATE `_platform_run_note_20260803` 早就寫下、本輪是它的第三次坐實：73h 那次、08-04 13.5h 那次、這次 18h）；② **接手輪的第一動作是 `git status` 而不是讀 STATE**——STATE 描述的是「輪次」，磁碟描述的是「工作」，而工作才是資產。
    - ⚠️ **dark 23.6h＝差 24 分鐘就跨過 catchup 門檻，而跨不過的那一側是「可以合法讓路」**。若本輪 08:32 才進場（延遲 24 分），`catchup_if_dark_hours: 24` 就會觸發、本輪**被禁止讓路**；反過來說，在 23.6h 這一側，`lead_track=games` 是准讓路的 ⇒ **若沒有船長 [ENG-1] 的明文指派，本輪讓路是完全合規的，而 #109 就會再多躺 12 小時（等 14:00 窗）**。這揭露一個門檻設計的盲點：`catchup_if_dark_hours` 量的是「**本軌多久沒收尾**」，但它量不到「**磁碟上有沒有前手做完卻沒提交的工作**」——後者是更急的訊號、而且是**可機械偵測**的（`git status` 有 `prototype/` 未提交檔 × mtime > 2h × 非本輪所寫）。⇒ 建議（記在此供船長裁決、本輪不擅自改 CONTROL 語意）：三軌第 0 步加一條「**孤兒 WIP 優先於讓路**」——偵測到符合上述三條件的未提交工作時，禁止讓路，先驗收尾。
    - **台帳審「金流」5 模組**（08-17＝全 8 分類最舊）⇒ 全數回填 08-21、狀態**全維持**：① 支付/收銀台 **partial**——`HL.payment|payMethod|payments.register` 與 `psp|對帳|reconcil` 皆 **0 命中**、付款方式仍是 `app-shell.js` 兩個純展示常數陣列（無 enabled／地區／幣別／限額），**缺口形狀連第六輪完全相同**，#82 在佇列中故不重複開卡。② 錢包/帳本 **present**（9 個真插樁點在位、`TXN_KINDS` 三型別仍在）。③ 提款審核佇列 **absent**（唯一命中是「在描述本功能不存在」的面板文案）。④ P2P 交易/商城 **partial**（單一理由＝單向且無對手方，逐位無變化）。⑤ 獎金/流水引擎 **partial**（`badd(n,opts)` 仍只認 `wagerFree` 布林＝**逐筆 `wagerMult` 覆寫**仍缺；倍數依段位 #74 ＋ 範圍逐筆 #89 兩維在位）。**本輪 0 新缺口 ⇒ 依 `ban_busywork_heartbeat` 不硬開卡**（`max_cards_per_run` 額度未用）。
    - ⭐ **「命中數上升 ≠ 功能出現」本輪長出第三個載體，而這次的源頭是我們自己**：`HL.ledger.record(` 的 naive 命中數 **9 → 10**，新增那一行是 **#109 `defineEvent` 的 `src` 字串**（`src: "HL.ledger.record(type, amount, meta)"`＝在**描述**這個插樁點長什麼樣）；同輪另一例：`wagerMult` 全 src 僅 1 命中，是 `econ-config.js:5` 的**註解**在描述 #74 的「段位 wagerMult」＝本格所缺那一維的對立面。前兩個載體是**註解**（08-17 提款佇列）與 **i18n 譯文**（08-07 匯出出口）。⇒ 通則要再收一層：**凡是「把系統自己的形狀寫成資料」的新設施（#109 事件 schema、#90 econCfg 的 note、i18n 字典），都會讓所有以關鍵詞計數為基礎的台帳指標系統性偏高**；此後這類指標須排除「字串字面量中的述詞」，不只排除註解。**這條反過來也是好消息**：台帳指標偏高的方向是「看起來已經做了」，比漏報更危險——而 #109 恰好是個會製造這種偏差的設施，所以它落地當輪就把紀律補上。
    - **回填 4 筆（2 筆由 `ledger-card-sweep` 點名、2 筆由 #109 落地觸發）**：① 🆕 **`資料/報表與匯出` partial → `present`**＝回填紀律第四個正面實例（自 08-05 起連五輪同一組 partial 理由本輪雙雙消滅：出口不再只有注單〔6 張報表〕、且已有可註冊定義清單〔加報表＝加一筆註冊〕；08-16 那句「第二個真出口尚未出現故不開卡」隨之作廢）。② `資料/資料分析` **維持 partial 但缺口改形**——連五次 0 命中的 `eventSchema|funnel|cohort` 量測**本輪起失效**（`defineEvent` 已存在、4 個 schema 已定義、`event-schemas` 報表是出口）⇒ 敘述改為「schema 與出口已有，缺的是跨裝置事件流（需後端）」，漏斗/世代留存延用 08-16 判定＝**§11 前不值得做、不開卡**。③④ sweep 的兩筆 ⚠️（`後台/經濟RTP`、`前端UI-UX/大廳分群軸`）**經人工確認皆為真陽性**：#103 已於 08-20 落地（`ddc70d1`）而兩處 evidence 仍寫「🟦待遊戲軌裁決」。
    - ⭐ **回填 #103 時查出一件比回填本身更有價值的事：#103 落地並沒有讓 RTP 軸多蓋一款遊戲，而這正是兩邊設計對上的結果**。vm 實跑：`isParameterized("plinko")=true`、`rangeOf("plinko")` 回 9 種 `rows×risk` 的解析值 **98.8125%–98.9844%**（離散 0.1719pp、成因是賠付表取整），但 `of("plinko")` 回 **`null`**、`gameRtp.ids()` 仍 **17 款**（與 08-17／08-20 逐位相同）——因為 #103 (c) 的不變量是**雙向**的（`declareRange` 拒收已有單值的 id、`declare` 也拒收已參數化的 id），參數化值**永遠不出現在單值 API**；而大廳軸的規則是「缺值不進軸」⇒ plinko 仍不進任何 RTP 桶，**但它現在是「有意義地不出現」**（一個橫跨 0.17pp 的區間本來就沒有正確的桶可落，硬塞任一桶＝在軸上說謊，比不出現更糟）。⇒ 順帶給第三條軸（`volatility`，唯一還缺的一條）一個現成形制：**波動度要的可能不是「每款一個數字」，而是照 `declareRange` 允許區間與缺值**（桌遊每注型不同、slot 依設定不同，本來就沒有單值）。
    - **不取材**：`db/platforms.json` ACTIVE overdue **0/36**，最近到期 bet365／rollbit **08-24** ⇒ 依 `stale_days` 未到期，不刷（非閒置：本輪有 #109 收尾＋9 筆台帳回填的真工作，`consecutive_idle_rounds` 維持 0）。
    - **下輪（08-21 14:00）**：台帳依輪替序＝**`功能`（13 模組·08-18）或 `活動`（6 模組·08-18）或 `擴充性`（5 模組·08-18）三者同為最舊**——建議取 **`擴充性`**（5 模組·量最小且與 #82／#109 的註冊表形制直接相關）。實作候選 **#82 支付通道註冊表**（**本輪特別推薦**：它已在佇列裡躺了六輪，而 #109 今早剛把「帶必填欄位閘的註冊表」樣板做出來——`register()` 拒收空殼／拒收同 id 覆蓋／**授權欄位與分群欄位分開**，#82 要的 `enabled／地區／幣別／限額` 正是同一形制，其中**地區/幣別/限額是授權語意、只有 `enabled` 是開關**，比照 #109 的 `cat`/`aud` 分欄可避免把法規閘寫進顯示層＝§2 目標 4 money-mode/jurisdiction 抽象層將來最難拆的耦合）；或 **#112**／**#107**／**#93**（仍建議排有 preview 的輪）。**下一個可靠 preview 輪的必做事項（本輪新增第一項）**：① 開 ⚙ DEMO →「📊 報表中心（營運）」與注單頁「📊 報表中心」，確認同一支中心頁在兩個入口下**只列各自受眾的報表**（營運 3 張／玩家 3 張）、切分群 tab、按「匯出這張報表」確認 CSV 真的下載；② 逐支點開 #110 的 5 個入口（🌐 全球獎／直播間／賞金局／對戰 slot／⚙ 儀表板）確認占位一閃而過；③ 開 ⚙ →「🧪 自我檢測」確認總數 **215** 且全綠。

<!-- 2026-08-21 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入 -->

- **2026-08-19（平台軌 · **20:00 窗**建置輪 · 實作 #110 首屏次批延遲載入〔**候選清單上的 KB 數是誘因，判準是「首屏那一次渲染有沒有同步碰到它的全域」**〕· 台帳審「前端UI/UX」5 模組＋同輪回填模組 46 · 開卡 #111）**
    - **入場**：`build_lock` 進場乾淨 `false` → claim `p-201200-c4e2`（帶心跳 20:12→20:41、未奪鎖、`stalled_rounds` 不動）。dark **5.6h**（14:35→20:12）<24h 非 catchup；`lead_track=games` 准讓路，但前手 14:00 窗明文指派台帳分類與實作候選 ⇒ **做而不讓路＝跨輪指派連續第 28 輪**。
    - **實作 #110**（S·選它而非 #109 的理由：#109 會**新增**一支 core 檔＝把首屏餘裕再吃掉一塊，而本輪進場時餘裕只剩 3.8%⇒ 先把餘裕買回來再談新出口）：新增 `core/lazy-load.js`（兩個容器共用的注入原語）＋ `data/lazy-views.js`（`HL.lazyViews`＝route／全域型出口的延遲載入容器）。首屏 **1539.9KB／91 支 → 1467.9KB／88 支**（餘裕 3.8% → **8.3%**）。細節與四條教訓見 #110 落地段。
    - ⭐ **本輪四條值得後手知道的事**：① **卡上的「那六支開站都用不到」對最大的一支是錯的**（arena 42KB 是大廳首屏的同步依賴），已立常駐鎖把結論釘死；② **阻塞事實不在 `evidence` 而在 `stowable_note`**（模組 46 早已寫明 route 型需另一套 stub）⇒ 開卡前要讀完來源模組的**兩個**欄位；③ **注入器與載入態表只能有一份**（兩份會讓同一 src 被注入兩次＝view 檔重複執行）；④ **route 型 view 不只有 `render`**（`liveroom.enter` 被別的檔同步呼叫，而「點了沒反應」與「還在載入」在畫面上完全同形）。
    - **台帳審「前端UI/UX」5 模組**（08-17＝全 8 分類最舊）⇒ 全數回填 08-19、狀態皆維持：① 大廳/遊戲牆 **present**（`casinoGames` 51 筆〔playable 2／非 49〕、分佈逐位相同＝**連四輪零漂移**）② 導覽殼層 **present**（#93 的 registry 缺口連三輪逐位未動：`SIDE` 實跑 5 筆且**無 `enabled`/`order`**、`bottombar()` 行內 `item(` 仍 5 個、`registerNav`/`navRegistry`/`HL.nav` 仍 0 命中；**本輪新增事實：導覽第 2 筆 `go:"globe"` 的目標從今天起不在首屏**⇒ 導覽項目與其目標檔的載入時機正式解耦，這對 `stowable_note` 想要的「未啟用模組不出現」其實是有利前置，但新增一條紀律：未來做 `order`/拖排時不得在渲染導覽時就去讀目標 view 的全域）③ 遊戲客戶端框架 **present**（六顆工具連五輪未腐爛、`isPF` 去註解 2 處）④ i18n **partial**（vm 實跑權威鍵數 en **1218**／zh-Hans **1040**，08-07 為 1051／910；覆蓋率同法 38.9%→**40.2%**）⑤ 大廳分群軸 **partial**（`all()` 實跑 **2 條軸**各 3 桶、`casino.js` 對五個軸字樣 0 命中＝#94 性質第四次證成、`volatility` 仍無來源且屬遊戲軌）。
    - **同輪回填模組 46「首屏載入架構」**（回填紀律第四個正面實例）：把本輪兌現的 route 型 stub 機制與**新的下一批候選判準**寫回 `stowable_note`，並明文寫下「`arena.js` 不得入列」。
    - ⚠️ **量測法陷阱本輪自己踩到兩筆並當場更正**（同家族連 N 輪）：① 取軸清單用了一個**根本不存在的 API** `ax.list()` 得 **0 條**（真 API 是 `all()`/`enabled()`/`active()`）⇒ **「呼叫了不存在的方法」與「真的沒有軸」在輸出上完全同形（都是 0）**，差別只在有沒有先讀過被量測物的公開 API；② i18n 鍵數首版用靜態計得 1245／1056（把註解裡的示例鍵也算進去），權威值改以 vm 實跑 `HL.i18n.register` 收到的 pack 為準。另記一條：`core/activity.js` 覆蓋率 23% 中**相當比例是量測法高報**（該檔中文字面量大多是寫給後手看的註解）⇒ 別照數字去把註解翻譯了。
    - **開卡 #111**（只用 1 張額度、質>量）：把「這支 view 能不能搬出首屏」從人工判斷改成 node 工具算得出來——來源就是本輪 arena 差點發生的錯。⚠️ #103 屬遊戲軌、#104／#105 含後端需船長批准，平台軌均不得代裁。
    - **不取材**：`db/platforms.json` ACTIVE overdue **0/35**、最近到期 bet365／rollbit **08-24** ⇒ 本輪不深挖（非閒置：有真實作＋真台帳審計＋三條新鎖，`consecutive_idle_rounds` 維持 0）。
    - **下輪（08-20 08:00）**：台帳依輪替序＝**`後台`（7 模組·08-17＝現存最舊）**；實作候選 **#109**（報表/匯出註冊表·headless 可安全落地·且餘裕已買回 8.3% 故新增 core 檔的成本可承受）或 **#111**（純 node 工具·headless 最佳）；**#107**／**#93** 仍建議排有 preview 的輪。**下一個可靠 preview 輪的必做事項**：逐支點開 #110 的 5 個入口（🌐 全球獎／直播間／賞金局／對戰 slot／⚙ 儀表板），確認「載入中…」占位一閃而過後畫面與改版前一致。

<!-- 2026-08-21 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入 -->

- **2026-08-19（平台軌 · **14:00 窗**建置輪 · 實作 #108 光環返水加成〔**「看起來重複」與「真的重複」的分界線是消費者花的成本，不是數字長得像不像**〕· 台帳審「資料」3 模組 · 開卡 #109／#110 · 不取材〔ACTIVE overdue 0/36、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 10:00 窗已釋放）→ claim `p-141230-b7c3`（帶心跳 14:12→15:xx）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.2h**（本日 09:58 → 本輪 14:12）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗明文指派三項**（台帳＝`資料`／實作候選 #108 或 #107／#103 屬遊戲軌、#104／#105 不得代裁）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十七輪**。
    - **實作 #108**（S · 選它而非 #107 的理由就寫在前手交棒裡：#107 會改日曆/清單渲染、建議排有 preview 的輪，本輪同樣是 headless 排程輪）：`core/activity.js` 加 `TIERS[].rb` 欄 + `rbMultFor(windowXp, mode, scale)` + 真站旋鈕 `RB_LIVE_SCALE = 0`，以 `HL.rakeboost.register` 掛成返水加成表**第五筆**。**`core/rakeboost.js` 一 byte 未改**（含被常駐測項盯著的 `CAP`）⇒ 真站硬不變量 `maxPct(live) × CAP.live = 0.2175 < 1` 是**恆等式繼續成立**，不是重新證一次。
    - ⭐ **本輪最重要的發現＝有一種「重複」不能去重，而它偽裝得跟去重債一模一樣**：`mult`（進度加速 1.05/1.10/1.20）與新加的 `rb`（返水加成 1.03/1.06/1.10）數字很接近，任何下一輪來讀這張表的人都會想「這不是同一組數字抄兩份？」。**但兩欄花的不是同一種東西**：`mult` 動 XP（帳面速度、零成本），`rb` 動真的付出去的返水（§11 送幣成本）。合成一欄之後，「把光環加速調快一點」這種純體驗調整會**同時**推高真站送幣、而且沒有任何地方會提醒。⇒ 已上反向鎖 `activity/rb-two-columns`（兩欄逐段相同即紅，擾動②實證）。這與 #59 的「兩把尺 `w`/`x` 都要存」同一家族第二例 ⇒ **判斷「重複」的依據是消費者付的代價，不是字面像不像**。
    - ⭐ **第二個發現＝同一個機制的兩條路徑，站別結構可以完全相反，所以前一輪的結論不能照搬**：#59 那條路真站是「想給也給不了」（下游 `BOOST_CAP.live = 1.0` 夾成恰好零）；本卡這條路下游 `CAP.live = **1.5**`，真站是「給得出去、而且真的要付錢」。⇒ 真站幅度做成顯式旋鈕（預設 0＝零成本落地），並補 `activity/rb-live-scale-wired` 證明**旋鈕不是死碼**（scale=1 → 等於假站、0.5 → 半幅、>1 → 夾回 1、負數/垃圾 → 夾回 0）；**沒有這條測項，「預設 0」與「這段根本沒接線」在測項上完全同形**。
    - ⭐ **第三個發現＝#59 那條教訓的正向用法：讓文案由常數推導**。#59 記的是「卡上形容詞與常數矛盾時讓常數贏」；本輪把它反過來用——面板站別說明句改成 `rbMultFor(9e9,"live") > 1 ? 已開啟句 : 不提供加成句`，說明中心也逐項向 `tiers()` 求值 ⇒ 船長哪天轉開旋鈕，文案**自動換句**，結構上不可能出現「文案說沒有、實際有」。vm 實測（把常數改 1）確認換句且「不提供任何額外加成」消失。
    - **台帳審「資料」3 模組**（08-16＝全 8 分類最舊）⇒ 全數回填 08-19、狀態皆維持：① 報表與匯出 **partial**——「betlog 為全站唯一真匯出出口」第三次複驗仍成立，且**更正一個量測陷阱**：naive grep 命中檔數 4→5 純係 #100 拆 i18n 造成（同一份譯文現住兩檔），**不是新出口** ⇒ 開卡 **#109**。② 資料/分析 **partial**——`eventSchema|funnel|cohort|retentionCohort` 連三輪 0 命中，但 **#59 之後「時間維度」首次真的存在於前端**（`wageredSince/xpSince/betsSince` 任意窗長、日桶保留 90 天）⇒ 缺的已不是資料而是定義與出口，**刻意不獨立開卡**（與 #109 同出口形狀＝#95/#72 雙胞胎前例），併入 #109 第二批註冊者。③ 玩家注單 **present**——`betlog.js` 265→268 行、中央寫入點行號自 `live-stats.js:47` 漂到 `:57`（是 live-stats 自己在長）⇒ ⭐ **evidence 記死行號本身是個雷**（行號被同檔其他改動推走，而「照行號看不到」與「功能沒了」在畫面上同形），本輪起改記可重算判準 `grep -n "HL.betlog" core/live-stats.js` 恰 1 命中。`ledger-card-sweep` 實跑 **⚠️ 0 筆**。
    - **開卡 #109／#110（本輪用滿 2 張額度，兩張來源不同且各有機械實證）**：#109 報表/匯出註冊表（S–M·來自台帳連四輪 partial + 其 stowable_note 早已寫明的形狀）；#110 首屏次批延遲載入（S·**M6 閘實測 1540/1600KB＝96.3% headroom 僅剩 3.7%**，而 #80 已建好的容器旁邊還躺著 123KB 現成的第二批：arena 42／ops-dashboard 21／bounty 18／vsslot 17／liveroom 13／global-prize 13KB，全是開站看大廳用不到的）。
    - ⭐ **順帶量到一件與本軌紀律直接衝突的事，已寫進 #110 交船長裁決**：本專案**刻意無 build step**，所以**每一行中文註解都照原樣送進玩家瀏覽器**。本輪一張 S 級卡就讓 `activity.js` 由 31.0KB → **44.0KB（+13.5KB，絕大多數是註解）＝一口氣吃掉剩餘 headroom 的 23%**。⇒ 「把理由寫在程式裡」這條核心紀律**有可量測的首屏成本**；真正便宜的解法是部署期剝註解/minify，但那就是引入 build step（架構明文避開）⇒ **不代裁**，只把數字放上桌。
    - **驗證**：node **184 → 187 全綠**（新增 3 條 both-env 測項）·**負向擾動 10/10 全被抓**（先證乾淨樹 187 全綠，每例跑完立即還原原檔）·**瀏覽器路徑（node vm + dom/ui stub 載入真實 rakeboost.js + 真實 activity.js）46/46**（三情境：假站／真站旋鈕 0／真站旋鈕 1，各驗註冊表 5 筆、零活躍不入 active() 且 mult()=1、第二段 1.03、頂段 1.10、**31 天後淡出回 1**、CAP 未動、面板文字與數字、新 browser 測項實跑）。i18n EN/zh-Hans 同步並**順手去掉 #59 留下的一個重複鍵**（`"🔥 活躍光環"` 在 en.js 有兩份、值 byte-identical、後者靜默覆蓋前者）。首屏 1527→**1540KB／92 支**（HEAD 亦為 92＝本輪未加 script；前手記的「91 支」係量測法差異）·sw v180→**v181**。
    - ⚠️ **據實記兩個限制**：(a) 本輪**無 preview／無目視**（§9 headless 排程沙箱過不了登入 gate）——面板新增那一列的**版面**與三語顯示皆未經視覺確認；(b) 假站的**新玩家**頭 24 小時本來就有 `newcomer ×2`，`resolve()` 取最大 ⇒ 該期間光環返水加成**看不出效果**（設計如此，非壞掉；本輪的 vm 驗證因此刻意改用「老玩家」基準才量得出本卡貢獻）。
    - **下輪（08-19 20:00）首要**：台帳依輪替序＝**`前端UI/UX`（5 模組·08-17＝現存最舊）**；實作候選 **#110**（S·純遷移進既有 #80 容器·但**六支入口需逐支開起來確認**⇒ 有 preview 的輪最佳；headless 下可先做 ops-dashboard 一支當樣板）或 **#107**（受眾述詞·S–M·仍建議排有 preview 的輪）或 **#109**（S–M·容器新建、headless 可落地）；⚠️ **#103 屬遊戲軌、#104／#105 含後端需船長批准，平台軌均不得代裁**。**下一個可靠 preview 輪請開 ⚙ →「🧪 自我檢測」確認總數 187 且全綠**（此前 184），並目視 🔥 活躍光環面板的**「目前加成」新列**（假站應顯示「經驗加速 1.00× · 返水加成 1.00×」起、達標後上升）＋切 EN/zh-Hans 確認該列兩個標籤換字。調研：最近到期 bet365／rollbit **08-24**（未到期，下輪多半仍不取材）。

<!-- 2026-08-20 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入 -->

- **2026-08-19（平台軌 · **08:00 窗**建置輪 · 實作 #59 活躍光環滾動視窗〔**卡上的形容詞「保守」與程式裡的常數 1.0 不一致時，讓常數贏並把差異寫下來**〕· 台帳審「資安」5 模組〔**結清 08-15 留下的口徑問號**〕· 開卡 #108 · 不取材〔ACTIVE overdue 0/36、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 08-18 22:00 窗已釋放）→ claim `p-081500-e5b1`（帶心跳 08:15→09:5x）→ 停頓做調研查核/台帳審計/源碼量測 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.6h**（08-18T20:35 → 本輪 08:15）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗明文指派三項**（台帳＝`資安`／實作候選 #59 或 #107／#103 屬遊戲軌、#104／#105 不得代裁）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十六輪**。
    - **實作 #59**（新檔 `core/activity.js`）：環形日桶（保留 90 天、光環窗 30 天且窗長為 config）＋四段光環＋效果走 `progressSrc.registerBoost` 第三筆註冊＋公用查詢出口 `wageredSince/xpSince`。詳見卡片落地註記。node **177→184 全綠**、vm+stub 瀏覽器路徑 **33/33 手工 + 12/12 測項**、**負向擾動 14/14**（先證乾淨樹全綠）、首屏 1493→**1527KB／91 支**、sw v179→**v180**。
    - ⭐ 三個值得後手知道的教訓：① **卡上的形容詞可能與程式裡的常數矛盾，而繞過常數就是再造第二真相**——卡寫「真站加成須保守」，`BOOST_CAP.live = 1.0` 給的是恰好零；選擇讓常數贏、真站只給徽章、並在檔頭與面板明說，而不是默默做成 1.02×。② **一個滾動桶要存幾把尺**：段位要加權 XP、資格閘要真實金額，只存一把時另一個消費者拿到的錯數字**看起來完全正常**（同樣是遞增的錢）⇒ 存兩欄並立反向鎖，這與「第二份真相」相反（是誠實承認兩個量）。③ **名字很像的兩條不變量各自要有各自的鎖**：擾動「`sweep` 不汰除過期桶」只被 `ring-bounded` 抓到、`window-decays` 完全沉默——因為衰退其實住在 `sumSince` 的視窗過濾，`sweep` 只管儲存上界。
    - **台帳審「資安」5 模組**全數回填 08-19。`ledger-card-sweep` 進場實跑 **0 筆待人工確認**。真/假站軸 present（`isLive()` 閘 54→**69 處/34 檔**，08-05 立的基準連四輪正訊號）／風控反詐欺 weak（權威樣式**連五輪 0 命中**）／KYC absent（4 命中 3 檔**逐筆全是說明本項 absent 的註解**，08-16 的更正經獨立重跑確認）／負責任博弈 present／RBAC partial（`\brole\b` 19/6，過濾 aria 後**非 aria 恰 0**）。
    - ⭐ **兩則量測紀律**：① **結清 08-15 的口徑問號**：`HL.rg` 閘 08-14 記 27、08-15 記 26 並歸因「去註解口徑差」；本輪逐筆列出全部命中位置，**沒有一筆在註解行** ⇒ 權威值就是 27 處／20 檔，08-15 是過濾式誤刪一筆。② **`isLive()` 這個基準自 08-05 起混了兩種用途**——它原本是「假活動有沒有加閘」的訊號，但 #59 貢獻的那一處是**經濟慷慨度閘**（真站倍率恆 1）⇒ 數字上升時**不能再一律讀成「假活動有加閘」**，判漏閘仍須逐筆看用途。已寫進該模組 evidence。
    - **開卡 #108**（光環的第二個消費端＝返水加成·S）：形狀沿用 `HL.rakeboost.register`，並**先把關鍵差異抄進卡**——rakeboost 的真站 CAP 是 **1.5 而非 1.0**，故這條路在真站**真的有錢的成本**（與 #59 結構上恆零完全不同），且既有測項盯著 CAP 不得改動 ⇒ 卡上明訂只能在 CAP 內加乘數、真站段位倍率預設全 1、要開多少留給船長一行指令。本輪只開 1 張（額度 2）＝去重紀律優先於填滿額度。
    - ⚠️ **驗證誠實聲明（連續第十五輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。光環在真實 VIP 面板裡的排版/間距、四段徽章的視覺層級、切 EN/zh-Hans 後段位名是否真的換字，未經人眼或真實渲染引擎確認（vm+stub 只能證「渲染得出節點與文字」，證不了「看起來對」）。另據實記：vm harness 上有 3 個固有紅燈（`core/namespace`／`progress-src/panel-opens`／`progress-src/vip-panel-opens`），**已逐一證明為 stub 造成**（前者因未載入 `app-state.js`、後兩者因 stub 的 `document.querySelector` 恆回 null），非本輪改動所致。
    - **下輪（08-19 14:00）首要**：台帳依輪替序＝**`資料`**（與「後台」並列最舊，看 journal 接續）；實作候選 **#107**（受眾述詞·S–M·**本輪已為它補上「近 30 天是否活躍」維度**，但會改日曆/清單渲染 ⇒ 建議排在有 preview 的輪）或 **#108**（S·純註冊·headless 可安全落地）；⚠️ **#103 屬遊戲軌、#104／#105 含後端需船長批准，平台軌均不得代裁**。**下一個可靠 preview 輪請開 ⚙ →「🧪 自我檢測」確認總數 184 且全綠**（此前 177），並目視 VIP 面板的光環列＋🔥 活躍光環面板四段徽章、切 EN/zh-Hans 確認段位名換字。調研：最近到期 bet365／rollbit **08-24**（未到期，下輪多半仍不取材）。

<!-- 2026-08-19 平台軌 14:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入 -->

- **2026-08-18（平台軌 · **20:00 窗**建置輪 · 實作 #106 說明中心註冊三筆〔**卡上寫「只要加兩筆 register」，實際有一個擁有者在結構上註冊不到**〕· 台帳審「活動」6 模組〔**更正一句自己寫下、然後連五輪抑制開卡的話**〕· 開卡 #107 · 不取材〔ACTIVE overdue 0/35、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:00 窗已釋放）→ claim `p-201530-a4f7`（帶心跳 20:15→21:xx）→ 停頓做台帳審計與源碼量測 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.3h**（本日 14:50 → 本輪 20:10）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳＝`活動`／實作候選 #106 或 #59／#103 屬遊戲軌、#104／#105 含後端皆不得代裁）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十五輪**。
    - **實作 #106**：三筆 `HL.support.register`（#57 限量挑戰／#58 推薦制／#94+#102 分群軸），body 全為函式、數字一律當場向擁有者求值。⭐ **卡上那句「順手複查是否也漏註冊」，查出來的不是「忘了」而是「註冊不到」**——`core/game-axes.js` 為了當 `views/casino.js` 的相依必須早載，排在 `core/support.js` 之前，於是它的軟依賴守衛恆為 false、整段靜默略過（#66／#101 之後**同一個坑第三次**）。修法＝把 `support.js` 上移到 `dom.js` 之後（它載入期只取 `HL.dom.el`）＝「早於所有註冊者」由**每次新增模組都要重想一次**變成**結構保證**；刻意不仿 `HL._selftestQ` 佇列，因為既有鎖已釘死守衛的規範形狀，再長第二種寫法就是兩套慣例並存。
    - ⭐ **我自己差點在容器裡種下第二份真相，被既有鎖當場擋下**：分群軸說明首版把「rtp 向單一真相求得／pace 依互動結構判定」寫成 `game-axes.js` 內的 `field → 說明` 對照表 ⇒ 撞上 `platform/game-axes-no-second-rtp`（「容器層不得認識 rtp 這個欄位」）。**鎖是對的**：容器一認得某條軸的名字就不再是容器。改為 `register()` 收選用的 `source` 描述子、出處寫在軸自己的定義裡（`data/game-traits.js`）；搜尋關鍵詞同理不列舉軸名——**不必列也搜得到**，因為 `search()` 比對的是當場求值的 body。這是「反向鎖真的擋住了它要擋的事」的一次正面實證。
    - **順手補掉一個 P3 漏洞並機械化**：掃全庫 10 條說明標題的兩語覆蓋，查出 **#71「紅利有效期限」（08-17 落地）EN/zh-Hans 皆缺**（漏翻不報錯、只在切語言時靜靜露出中文），已補；並新增常駐鎖 `platform/support-title-i18n`＝標題須兩語齊備、body 須為函式、樣本量下限 10。
    - **台帳審「活動」6 模組**（08-16＝與資安/資料並列最舊）⇒ 全數回填 08-18：① 季票 present（`XP_PER_TIER=1500`＋單一 `SEASON` 物件驅動）② **促銷框架 partial**——容器採用度 **3→5**（#57／#58 兩張新卡都自行掛上排程軸），但 `grep -c audience` 連六輪 **0**；⭐ **更正本欄自己的抑制理由**：evidence 自 08-14 寫「已開卡 #71 故不重複開卡」，而 #71 是**紅利到期軸**、與受眾分群無關且已完成 ⇒ 受眾缺口約五輪既無卡又被抑制；**`ledger-card-sweep` 對這一類免疫**（它只查「卡完成但 status 沒回填」，本例是**卡與缺口不對應**＝需語意判斷；本輪 sweep 實跑 0 筆、對此完全沉默）③ 世界活動 present（`lobby.js:30`／`:183` 兩道真站閘行號未移）④ 抽獎/Rain present（`redeem.js:17` 的 `CODES` 仍兩欄＝兌換碼缺資格述詞連四輪，**刻意不獨立開卡**、併入 #107 當第二批消費端以免長雙胞胎）⑤ 推薦/聯盟 partial（14:00 窗已即時回填）⑥ 新手引導 present（grace 76 命中，與 08-16 一致）。
    - **開卡 #107**（促銷受眾述詞·S–M·**複用 #54 `HL.release.AUDIENCES` 詞彙、明文禁止第二套受眾表**，並把 release 的三項阻塞事實〔ctx 維度不足／early-vs-窗口的階段語意不同／真站模擬活躍度恆 false〕先抄進卡，別讓實作輪重新發現一次）。本輪只開 1 張（額度 2）：受眾述詞與兌換碼資格是同一個出口形狀，硬拆兩張就是 #95/#72 那種雙胞胎。
    - **驗證**：node **176→177 全綠**；**node vm + HL stub 跑瀏覽器路徑 15/15**（假站/真站兩態註冊與求值、四條搜尋詞、反手抄實證）；**負向擾動 6/6 全被抓**。首屏 1484→**1493KB／90 支**（餘裕 107KB）。sw v178→v179。
    - ⚠️ **驗證誠實聲明（連續第十四輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。三條新說明在真實面板裡的觀感、切語言後標題是否真的換字，未經人眼或真實渲染引擎確認。**待目視清單增為九項**（新增：**#106 說明中心三條新條目 + 三語標題**），其餘八項不變。**下一個可靠 preview 輪請開 ⚙ →「🧪 自我檢測」確認總數 177 且全綠**（此前 176）。
    - **下輪（08-19 08:00）首要**：台帳依輪替序＝**`資安`（5 模組·08-16）**；實作候選 **#59**（近 30 天活躍光環·M·#50 已落地故可動工）或 **#107**（受眾述詞·S–M·純資料描述子，但日曆/清單兩處會改渲染＝**建議排在有 preview 的輪**）；⚠️ **#103 屬遊戲軌、#104／#105 含後端需船長批准，平台軌均不得代裁**。調研：最近到期 bet365／rollbit **08-24**（未到期，下輪多半仍不取材）。

- **2026-08-18（平台軌 · **14:00 窗**建置輪 · 實作 #58 推薦/邀請好友〔**卡上「雙方各得獎勵」這句話在純前端的真站是結構上不可能的，不是還沒做**〕· 台帳審「功能」13 模組〔**兩個數字不合時，錯的往往是我手上那把尺**〕· 開卡 #105／#106 · 不取材〔ACTIVE overdue 0/35、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 10:00 窗已釋放）→ claim `p-141530-c9e4`（帶心跳 14:15→15:xx）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.9h**（本日 09:15 → 本輪 14:10）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派三項**（台帳＝`功能`／實作候選 #58 或 #93／#103 屬遊戲軌、#104 含後端皆不得代裁）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十四輪**。
    - **實作 #58**：新檔 `core/referral-core.js`（`HL.refCore` 純函式：邀請碼／歸因／分階釋放／冪等·node 可 require）+ `core/referral.js`（殼層：`?ref=` 落地歸因、好友清單、領獎、面板）；接線 #49 活動日曆（`sched:"always"`）、#90 經濟旋鈕表、福利中心「成長 · 商城」入口。里程碑尺採 `HL.vip.status().wager`＝**已是 #50 成本加權後的量**（推薦獎勵因此不會被低 edge 遊戲刷）。
    - ⭐ **本輪最重要的發現＝「雙方各得獎勵」在純前端的真站不是還沒做，是做不到**：被邀請者的歸因寫在**他自己的 localStorage**，兩台裝置間**沒有任何通道** ⇒ 真站好友清單**恆為空**、推薦人側獎勵永遠不會觸發；被推薦人側若照發則是「自己貼碼給自己就領錢」＝**無限印幣**（§11）。⇒ 沿 #57 先例：**無見證者就據實不供應獎勵、面板說明原因，但歸因照記**（記錄不是付款）。⚠️ 這條限制**不只屬於本卡**——所有「兩個玩家之間」的功能（好友、私訊、贈禮、公會邀請）都撞同一堵牆，故**已回填進台帳「會員/身分系統」模組**，讓下一張同型卡不必重新發現一次。
    - ⭐ **第二個發現＝「有生成」不等於「看得到」**：假站模擬好友的窗口起點原設在「本機首次開站那一刻」，於是新玩家第一次打開面板時**一位好友都沒有**（所有模擬加入時刻都在未來）——卡上「展示完整體驗」落空，更糟的是**它與真站無見證者的空清單長得一模一樣**（兩個完全不同的原因、同一個畫面）。修法＝假站窗口起點回推 10 天（受 isLive 閘管制的假站種子）。**假站種子要問的不是「有沒有生成」，是「玩家第一眼看不看得到」。**
    - ⚠️ **量測法自己錯一次（「先懷疑那把尺」家族本輪再一例）**：台帳審成就徽章牆時改用 brace matching 取權威種子數，第一版只追 `[ ]` 深度、在 bracketDepth===1 看到 `{` 就計數 ⇒ **元素內部的巢狀物件也被算成元素**、得 **21**；`cat:` 分類交叉加總只有 **19**，兩數不合才發現壞的是尺不是資料（修法：同時追 `{ }` 深度）。同一把修好的尺順帶**結清 08-15 窗留下的問號**：公會種子權威值 **6**（naive `grep -c "name:"` 的 9 含 3 個非公會物件）⇒ 舊記載正確。
    - ⚠️ **負向擾動 22/22 全抓，其中兩例是擾動自己抓出來的**（先證明乾淨樹 176/176 全綠才開始，08-18 08:00 教訓③）：(i) **真洞**——架構鎖原用「全檔存在 `id: "referral"`」判日曆註冊，但該字串在本檔**出現兩次**（日曆＋econCfg）⇒ 把日曆 id 改壞（活動從日曆上消失）**仍全綠**；已改成兩個 register 各自切區段驗（#57 教訓②的第二個變形）。(ii) **harness 缺陷**——`bots: !isLive()` 在檔頭註解裡也有一份，replace 命中註解、程式碼一字未改，差點記成「鎖是空的」。**擾動必須錨定程式碼本體，否則「沒抓到」與「沒擾動」同形。**
    - **台帳審「功能」13 模組**（08-15＝全庫最舊）⇒ 全數回填 08-18：① 遊戲引擎 partial（views 33 檔／自我註冊 19／`HL.fair` 23／第三方聚合 0；⚠️ registry.json **記 HEAD 的 1 筆**不記工作目錄的 2 筆——差額是他人未提交 WIP，§7）② 可驗證公平 present ③ **中央結算 21→22**（新增 `challenges`；⭐ 反例＝#58 **刻意不掛**中央點、改拉取式，**擴充性也表現在「新功能可以不訂閱」**）④ VIP present（`addWager` 呼叫點 2 處＝單一入口未被繞過）⑤ **任務/成就/簽到＝本輪 `ledger-card-sweep` 告警的那一筆**：連四輪記載的「稀缺型限量名額」缺口**已被 08:00 的 #57 關閉**，回填後該模組**現無開放缺口**（工具告警 1 筆、逐筆讀 evidence 確認為真非誤報，回填後重跑 **0 筆**）⑥ 成就 19 枚 ⑦ JP/錦標賽 present ⑧ 排行榜 present ⑨ 會員 present（+ 上述跨裝置限制）⑩ 公會 partial（結清問號＝6）⑪ PWA v168→**v178** ⑫ 通知 present（#58 未新增管道）⑬ 支援中心 present（`register` 7 次／6 檔；⚠️ #57／#58 皆未註冊說明＝**該模組現唯一開放缺口**，開成 #106）。另**即時回填**「活動／推薦聯盟」**absent → partial**（本輪自己落地的卡，不留高報缺口）。
    - **開卡**：**#105**（推薦歸因的伺服器見證者·含後端故 ⬜待批准；與 #104 刻意同形＝「跨客端互斥」與「跨客端關係」同一族，建議一併排程）、**#106**（把 #57／#58 註冊進說明中心·S·**純文字出口、零視覺回歸＝排程輪也能安全落地**，回應近十二輪「沒有 headless-safe 候選」的抱怨）。
    - **驗證**：node **173→176**（3 個純函式測項 + 3 條架構鎖）；負向擾動 **22/22**；排程輪結構上取不到 dev server ⇒ 以最小 `HL` stub 在 node vm 內載入**真正的** `econ-config.js`／`referral-core.js`／`referral.js` 跑完整瀏覽器路徑 **51/51**（含真站不供應→歸因仍記→註冊見證者恢復→撤除後回到不供應）。i18n EN/zh-Hans **+26／+25** 鍵（串接型 toast 依 P3 刻意不加），兩包重複鍵 **0/0**。首屏 1447→**1484KB／90 支**（門檻 1600/120·餘裕 116KB）。sw **v177→v178**。⚠️ **無 preview／無目視（連續第十二輪）**，待目視清單增為十一項。
    - **下輪（08-18 20:00）首要**：台帳依輪替序＝**`活動`（6 模組·08-16，扣掉本輪即時回填的推薦聯盟一筆）**；實作候選 **#106**（S·headless 可落地）或 **#59**（近 30 天活躍光環·M·#50 已落地故可動工）；⚠️ **#103 屬遊戲軌、#104／#105 含後端需船長批准，平台軌均不得代裁**。調研：最近到期 bet365／rollbit **08-24**（未到期，下輪多半仍不取材）。
<!-- 2026-08-18 平台軌 14:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入 -->

- **2026-08-18（平台軌 · **08:00 窗**建置輪 · 實作 #57 限量挑戰「先搶先贏」〔**卡上那句「真站只計真實玩家」在純前端做不到，而且失敗方向恰好是最壞的那一邊**〕· 台帳審「擴充性」5 模組〔**首屏數字量了十幾輪，這輪才第一次寫下那把尺**〕· 開卡 #104〔伺服器名額仲裁·含後端故標待批准〕· 不取材〔ACTIVE overdue 0/35、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:00 窗已釋放）→ claim `p-081115-c4a2`（帶心跳 08:11→09:1x）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **10.9h**（08-17T21:15 → 本輪 08:09）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳＝`擴充性`／實作候選 #57 或 #93／#103 屬遊戲軌不得代裁）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十三輪**。
    - **實作 #57**：新檔 `core/challenge-slots.js`（`HL.chalSlots` 純函式名額仲裁·node 可 require）+ `core/challenges.js` 接線；spec 新增選用描述子 `slots`／`expiresAt`，首位註冊者 `rush25`（25×·名額 3·獎金 3000），並 register 進 #49 活動日曆。既有三條 DAILY **零回歸靠「欄位不存在」**而非比對。
    - ⭐ **本輪最重要的發現＝一條房規在真站會靜默反向，而外觀完全一樣**：依 §4 關掉模擬對手後，「先搶先贏」在真站只剩一人在搶＝必贏 ⇒ **真站送幣成本高於假站**，違反 §11 方向。**退化成無限名額是最糟的處置**（規則失效但畫面照舊）⇒ 改為**真站無仲裁者就不供應**、面板據實說明，容器 `setArbiter` 留著（#104）。**通則：把假活動閘上之後，要回頭問「剩下的那個機制還成立嗎」——閘住的是模擬，不是規則本身。**
    - ⭐ **第二個發現＝一道閘只要有第二個讀者就等於沒有**：首版 `specOf` 直接掃 `DAILY`，繞過整道真站閘；當時**沒出事純屬巧合**（claim 另有 grab 前置條件擋著）。已改走 `specs()` 並把「`DAILY` 只准兩個讀者」寫成鎖。
    - ⚠️ **本輪量測法自己錯三次，三次同一形狀「看起來完美通過、其實什麼都沒鎖到」**：(i) 空窗期斷言鎖不到 `LEAD`（連續值恰等於開窗那一毫秒的機率趨近 0，改 LEAD=0 仍全綠）；(ii) `[^}]*setArbiter` 前綴誤匹配（出口改名成 `setArbiterX` 仍全綠）；(iii) **擾動 harness 本身**沒有乾淨樹基線，於是「擾動抓到了」與「本來就紅」輸出同形，一例被誤記通過。三者皆已修。⇒ **通則：任何「破壞它、看它報錯」的驗證法，都必須先證明未破壞時是綠的。**
    - **台帳審「擴充性」5 模組**（08-15＝並列全庫最舊）⇒ 回填 08-18：① Dockable Layout partial（註冊者仍 3·本輪刻意未新增第四個）② Design Tokens present（`HL.ui` 出口 **19**·本輪未增元件·名額圓點走 token 色但尺寸裸值＝據實記小額繞道）③ Automated Tests partial（**166→170**·19 例負向擾動全抓·harness 盲區已修）④ Code-splitting present（**1424→1447KB / 88 支**·餘裕 153KB）⑤ Release Scheduling present（查獲**稀缺性排程與受眾排程正交**·樣本數 2 尚不值得抽象·`HL.promoCal` 是天然匯流點）。
    - ⭐ **本輪把「首屏」那把尺第一次寫進台帳**：口徑＝`index.html` + 本地 `<script src>` + 本地 CSS 三者位元組和（CDN 不計）。**交叉驗證 HEAD 版重算＝1424KB，與維護軌 00:00 窗回報逐位相符，且與 `platform/first-screen-budget` 常駐鎖同一口徑** ⇒ 人工回報與機械閘量的是同一件事。此前只有數字沒有尺（我自己第一次量得 1170KB＝只算 script，差 254KB）——**同一個名詞、兩個口徑，恰好從未撞上**。
    - **驗證**：node **170/170**、負向擾動 **19/19**；排程輪結構上無 dev server ⇒ 另以最小 `HL` stub 在 node vm 內把瀏覽器路徑真的跑一遍（搶名額→領獎→耗盡→真站濾除→仲裁者恢復→日曆接線）**32/32**。i18n 兩包各 +6 鍵、重複鍵 0/0。sw `v176→v177`。⚠️ **無 preview／無目視（連續第十一輪）**，待目視清單增為九項。
- **2026-08-17（平台軌 · **20:00 窗**建置輪 · 實作 #102 回報率軸〔**「容器就緒、只缺內容」漏問了一句：缺的內容是隨機缺的，還是剛好缺在會讓結論反過來的那一邊？**〕· 台帳審「後台」7 模組 · 開卡 #103〔plinko 沒有單一 RTP·跨軌〕· 不取材〔ACTIVE overdue 0/33、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:00 窗已釋放）→ claim `p-201015-8d4f`（帶心跳 20:10→21:0x）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.0h**（本日 15:10 → 本輪 20:09）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳＝`後台`／實作候選 #102 或 #57／調研 bet365·rollbit 08-24 未到期）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十二輪**。
    - ⭐ **本輪最重要的發現＝「覆蓋率不是進度條，它有方向性」**。#102 卡（6 小時前由 14:00 窗自己開的）把 RTP 軸的處境寫得很清楚：容器 08-15 就好了、阻塞的 `HL.gameRtp` 已由 #98 於 08-16 解除，剩下的只是「加一筆 register 讓桶去查」，並誠實附註「(b) `HL.gameRtp` 只覆蓋 7 款而可玩有 24 款 ⇒ 實作前要先判斷這樣的軸對玩家是否還有意義」。**動手前的機械複驗給了那個判斷一個很硬的答案**：那 7 款是 4 slot + chicken/cases/bounty，而**全站 RTP 最高的 10 款 originals 一款都不在裡面**（dice/limbo/crash-x/mines/keno/towers/hilo/dice-duel/picks 99%、pump 98%）⇒ 照卡上字面上線，玩家點「💎 RTP 99%+」會看到 cases 98.5% 與 bounty 100%、**看不到 Dice**＝一條「最高 RTP」桶裡沒有最高 RTP 遊戲的軸，**比沒有這條軸更糟**。⇒ 走卡上 (b) 授權的第二條路「先把覆蓋率補上去」。**通則：卡上寫「容器已就緒、只缺內容」時要多問一句——缺的那部分，是隨機缺的，還是剛好缺在會讓結論反過來的那一邊？**（這與 08-17 14:00 窗的「鎖守的是代理指標」是同一個家族的第 8 變形：**指標的數值對、方向錯**。）
    - ✅ **實作 #102（登記數 7→17、`views/casino.js` 零 diff、側表零副本）**：`data/game-traits.js` 新增**求值型欄位** `DERIVED.rtp`＝求值當下向 `HL.gameRtp.of(id)` 要 ⇒ 「大廳能依 RTP 分群」與「RTP 只有一份」同時成立（鎖法＝**拔掉 `HL.gameRtp` 後必須全部缺值**，私藏副本就會有殘值）。桶＝💎 99%+（9 款）／🟢 98–99%（2）／🔵 96–98%（5）＝16 款落桶；未覆蓋 8 款依「缺值不進軸」不出現在任何桶。
    - ⭐ **平台軌補登記 10 款 RTP 卻沒有「代填」任何一個數字，關鍵在正當性必須每輪重新成立一次**：#94 定案 `rtp` 屬**遊戲軌**權威（需蒙地卡羅或解析證明），所以本批的每個值都是**從該遊戲自己的 `module.exports` 常數與純函式窮舉重算**得來——dice ∀target×∀方向（194 組）、mines ∀雷數×∀翻格（300 組）、towers ∀難度×∀層（24 格）、pump ∀難度×∀打氣數（81 組）、keno ∀選號數、limbo/crash-x ∀兌現點、hilo ∀牌面×∀方向、dice-duel/picks 模組自帶 `fairRTP()` ⇒ **min 與 max 皆恰等常數×100、零離散**。新鎖 `platform/game-rtp-derived-from-module` **每輪重新算一次再比對**（不比對任何寫死數字）⇒ 遊戲軌哪天調了 edge 常數，紅的是鎖、不是玩家看到的數字。**這條鎖就是那個正當性本身，不是它的紀錄。**
    - ⭐ **開卡 #103｜查獲 plinko 沒有單一 RTP，而它是 originals 五天王之一**：其餘 10 款皆零離散，唯 plinko 由 `buildTable×comb` 算出 9 種 rows×risk 落在 **98.8164–99.1014%**（離散 0.285pp，且 high/16 排**超過** edge 常數宣稱的 99%），成因是賠付表倍數取整。**登記 99＝高報 9 種中的 8 種；登記均值 98.88＝發明一個沒有任何設定真正等於的數字** ⇒ 兩者都是 `game-rtp-no-false-claim` 家族要防的形狀。據實**不登記**、開 #103 交遊戲軌裁決（三個選項＋平台軌傾向已寫進卡），並在鎖裡設**雙向**反向鎖：登記單值就紅、九種設定哪天真收斂到單值（離散 ≤0.05pp）**也紅**並要求回頭關卡（防「修好了卻沒人來關卡」）。⭐ 附帶一個誠實的細節：`instant-games.js:267` 的 `edge: "~1% 莊家優勢"` 是**全站唯一帶波浪號的莊優宣稱**——這個不確定性早在建置期就被誠實標示了，只是沒人回頭收斂它。
    - ⚠️ **本輪自己的量測法先出了兩次錯，兩次都是同一個形狀「看起來完美通過，其實零參數被掃過」**：① 探測腳本把 `towers.fairMult(k, diff)`／`pump.fairMult(k, spikes)` 的參數**傳反**（傳成 `(diff, k)`）⇒ `for (i=0; i<k; i++)` 因 k 是物件而**一次都沒跑**，兩函式各自回「EDGE 本身」與「1」，相乘**恰好等於 edge 常數** ⇒ 我一度把 `min===max===常數` 當成「零離散」的鐵證，而它其實是「根本沒動過」的同義詞。**抓到它的是我自己設的樣本數下限**（`t.ok(s.n >= c.min)`）——towers 只掃到 8 組（應 24）當場紅。② hilo 首版把 A/K 的**鎖向**（不可能更低／更高，`pHi`/`stepMult` 皆回 0）算成「RTP 0% 的注」⇒ 改為明確排除，並用 `nExact: 24` 把「只准排除 2 個」釘死。⇒ **通則：把「樣本數下限」寫進每一條窮舉型鎖裡，否則零樣本與完美通過在輸出上完全同形**（「先懷疑量測法」家族本輪連兩例）。
    - ⚠️ **順手修掉一個既有鎖的盲區（負向擾動查獲）**：`platform/game-axes-title-i18n` 以 shim 載入時**沒注入 `HL.gameRtp`** ⇒ 在它眼中回報率軸「零非空桶」而整條不渲染、三個新標題**從未被檢查**。實證：把 EN 的一條標題整行刪掉，**本鎖照樣全綠**。已補注入並**釘死被檢查的軸集合＝`pace,rtp`**。**教訓：用 shim 載入的鎖，少注入一個依賴不會報錯，只會讓被檢查的集合默默變小**（同 08-15「鎖對它唯一要禁的形制無效卻全綠」）。另**改掉兩條把「目前恰好只有一條軸／dice 恰好沒登記」當成不變量的舊斷言**（`ax.all().length===1`、`R.of("dice")===null`）——**本輪一口氣踩到兩個**，都是被正常演進推翻的前提。
    - **驗證**：node **166/166 全綠**（+2 新常駐鎖、修 1 條盲鎖、改 2 條過時斷言）；**負向擾動 13 例全數被抓**（登記值高報/低報、偷登記 plinko、basis 造假、側表私藏副本、桶界裂縫、桶界重疊、缺值歸桶、i18n 缺鍵、未來登記 92% 遊戲落裂縫、多註冊一軸沒補鎖…），**擾動前後乾淨樹皆 166/166**（落實「基線是髒的」教訓）。其中兩例首輪未被抓：一例是**擾動本身是空的**（桶界 96→90 在現行資料下行為零變化，因最低登記值 96.145 落在兩者之間），但它讓我補上真正該有的鎖＝**桶必須窮盡所有登記值**（有值卻落不進任何桶＝在軸上**靜默消失**，外觀與「缺值」完全一樣）。i18n 3 條結果牆標題各補 EN/zh-Hans，**桶標籤刻意零中文**（`💎 RTP 99%+` 三語同形）⇒ 免三條鍵、也避開 P3 的串接陷阱。首屏 1412→**1424KB／87 支**（餘裕 176KB）。sw v175→v176。
    - **台帳審「後台」7 模組**（08-15＝與 `擴充性`/`功能` 並列全庫最舊）⇒ 回填 08-17、**全庫最舊變 `擴充性`/`功能`（皆 08-15）**，依輪替序下一個＝**`金流`**（08-17 剛審過）⇒ 實際下一棒建議 **`擴充性`**。① `營運後台儀表板` partial，唯讀性複驗 `onClick|onInput|onChange` **仍恰 2 處**（連四輪逐位相同）＝它仍是**報表**不是後台；② `CMS` absent，權威口徑 `grep -c '^  var '` 實測 **26**（連四輪逐位相同）；③ `GameList` partial，兩張清單形制仍不同構（內建 MANIFEST 19 檔/22 款/欄位聯集 12 vs 社群 `registry.json` **HEAD 1 筆純路徑字串**）；④ `會員管理` absent（`admin|cms|backoffice` 視圖 0 命中·連四輪）；⑤ `活動/紅利設定` partial，promoCal 外部註冊者**仍 4 個**（未腐化但也沒長）；⑥ `經濟/RTP 設定` partial ⇒ **經濟旋鈕表 11→12 張**（新增者 `core/bonus-ttl.js`＝#71 今日 08:00 窗落地，**「落地時同步註冊」紀律自動生效、無人回頭補**＝#90 那條反向覆蓋鎖想造成的效果），且**RTP 側由 7→17 款可查詢**（本輪 #102）；⑦ `金流模式/法域切換` partial，`jurisdiction` 外部讀者**仍 0**（連三輪）。
    - ⚠️ **兩筆據實記下的量測法處置**：① `營運後台儀表板` 的 `HL.econCfg` 命中數，08-15 記「8 次」但**未寫明是行數還是出現次數**，本輪逐位重數為**出現次數 13／行數 7**＝**兩個都不是 8** ⇒ **刻意不宣稱「從 8 長到 13」**（那會是拿兩把不同的尺相減），改為當場**釘死口徑＝出現次數**、自本輪起可跨輪比較（沿用 08-17 14:00 窗把 i18n 尺寫成檔的處置）。② `GameList` 的社群清單筆數以 **HEAD** 為準記 1 筆——工作樹另有一筆**未提交**新增（`games/slot-engine/poc-spin/game.js`，mtime 08-03，伴隨未追蹤目錄）＝**別的 session／前景的 WIP，依 §7 一律不碰、不計入、也不代為提交**。
    - **本輪不取材**：`ACTIVE overdue 0/33`（連續歸零），最近到期 bet365／rollbit **08-24** 未到 ⇒ 配額改投實作與台帳（同前四輪處置）。`ledger-card-sweep` **0 筆待人工確認**。
    - ⚠️ **驗證誠實聲明（連續第十輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪所有結論來自 node 166 項 + 13 例擾動 + 逐款解析重算，**但「回報率頁籤真的出現在大廳頁籤列、三桶點進去各自過濾正確、EN/zh-Hans 標題不殘留繁中」未經任何人眼或真實渲染引擎確認**。⇒ **待目視清單增為八項**（新增：**#102 回報率三頁籤 + 結果牆標題三語**），其餘七項不變（#100 語言切換閃爍／#71 領取中心倒數+逾期通知／責任博弈自我排除區／8 款遊戲資訊列／大廳分群軸頁籤列／經濟旋鈕面板／#72 說明中心面板）。**下一個可靠 preview 輪請開 ⚙ →「🧪 自我檢測」確認總數 106 且全綠**（此前 104）。
    - **下輪首要**：台帳依輪替序＝**`擴充性`（08-15，5 模組）**；實作候選 **#57**（限量挑戰·S–M·掛 #49 promoCal + 中央結算點）或 **#93**（導覽入口註冊表·⚠️ 仍不適合 headless 輪）；⚠️ **#103 屬遊戲軌，平台軌不得代裁**。調研：最近到期 **bet365／rollbit 08-24**（未到期，下輪多半仍不取材）。

- **2026-08-17（平台軌 · **14:00 窗**建置輪 · 實作 #101〔**卡上的「36 項／7 支」是錯的，而它錯的方式比它想修的東西更值得記**〕· 台帳審「前端UI/UX」5 模組〔**連三輪不敢回填的真因：那把尺從來沒被寫下來**〕· 開卡 #102 · 不取材〔overdue 0/33、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 10:00 窗已釋放）→ claim `p-141230-4b7e`（帶心跳 14:12→15:0x）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.1h**（本日 09:05 → 本輪 14:09）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派三項**（台帳＝`前端UI/UX`／實作候選 #101 或 #57／調研 bet365·rollbit 08-24 未到期）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十一輪**。
    - ⭐ **本輪最重要的發現＝一條鎖可以「守著一個代理指標」，而代理與真相早就分岔了**。#101 卡（11 小時前由 08:00 窗自己開的）宣稱「7 支模組、合計 **36** 個測項在瀏覽器端從未註冊過」，並立了棘輪記債。本輪動手前先機械複驗——寫了一支**瀏覽器載入模擬器**（vm + DOM shim，依 `index.html` 的 `<script>` 序真的把整站跑一遍，再 fire `DOMContentLoaded`，然後直接讀 `HL.selftest._reg`）——結果**7 支裡有 4 支本來就註冊得到**：`econ-config`(2)／`ledger`(3)／`rakeback-core`(6)／`rakeboost`(9)＝**20 項**早就有 `else addEventListener("DOMContentLoaded", …)` 的延後分支（分別由 #90／#56／#60／#52 補過，`git log -S` 可查）。真值是 **16 項／3 支**。**錯因是機械的**：舊棘輪用 grep 判「載入位置是否早於 `selftest.js`」，而位置只是**代理**——它把「排得早」直接當成「收不到」，對已經自己延後註冊的模組**全部誤報**。⇒ 這是「卡片範圍是上一輪的推論」家族的**第 7 變形：鎖守的是代理指標，而代理與真相已經分岔**（前六＝範圍被低估／前提被推翻／要求與從未建造的模組整合／宣稱其餘已被中央機制覆蓋／鎖的錨點不存在／卡片描述的是症狀的一半）。**通則：立棘輪時要問「我記的這個數，是我要防的那件事本身，還是它的影子？」**
    - ⭐ **第二個發現＝真正在漏的那三支，漏的方式是「連錯都不報」**。`rewards.js` 是 `if (HL.selftest) registerTests(...)` **有 if 沒 else**；`wager-scope.js`／`score-axis.js` 更隱蔽——它們**無條件呼叫** `registerTests(HL.selftest)`，看起來最理直氣壯，但 `registerTests` 開頭就是 `if (!st || !st.register) return;` ⇒ 傳進去一個 `undefined`，**函式安靜地什麼也不做**。防禦性守衛（本來是好習慣）在這裡把一個載入序錯誤轉成了**完全無聲的失敗**。
    - ✅ **修法不是逐檔調位置，而是讓位置不再有意義**（直接沿用 08:00 窗自己寫下的通則：能用結構消滅的相依，就別只用斷言看守）：`selftest.js` 新增**延後註冊佇列 `HL._selftestQ`**——排在它前面的模組把 `registerTests` 推進佇列，它一載入就 `while`/`shift` 清算完畢（**同步**，不必等 `DOMContentLoaded`）。**15 支模組全部收斂成同一個形狀**，連排在後面、目前完全正常的 8 支也一起改，理由是「哪天 `index.html` 被重排也不會再靜默掉測項」；原本 4 份複製貼上的 `DOMContentLoaded` 寫法一併退場＝同一件事不再有兩套機制。棘輪則**保留同一個 id、換成守真不變量**（不新增也不刪除，避免看起來像有鎖消失）：改為斷言「沒有任何模組能再寫出『拿不到 `HL.selftest` 就默默不註冊』的形狀」，容器側還要驗佇列**真的被清空**（只讀不 shift 會被抓）。
    - **驗證**：node fast **164/164 全綠**；**模擬器 87 → 104**（+16 復活 +1 新增），且「由 `DOMContentLoaded` 補回的項數」由 20 **降為 0**。**負向擾動 8/8 全被抓**，且**擾動前先確認乾淨樹全綠**（164/164、sim 104）＝落實 08:00 窗記下的「基線是髒的」教訓。⭐ 最有說服力的一例：**把清算整段刪掉，模擬器由 104 掉到 68＝正好少 36 項** ⇒ 佇列現在確實承載著卡上點名的那整組 36 個註冊，只是其中 20 個原本就有另一條路可走——**卡上的集合是對的，對那個集合的判斷是錯的**。sw v174→v175。
    - **台帳審「前端UI/UX」5 模組**（08-14＝全庫最舊）⇒ 回填 08-17、**全庫最舊變 `擴充性`/`後台`/`功能`（皆 08-15）**，依輪替序下一個＝**`後台`**。① `大廳/遊戲牆` present，**量測法本輪升級為整站實跑**：種子卡 `casinoGames` 仍 **51 筆（playable 2）**、分佈與 07-30/08-04/08-07/08-14 **五輪逐位相同**，且**首次拿到執行期數字** `HL.games.all()` = **64 筆／playable 24**（與種子卡 51 是不同層次的兩個數，過去只量得到前者）；② `導覽殼層` present，#93 缺口逐位未動（`bottombar()` 行內 `item(` 仍 5、導覽 registry 關鍵字仍 0）——⚠️ **本輪自己踩到量測法陷阱並當場更正**：`SIDE` 陣列 naive 數 `{` 得 **6**，權威值 **5**，多的那個是最後一項的 `open: function () {`（#72 改死巷時加的），同 08-11 `dock.register` 鏈式／08-15 `guild.js` 一族；③ `遊戲客戶端框架` present，工具列六顆全在，**更正 08-14 一個 naive 口徑**：`HL.fair.isPF` 記「4 處」係含註解值，去註解**權威值 2 處**（數字更正、單一真相結論不變）；④ `i18n` partial（見下）；⑤ `大廳分群軸` partial，**缺口形狀改變**（見下）。
    - ⭐ **`i18n` 這筆是本輪第二個值得記的東西：連三輪（08-14／08-16／08-17 08:00）都標 stale 不敢回填，真因不是量測太難，是那把尺從來沒有被寫下來**。覆蓋率自 08-04 起就是判 partial 的依據，但歷輪都用一次性 `node -e` 的正則，每輪略有出入（分母 2404／3634／6808 各不相同），依「同法才可比」的紀律就只能一輪一輪標 stale——**一個永遠無法收斂的迴圈**。⇒ 本輪**把尺寫成檔**：新增 `intel/tools/i18n-coverage.js`（vm 實跑 `en.js` 攔截 `register` 取權威鍵集 + 固定掃描目錄與字面量長度上限 + 檔頭寫明三項已知偏差與「聚合值不可當 KPI、只看逐檔相對排序」）。**新基線 1750/4326 = 40.5%，並刻意宣告與前三輪的數字不可比**——從本輪起才開始可跨輪比較。字典鍵數以權威法複驗＝**EN 1143／zh-Hans 967，與 08-16 逐位相同**（#100 拆檔後零漂移），另新增一個角度：zh-Hans 對 EN 鍵集覆蓋 **84.6%**。⭐ 最低覆蓋群裡**只有 `views/auth-view.js`（0/17）是玩家面向**，其餘（`selftest` 0/62、`rakeback-core` 0/58、`bonus-ttl` 8.8%、`score-axis` 9.6%、`ops-dashboard` 8%）皆為測試訊息密集的機制層或營運者面向＝**分母被灌大的預期偏低** ⇒ 據實記錄並**點名維護軌 i18n 維度（U33 家族）**，本軌**不開卡**（避免把「數字難看」當成工作，沿用 08-14 處置）。
    - **開卡 #102 · 並回填一個已經解除的阻塞**：`大廳分群軸` 08-15 記「rtp 軸有一個**具體且已被查證的阻塞**（RTP 只以顯示字串存在、不可查詢）」——該前置條件 **#98 `HL.gameRtp` 已於 08-16 落地** ⇒ 阻塞不再成立，缺口形狀**由「被阻塞」變成「已解鎖未做」**，若照抄舊 evidence 就會繼續高報。開卡時**依 SKILL 第 3 步把三項阻塞事實先抄進卡**：(a) `rtp` 值必須向 `HL.gameRtp` 求值、**不得寫進側表**（既有反向鎖 `platform/game-axes-no-second-rtp` 會擋，且它對兩種寫法都有效）；(b) `HL.gameRtp` **只覆蓋 7/24 款**，且 `shadow-ritual` 是**刻意不登記**的（實測 full RTP 1132%）⇒ 軸上線後只分得到 7 款，實作前要先判斷還有沒有意義；(c) `volatility` 的權威在**遊戲軌**（#94 定案）⇒ 跨軌，平台軌不得代填。
    - **`ledger-card-sweep` 本輪唯一 ⚠️ 已人工複核並回填**：`活動/促銷框架` evidence 寫「已開卡 #71」，而 #71 已於今日 08:00 落地 ⇒ 回填為「已關閉」，同時**據實記只關了一半**（`entries` 有壽命、`unlocked` 側刻意未做且屬船長裁決），**狀態維持 partial**（判 partial 的理由是「統一事件驅動 campaign 引擎仍缺」，與 #71 無關），**`last_audited` 不回填**（只複核卡狀態、未重測本模組其餘各項）。掃描器現為 **0 筆待確認**。
    - ⚠️ **驗證誠實聲明（連續第九輪，且本輪的範圍要特別講清楚）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪所有「瀏覽器端」結論**全部來自 vm + DOM shim 模擬，不是真實瀏覽器**（shim 有一處已知落差：某個 `DOMContentLoaded` handler 因 `querySelector` 回 null 而拋錯，已隔離、不影響註冊統計）。⇒ **下一個可靠 preview 之輪請開 ⚙ →「🧪 自我檢測」確認總數為 104 且全綠**（此前 87）。**待目視清單維持七項**（#100 語言切換閃爍／#71 領取中心倒數+逾期通知／責任博弈自我排除區／8 款遊戲資訊列／大廳分群軸頁籤列／經濟旋鈕面板／#72 說明中心面板），**本輪未新增**（零 UI 變更）。首屏 **1419KB／87 支**（門檻 1600KB/120，餘裕 **181KB**）。
    - **本輪不取材（非閒置）**：ACTIVE **33**、**overdue 0/33**、最近到期 `bet365`/`rollbit` **08-24**（尚有 7 天）⇒ 無到期可深挖，火力全給明文指派項。⚠️ `_queued_candidates` 兩筆（caliente／bet.br）**仍待船長裁決**。
    - **下輪首要**：台帳依輪替序＝**`後台`（08-15、7 模組）**；實作候選 **#102**（S–M·headless 可落地·且它只需改 `data/game-traits.js` 一個檔）或 **#57**；⚠️ **#93 仍不適合 headless 輪**。調研：最近到期 **bet365／rollbit 08-24**（未到期·下輪多半仍不取材）。

- **2026-08-17（平台軌 · **08:00 窗**建置輪 · 實作 #71 紅利壽命軸〔**卡上那條「不得回頭作廢」的紅線，最後不是靠斷言守住的，是靠函式簽章裡沒有那個參數**〕· 台帳審「金流」5 模組〔**去註解還不夠：剩下那一筆命中是「說明本功能不存在」的字串**〕· 開卡 #101 · 不取材〔overdue 0/33、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:00 窗已釋放）→ claim `p-081015-9a3d`（帶心跳 08:10→09:0x）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.1h**（08-16T21:05 → 本輪 08:09）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳＝`金流`／實作候選 #71 或 #57／調研 bet365·rollbit 08-24）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第二十輪**。
    - ⭐ **本輪最重要的發現＝把不變量做成「結構上做不到」，比做成「有一條測項盯著」強一級**。卡上寫的風險是「已達流水而轉入 `unlocked` 的錢不得因原 entry 的 TTL 被回頭作廢」，而卡也直接指定了做法＝「寫一條測項斷言它」。實作時選了另一條路：把清理函式的簽章訂成 `sweep(entries, now)`——**裡面根本沒有 unlocked 這個東西**，而達標的 entry 早就被 `entries.shift()` 移出 ledger ⇒ 那條紅線不是「有人在看」，是**沒有路徑可走**。測項因此退化成「看守這個結構」（掃 `bSweep` 全篇不得出現 `unlocked`、`sweep.length === 2`），而不是「看守這個行為」。**通則：能用作用域表達的不變量，就別只用斷言表達——斷言會被改，簽章被改時會有一整排東西跟著壞。**
    - ⭐ **第二個發現＝負向擾動跑出 22/26 的那一輪，數字是雜訊，因為乾淨樹本來就是紅的**。首輪擾動 4 例未被抓，我據此去「加強」三條鎖；加強後再跑，才發現我新寫的斷言 `\b(false|0)\s*\?\s*el\(` **在乾淨樹上就會 FAIL**——`\b0` 咬到了既有的 `rest > 0 ? el(`（#20 那行「其餘排隊中」）。而它一紅，**每一例擾動都會看到這條紅燈**，於是 11 例全被判成「鎖有效」。⇒ **擾動前必須先確認乾淨樹全綠**，否則量到的是自己測項的傷口。這與 08-16 20:00 窗「擾動是空的」是**同一個家族的第三個變形**：① 鎖是空的 ② 擾動是空的 ③ **基線是髒的**。
    - ⭐ **第三個發現＝把守衛改成 `if (false)` 時，四條「檢查那幾行還在不在」的鎖全部綠燈**。因為被停用的程式碼**仍然存在於原始碼裡**，grep 找得到。改為逐一檢查每個出口的**守衛條件本身**（比照 `platform/self-exclusion-single-writer` 的「只准這幾種右手邊」形制）。**而修完之後第二輪擾動又抓到一個真的洞**：`bSweep` 裡有**兩個** `HL.notify.add(`（逾期作廢／到期前提醒），我用 `indexOf` 只看到第一個 ⇒ **把「已逾期」那則整行刪掉時，測項會找到「即將到期」那則而全綠**。改為逐則以標題字串定位、逐則驗守衛，並釘死「有且只有兩則」。⇒ 最終**負向擾動 37 例全數被抓**（其中 2 例是我預期的 id 猜錯、實際由另一條鎖抓到，據實記為「抓到但非預期來源」）。
    - **實作 #71**（新檔 `core/bonus-ttl.js`＋`progress.js` 三處接點＋`ledger.js` 新型別＋`ops-dashboard.js` 一行）：壽命依**送幣來源**註冊，`id` 直接就是 `badd` 的 `source` 字串 ⇒ **不另立第二套識別**。首批 6 個來源有壽命（假站寬鬆／真站業界值，`daysLive` 未宣告時**恆等於** `days` ⇒ 「真站不得比假站寬鬆」是恆等式而非宣稱），**其餘 17 個維持永不到期**。零回歸靠**欄位不存在**：未註冊來源 `expAt()` 恆回 0 ⇒ `if (exp>0)` 不成立 ⇒ entry 裡沒有 `exp` 這個鍵，與改版前逐位相同；舊存檔在 `now=9e15` 最嚴苛掃描下**清 0 筆、留 2 筆、逐位不變**。帳本新增 `bonus_void` 做成本回沖（`promo = bonus + faucet − bonus_void`，**毛額刻意不動**＝發出去多少與真被拿走多少同時可見；回沖夾在 `[0, bonus+faucet]` 內，防存檔被清時 NGR 憑空變好看）。
    - ⚠️ **刻意只做一半並把理由寫在卡上**：卡另要求 `unlocked` 側也加壽命，**不做**——它是單一純量、無逐筆授予紀錄（要做得先改成第二本 ledger），且那是「銷毀玩家已賺到、隨時可領的錢」＝風險最高的變體，屬**船長裁決**。更關鍵的是**不預留欄位**：若先塞個 `uat` 時戳等以後用，就會長出零讀取的死欄位——**維護軌 08-17 00:00 窗（11 小時前）剛開的 T34 正是同一種病**（`HL.rg` hint 死欄位·born-dead）。
    - **台帳審「金流」5 模組**（08-14＝與 `前端UI/UX` 並列全庫最舊）⇒ 回填 08-17、**全庫最舊變 `前端UI/UX`（08-14）**，輪替序下一個＝**`前端UI/UX`**。① `支付/收銀台` 維持 partial，`psp|對帳|reconcil` 去註解**連五輪 0 命中**、#82 支付通道註冊表仍未落地 ⇒ 缺口形狀連五輪逐位相同，**不重複開卡**；② `錢包/帳本` 維持 present，中央插樁 59 處、`ledger.TYPES` 11→**12**（隨 #71 同輪回填）；③ ⭐ `提款審核佇列` 維持 absent，**把 08-14 立的量測通則再收一層**——08-14 記「命中 3 筆全是註解 ⇒ 判 status 前要去註解」，本輪照做後**仍有 1 筆**，逐筆查證＝`service-level.js:598` 的**字串字面量**「…不是送人工審核。」，內容同樣是在**說明本功能不存在** ⇒ **去註解不夠，還要看命中內容是不是「在描述缺席」**（註解與字串字面量是同一種失真的兩個載體，只擋前者下一輪會重新誤判）；④ `P2P` 維持 partial，逐位無變化；⑤ `獎金/流水引擎` 維持 partial（`opts.wagerMult` 仍 0 命中＝partial 的**唯一**理由），但隨 #71 補上**台帳從未記過的第三條軸** ⇒ 本模組三軸現已齊備：**多少**（#74 依段位 req）×**在哪打**（#89 scope）×**多久內打完**（#71 ttl），三者都掛同一個 `opts` 描述子、都在授予當下凍結、都不追溯改既有紅利。
    - **開卡 #101**（自我檢出）：7 支核心模組 **36 個測項在瀏覽器端從未註冊過**（排在 `selftest.js` 之前），而 node 端因 `run.js` 逐檔 require 全跑得到 ⇒ **在 CI 上完全看不出來**。**刻意不順手全修**：`econ-config.js` 排第 3 支是必要的（六支模組在載入期 `if (HL.econCfg && …)` 自我註冊，往後移會讓那些註冊**靜默變 no-op**）⇒ 先立**棘輪**（名單只能變短；修好卻沒從名單刪也會 FAIL，防棘輪自己鬆掉），雙向擾動各 1 例皆被抓。
    - **本輪不取材（非閒置）**：ACTIVE **33**、**overdue 0/33**、最近到期 `bet365`/`rollbit` **08-24**（尚有 7 天）⇒ 無到期可深挖，火力全給明文指派項。⚠️ `_queued_candidates` 兩筆（caliente／bet.br）**仍待船長裁決**，未裁決前不自行加進 `platforms[]`。
    - ⚠️ **驗證誠實聲明（連續第八輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪新增的**領取中心到期倒數兩行**（含 24h 內轉紅）與**說明中心「紅利有效期限」條目**，其實際渲染未經任何人眼或真實渲染引擎確認；逾期通知與到期前提醒同理（`HL.notify` 為瀏覽器側）。⇒ **待目視清單增為七項**：#100 語言切換閃爍（最高優先）／**#71 領取中心倒數 + 逾期通知（新）**／責任博弈面板自我排除區／8 款遊戲資訊列／大廳分群軸頁籤列／經濟旋鈕面板（現 12 張表）／#72 說明中心面板。首屏 1392→**1412KB／87 支**（+20KB／+1 支，門檻 1600KB，餘裕 **188KB**）。sw v173→v174。
    - **下輪首要**：台帳依輪替序＝**`前端UI/UX`（08-14、5 模組）**；實作候選 **#101**（S–M·但需逐檔判載入期相依、非機械調換）或 **#57**；⚠️ **#93 仍不適合 headless 輪**。調研：最近到期 **bet365／rollbit 08-24**（仍未到期，下輪多半仍不取材）。⚠️ **下一個可靠 preview 之輪請優先目視 #100 的語言切換與 #71 的倒數/通知**。

<!-- 2026-08-17 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入的最舊一則 -->

- **2026-08-16（平台軌 · **20:00 窗**建置輪 · 實作 #100 i18n 按語言拆檔〔**全站最大單檔搬走 145KB，而一條翻譯都沒改**〕· 台帳審「資料」3 模組〔**連四次 0 命中本身就是結論：它不是待辦，是純前端算不出來的東西**〕· 不取材〔ACTIVE overdue 0/33、最近到期 08-24〕）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:00 窗為純 intel/ 退避輪、未 claim）→ claim `p-201015-f3a9`（帶心跳 20:10→21:0x）→ 停頓做源碼量測與台帳審計 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.5h**（08-16T14:38 → 本輪 20:09）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳＝`資料`／實作候選 #100 或 #71／調研最近到期 bet365 08-24）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第十九輪**。
    - ⭐ **本輪最重要的發現＝拆檔會讓「掃字典原始碼」的測項靜默轉綠，而它們全都是為了防止漏翻才存在的**。既有兩條鎖（`platform/game-axes-title-i18n` 檢查結果牆標題有無 whole-key、`platform/game-rtp-i18n-second-copy` 檢查字典裡那份 cases RTP 有沒有漂）都是 `fs.readFileSync(src/core/i18n.js)` 然後在字串裡找鍵——**拆檔後那支檔裡一條鍵都沒有** ⇒ 兩條鎖會「掃到 0 條、找不到任何違規、報 PASS」。
      ⚠️ **這比 FAIL 危險得多**：FAIL 會停下建置，而**空掃的綠燈會一直綠下去**，直到某天真的漏翻、玩家看到中文才有人回頭查。⇒ 兩條鎖已改讀新增的 `i18nPacksSrc()`（掃 `src/i18n/*.js` 全部語言包），並各自補一條**不空心斷言**（`i18n.length > 0` / 語言包數 ≥2），讓「目錄空了」變成 FAIL 而不是靜默 PASS。**通則：任何以「讀某個檔的原始碼」為實作的測項，都與那個檔的位置耦合；搬檔案時要把它們一起搬，否則搬的當下就變成裝飾品。**
    - ⭐ **第二個發現＝我自己寫的擾動也是空的（同一課題本輪第二次應驗，換到擾動這一側）**。負向擾動第一版有一格「把結果牆標題從語言包移除」判定為**沒被抓**，看起來像是改讀 pack 之後鎖失效了；實查發現我擾動用的 key `"節奏 · 即時開牌"` **在字典裡根本不存在**（真實鍵是 `"節奏 · ⚡ 一鍵見分"`／`"節奏 · 🎚️ 逐步兌現"`）⇒ 那次擾動**什麼都沒改**，測項當然全綠。改用真實鍵重跑：只從 `en` 漏一條（occ 2→1）與兩包都漏（occ 2→0）**兩種形狀都被抓**。
      ⇒ **與 08-15 20:00 窗 `isLive()[\s)]*\?`、08-16 14:00 窗「JS 字串字面量吃掉一層反斜線」是同一條教訓的再一次應驗，但這次載體是擾動本身**：擾動必須用**真實世界會出現的形狀**，而且「擾動後測項仍綠」有兩種解釋——鎖是空的、或**擾動是空的**——不查清楚就會把後者誤報成前者，並據此去「修」一條其實沒壞的鎖。
    - **實作 #100**（新目錄 `src/i18n/`：`en.js` 82.3KB／`zh-Hans.js` 72.2KB；`core/i18n.js` **163.9KB → 12.9KB**）：引擎只留 walker/observer/setLang/語言選單 + 一張帶 `src` 的 `LANGS` 註冊表 + `register/ensure/ensureSync`，字典各自 `HL.i18n.register(code,{dict,prefix,suffix})` 上架 ⇒ **加一種語言＝ LANGS 加一筆 + 一個檔，引擎一行不改**（形制同 `lazy-games.js` 的 MANIFEST，也才是目標 3「可擴充 i18n」的正確形制）。
      - **零閃爍是這張卡真正的難點，不是省 KB**：拆檔前字典是 `i18n.js` 的一部分，於 `index.html` 第 98 行、`main.js` 第 156 行**之前**就位；若改成動態注入 `<script>`（不阻塞剖析、也不延後 DOMContentLoaded），首屏會**先渲染一整屏中文再翻成外語**＝比省下的 145KB 更糟。⇒ 開機分支走 `ensureSync`＝剖析期 `document.write` 注入（**阻塞剖析直到執行完**，與拆檔前時序等價），剖析已結束才退回非同步；玩家在選單切語言則走 `ensure`，**載完才 `commitLang`**（否則會先 restore 成中文再閃成外語）。兩條路徑都寫成測項。
      - **一條翻譯都沒改，並且是逐鍵證明的**：HEAD 版 vs 本輪版 `dict()` 逐鍵逐字比對 —— `DICT.en` **1143/1143**、`DICT["zh-Hans"]` **967/967**、`PREFIX`/`SUFFIX` 四張表 **44/44**（缺 0／多 0／譯文不同 0）＝**2154 鍵全同**（全鍵，非抽樣）。另做**行為 A/B**：同一組文字節點餵給兩版引擎，涵蓋精確比對／PREFIX 前綴／SUFFIX 後綴／trim 命中／不命中五條路徑 × 三語系＝**51 格逐格相同**，並附**不空心證明**（其中 23 格 HEAD 版真的產生了翻譯，若為 0 代表兩版都沒跑到翻譯路徑）。
      - **首屏預算 1532KB → 1387KB（−145KB）、86 支不變**，餘裕 **68KB → 213KB**（門檻 1600KB）＝從「約 2–4 個建置輪觸警」回到約 **9–13 輪**。卡上不變量 (c) 要求「下降 ≥100KB」達成。
    - **驗證**：node fast **149→153 全綠**（新增四條 `platform/i18n-packs-registered`〔語言包真的上架、字典非空、無空值〕／`-packs-not-eager`〔反向鎖：語言包不得進 index.html 首屏〕／`-engine-size`〔引擎 ≤40KB，防字典回流〕／`-boot-sync-load`〔同步載入路徑不得被拿掉〕）；**負向擾動 11/11 全被抓**（含「整個 `src/i18n/` 目錄消失」與上述兩條既有鎖改讀 pack 後的重驗）。另修一處**同型的靜默轉綠**：瀏覽器端 `i18n/dict` 測項在預設 zh-Hant 下 `dict()` 回空物件會掃 0 條而假綠 ⇒ 改為明確 `skip` 並指向 node 端的全語言鎖。sw v172→v173。
    - **台帳審「資料」3 模組** ⇒ 回填 08-16、**全庫最舊變 `前端UI/UX`／`金流`（皆 08-14）**，依輪替序下一個＝**`金流`**。① `報表與匯出` 維持 partial（`toCsv|.csv|download=|Blob(` 命中**仍僅 `core/betlog.js` 4 處**、與 08-14 逐位相同）——但附一筆新對照：本模組要的 `HL.reports.register` 形制**本輪已在 i18n 語言包上被實作出來**、不再是空想，**仍不開卡**的理由是第二個真出口尚未出現（只有一種資料時的註冊表＝為擴充性而擴充）；② ⭐ `資料/分析` 的 `eventSchema|funnel|cohort|retentionCohort` **連四次 0 命中（08-05/08-07/08-14/本輪）**——本輪**改判它不是待辦**：漏斗/世代留存要有意義需要跨 session 的玩家事件流，而純前端 localStorage 只看得到**這一台裝置**，算出來的「留存曲線」其實是單人重複造訪 ⇒ 據實記為 **§11 後端/真金前不值得做**（同 KYC/風控），**不開卡**，免得每輪重複偵測同一個 0 卻讓它看起來像欠著；③ `玩家注單` 維持 present（`betlog.js` 265 行、`CAP=500`、寫入點仍只有 `live-stats.js:53` 1 處；**08-14 記 266 行係計數口徑差，`git log` 證實自 #51 落地 `1dc4991` 後零 commit＝內容一字未改**）；④ `ledger-card-sweep` **0 筆待人工確認**。**另隨 #100 落地同輪補記 `前端UI/UX / 多語系前端 i18n`**：`stowable_note` 舊述「加語言＝加 DICT 分支」已過時、更正為新形制，字典鍵數以權威 vm 法更新（EN 1099→**1143**／zh-Hans 926→**967**）；⚠️ **但刻意不回填該模組 `last_audited`（維持 08-14）**——它是 partial 的理由**從來是覆蓋率而非架構**，而覆蓋率本輪未重量測，依 08-13 收緊的紀律「只有真正重新量測過的模組才回填日期」。
    - **本輪不取材**（非閒置）：ACTIVE **33**、**overdue 0/33**、最近到期 `bet365`/`rollbit` **08-24**（尚有 8 天）⇒ 無到期可深挖，火力全給明文指派的實作與台帳。⚠️ **`_queued_candidates` 兩筆（caliente／bet-br）仍待船長裁決**，未裁決前不自行加進 `platforms[]`。
    - ⚠️ **驗證誠實聲明（連續第七輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪的行為與結構由 153 項測項 + 11 次擾動 + 2154 鍵逐字比對 + 51 格行為 A/B 保證，**但「切到 EN 重載後不閃中文」是真實瀏覽器的時序現象，node 證不了**（`document.write` 的剖析阻塞行為在 vm 裡沒有對應物）⇒ **待目視清單增為六項，且本項應排最高優先**：**#100 語言切換閃爍（切 EN → 重載 → 確認不閃中文；記得先清 SW/caches 或帶 cache-buster，否則會驗到舊檔）**／責任博弈面板自我排除區與確認流程／8 款遊戲資訊列／大廳分群軸頁籤列／經濟旋鈕面板（11 張表）／#72 說明中心面板。
    - **counters**：`platform_cards_implemented` 58→**59**；`platform_cards_opened` **不加**（本輪未開新卡——`報表與匯出` 與 `資料/分析` 兩個缺口都經判斷**刻意不開卡**並寫明理由）；`platforms_researched` **不加**（本輪未取材）；`consecutive_idle_rounds` 維持 **1**；yield/stalled 不加。
    - **下輪首要**：台帳依輪替序＝**`金流`（08-14、5 模組）**；實作候選 **#71**（S–M·紅利到期軸·`release.js` 的 `AUDIENCES` 述詞表是現成零件）或 **#57**；⚠️ **#93 仍不適合 headless 輪**。調研：最近到期 **bet365／rollbit 08-24**（仍未到期，下輪多半仍不取材）；⚠️ **`_queued_candidates` 兩筆仍待船長裁決**。⚠️ **下一個可靠 preview 之輪請優先目視 #100 的語言切換**。
    - ⚠️ 磁碟仍留 `prototype/games/registry.json`(M)+`games/slot-engine/`(??)+`Game assets/` churn（mtime 08-03＝他人未提交工作·非孤兒）依 §7 原樣未動。

<!-- 2026-08-17 平台軌 14:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入的最舊一則 -->

- **2026-08-16（平台軌 · **14:00 窗**建置輪 · 實作 #96 自我排除〔**把第二種鎖做成同一張註冊表的第二個 kind，而不是第二套機制**〕· 台帳審「資安」5 模組〔**負責任博弈 partial→present，同輪回填**〕· **調研查出第三種取材漏法：維度清單本身沒有這個表面**）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 12:00 窗已釋放）→ claim `p-141015-c2e8`（帶心跳 14:10→14:27）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.1h**（08-16T10:05 → 本輪 14:09）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派三項**（台帳＝`資安`／實作候選 #96 或 #71／調研最近到期 bet365 08-24）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第十八輪**。
    - ⭐ **本輪最重要的發現＝我們有四張責任博弈的卡，沒有一張是看著對手做的——因為取材維度清單裡從來沒有這個表面**。實作 #96 前想找對手形制，`grep -rliE "responsible|自我排除|冷靜期|self-exclu|負責任" intel/platforms/` 實測 **35 份調研檔命中 1 份**，而那唯一一份（`card-crush.md`）**還是在引用我們自己的 #67/#72**。成因可機械說明：`db/sourcing-methods.md` A 節的四類訊號（流量／名氣／營收／主題機制）與 SKILL 第 1 步的 dossier 維度（遊戲/留存/促銷/UX/社群/金流）**沒有任何一項會走到各站的 Responsible Gambling 頁** ⇒ #67／#70／#86／#96 四張卡**全部只能由台帳自審生出來**。
      ⇒ 這是取材漏法家族的**第三種**：① 鏡頭濾掉整個垂直（08-15 caliente）② 上游來源自己看不見（08-16 08:00 bet.br）③ **維度清單根本沒有這個表面**（本輪）。**前兩種換榜單/換來源可修，第三種換多少來源都修不掉**，因為每次取材都照同一份清單抄。⇒ 已把「玩家保護訊號」補進 `sourcing-methods.md` A 節與 SKILL 第 1 步（含「開新維度前先問這份清單漏了什麼」的通則）。
    - ⭐ **補上這條維度的第一次執行，當場就改掉了卡上的設計**：卡推測期間為 90 天／180 天／1 年／永久；實查 Stake「Break in Play」＝1 天/2 天/1 週/1 個月（＝我方冷靜期同族），其 **self-exclusion 為無限期、最短 6 個月起跳**、期滿還需 formal return to play review 才復場，業界通用則是 **6 個月／1 年／5 年＋永久** ⇒ 依調研改採後者。**成本＝多一次 WebSearch**。另有一筆正面佐證：Stake 的儲值限額「設定/調降立即、調升/移除等 24h」與我方 #67 `RAISE_DELAY_MS=24h` **逐字相同**（08-05 自行推導、非抄來）。
    - ⭐ **實作 #96 的核心決策＝不做第二套機制**：自我排除與冷靜期對閘的要求逐字相同（擋注+擋儲值、不可提前解除、到期恢復），差別只有長度與確認強度 ⇒ 做成**同一張 `PAUSES` 註冊表的兩個 kind**，全站只留**一個**被閘讀取的欄位 `pause.until`。效果是**既有 27 個閘呼叫點一處未改**就自動涵蓋自我排除；反面若做兩套，閘就有兩個入口、第二個哪天漏接就是不變量 (a) 明文要防的「UI 擋得住、console 擋不住」。
    - ⭐ **「不可提前解除」寫成結構上做不到**：`planPause` 是 pause 的唯一寫入口且只能加長 ⇒ 已排除 5 年者點 24 小時冷靜期是 no-op、永久後點任何選項都是 no-op。**「永久」用最大合法時戳而非 `Infinity`**——`JSON.stringify(Infinity)` 會變成 `null`，存進 localStorage 讀回來**鎖就自己開了**（已寫成測項：永久值必須能 JSON 往返）。
    - **驗證**：node fast **142→149 全綠**（4 條行為鎖＋3 條源碼結構鎖）；**負向擾動 13/13 全被抓**；**零回歸以 HEAD 版 vs 本輪版 `evaluate` 逐格比對＝10935 格全同**，其中 **2936 格真的走到擋注分支**（附不空心證明：同一 pause 物件在 HEAD 版回 `{ok:true}`、本輪版回 `{ok:false,kind:"exclude"}`）。sw v171→v172。
    - ⚠️ **本輪首版的鎖又是空的，而且是測項自己抓出來的（同一教訓第四次應驗，換載體）**：源碼鎖首版用 `new RegExp("function " + fn + "\\(…")` 組樣式，**JS 字串字面量先吃掉一層反斜線**（`\(`→`(`、`\s`→`s`）⇒ regex 失效、`exec` 恆 null。與 08-14「測項寫進 template literal 使 `\s` 被吃掉」同病不同載體 ⇒ **通則升級：含正則的測項一律用字面量正則；需要動態片段就「先切片、再用字面量正則」**。第二版又踩 lazy 量詞（`allowed` 寫成 `evaluate(load().limits,…)`，`[^;]*?` 停在 `load(` 的右括號）⇒ 改貪婪。
    - **台帳審「資安」5 模組**（輪替接續 08-16 08:00 的`活動`）⇒ 回填 08-16、**全庫最舊變 `前端UI/UX`/`資料`/`金流`（皆 08-14）**，輪替序下一個＝**`資料`**。① ⭐ **`負責任博弈` partial → present（同輪落地同輪回填＝回填紀律第三個正面實例）**：08-14 已把維持 partial 的理由收斂為單一項＝自我排除，本輪 #96 關閉它 ⇒ **純前端可做的範圍內已無缺口**；升級的**誠實界線**同時寫進 evidence——仍缺的兩項（跨裝置/伺服器權威強制、期滿人工復場審查）皆需後端/牌照、在 CONTROL.avoid 內，回到 §11 checklist。② ⚠️ **KYC 的命中數更正，是「上一輪被低報」而非「功能出現」**：用 08-15 自己寫下的權威樣式重跑得 **4 命中**（08-15 記 1），逐筆讀完四筆**全是在說明本項為 absent 的邊界註解**，且其中三筆自 08-05 即存在 ⇒ 08-15 的「1」用同一樣式**不可重現**。**通則再證：命中數變動要逐筆讀，且低報與高報同樣要更正。** ③ `真/假站軸` 的 `isLive()` 回歸基準 **52→54 處/30 檔**（連三輪為正訊號，下次基準 54）；④ `風控/反詐欺` 權威樣式連四輪 **0 命中**、`權限/角色` 非 aria 的 `role` 仍 **0** ⇒ 兩者維持原判、不開卡（皆 §11 真金前項）。
    - **開卡 #100 i18n 按語言拆檔**（M6/M8 預算閘的趨勢外推）：首屏 **1532KB／86 支**、門檻 1600KB ⇒ **餘裕僅 68KB**，近日 +16~50KB/建置輪＝**約 2–4 個建置輪觸警**；而 `core/i18n.js` 已達 **160KB**（08-11 151→08-15 157→本輪 160，**仍在長**，因為每張新卡照 P3 補 EN/zh-Hans 就會再長）＝全站最大單檔，**預設語言 zh-Hant 一份字典都不需要** ⇒ 白帶約 140KB 從不執行的資料。M8 結案時就點名過這件事、9 天無人認領 ⇒ 本輪正式立卡（形制比照 #80 `lazyGames` 的 MANIFEST：**加一種語言＝多一筆 register + 一個檔**，同時也才是目標 3 的正確 i18n 擴充形制）。
    - ⚠️ **驗證誠實聲明（連續第六輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪新增二次確認 modal、生效期間的入口收起態、兩排 chip 的 ≤720px 換行**皆未經人眼或真實渲染引擎確認** ⇒ **待目視清單增為五項**：責任博弈面板的自我排除區與確認流程（新）／8 款遊戲資訊列／大廳分群軸頁籤列／經濟旋鈕面板（11 張表）／#72 說明中心面板。
    - **下輪首要**：台帳依輪替序＝**`資料`（08-14、3 模組）**；實作候選 **#100**（M·headless 可完整落地·且它是首屏預算的解法）或 **#71**（S–M·紅利到期軸）；⚠️ **#93 仍不適合 headless 輪**。調研：最近到期 **bet365 08-24**（watch 1/2）；⚠️ **`_queued_candidates` 兩筆（caliente/bet.br）仍待船長裁決**，未裁決前不自行加進 `platforms[]`。
    - ⚠️ 磁碟仍留 `prototype/games/registry.json`(M)＋`games/slot-engine/`(??)＋`Game assets/` churn（mtime 08-03＝他人未提交工作·非孤兒）依 §7 原樣未動。

<!-- 2026-08-17 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入的最舊一則 -->

- **2026-08-16（平台軌 · **08:00 窗**建置輪 · 實作 #98 宣告 RTP 單一真相〔**卡上說「只是顯示字串」，實際散在三處而且已經漂了**〕· 台帳審「活動」6 模組 · **調研找出第二種取材漏法：上游榜單自己看不見**）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:00 窗已釋放）→ claim `p-081530-d4e6`（帶心跳 08:15→09:40）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.1h**（08-15T21:05 → 本輪 08:09）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳＝`活動`／實作候選 #98 或 #96／調研最近到期 bet365 08-24）⇒ **做而不讓路、三項全數處理＝跨輪指派連續第十七輪**。
    - ⭐ **本輪最重要的發現＝卡片把問題說小了一半，而「說小了」的那一半正是它想防的事已經發生了**。#98 卡寫「RTP 只是顯示字串、全站無法列舉」——前半對（前端確實問不到），**後半錯**：宣告 RTP 早就有第二份機器可讀副本，在 `tests/checks-games.js` 的 `GAMES[].declaredRTP`。⇒ migration 前這個數字散在**三處**（玩家看到的字串／買入價推導的註解／測試表），照卡上字面「把字串改成數字」只會造出**第四份**。⇒ 收斂為單一真相 `HL.gameRtp`，**view 與 harness 都改讀它**。這是「卡片範圍是上一輪的推論」家族**第 6 變形：卡片描述的是症狀的一半，另一半已經惡化成實例**。
    - ⭐ **第二個發現＝那三份已經漂了，而漂的方向恰好是保真閘第 14 項要防的形狀**。pirots：玩家看到 96.0%，但買入價 `103.7×` 的推導是 `99.68/0.96145`、deep 鎖也對 0.96145 收斂 ⇒ **玩家以為基礎局 96.0%，買入路徑實得 96.12%＝買入看起來比基礎划算**。兩者都在真值 96.187% 的 ±0.5pp 內故非閘失敗——**這正是它能安靜存在的原因：每一個檢查單獨看都是綠的，沒有任何一條規則在比對「這兩個數字是不是同一個」**。⇒ 依 #94 定案 `rtp` 屬遊戲軌權威，**平台軌不代改**，改為登記成 `gateRtp` 並用 `divergence-pinned` 釘死（白名單只有 pirots，再多一筆就紅；要關掉必須刪掉那行＝逼出裁決），另開 **#99** 交遊戲軌。
    - ⭐ **第三個發現＝容器落地時，內容不該照單全收**。8 處 rtp 只登記 7 處：`shadow-ritual` 顯示 `~97%`，但 DEBT `S-slot-rtp` 實測 full RTP＝**1132.68%**。登記它＝**把已知為假的數字鑄成可查詢 API**，之後 RTP 軸會把旗艦排進 97% 那一格 ⇒ 刻意不登記 + 反向鎖擋住往後偷加。**「缺值即不進軸」（#94 的設計）在這裡第一次真的救了一次**。
    - ⚠️ **負向擾動又抓到我自己的鎖是空的（同一條教訓第三次應驗）**：`derived-from-game` 首版以為 `require()` 直接回傳數學物件，實際兩檔都包一層 ⇒ 取值 `undefined`、走進 `else { t.ok(true) }`，**正向測試全綠**（把登記值改成 99 也不紅）。改為「取不到就直接紅、不留跳過分支」才真的鎖住。⇒ **通則再補一句：跳過分支（skip/else-ok）是空心鎖最常見的藏身處，寫鎖時要問「取不到值時我是紅還是綠」。**
    - **驗證**：node fast **135→142 全綠**；**負向擾動 14/14 全被抓**；**零視覺回歸以真的 `gameInfoBar` 證明**（DOM shim 載 HEAD 版與本輪版兩份 `ui.js` 逐字比對）＝**7/8 逐字相同**，唯一差異是 pirots 少一個尾隨 0（值相同），既有 i18n 鍵全保留。sw v169→v170。
    - **台帳審「活動」6 模組**（08-14＝全庫最舊）⇒ 回填 08-16、**全庫最舊變 `前端UI/UX`/`資料`/`金流`/`資安`（皆 08-14）**，輪替序下一個＝**`資安`**。① `促銷框架` 外部註冊者**連三輪仍為 3**、`audience` **連五輪 0** ⇒ 維持 partial、已有 #71 不重複開卡；② `推薦/聯盟` referral 家族 grep **連五次 0** ⇒ absent 確為現況、已有 #58；③ `新手引導` 08-14 的 partial→present 上修本輪複驗成立（grace 家族 74→76）；④ `兌換碼` 的 `CODES` 仍兩欄＝08-10 刻意只認領一半的缺口連三輪未變（避免與 #71 長成雙胞胎）；⑤ `ledger-card-sweep` 本輪 **0 筆待人工確認**。
    - ⭐ **調研＝找出與 08-15 caliente 同族、但機制不同的第二種漏法**：`bet.br` 月訪約 **62.2M**（量級在 caliente 56.05M 之上），卻**被明文排除在 top-20 榜單外**——因為巴西 2025 合法化後強制持牌業者遷入 `.bet.br` 域，流量統計商尚未合併口徑。⇒ **caliente 是「我們的鏡頭」濾掉了它；bet.br 是「上游來源自己看不見」**。後者用「換一個榜單」修不掉（每個榜都吃同一份被打散的資料），只能靠**事件驅動取材**（監管重組 → 主動去查該市場）補。全球前五現可逐筆對照：stake.com 140.4M✅／stake.us 79.4M✅／crowncoins 72.1M✅**在庫**，bet.br ~62.2M❌／caliente 56.05M❌**被計數上限擋住** ⇒ **crypto/social 垂直覆蓋完整、受監管法幣垂直覆蓋為 0**。依既有指派**未自行加進 `platforms[]`**，入 `_queued_candidates`（現 2 筆）並附負載帳單（兩筆全收 +0.39 次深挖/週＝+3.9%，overdue 仍 **0/33**）供船長裁決。active 仍 **33**·**CONTROL 節流設定一字未改**。
    - ⚠️ **驗證誠實聲明（連續第五輪）**：排程輪起不了 dev server ⇒ **無 preview、無目視**。本輪改的是**玩家看得到的數值列（8 款遊戲的資訊列）**，雖已用真的 `gameInfoBar` 逐字證明字串相同，但那是字串不是像素 ⇒ **待目視清單現為四項**：8 款遊戲資訊列（新·最高優先）／大廳分群軸頁籤列／經濟旋鈕面板（11 張表）／#72 說明中心面板。
    - **下輪首要**：台帳依輪替序＝**`資安`（08-14、5 模組）**；實作候選 **#96**（S–M·`HL.rg` 側的閘與測項可 headless 落地，二次確認流程與入口收起態需 preview）或 **#71**（促銷分群·`release.js` 的 `AUDIENCES` 述詞表是現成零件）；⚠️ **#93 仍不適合 headless 輪**；**#99 屬遊戲軌不要代做**。調研：最近到期 **bet365 08-24**（watch 1/2）；⚠️ **`_queued_candidates` 兩筆待船長裁決**。

<!-- 2026-08-16 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入的最舊一則 -->
<!-- 2026-08-16 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移出的最舊一則 -->
- **2026-08-15（平台軌 · **20:00 窗**建置輪 · 實作 #94 大廳分群軸〔**卡上的鎖被查證為「沒有錨點」，追出前置卡 #98**〕· 台帳審「功能」13 模組 + 隨 #94 回填 `大廳分群軸` · **調研撿到一個全球前五、我們從沒看過的站**）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:00 窗為純 intel/ 退避輪、未 claim）→ claim `p-201015-b8d4`（帶心跳，逐步驟更新）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.2h**（08-15T15:55 → 本輪 20:09）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳最舊＝`功能`／「先汰一筆」建議改判／實作候選 #94 或 #96）⇒ **做而不讓路，三項全數處理**＝跨輪指派**連續第十六輪**。
    - ⭐ **本輪最重要的發現＝一張卡可以寫出一條「鎖不到任何東西」的不變量，而阻塞事實早就躺在台帳裡**。#94 卡的不變量 (d) 要求「大廳的 rtp 值必須與 `games-catalog.json` 的 `gate_log` 對得上」——實作時查證：`gate_log` 是**自由散文**（整段敘述、無結構化欄位），且 `intel/` 不被前端服務 ⇒ **那條鎖沒有錨點**。而該模組 **08-07 的 evidence 早就寫著**「各遊戲的 RTP 只以顯示字串存在＝不可查詢」。⇒ 這是「卡片範圍是上一輪的推論」家族的**第 5 種變形：鎖的錨點不存在**（前四種：範圍被低估／前提被推翻／要求與從未建造的模組整合／宣稱「其餘已被中央機制覆蓋」）。**通則已寫進 SKILL 第 3 步：開卡前掃來源模組 evidence 的「不可／零命中／只以…存在」句式，要嘛寫進依賴、要嘛先開成前置卡。**
    - ⭐ **第二個發現＝那條鎖真正要防的事，答案比卡上寫的更近也更嚴重**。它怕「UI 端自己編一個好看的 RTP」；但 RTP **早就有一份玩家看得到的真相**＝各遊戲 view 的 `gameInfoBar({rtp:"96.27%"})`（**8 處/8 檔**）。在大廳側表再抄一次＝第二份真相，而且是最壞的一種——**遊戲軌改了賠付表只會去改 gameInfoBar，不會想到來改大廳**。⇒ 處置：側表**刻意不收 `rtp`**（反向鎖擋住往後偷加），並把「先讓 gameInfoBar 的 rtp 可列舉」開成 **#98**（同時解鎖四件事：RTP/波動軸、儀表板「slot 無 RTP 模型」健檢改用真值、說明中心自動生成 RTP 一覽、與 #50 `HL.edge` 對帳）。
    - **實作 #94（純前端 · 容器先於內容 · 詳見上方卡片）**：`core/game-axes.js`（容器，零內容）＋ `data/game-traits.js`（節奏軸 + 24 款值）＋ `casino.js` 三個接點。**權威分工就此定案**：`pace` 屬平台軌（互動結構、一行 grep 可複驗）、`volatility`/`rtp` 屬遊戲軌（需數學證明）⇒ 卡上的跨軌依賴解除。⚠️ **判準當場改掉我一個憑印象的分類**：`picks` 原被歸「等待開獎」，讀碼發現它 `betBtn → settle()` **當下就結算** ⇒ 改 instant；`pending` 桶因此 0 款、依規則不渲染＝**「空的不出現」在真實資料上被實證**。node fast **131→134 全綠**、**負向擾動 16/16 全被抓**、**零回歸以 HEAD vs 新版 504 格逐格比對證明**（附不空心證明：同一軸 key 在 HEAD 命中 0 款）。sw v167→v168。
    - ⚠️ **負向擾動第二次證明「只驗綠燈看不出鎖是空的」**：`no-second-rtp` 首版只擋 `rtp:` 物件字面量形，**正向全綠**，但真實會出現的另一種寫法 `put(id, "rtp", 96.5)`（欄位名走**字串引數**）當場穿過去 ⇒ 改成「欄位名以任何形式出現都算」。**與 08-15 08:00 窗 `isLive()[\s)]*\?` 同一條教訓的第二次應驗。**
    - **台帳審「功能」13 模組**（08-13＝全庫最舊）⇒ 回填 08-15、**全庫最舊變 `前端UI/UX`／`資料`／`金流`／`活動`／`資安`（皆 08-14）**，依輪替序下一個是 **`活動`**。① `中央結算掛鉤` 下游訂閱者 **18 → 21**（新增 `rg`/`edge`/`betlog`），⭐ **三者都是後來的卡自己掛上來的、沒有任何一輪需要改 `live-stats.js` 的架構**＝這個中央點的擴充性在多輪實測下成立；② `遊戲引擎與聚合` 四個數字（views 19／fair 23／registry 1／聚合 0）與 08-06 **逐位相同**＝結論未腐爛；③ ⚠️ **量測法陷阱本輪選擇「不寫」**：`guild.js` naive `grep -c "name:"` 得 **9**、舊記載「種子 6 公會」，兩者差距源自 naive 含非公會物件 ⇒ **不憑 naive 值改寫記載**、留待下輪 brace matching 複驗（比照 14:00 窗 CMS `^  var ` 的處置）；④ `任務/成就/簽到` 的稀缺型任務缺口**連三輪未認領**、據實記錄但**不開卡**（避免與 #57 長成同來源雙胞胎）；⑤ 另跑一次全庫檢查「`last_audited` 是否晚於 evidence 內最新日期」＝**0/49**（新鮮度戳沒有跑在量測前面）。
    - ⭐ **調研＝重新取材撿到一個全球前五、本庫從未出現過的站（sourcing 盲點自我揭露）**：兩來源交叉（Exploding Topics 全球 gambling 榜 + Semrush 全球/墨西哥榜）⇒ **caliente.mx 2026-07 月訪 56.05M、全球第 3–5**，**墨西哥市場領先次名約 7 倍**（41.07M vs playdoit 5.71M）。本庫調研 53+ 平台、追蹤 33 筆（22 筆是 tier-3 小站）卻**從未出現一個全球前五大**——成因可機械說明：`relevance_lens` 寫「對標 Stake 類 crypto/social casino」，歷輪取材全走 crypto/social 榜，而 caliente 是**受監管的 LatAm 法幣 sportsbook-casino**，在那些榜上永遠不會出現。⇒ **與 08-13 撿到 rainbet 是同一種漏法的另一個方向**（那次漏 crypto 榜第一名，這次漏整個垂直）。
    - ⚠️ **它沒有被加進 platforms[]，而是進了新的 `_queued_candidates` 區**：連續多輪的跨輪指派是「再取材前先汰一筆」，而 14:00 窗已證**單輪之內結構上不可能達成** ⇒ 直接加進去等於無聲違反指派、丟掉又等於無聲遺漏。**處置＝把計數上限的實際代價變成可稽核的證據**：記下它、標明「僅因計數上限而被擋」、附上接受它的負載成本（45 天週期＝ **+0.16 次深挖/週**，對現況 10.05 次/週的衝擊 **<2%**，而 ACTIVE overdue 連續多輪 **0/33**）。**這是 14:00 窗那條建議（把『active ≤ 約 32』改為以逾期率+負載為準）從抽象主張變成具體帳單**，供船長裁決。active 仍 **33**、overdue **0/33**、CONTROL 節流設定一字未改。
    - ⚠️ **驗證誠實聲明（連續第四輪，且範圍再擴大）**：排程輪起不了 dev server（工具明確回「unattended session 不能開 dev server」）⇒ **無 preview、無目視**。本輪**在大廳頁籤列新增了 2 個頁籤**，其 ≤720px 換行/橫捲表現未經任何人眼或真實渲染引擎確認 ⇒ **待目視清單現為三項**：大廳分群軸頁籤列（新）、經濟旋鈕面板（11 張表）、#72 說明中心面板，桌機與 ≤720px 兩態。首屏 **1254KB／85 支**（門檻 1600KB/120，harness 常駐鎖已綠）。
    - **下輪首要**：台帳依輪替序＝**`活動`（08-14、6 模組）**；實作候選 **#98**（S–M·headless 可落地·且它是 #94 剩兩條軸的前置）或 **#96**（S–M·二次確認流程與入口收起態需 preview，headless 輪只能先做 `HL.rg` 側的閘與測項）；⚠️ **#93 仍不適合 headless 輪**。調研：最近到期 **bet365 08-24**（watch 1/2）；⚠️ **`_queued_candidates` 待船長裁決**，未裁決前不自行加進 platforms[]。⚠️ **下一個可靠 preview 之輪請優先目視上述三項**。
- **2026-08-15（平台軌 · **14:00 窗**建置輪 · 實作 #97 經濟旋鈕殘缺面〔**規則自己多掃出第 3 張表**〕· 台帳審「後台」7 模組〔**兩筆 sweep 告警回填、確認非誤報**〕· stake 深挖增量歸零起算飽和 · **查出「先汰一筆」為何連三輪做不到**）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 10:1x 已釋放）→ claim `p-141200-a3f7`（帶心跳 14:12→15:35，逐步驟更新）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.5h**（08-15T09:40 → 本輪 14:09）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派三項**（台帳最舊＝`後台`／再取材前先汰一筆／實作候選 #97 或 #94 或 #96）⇒ **做而不讓路，三項全數處理**＝跨輪指派**連續第十五輪**。
    - ⭐ **本輪最重要的發現＝「先汰一筆」連三輪做不到，不是執行不力，是那條指派把跨週期的判定寫成了單輪的動作**。前兩輪（08-14 20:00、08-15 08:00）都據實寫下「未達成」，本輪不再寫第四次，改**機械列出每一筆 active 的退場資格**：`active 33`、目標 ≤32，而**唯一達到 watch 2/2 門檻的就是 bc-game**——正是今晨 08:00 窗**依 tier-1 保守紀律刻意不汰**的那一筆；其餘已起算 watch 的 4 筆（bet365／roobet／1xbet／cybet）**第 2 輪零增量最早也要等 9～29 天後的 `next_due`**，剩下 28 筆連第 1 輪都還沒起算。⇒ 退場門檻是「連續 2 個到期週期零增量」、而週期是 14–30 天 ⇒ **任一筆從起算到可汰除，最短就要跨越一個完整週期，單輪之內結構上不可能達成。**
    - ⭐ **而且那個 `≤32` 的計數目標，量的是錯的東西**：它真正想防的是「調研負載超過供給」，但該不該擔心有現成指標——**ACTIVE overdue 已連續多輪為 0/33**（本輪亦然）。改量負載本身：Σ(1/週期) ＝ **10.70 次深挖／週**（tier1 2.33／tier2 3.47／tier3 4.90），而平台軌實際每輪深挖 1 筆、每日 1–3 輪 ＝ 7–21 次／週 ⇒ **供給是夠的，逾期率 0 就是證據**。⇒ **`saturated` 與「延長週期」其實是同一件事的兩種強度**：本輪把 stake 由 7→21 天，負載當場由 10.70 降到 **10.03 次／週**——**釋出的配額與汰除一筆相同，卻不必刪掉旗艦的稽核價值**。⇒ 建議船長把「active ≤ 約 32」改為**以逾期率與負載為準**（全域旋鈕＝船長裁決，比照 P6 先例，本輪一字未改 CONTROL 節流設定）。
    - **實作 #97（純前端 · 一個經濟數字都沒改 · 詳見上方卡片）**：兩個「不可描述形制」收斂為兩站別表、新增 3 張註冊表（**8→11**）、`STATIC_RISKS` 改函式陣列當場求值。⭐ 卡上只寫 2 個殘缺面，**宣告性質的規則自己多掃出 `progress.js` 舊制流水倍數與 `jackpot.js` 整張 TIERS**（§11 印鈔黑洞修補處、兩站別值早已並存卻沒有出口）＝08-15 08:00 通則第二次成立。⭐ **規則的鑑別力兩個方向都證了**：窄了會漏（`faucet.js` 的站別檢查藏在本地別名 `liveOn()` 後）、寬了會誤抓（naive 版會把 `raffle.js` 假券氛圍量當旋鈕）。⭐ **查獲一筆「值沒漂、義已遷」**：「返水 0.1–0.3%」是改制前口徑，#60 自 08-01 起已換機制，只因換算後仍落同一帶 ⇒ **逐項比對數字永遠看不出來**。node fast **130→131 全綠**、**負向擾動 11/11 全被抓**、**不變量 (a) 以 `git show HEAD:` 證明規則在 HEAD 為紅**、**零經濟回歸以兩站別 × 10 個觀測值逐位相同證明**。sw v166→v167。
    - **台帳審「後台」7 模組**（08-12＝全庫最舊）⇒ 回填 08-15、**全庫最舊變 `功能`（08-13、13 模組）**。① **SKILL 第 2 步的 sweep 工具兩筆 ⚠️ 告警逐筆讀 evidence 確認為真、非誤報**（`營運後台儀表板`／`經濟/RTP` 皆引用 #90，而 #90 今晨已落地）⇒ 回填後 **待人工確認 0 筆**；兩者維持 `partial` 但理由已改變——儀表板的「一個具名旋鈕表都沒讀」已由 `HL.econCfg` **8 次命中**取代，維持 partial 是因為**唯讀性未腐爛**（`onClick/input/select` 仍恰 2 處＝仍是報表不是後台）。② ⚠️ **量測法陷阱再一例**：CMS 的 mock-data 常數，naive `grep -c '^\s*var '` 得 **55**（含函式內區域變數），權威口徑＝模組級縮排 `^  var ` 得 **26**＝與 08-06／08-12 **連三輪逐位相同**（差 2 倍以上，用 naive 值會誤判為「腐化中」）。③ `活動/紅利設定` 容器採用度 **3→4**（新增 `responsible.js`），但 `audience` 連四輪 0、`source:` 自由字串仍 22 個 ⇒ 維持 partial、**仍不另開卡**（與 #88 同源，避免長出 #95/#72 那種雙胞胎）。④ `金流模式` 的 `jurisdiction` 外部讀者**仍 0**＝「旋鈕在、線沒接」第三種變形持續成立。
    - **調研＝stake 深挖（提前 2 天做掉 08-17 票，同今晨對 bc-game 的先例）⇒ 增量歸零、首次正式起算 `saturation_watch` 1/2**：兩路 WebSearch 交叉（VIP/獎勵路線 + 平台功能/忠誠度路線），VIP 15 階 Bronze $10k→**Obsidian 頂階**、升階即發獎、週六 12:30 GMT 固定週獎金、Daily Race $100k／Weekly Raffle $75k／遊戲別錦標賽／solo challenges ＝**與 08-04／08-10 兩輪記載逐項相同、零變化**且 ApexWin 皆已覆蓋；Stake.us 四週年（08-02）＝**里程碑公關稿非機制**；2026-01 新 Original **Drill** 已由遊戲軌 08-11 判 novelty LOW ⇒ 平台軌不重複開卡。**淨新缺口 0**、比照 tier-1 保守紀律**只延週期不汰除**（`refresh_interval_days` 7→21）。ACTIVE overdue **0/33**。
    - ⚠️ **未驗證面（誠實聲明·連續第三輪同一項且範圍在擴大）**：排程輪起不了 dev server ⇒ 無 preview、無目視。「🎛️ 經濟旋鈕」面板本輪由 **8 張表變 11 張**，其 ≤720px 是否橫捲**仍未經任何人眼或真實渲染引擎確認**；與 08-14 的 **#72 說明中心面板**並列待目視。本輪風險文案已由 shim **走玩家實際路徑**（攔截 `HL.ui.rules()` 實際收到的陣列）驗出 6 條正確求值，但那是結構與行為、**不是像素**。首屏 **1494KB／84 支**（餘裕 106KB·本輪 +9KB 無新 script）。
    - **下輪首要**：台帳最舊＝**`功能`（08-13、13 模組）**；**「先汰一筆」建議改判**（見上，結構上單輪不可能；真正可做的是**續推 watch 到期的那幾筆**——最近為 **bet365 08-24**）；實作候選 **#94**（M·headless 可落地）或 **#96**（S–M，二次確認流程與入口收起態需 preview，headless 輪只能先做 `HL.rg` 側的閘與測項）；⚠️ **#93 仍不適合 headless 輪**。⚠️ **下一個可靠 preview 之輪請優先目視三項**：#90/#97 的經濟旋鈕面板（11 張表）、#72 說明中心面板、兩者的 ≤720px 態。

- **2026-08-15（平台軌 · **08:00 窗**建置輪 · 實作 #90 經濟旋鈕自我描述層〔**範圍由 5 張擴為 8 張，並當場掃出第 9 張**〕· 台帳審「擴充性 + 資安」10 模組 · bc-game 深挖達飽和 2/2 · 開卡 #97）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 22:2x 已釋放）→ claim `p-081540-c7b2`（帶心跳）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **9.6h**（08-14T22:35 → 本輪 08:09）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳最舊＝`擴充性`/`資安`／再取材前先汰一筆／實作候選 #90 或 #94 或 #96）⇒ **做而不讓路，三項全數處理**＝跨輪指派**連續第十四輪**。
    - ⭐ **本輪最重要的發現＝一條反向鎖抓到的東西，比它被寫出來時想抓的東西更多**。#90 的卡上說「五張 config 表」，我沒有照抄，而是把它變成一條**機械判準**（凡 `src/core/*.js` 出現站別分歧的經濟常數就必須註冊自我描述）⇒ 判準立刻找出卡上沒有的第 6、7、8 張（`safetynet`／`rewards` 的 `GRACE_SPECS`／`cashback`），**接著在負向擾動階段又找出第 9 張**：`core/progress.js:318` 的 `RB_LEGACY` 是與 `rakeback-core.js` `LEGACY_RATES` **逐位相同的第二份返水率真相**——而那個檔案的上一行才剛寫「純資料在 rakeback-core.js，本檔只負責取用+記桶+UI」。**宣告性質的鎖會替你找出你還不知道的實例；列白名單的鎖只會確認你已經知道的事。**
    - ⭐ **而第 9 張之所以被找到，是因為負向擾動抓到我自己的鎖是空心的**。禁止「載入期三元式」那條鎖，首版正則寫成 `isLive\(\)\s*\?\s*\[`；但真實世界的寫法是 `(HL.site && HL.site.isLive()) ? [...]`——`isLive()` 與 `?` 之間隔著 `&&` 群組的**右括號** ⇒ **這條鎖對它唯一要禁的那個形制完全無效**，而它在正向測試裡是綠的。修成 `isLive\(\)[\s)]*\?\s*\[` 之後，`progress.js` 當場落網。⇒ **通則（08-14 #72「負向擾動也要驗鎖本身」的第二次、更銳利的一次）：擾動時必須用「真實世界會出現的那個形狀」去擾動，而不是用你腦中的理想形狀——否則你驗的是鎖對想像中的敵人有效。**
    - **實作 #90（容器先於內容 · 一個經濟數字都沒改）**：新檔 `core/econ-config.js`＝`HL.econCfg` 登記表；八張表各自註冊 `describe()`（值一律當場求值、不手抄）；儀表板新增「🎛️ 經濟旋鈕（唯讀）」**遍歷** `all()`＋把 `audit()` 推導的「真站不得比假站寬鬆」併入既有健檢。⭐ **健檢方向不另外手寫**：`service-level` 的 `strict` 直接由既有的 `better:"higher"|"lower"` 欄位推導 ⇒ 方向只有一份真相。`progress.js` 的第二份真相已收斂為從單一來源取值，**零回歸以兩站別 × 5 段位共 10 個取值逐位比對證明**。node fast **125→130 全綠**、**負向擾動 14/14 全被抓**。sw v165→v166。
    - **台帳審「擴充性 + 資安」10 模組**（皆 08-11 最舊）⇒ 回填 08-15、**全庫最舊變 `後台`（08-12）**。三筆值得記的：① ⭐ **量測法陷阱在 Dockable Layout 上連三輪各踩一種**——本輪首版改用「帶 `HL.dock` 前綴掃 `.register(`」只得 2，因為**鏈式寫法的第二筆前面沒有前綴**（`HL.dock\n.register({…}).register({…})`）＝**前綴式會低報**；08-11 訂的權威法（去註解後掃 `.register({`）＝3 且成員正確，經本輪反向檢驗仍成立。② ⚠️ **「grep 命中 ≠ 功能出現」第 2 例**：`KYC` 命中由 0 變 1，但那一筆在 `ops-dashboard.js` 且**內容正是在說「上線前建議綁 KYC 提款閘」的風險提醒**＝仍是 absent 的證據（同 08-14「提款審核佇列 3 筆全是註解」）。③ **`isLive()` 閘 46→52 處**＝08-05 立的回歸基準連二輪為正訊號、無漏閘。④ 首屏 **83 支/1459KB**（餘裕 141KB），以近日 +25KB/輪估算**約 5–6 個建置輪即觸警**，`core/i18n.js` 由 151KB 長到 **157KB**。
    - **調研＝bc-game 深挖（提前 2 天做掉 08-17 到期票）⇒ 增量歸零第 2 筆、`saturation_watch` 1/2 → 2/2**：五個當期獎勵軸（週/月 cashback／每日轉盤 VIP 縮放／crash 錦標賽／BC Engine XP + 72h 安全網／體育書加成）**逐一皆已覆蓋或屬另一垂直**；08-04 記的唯一開放缺口「支援/透明度中心」**已由 08-14 落地的 #72 關閉**。**處置刻意不採 `status=saturated`**（依該筆 08-10 自訂的 tier-1 保守紀律：旗艦改版頻率高且是多數 alternatives 榜的基準點）⇒ 改為 `refresh_interval_days` **7 → 21** 釋出配額。⚠️ **本輪因此仍未把 active 由 33 降到 ≤32**（bc-game 這條路徑結構上做不到）＝**據實記錄未達成，不假裝達成**；ACTIVE overdue **0/33**。
    - ⚠️ **本輪最大的未驗證面**：排程輪起不了 dev server ⇒ **新增的「🎛️ 經濟旋鈕」區塊沒有任何人眼或真實渲染引擎看過**（八張表 × 3–7 列的表格在 ≤720px 是否橫捲未知）。與 08-14 留下的 **#72 說明中心面板同樣待目視** ⇒ **下一個可靠 preview 之輪請一併處理這兩個**。
    - **開卡 #97**（#90 落地當輪自我檢出的兩個殘缺面：反向鎖抓不到「純量型」旋鈕〔實測 `faucet.js` `LIVE_CAP=5`、VIP 升級金 `×0.4`〕＋儀表板 `STATIC_RISKS` 手抄 5 組數字〔本輪比對確認**尚未漂移**，屬防漂移〕）。

<!-- 2026-08-15 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入的 1 則 -->

- **2026-08-14（平台軌 · **20:00 窗**建置輪 · 實作 #72 說明中心〔**查獲它與 #95 是同一張卡並就地合併**〕· 台帳審「金流 + 活動」11 模組 · **台帳自我查核查獲「我們自己關掉的缺口沒回填」並做成常駐工具** · cybet 起算飽和觀察 · 開卡 #96）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:5x 已釋放）→ claim `p-201530-a7c4`（帶心跳 20:15→22:20，逐步驟更新）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.5h**（08-14T15:40 → 本輪 20:09）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳最舊＝`金流`/`活動`／再取材前先汰一筆／實作候選 #95 或 #90 或 #94）⇒ **做而不讓路，三項全數命中**＝跨輪指派**連續第十三輪**乾淨運作。
    - ⭐ **本輪最重要的發現＝台帳有一種失真是「我們自己造成的」，而且現有的新鮮度指標對它完全免疫**。台帳寫下「模組 X 的缺口是 A ⇒ 開卡 #N」之後，**那筆敘述的有效期就等於 #N 的落地時刻**，不是 `stale_days`；#N 落地卻沒回填，台帳就開始**高報一個自己已經關閉的缺口**。實測全庫 11 個「開卡 #N」引用中**兩筆正處於這個狀態**，且兩筆的卡落地日都在該模組 `last_audited` 的**當天或隔天**（＝審完隔天就失真，而 `last_audited` 仍顯示「很新」）：
        - `活動/新手引導`：08-10 記「零容錯、`grep grace|寬限|freeze|凍結|protect|補簽|保護` **0 命中**⇒ 開卡 #84」，而 **#84 於 08-11 落地**；本輪重跑同一條 grep 得 **74 命中**（`GRACE_SPECS`／`GRACE_GAP`／`nextStreakOf` 俱在）⇒ 該缺口早已關閉，**partial 上修 present**。
        - `資安/負責任博弈`：08-11 記「slot 押注吃限額卻永遠不被擋 ⇒ 開卡 #86」，而 **#86 於同日 14:00 窗落地**；本輪實測 `HL.rg.(allow|check|gate|canBet)` **27 處／20 檔**含四款買入型 slot ⇒ 不對稱已關閉（維持 partial，但理由**收斂為單一項＝自我排除**，並就此開卡 **#96**）。
        - ⇒ **做成可重現工具 `intel/tools/ledger-card-sweep.js`**（掃全庫 evidence 的「開卡 #N」× BACKLOG 卡狀態），建議平台軌 SKILL 第 2 步開頭先跑。⚠️ **工具自身的誤報也據實寫進檔頭**：首跑 3 筆告警中 1 筆（`功能/可驗證公平` × #92）其實 14:00 窗已回填、只是措辭沒被關鍵詞命中 ⇒ 定位為「**收窄檢查範圍**」而非自動改寫依據。
    - ⭐ **第二個發現＝我們開了一張重複的卡，而且是連續兩輪審同一個模組長出來的雙胞胎**：**#95（08-14 14:00 窗開）與 #72（08-06 開）來源同一個模組、出口形狀同一種、首批註冊者同一批**＝實質重複。SKILL 第 3 步明訂「去重：已在佇列的剔除」，14:00 窗**只比對了卡名、沒比對來源模組與出口形狀**。⇒ **通則：開卡去重要比對「來源模組 + 出口形狀」，同一模組被連續幾輪審到時最容易長雙胞胎。** 處置＝**以較早的 #72 為主體落地、#95 標 ♻️併入**，並吸收 #95 兩個較好的設計（`when()` 情境述詞、「不新增第 N 顆常駐鈕」的入口紀律）。**順帶查獲 #95 一條不可能滿足的前提**：它要求「本卡應是 #61 `HL.content` 的 type 消費者」，但 **#61 從未落地**（`HL.content` 全 repo 0 命中）⇒「卡片範圍是上一輪的推論」家族的**第三種變形：要求與一個從未被建造的模組整合**。
    - **實作 #72（本輪唯一實作卡）＝說明中心 `HL.support`**（新檔 `core/support.js`／`fair.js`／`edge.js`／`responsible.js`／`service-level.js`／`progress.js`／`app-shell.js`／`ai-concierge.js`／`i18n.js`／`components.css`／`index.html`／`sw.js`／`tests/checks-platform.js`）。
        - **容器先於內容、一條規則的「內容真相」都沒新增**：五個規則擁有者**各自註冊自己那條說明**，`body` **一律是函式、開面板當下才求值** ⇒ 讀的是各模組活值（承諾雜湊／逐款莊優區間／分階額度／限額型別數／流水倍數與可用範圍數），**不手抄任何一個數字**。這正是根治本卡 :457 所記那種漂移（罐頭答案指向不存在的 UI）的方式。
        - **入口＝把死巷改成真入口，且一顆常駐鈕都沒新增**：側欄「更多」原是 `soon:"更多"` → `ui.comingSoon()` 死巷，現指向說明中心；`SIDE` 維持 **5 筆**，側欄與抽屜仍讀同一份 `SIDE`（#93 的單一真相不被破壞）。`ai-concierge` 改為 **KB 未命中才問 `HL.support`**（既有罐頭答案逐字零回歸）。
    - ⭐ **驗證過程本身抓到「我自己的鎖是空心的」兩條，這是本輪第三個值得記的事**：node fast **121→125 全綠**後跑負向擾動，第一輪只有 **8/10** 被抓——(a)「edge.js 不再註冊」用 `if (false && HL.support …)` 就能繞過，因為原鎖只驗「文字出現過」＝**又一次「出口 vs 提及」**，改為比對**規範形狀** `if (HL.support && HL.support.register) {`；(b)「抽屜漏處理 `it.open`」沒被抓，因為原鎖**全檔數 `it.open()` 筆數**，而福利中心 hub 另有一個同名區域變數 `it` 也呼叫 `it.open()` ⇒ 全檔得 3、拿掉抽屜那個仍有 2 而照樣綠＝**naive 口徑陷阱**（本專案第 N 次），改為**逐表面各自檢查自己的 render 區塊**。修正後 **10/10 全被抓**。⇒ **通則：負向擾動不只驗程式，也驗「鎖本身」；只驗綠燈永遠看不出鎖是空的。**
        - 另以 **shim 載入「真的那份 `support.js`」跑瀏覽器區 13/13**——node 端 `module.exports` 會 early-return，`open()`/`renderInto()`/`rowOf()` 這條**玩家真正看到的路徑在 node 測項裡一行都沒被執行過**；shim 驗了空狀態、`when()=false` 不進 DOM、整群被藏時分群標題不渲染、函式 body 渲染活值、action 鈕真的呼叫擁有者 `run()`、搜尋只留命中者。新增 10 個 class **全部有 CSS**、CSS 用的 15 個 token **全部存在**（防死 class／死 token）。
    - **台帳審「金流 + 活動」11 模組（皆 08-10＝當時全庫最舊）⇒ 兩分類回填至 08-14、全庫最舊變為 `擴充性`/`資安` 08-11**。11 筆中 **10 筆據實記「無變化」**（不為了有事做而改動）：`支付/收銀台` psp/對帳仍 0 命中且 **#82 支付通道註冊表仍未落地**、`促銷框架` 的 `promoCal.register` 外部註冊者**仍 3**（去註解權威口徑）且 `audience` 仍 0、`推薦/聯盟` 連四次審計皆 0 命中、`redeem.CODES` 仍兩欄。**唯一據實新增的一筆＝`獎金/流水引擎` 漏記了 #89 `HL.wagerScope`**（08-13 落地，本模組前四次審計都沒提到）⇒ 流水條款其實已有**兩個維度**（倍數依段位 #74／範圍逐筆 #89），缺的只剩「倍數的逐筆覆寫」。
    - ⚠️ **另更正一個會誤導後續 grep 的訊號**：`提款審核佇列` 的關鍵詞命中數由 0 變 **3**，但三筆**全是 `service-level.js:35-38` 的註解，且內容正是在說明本模組維持 absent**。⇒ **grep 命中數上升 ≠ 功能出現；這次上升的來源是「對缺席的說明文字」**——用關鍵詞計數判 status 時必須排除註解，否則「把缺口寫清楚」這件好事會被讀成「缺口被補上了」。
    - **調研＝cybet 起算飽和觀察（`saturation_watch` 0 → 1/2）**：多源評測交叉後**對 ApexWin 增量歸零**——20% rakeback／`.b` 獎勵幣（每注依段位累積、過門檻可轉可提）皆與 `HL.rakeback` 同構；3 段 VIP 遠不如 `HL.vip`(5 階)+#63 `HL.sla` 細；週/月/生日紅利已由 #24 檔期軸覆蓋。**唯一新鮮的「VIP transfer（憑他站 VIP 身分跳級）」本質是跨營運商身分證明**，純前端單機做只能做成「自己宣告自己是鑽石」＝假機制，且近 CONTROL.avoid ⇒ **判不可移植、不開卡**。⚠️ **本輪並未真的汰掉一筆**（active 仍 **33** > 「≤約 32」）——只是**把退場時鐘起算**（需 2026-09-13 第 2 輪零增量才可改 saturated）；也**刻意不新取材**（淨增 0）。ACTIVE overdue **0/33**，下一張到期票＝08-17 stake/bc-game 兩張 tier-1。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON／測項一律 Write 暫存檔再 `node 檔.js` + `timeout`，commit 訊息用多個 `-m`；分段提交（實作一筆、調研+台帳+開卡一筆）；只逐檔 add 自己的檔。⚠️ **自我糾正一筆**：首次追加測項時把測項寫進 JS **template literal** 再輸出，`\s`/`\.` 等跳脫**被 template literal 吃掉**（`\s*` 變成 `s*`）導致 harness 當場 SyntaxError ⇒ 已 `git checkout` 還原並改為 **Write 成純文字檔再由 node 讀檔追加**。這與 08-08 那輪「`cat >> file << 'EOF'` 被跳脫吃掉 `\\`」是**同一個病的不同載體**：⇒ **凡是要寫「含正則的程式碼」，一律走 Write 工具寫實體檔，不要讓它經過任何一層字串跳脫。**
    - **下輪首要**：台帳最舊分類＝**`擴充性`／`資安`（皆 08-11，各 5 模組）**；**再取材前仍須先汰一筆**（active 33 > ≤32；最快的一張是 **08-17 到期的 stake／bc-game 兩張 tier-1 深挖**，bc-game 的 `saturation_watch` 1/2 恰好也排在 08-17 複驗＝**深挖與飽和判定可同一輪完成**）；實作候選 **#90**（S–M·後台投報比最高·headless 可落地）或 **#94**（M）或 **#96**（S–M，但**二次確認流程與入口收起態需 preview 目視**，headless 輪只能先做 `HL.rg` 側的閘與測項）；⚠️ **#93 仍不適合 headless 輪**（三導覽表面需 preview）。⚠️ **本輪最大的未驗證面＝#72 說明中心面板從未被人眼或真實渲染引擎看過**（shim 驗結構與行為、不驗像素）⇒ **下一個可靠 preview 之輪請優先目視此面板**（桌機 + ≤720px 兩態）。⚠️ `consecutive_idle_rounds` **刻意維持 3 不歸零**：那個 3 是維護軌 08-14 00:00 headless 飽和輪寫的**自身**退避門檻、本欄三軌共用；平台軌有工作不能證明維護軌的債佇列不是乾的 ⇒ 歸零＝用 A 軌產出抹掉 B 軌的升級狀態（同 08-12/08-13/08-14 各輪處置）。

<!-- 2026-08-15 平台軌 14:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入者 -->
- **2026-08-14（平台軌 · **14:00 窗**建置輪 · 實作 #92〔範圍實測後由 1 個消費端擴為 2 個〕· 台帳審「功能」12 模組 + 補完 08-07 留 stale 的 2 筆 · 汰除 courtside · 開卡 #95）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 10:4x 已釋放）→ claim `p-141520-b3e9`（帶心跳 14:15→15:05）→ 停頓做源碼量測與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **4.6h**（08-14T09:35 → 本輪 14:09）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派四項**（台帳最舊＝`功能`／補完 `大廳/遊戲牆`+`i18n` 且須用權威量測法／再取材前先汰一筆／實作候選 #92）⇒ **做而不讓路，四項全數命中**＝跨輪指派**連續第十二輪**乾淨運作。
    - **實作 #92（本輪唯一實作卡）＝主播跟注的「真桌結果」單一出口 `HL.liveTable`**（新檔 `core/live-table.js`／`views/liveroom.js`／`layout/streamer.js`／`i18n.js`／`index.html`／`sw.js`／`tests/checks-platform.js`）。
        - ⭐ **本輪最重要的發現＝這張卡的嚴重度被低估了一個量級，而且是我們自己上一張卡造成的**：`git show` 逐版比對證明，**#80 內建遊戲延遲載入（08-07 落地）把 `table-baccarat.js` 移出了 `index.html`**（#80 前命中 1 次、HEAD **0 次**），而 `liveroom.js`／`streamer.js` 仍是靜態載入 ⇒ **玩家只要沒開過百家樂，`HL.baccarat` 就不存在**，於是那段「取不到真桌就 `Math.random()` 翻硬幣」的後備分支**從近乎死碼變成了預設結算路徑**——而該路徑真扣真派。⇒ **通則：延遲載入會把休眠的「防呆分支」升級成「預設分支」**；日後改任何模組的載入方式，都必須重掃所有 `HL.<模組> &&` 形式的守衛。
        - ⭐ **範圍實測後由 1 個消費端擴為 2 個**（#86「卡片範圍是上一輪的推論」家族第 2 次兌現）：卡上只列 `views/liveroom.js:140`，`grep HL.baccarat` 查出 `layout/streamer.js:156`（子母畫面 PiP 跟注）有**逐字同型**的分支且同樣真扣真派。**只修一個的話，反向鎖就得被改鬆才能通過**＝為了讓測項變綠而弱化測項，故兩個一起修並抽出共用出口。
        - **修法選了卡上第二條路（退回未結算跟注）而非第一條（改走 `HL.fair`）**：50/50 硬幣與百家樂真實勝率／莊 1.95× 賠付本來就對不上，改走 fair 只會讓它**「可驗算地錯」**；取不到真桌＝錯誤狀態，正解是比照 `liveroom.js:134`（離開直播間退回跟注）與 `streamer.js` `cancelFollow(true)` 兩處**既有形制**。卡上的禁區也守住：**未動**假聊天的 `Math.random`（動它會吃掉 `HL.fair` 的 nonce 序列，使 `betlog` 的 `nonce_end` 對不回該局）。
        - ⭐ **自癒設計是被負向擾動逼出來的**：第一版把「請載入器拉 baccarat」放在入口與失敗分支，擾動測試顯示「拿掉入口那句」**不會被任何鎖抓到**。與其硬加一條脆弱的原始碼形狀鎖，改為讓 `result()` 取不到時自己請一次（冪等）⇒ 消費端漏喊 `ensure()` 時**最壞只是第一局不結算、不會永遠不結算**，並順手刪掉失敗分支裡重複的請求。**擾動先抓到的是「鎖太弱」，但真正該修的是「設計太脆」**。
        - **驗證**：node fast **118→120 全綠**；**負向擾動 7/7 皆被抓**（winner 改回硬幣翻轉／繞過出口直接碰 `HL.baccarat`／`result()` 編造勝負／拿掉自癒／`ensure()` 失去冪等／`index.html` 漏掛／入口漏喊 `ensure()`）。反向鎖含**不空心證明**（假聊天的 `Math.random` 必須還在）與**樣本量下限 2**。i18n 補 EN/zh-Hans 各 2 鍵（P3 紀律，已先 grep 確認無撞既有語意）。sw v162→v163。⚠️ **驗證誠實聲明**：排程輪起不了 dev server ⇒ **無瀏覽器 e2e**；且入口預拉之後 `available()` 幾乎恆為 true ⇒ 新文案在正常流程下**不會出現＝視覺零變更**，該分支目視留待可靠 preview 之輪。
    - **台帳審「功能」12 模組（08-06＝當時全庫最舊）＋ 補完 08-07 刻意留 stale 的 2 筆**。三筆值得後續 session 知道：
        - ⭐ **「台帳與現況不符」家族出現反方向首例**：先前四例都是台帳**高報**（把設計意圖寫成現況），本輪 `支援與透明度中心` 是**低報**——08-06 記「完全空白、僅 1 處註解命中」，但 `core/service-level.js`（#63，**08-05 落地、早於那次複審**）本就有 `support-level` 客服層級維度。把模組拆成兩半分別量測後：**支援入口＝真的零命中**（排除 i18n 字典與註解），**透明度＝已有 `HL.sla`／`HL.release`／`HL.responsible` 三個真出口** ⇒ `weak` **上修為 `partial`**，並就真空那一半開卡 **#95**。**⇒ 複審要同時查兩個方向，不能只防高報。**
        - ⭐ **08-07 自己立的口徑被本輪推翻（權威量測的價值）**：`HL.games.register` 舊記「49 處／24 檔」是**含註解與文件字串的 naive 口徑**；去註解且要求帶括號後，**權威呼叫點僅 23 處／20 檔**（naive 全站提及 **54**）＝逾半數命中是註解。同型於 08-11 的 `dock.register` 鏈式寫法陷阱，也同型於「出口 vs 提及」判別法——本輪第 4 次用到它（另一次：`名額|先搶先贏|限量` 命中 5 處，逐筆檢視全是既有抽獎文案，**#57 稀缺性缺口不變**）。
        - **i18n 以 vm 實跑 `dict()` 取權威值**：EN **1099** 鍵／zh-Hans **926** 鍵（08-07 為 1051／910）。逐檔覆蓋率（與 08-04/08-07 **完全同法故可比**）：`promo-cal.js` **30%→58%**＝08-07 點名的全站最低者已被維護軌 U33 補起來，`dock-growth.js` 維持 94%、本輪 #91 的 `reload.js` 86% ⇒ 兩條路徑都仍生效。**最低覆蓋群已換人**：現為 `ledger.js` 0%／`score-axis.js` 6%／`wager-scope.js` 6%／`ops-dashboard.js` 9%，但前三為機制層字面量、第四為**營運者面向**的 ⚙ DEMO 儀表板 ⇒ **據實記錄但刻意不開卡**（不把「數字難看」當成工作）。另更正一筆會誤導後續 grep 的舊記載：三級彩池代號實為 **`mega`/`major`/`mini`**，非舊述的 minor/major/grand。
    - **調研＝汰除一筆（本輪刻意不新取材）**：`courtside` 完成 `saturation_watch` **1/2 → 2/2** → `status` **saturated**（30 天輪替 → 180 天，`next_due` 2027-02-10）。證據：sweepskings 逐項表 + deadspin 交叉，**九項留存機制一致 N/A**（VIP/登入連續/任務/排行榜/contests/商城/JP/返水/轉盤）＝**ApexWin 九項全有**；仍在營運的三項促銷逐項不可移植（餘額歸零補幣＝#39 已有；推薦禮**需 SSN**＝撞 avoid 且 #58 已開卡；**Card Rips 實體卡包**需真實履約，唯一可移植的「開包揭曉」已由 #38＋#17 覆蓋、「機率型兌換」已記在 `deal-or-no-deal-win` 的 saturated_reason）。⚠️ 順帶**降級一筆舊斷言**：本檔 07 月記該站「改用排行榜 + 輪替 contests 維持黏著」，但本輪逐項表把兩者也列 N/A ⇒ 判為評測語氣的推論、不再作對照素材（同 08-14 rainbet「二手記載互相矛盾時只抄結構」）。⇒ **active 34 → 33**，仍高於 SKILL 第 1 步的「≤ 約 32」，故**淨減一筆而非汰一補一**；ACTIVE overdue **0/33**，最近到期 stake／bc-game 08-17。
    - **寫檔紀律**：複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；**分段提交**（實作一筆、調研+台帳+開卡一筆）；只逐檔 add 自己的檔。⚠️ **未碰** `prototype/games/registry.json`（mtime 08-03）與 `prototype/games/slot-engine/`＝他人未提交工作，依 §7 原樣不動（本輪台帳量測放置區筆數時，**刻意改取 `git show HEAD:` 的值而非工作區**，即為防 08-13 那次「差點把別人未提交的工作寫成台帳事實」）。⚠️ **自我糾正一筆**：本輪中途誤下了一次 `cat >> /dev/null << 'X'` 的空指令＝**§6 踩雷 #5 stdin heredoc 家族**（本次未掛死，但該規則的價值正在於不賭運氣），其餘全程 Write/Edit。
    - **下輪首要**：台帳最舊分類＝**`金流`／`活動`（皆 08-10，各 5/6 模組）**；**再取材前仍須先汰一筆**（active 33 > ≤32；現成觀察對象＝`saturation_watch` 尚未起算的 tier-3，或 08-17 到期的 stake/bc-game 兩張 tier-1 深挖）；實作候選 **#95**（S–M·headless 可落地·容器型）或 **#90**（S–M）或 **#94**；⚠️ **#93 仍不適合 headless 排程輪**（三個導覽表面需 preview 逐一目視）。⚠️ `consecutive_idle_rounds` **刻意維持 3 不歸零**：那個 3 是維護軌 08-14 00:00 headless 飽和輪寫的**自身**退避門檻、本欄三軌共用；平台軌有工作不能證明維護軌的債佇列不是乾的 ⇒ 歸零＝用 A 軌產出抹掉 B 軌的升級狀態（同 08-12/08-13/08-14 各輪處置）。

<!-- 2026-08-15 平台軌 08:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入者 -->
- **2026-08-14（平台軌 · **08:00 窗**建置輪 · 續查 rainbet 費率階梯〔結果為「查不到」且該結果改寫了開卡判斷〕· 台帳審「前端UI/UX + 資料」8 模組〔實回填 6、刻意留 2 stale〕· 開卡 #93/#94 · 實作 #91 檔期軸）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:2x 已釋放）→ claim `p-081900-7f3a`（帶心跳 08:19→09:05）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.1h**（08-13T21:05 → 本輪 08:09）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳次舊分類＝`前端UI/UX`/`資料`／深挖候選＝rainbet 費率階梯／實作候選 #91 或 #92 或 #90）⇒ **做而不讓路，三項全數命中**＝跨輪指派**連續第十一輪**乾淨運作。
    - **調研（配額 2 只用 1、刻意不新取材）**：全庫 **live 35 筆逾期 0**（最早到期票＝ stake／bc-game 的 08-17，皆未到期），故本輪無「到期票」可深挖；配額改全數投入前輪明文指派的定向續查。
        - ⭐ **rainbet 續查＝「查不到」，而這個否定結果直接改寫了開卡判斷（本輪最值得記的一筆）**：前輪把「逐段位 rakeback 費率階梯」列為續查第一順位。多源交叉後確認 VIP 為 **7 段位 × 25 級**，但**只有 Bronze（5%）與頂段（~20%）有數字，中間五段連該站自家 help center 都沒有**（官方說明頁只寫「Increased % / Higher % / Premium % / Maximum %」這種無數值描述詞）⇒ **不是本輪沒查到，是該站刻意不公開**。
        - ⚠️ **並抓到前輪一筆記載需要降級**：業界評測寫「rakeback **每 15 分鐘**可領」，但**該站自家 rakeback 說明頁寫的是「每 24 小時週期結算後自動入餘額、無須手動領取」**＝兩個來源互相矛盾；且「**Pre-Monthly**」這個詞在該站自家文件中**完全查無**（僅見於第三方彙整）⇒ 前輪據二手來源記下的「每 15 分」與「Silver+ 解鎖」**皆降級為未定論**，已回寫 dossier。
        - ⭐ **這對開卡的實際影響（本輪據此調整而非照抄）**：既然對手的**數字**不可得、**節奏**還互相矛盾，可抄的就只剩**結構**——「檔期桶是可註冊的、且可帶解鎖條件」。這正好是 **#91 只做出口與閘、一個獎勵金額都不新增**的理由；**若本輪照抄了那些二手數字，抄到的會是錯的**。同時據此**暫緩** dossier 原列第 2 順位的「15 分鐘 rakeback 桶」開卡（節奏未定論 + sub-hour 送幣頻率的真站經濟值未收斂，§11）。⇒ **通則：調研的否定結果不是白跑——它會告訴你「哪一層可以抄、哪一層不能抄」**。**不重設 `next_due`（維持 08-27）**：定向續查 ≠ 完整重新深挖，不該把新鮮度時鐘往前撥。
    - **台帳審「前端UI/UX + 資料」（兩分類共 8 模組，皆 08-07＝「功能」以外最舊）**：
        - ⭐ **最有價值的查獲＝`導覽殼層` 自己的 `stowable_note` 寫了很久卻從未兌現 ⇒ 開卡 #93**。導覽有**三個表面**：桌機側欄 `sidebar()`（`app-shell.js:505`）、手機抽屜 `mobileDrawer()`（:643）、底部列 `bottombar()`（:600）。**前兩者已共用同一份私有陣列 `SIDE`（:15，5 筆）＝單一真相不會 drift（這點做得好）**；**但底部列的 5 個獎勵入口 ＋ 右側 3 顆 FAB 是逐個行內 `item(...)` 硬寫**，零描述子/零 `enabled`/零 `order` ⇒ 該模組承諾的「registry 陣列（icon+label+view+enabled+order）可拖排/隱藏、**未啟用模組不出現避免入口擠壓**」**三個表面沒有一個做到**，而 `apexwin-ui-quality` 收錄的真實反例「**底部列 17 項擠壓**」根因正是缺 `enabled`/`order`。
        - ⚠️ **判別註記（本輪主動與 #90 家族劃清界線）**：這**不是**「台帳承諾與現況不符」——`stowable_note` 是**可收納設計意圖**、不是對現況的宣稱，故 status 正確維持 `present`（導覽功能完好、抽屜 a11y 齊備）。真正的訊號是「**意圖存在但沒有出口**」。⇒ 兩類要分開，否則下輪會把所有 `stowable_note` 都誤讀成謊報。
        - **其餘 5 筆複審皆「無變化」（據實記錄、不為了有事做而改動）**：`遊戲客戶端框架` 工具列仍 6 顆、`HL.fair.isPF` 仍 4 處命中（T28 單一真相未回退）；`大廳分群軸` 對 `volatility/pace/rtp/playstyle/quickWin` **仍全數零命中**（absent 滿一週 ⇒ 本輪開卡 **#94**）；`報表與匯出` 掃 `toCsv|.csv|download=|Blob(` **仍僅 `core/betlog.js`(4 處)**；`資料/分析` 的 `eventSchema|funnel|cohort` **仍 0 命中**；`玩家注單` `betlog.js` 266 行、`CAP=500`、寫入點仍只有 `live-stats.js` 1 處（中央掛鉤未被繞過）。
        - ⭐ **刻意只回填 6 筆、留 2 筆繼續顯示 stale（08-13 收緊紀律的第一次實戰）**：`大廳/遊戲牆` 本輪只量到一半（`lazy-games.manifest` **19** 筆、`HL.games.register` **27 次/22 檔**，但**種子卡 51 筆(playable 2) 未重新導出**——探針解析 mock-data 失敗）；`多語系前端 i18n` 本輪粗正則得 EN/HANS **972/954**，與 08-04 權威值（vm 載 `dict()`）**874/738 明顯不符 ⇒ 判為正則把巢狀引號算進去的假數字、據實不採用**。兩筆皆**維持 08-07 不回填**並在 evidence 記明原因。⇒ **通則：量到一半就回填，等於把「看過一眼」與「重新量測過」壓成同一時戳——那正是 08-13 那筆低估能潛伏一週的機制。**
    - **實作 #91（本輪唯一實作卡）＝週期獎勵「檔期軸」：`reload` 的私有 `PERIODS` 升為可註冊出口 + VIP 階級解鎖閘**（`core/reload.js`／`i18n.js`／`tests/checks-platform.js`／`sw.js`）。做法遵「容器先於內容」：`PERIODS` 改為空登記表 + `register(spec)`，**三檔種子改由同一出口註冊**（自己吃自己的狗糧，比照 #55 dock／#91 卡體要求）；新增 `unlocked(p)` 階級閘（`minTier`＝VIP index 下限，**不宣告＝現況**）；`status()` 加 `locked/minTier/minTierLabel` **加法式新鍵**；`claimableCount()` 與 `claim()` **雙雙**排除被鎖桶；`open()` 對被鎖桶渲染「🔒 尚未解鎖 + 解鎖條件」。i18n 補 EN/zh-Hans 各 2 鍵（P3 紀律：落地時同步補）。sw v161→v162。**本卡一個獎勵金額都沒新增。**
        - ⭐ **零回歸用「HEAD 版 vs 本輪版逐位比對」證明，而非宣稱**（沿用 08-13 那輪的 shim 手法：`new Function` 載入**真的那份** `reload.js`、固定假時鐘，不重寫演算法）：5 個 VIP 等級 × 3 種已領狀態下，`status()` 的**舊有 6 鍵逐位相同**、`claimableCount()` 相同、三檔 `claim()` 的回傳值／`bonus.add` 參數／`localStorage` 落點**逐位相同** ⇒ **67/67**。
        - ⚠️ **踩到一次假性報紅並修正量測法**：第一版 shim 只在**載入期**假時鐘、`status()` 時已還原 ⇒ 兩版本的 `msToNext()` 差 1ms 而報紅。**是量測法錯不是程式壞**（量測法陷阱家族第九輪）——改為假時鐘整支腳本常駐後 67/67。
        - **常駐鎖 2 條入 `tests/checks-platform.js`**：`platform/reload-period-outlet`（反向 grep：`PERIODS` 不得被檔外引用 ＋ **出口存在** ＋ 種子樣本量下限 3——後兩項是防「因為根本沒有檔期軸而恆綠」的空心鎖）、`platform/reload-tier-gate`（涵蓋卡上不變量 a/b/c/d，含「被擋時不得派彩、也不得寫入已領旗標」）。node fast **115→117 全綠**。
        - ⭐ **負向擾動 3/3 皆被抓**（沿用遊戲軌 08-13 的紀律，證明鎖非空心）：① 拿掉 `claim()` 的 `unlocked` 閘 → FAIL ② `claimableCount()` 忽略閘 → FAIL ③ 他檔引用 `PERIODS` → FAIL；三者還原後全綠。
        - ⚠️ **驗證誠實聲明**：排程輪**起不了 dev server**（沙箱拒無人值守 preview）⇒ **不宣稱做了 preview**。`open()` 的 🔒 渲染分支**在目前沒有任何種子宣告 `minTier` 的情況下不會被執行**（＝視覺零變更），該分支的目視驗證留待第一張真的帶 `minTier` 的卡。
    - **寫檔紀律**：⚠️ **自我糾正一筆（同一個坑三軌第三次）**：本輪寫測項時用了 `cat >> file << 'EOF'`，雖非 `node -` 但**同屬 §6 踩雷 #5 禁止的 stdin heredoc 家族**，且**當場就付出代價**——heredoc 把 `replace(/\\/g, "/")` 的跳脫吃成 `replace(/\/g, "/")`，整個測試檔語法錯誤、harness 直接崩。⇒ **後續一律 Write/Edit 工具寫檔**（本輪其餘寫入皆已照辦：複雜 JSON 與 e2e 一律 Write 暫存 `.js` 再 `node 檔.js`+`timeout`）。逐檔 add、**分段提交**（實作一筆／調研+台帳+開卡一筆）。⚠️ **未碰** `prototype/games/registry.json`（M）與 `games/slot-engine/`（??）＝mtime **08-03** 他人未提交工作、非本輪孤兒，依 §7 原樣不動；亦未碰 `Game assets/` 增刪雜訊。
    - **下輪首要**：台帳最舊分類＝**`功能` 剩 11 筆仍停 08-06**（本輪未動），其次 `大廳/遊戲牆`+`i18n`（08-07，本輪刻意留 stale，**下輪請用權威量測法補完**：i18n 走 vm 載 `dict()`、大廳走可靠解析而非正則）；**再取材前先汰一筆**（active 35 > 「≤ 約 32」，且 08-17 才有到期票）；實作候選 **#92**（S·一行修+一條鎖·headless 可落地）或 **#90**（S–M）或 **#94**；⚠️ **#93 不適合 headless 排程輪**（三個導覽表面需 preview 逐一目視，含 ≤720px 抽屜態與桌機 icon-rail 態）。⚠️ `consecutive_idle_rounds` **刻意維持 3 不歸零**：那個 3 是維護軌 08-14 00:00 headless 飽和輪寫的**自身**退避門檻、本欄三軌共用；平台軌有工作不能證明維護軌的債佇列不是乾的 ⇒ 歸零＝用 A 軌產出抹掉 B 軌的升級狀態（同 08-12/08-13 兩軌處置）。

<!-- 2026-08-14 平台軌 20:00 窗：日誌保留最新 3 則，以下為本次自 BACKLOG.md 移入者 -->
- **2026-08-13（平台軌 · **20:00 窗** · **catchup 認領孤兒輪** · 收尾前手 #89 WIP 並自驗 · 首次建檔 rainbet〔本輪最大研究發現〕· 台帳審「功能」13 模組 · 開卡 #91/#92）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（遊戲軌 16:4x 已釋放）→ claim `p-201230-5c8f`（帶心跳、逐步更新）→ 停頓做 WIP 檢視與調研 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**dark 35.3h**（08-12T08:55 → 本輪 20:12）**> 24h ⇒ catchup 生效、禁止讓路**（`lead_track=games` 本可讓路，但 catchup 優先且遊戲軌 16:00 窗**明文指派**「磁碟孤兒 #89 平台軌 WIP 待平台軌 20:00 窗自驗收尾」）⇒ **跨輪指派連續第十輪**乾淨運作。
    - ⭐ **本輪最重要的一件事＝認領並收尾前手的孤兒 WIP，而不是重做一份**。磁碟上有 7 個檔（`wager-scope.js` 新檔 + `index.html`/`i18n.js`/`live-stats.js`/`progress.js`/`checks-platform.js`/`run.js`）mtime 10:09–10:14，係 08-13 上午平台窗**凍結前未提交的 #89 工作**（遊戲軌 16:00 窗已依 §7 原樣不動並留言指派）。**處置＝完整重讀後自驗、補完缺口、逐檔 add 提交**（`b1a1df8`）：前手已完成程式與測項，**本輪補的是它沒做完的三件**——① `sw.js` 未 bump（v160→v161，改了 `prototype/` 卻未 bump 會讓 PWA 餵舊檔，§4 已知陷阱）② BACKLOG 卡未結（🟦→✅ 附短碼）③ **無任何瀏覽器/行為層驗證**。⇒ **通則：孤兒 WIP 的「程式看起來完整」不等於「這張卡做完了」，收尾清單（sw bump／卡片狀態／驗證）才是那條界線。**
    - ⚠️ **驗證誠實聲明＋替代方案（本輪方法論產出）**：排程輪**起不了 dev server**（沙箱明拒無人值守 preview）⇒ 不宣稱做了 preview。改以**瀏覽器 shim 載入「真的那份 `progress.js`」**（`new Function` 走其瀏覽器分支，**不重寫演算法**——重寫等於驗一份 copy）做 **48 項 e2e**：⭐ **把 `git show HEAD:progress.js` 與 WIP 版跑同一組未宣告 scope 的下注序列，`localStorage` 逐位相同、`freed` 回傳值逐位相同、entry 不多出 `sc` 鍵**＝零回歸不靠宣稱而靠逐位比對；並**代跑平常無人跑得到的 `env:"browser"` 架構鎖** `sla/bonus-wager-frozen`（`String(onWager)` 不得含 `reqFor|WAGER_MULT`＝本卡正好改寫了 `onWager` 本體）。**這條路徑值得沿用**：headless 排程輪過去把 browser-env 測項整批視為不可達，實際上「shim + 真檔 + 與 HEAD 逐位比對」比 preview 目視更能證明零回歸。
    - ⭐ **e2e 抓到一筆前手沒發現的 API 形狀變更**：`bonus.status().head` **多了 `scope`/`scopeLabel` 兩鍵**（未宣告時恆 `null`）⇒ 持久化狀態雖逐位相同、但**回傳形狀不是**。查證全站 `HL.bonus.status` 消費端只有 `service-level.js` 的架構鎖（檢查函式原始碼字串、不列舉鍵）⇒ 判定**加法式安全**，並把「僅允許新增這兩鍵且恆 null」寫成 e2e 斷言而非口頭結論。（我最初的斷言寫成「`status()` 必須與 HEAD 逐位相同」而報紅——**那是斷言下錯、不是程式壞了**，已據實改為加法式檢查。）
    - **調研（配額用於「新平台建檔 1 + 深挖同一筆」；`platforms.json` 逾期 **0/34＝0%**、下一個到期 08-17，故無到期票可深挖）**：
        - ⭐ **(a) 首次建檔 rainbet（tier-1／priority 94）＝本輪最大發現，也是一次「指標全綠但覆蓋有洞」的實證**：多訊號一致——SimilarWeb **Casinos 類別第 1**、月訪 **23.4M**、全球 ~1,495、**MoM +23.25%**，業界報導其**流量市佔超過 Shuffle + BC.Game + Roobet + Rollbit 之和**。**而本庫先前完全沒有這一筆**，卻追蹤著 24 個 tier-3 小站、還把 shuffle 記為 tier-3（pri 68）。⇒ **`platforms.json` 逾期率 0% 是帳面完美，但漏掉的那一筆永遠不會出現在逾期率裡——逾期率量的是「已知的有多舊」，量不到「不知道的有幾個」**（與 counters 非權威、`high_water_dossier_date` 零讀者同族：指標的盲區比指標的數值更值得記）。這也是 SKILL 第 1 步「每輪重新取材、不吃固定清單」的價值實證。
        - **淨新訊號＝「領取節奏」被產品化為五個獨立檔期桶**：Rakeback（**每 15 分鐘**）／Daily／Weekly／Monthly／**Pre-Monthly（Silver 段位以上才解鎖）**，領取本身可再疊 rakeboost。**開卡前先查證，結果推翻了「這是缺口」的直覺**：#24 `core/reload.js` **已是同型容器且長得很好**（三檔 × 懶判定週期閘 × VIP 放大 × 入 `HL.bonus` × 倒數）＝**五桶中 3 桶已覆蓋** ⇒ 缺口重新定義為**三個「出口」而非一條新軸**：① `PERIODS` 是私有硬寫陣列（`grep register|minTier|gate` **0 命中**）② **無階級解鎖閘**（全等級皆可領、只有金額分級 ⇒ 做不出 Pre-Monthly）③ 無 sub-day 節奏先例 ⇒ 開卡 **#91**（與 #90 同形狀＝表長得很好、缺的只是出口，故 S–M 非 L）。
        - **一筆佐證不開新卡**：評測普遍批評 Rainbet **自研 Originals 不顯示 RTP**＝與 ApexWin「RTP/莊優散在 `HL.edge` 與各遊戲檔、玩家端無處可讀」同型 ⇒ 寫進 **#72**（**第四平台佐證**）。
    - **台帳審「功能」13 模組**（08-06＝本輪前全庫最舊，前手明文指派）：
        - ⭐ **最尖銳的查獲＝「台帳承諾與現況不符」的鏡像型：現況優於記載**。`可驗證公平` **partial → present（據實上修）**——07-31 記載的兩個缺口**都已關閉**：① 舊述「旗艦 `slot.js`(Shadow Ritual) 與 `chicken.js` 仍 Math.random」**已不成立**（`slot.js:146` `frnd()=HL.fair.floatOr("slot")` 出象逐筆顯式傳入、`chicken.js:208` 每格生死走 `HL.fair.floatOr("chicken")`；殘留 Math.random 逐筆確認**全屬純美術**＝車色/動畫時長/延遲/障礙種類）；② 舊述「無逐局 nonce 對應表」**已由 #51 `betlog.js` 關閉**（逐注留 `cs`+`ne`、`verifyModal({clientSeed, nonce: ne-1})`，且**只對 `HL.fair.isPF` 為真的遊戲開放**、其餘停用按鈕＝誠實設計不假裝可驗）。**成因**：08-06 那輪是「**僅新鮮度覆核、未重複展開**」⇒ **台帳低估現況整整一週**。
        - ⭐ **因此收緊一條方法論並寫進 `_freshness_doc`：只有本輪真正重新量測過的模組才回填 `last_audited`，未重新展開者刻意留在舊日期讓它繼續顯示 stale**。本輪 13 模組僅 **2 筆**（可驗證公平／中央結算掛鉤）經逐筆機械複驗故回填 08-13，**其餘 11 筆維持 08-06**。理由：全分類齊頭回填會把「**看過一眼**」與「**重新量測過**」壓成同一個時戳，使新鮮度指標失去指示力——而這正是本輪那筆低估能潛伏一週的機制（同源於 counters 非權威／`high_water_dossier_date` 零讀者：**一個無法區分強弱的指標，比沒有指標更誤導**）。
        - **新查獲一個更窄但真實的殘留缺口 ⇒ 開卡 #92**（故 present 不是「零缺口」的意思）：`views/liveroom.js:140` 虛擬主播跟注在**取不到真桌結果時 fallback `Math.random()<0.5` 硬幣翻轉**，而該路徑**真扣真派**（§4/7d 走真 `HL.baccarat`）＝**唯一仍影響真實派彩卻不可驗算的分支**。卡上並記兩個實作陷阱：**別順手改同檔 :158 的假聊天**（會吃掉 fair 的 nonce 序列、使 `betlog` 的 `nonce_end` 對不回該局），且**先判斷「取不到真桌結果」是否本該退回未結算跟注**（同檔 :134 已有此形制）而非翻硬幣。
        - `中央結算掛鉤` 回填並記 **`game` 軸本輪補完最後一塊**：`edge`／`rakeback`／`challenges`／`heat`／`achievements`／`betlog`／`tournament`／`bonus` **全數收得到 `game`**；並補反向 grep 常駐鎖 ⇒ **這個形狀連犯兩次（#85/#89）後改由機械看守，不再靠記憶**。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`，commit 訊息用多個 `-m`；**分段提交**（實作一筆、調研+台帳+開卡一筆）；只逐檔 add 自己的檔。⚠️ **未碰** `prototype/games/registry.json` 與 `prototype/games/slot-engine/`（mtime **08-03**＝他人/前景未提交工作，非本輪孤兒）。⚠️ **自我糾正一筆**：審台帳時曾以 `node -e` 讀 `registry.json` 得「同仁遊戲 2 筆」，但那個 2 來自**未提交的 08-03 WIP**（HEAD 為 1）⇒ **差點把別人未提交的工作寫成台帳事實**，已捨棄不引用該數字。**通則：台帳數字必須來自 HEAD 或明確標註工作區狀態**。**下輪首要＝台帳次舊分類**（`前端UI/UX`/`資料` 皆 08-07；「功能」剩 11 筆維持 08-06 故仍為最舊，可續審未展開者）；**深挖候選＝rainbet 的逐段位 rakeback 費率與 Pre-Monthly 解鎖前置**（本輪只查到節奏未查到費率階梯）或 08-17 到期的 stake/bc-game；**實作候選 #91**（S–M、與 #90 同形狀）或 **#92**（S、一行修 + 一條鎖）或 #90。⚠️ **`consecutive_idle_rounds` 刻意維持 2 不歸零**：那個 2 是維護軌 08-12 12:00 headless 飽和輪寫的自身退避門檻，本欄三軌共用；平台軌有工作不能證明維護軌的債佇列不是乾的 ⇒ 歸零等於用 A 軌產出抹掉 B 軌的升級狀態（同 08-12 兩軌處置）。
- **2026-08-12（平台軌 · **08:00 窗**建置輪 · 深挖 leovegas〔`next_due` 08-13，本輪全庫唯一到期票〕· 台帳審「後台」7 模組 · 開卡 #89/#90 · 實作 #85 競賽計分軸）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:3x 已釋放）→ claim `p-081100-3f8d`（帶心跳）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **17.5h**（08-11T14:40 → 本輪 08:11）未達 catchup；`lead_track=games` 准讓路，但**前手 14:00 窗已明文指派三項**（台帳次舊分類＝`後台`/`功能`／再取材前先汰一筆／實作候選 #85 或 #87 或 #88）⇒ **做而不讓路，三項全數命中**＝跨輪指派**連續第九輪**乾淨運作。
    - **調研＝深挖 1、且據實只用掉一半配額**（`max_platforms_per_run: 2`）：ACTIVE overdue **0/33**、全庫**只有 leovegas 一張到期票**（`next_due` 08-13，提前一天刷），次早的 stake/bc-game 要 08-17 且 stake 才在 08-11 深挖過 ⇒ **硬湊第二張就是 busywork**（`ban_busywork_heartbeat`）。配額用不完就記下來，不製造工作。
        - **(a) leovegas 深挖** ⇒ ⭐ **淨新＝架構層的斷線，不是缺一檔活動**：Golden Chips 評測明載「**can be used only in Playtech games**」+「**split across five days**」。後半＝**#87（Roobet Vault 逐片到期軸）第二平台佐證**（同家另有「free spins 分三次存款發」「spins 須 3 天內完成流水」＝一家三處分片時限）⇒ 寫進 #87 卡體不另開卡。前半 grep 實證 `eligibleGames|allowedGames|gameScope|onlyGames` **0 命中**，根因是 **`HL.bonus.onWager(bet)` 簽章沒有 `game`** ⇒ 開卡 **#89**。
        - ⭐ **本輪最有價值的一條連結：`game` 軸只走到一半是這個中央掛鉤的系統性問題**。`liveStats.record(game,bet,win)` 帶著 `game`，卻只傳給 `edge`／`rakeback`／`challenges`／`heat`／`achievements`／`betlog`；`tournament`（**本輪實作 #85 時修掉**）與 `bonus`（**#89 待修**）都收不到。⇒ 調研發現與實作發現**在同一輪從兩端撞到同一個根因**，這比任一邊單獨成立都更有說服力。
        - **四項看似新的訊號經 grep 全數證實已覆蓋＝本輪省下四張假卡**：Weekend Booster「雙倍點數」＝`progress-src.js` 的 `BOOST_CAP.demo = 2.0` **恰好就是雙倍**、「週末」這個時間軸由 #49 `promoCal` recurring 與 #81 `rakeboost.registerTriggered` 提供／Monday Reload＝`reload.js`／Happy Hour＝`happyhour.js`／99 層 VIP 與 The Club 門禁＝舊點子（門禁已由 #54 `audience` 覆蓋一半）。另 Birthday Month `grep birthday` **0＝真缺**但需生日欄位（近 KYC）價值低 ⇒ 同 08-11 crown-coins 的處置：**據實記錄、刻意不開卡**。並據實**更正自己 07-30 記下的缺口**：「no-wager free spins ＝引擎缺型別」已由 #20 `wagerFree` + #63 段位 `wagerMult` 消化。
        - **(b) 汰除評估（前手指派「再取材前先汰一筆」）** ⇒ **courtside 回填 `saturation_watch 1/2`，但明白寫上「本輪未重新深挖」**：其 08-06 的 `last_note` 已載「零新增可移植機制」＝第 1 輪零增量，本輪只是把**既有證據**轉成可機械追蹤的欄位（同 08-10 對 bet365 的處置理由：別讓結論只活在散文裡）。⚠️ **mega-dice／thrill 兩張刻意不比照辦理**——它們的 `last_note` **是空的**，沒有任何零增量證據在檔，回填等於憑空宣稱 ⇒ 留給下輪真的深挖後再判。active 仍 **33**（09-05 courtside 若達 2/2 才會降到 32）。
    - **台帳審「後台」7 模組全審**（08-06＝當時全庫最舊之一，輪替接續前輪擴充性）：**七筆狀態一筆都沒硬改**（partial×5／absent×2），四處實質收斂：
        - ⭐ **最尖銳的查獲＝台帳自己的承諾與現況不符**：「經濟/RTP 設定」的 `stowable_note` 長期寫著『新旋鈕加進 config 表即被儀表板健檢納管』，但 `grep -o "HL\.(edge|rakeback|progressSrc|rakeboost|sla|ledger)" ops-dashboard.js` 實測**只命中 `HL.ledger`(8)＋`HL.site`(2)**＝**一張具名經濟旋鈕表都沒讀** ⇒ 儀表板看得到「已發生的結果」，看不到「旋鈕現在被設成幾」 ⇒ 開卡 **#90**。**這是設計意圖被寫成了現況**，屬「台帳自我查核」的新一類，與『出口 vs 提及』互補。
        - ⭐ **據實把『集中的具名經濟旋鈕表』由 3 更正為 5**：新計入 `rakeboost.js`（MULTS/CAP/TRIGGER_*，#81 於 08-10 落地）與 `service-level.js`（DIMS/LEGACY_MULT，#63 於 08-05 落地但 **08-06 那輪漏數**）⇒ **旋鈕表本身長得很好、缺的只是出口**（這個判斷直接決定 #90 是 S–M 而非 L）。
        - ⭐ **『出口 vs 提及』判別法的第三種變形＝旋鈕在、線沒接**：舊記載寫『全站並無任何 jurisdiction/法域軸』，實測**不成立**——`money.js:18` 有 `jurisdiction()`、`:34` 有 `setJurisdiction()`、`app-state.js:25` 種子 `"TW"`＝欄位/getter/setter/持久化四件都在；真正的事實是**零讀者**（排除自身後 grep **0 命中**）＝與被廢除的 `high_water_dossier_date` 同型。缺口重新定義為「**接線**」而非「建軸」，**刻意不開卡**（近 avoid、價值低於 #89/#90），但**必須記**——否則下輪會誤判成「連欄位都沒有」而重造一次。
        - **兩處複驗證明沒腐爛**（罕見地是好消息）：儀表板可編輯控制項仍恰 **2** 處、`mock-data.js` 仍恰 **26** 個 var 常數，皆與 08-06 逐位相同；另 `promoCal` 外部註冊者 **2→3**（`progress-src`/`rakeboost`/`release`）但 `grep audience promo-cal.js` **仍 0** ⇒ 促銷仍無分群，維持 partial 不升 present。另新增一條可執行的觀察：**全站有兩張遊戲清單且不同構**——內建走 #80 `MANIFEST`（每款 12 個欄位的物件），社群 `games/registry.json` **仍是裸路徑字串陣列** ⇒ 08-06 記的『能力已存在但未接線』現在有了**現成的捐贈者實作**。
    - **實作 #85 競賽計分軸**（詳見卡片落地段）：新增 `core/score-axis.js`（`turnover`/`bestWin`/`bestMult` 三軸 + `groupBy:"game"` 分組榜與分組獎池），`tournament` 的 `o.score += bet` 收斂為 `axis.accum(cur, ctx)`。⭐ **卡上不變量 (c) 在實作時被證實比卡上寫的更嚴重**：不只是「倍數會算錯」——舊的 `record(bet)` 掛在 `live-stats` 的 `bet > 0` 區塊內，而旗艦 slot／小雞把同一局拆成 `record(bet,0)` 與 `record(0,win)` 兩次結算 ⇒ **win-only 那半根本進不到錦標賽**，照卡面直接加軸的話 `bestWin` 會**永遠收不到 slot 贏分且不會報錯**。修法是補一行只在 `win>0 && bet<=0` 觸發的餵入，並讓 `record()` **分數沒變就完全不動**（不寫檔/不通知/不觸發逾期結算）⇒ 流水軸下恆為 no-op（node 實測寫入次數 5→5 一次未增）＝**新增餵入路徑卻零回歸**。
    - ⚠️ **控制組救回一個空過的測項（#86 教訓的直接應用、連兩輪）**：分組結算第一版顯示「總獎金 = Σ 各組獎金 ✅」，但當時 `prize` 其實是 **0**（決定性 stub 讓假 bot 全 250.6×、玩家只有 20× ⇒ 名次第 50、本來就沒獎金）＝**「發不出錢」與「金額正確」在該情境完全同形**。補上「玩家真的拿下 dice 組第 1 名」的控制組後才驗到獎金 **125,000 == 該組獎池 500,000 × SPLIT[0]** 且只入獎金錢包一次。**通則再確認：驗一個結果為零的斷言時，必須另設一個結果非零的對照組。**
    - ⚠️ **驗證手段誠實聲明（本輪與 08-12 00:00 維護軌同一個限制）**：排程輪 `preview_start` 被拒（「無人核准」）⇒ **無瀏覽器 e2e**。改以 `vm` 載入**同一份 `score-axis.js` + `tournament.js` 的瀏覽器分支**（非另寫一份模擬）跑 28 項 e2e：零回歸／win-only 零副作用／**舊存檔相容**（既有 localStorage 沒有 `axis` 欄位仍正常計分）／分組池 Σ≤總池／真站兩種榜皆無假 bot。node fast **102→107**、**負向擾動 9/9 全被抓**、擾動後 SHA256 逐位還原。**未驗到的部分據實列出**：賽事頁的視覺（新增的「計分方式」一行與分組分頁列）、375px 溢出、三語 DOM 實際渲染 ⇒ **建議下一個 preview 可達之輪補看一眼**（風險低：種子賽事仍全為 `turnover`/`none`，線上玩家可見變更只有一行文字）。
    - **寫檔紀律**：複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；**分段提交**（實作一筆、調研+台帳+開卡一筆）；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰** `prototype/games/registry.json`（他軌/前景未提交）。⚠️ **自我糾正兩筆**：dossier 追加用了 `cat >> file <<'EOF'`、實作 commit 訊息用了 `git commit -F-`，兩者雖非 `node -` 但**同屬 stdin heredoc 家族、違反 §6 踩雷 #5 的字面規定**（本次皆未掛死，但該規則的價值正在於不賭運氣）⇒ **後續一律改用 Write 工具寫檔／`-m` 多段寫 commit 訊息**。**下輪首要＝台帳最舊分類 `功能`（13 模組，08-06，本輪後為全庫唯一最舊）**；再取材前仍須先汰一筆（courtside 1/2 要 09-05 才複驗；bc-game/bet365/1xbet 皆 watch 中）⇒ 汰不掉就照本輪做法**據實少取材、不硬湊**；實作候選 **#89**（與 #85 同一根因、卡體已寫死 grep 證據）或 **#90**（S–M、後台分類投報比最高）或 #87。⚠️ **`consecutive_idle_rounds` 刻意維持 1 不歸零**：那個 1 是維護軌 08-12 00:00 輪判定 headless 飽和後寫的、用來累積它自己的退避門檻；本欄三軌共用，平台軌有工作不能證明維護軌的債佇列不是乾的 ⇒ 歸零等於用 A 軌產出抹掉 B 軌的升級狀態。

- **2026-08-11（平台軌 · **14:00 窗**建置輪 · 深挖 crown-coins + 1xbet〔並列全庫最早到期 tier-2〕· 台帳審「擴充性」5 模組 · 開卡 #87/#88 · 實作 #86 負責任博弈閘覆蓋）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 12:2x 已釋放）→ claim `p-141030-9e4c`（帶心跳）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.1h**（09:05 → 本輪 14:10）未達 catchup；`lead_track=games` 准讓路，但**前手 08:00 窗已明文指派三項**（台帳次舊分類＝`擴充性`/`後台`/`功能`／開卡指派＝roobet Vault／實作候選 #85 或 #86）⇒ **做而不讓路，三項全數命中**＝跨輪指派**連續第八輪**乾淨運作。
    - **調研＝深挖 2、刻意不新取材**（三條可查證理由：ACTIVE overdue **0/33** 收尾維持、active 仍 **33 > 「≤ 約 32」**、兩張到期票 crown-coins/1xbet 的 `next_due` 08-12 並列全庫最早且皆 tier-2）。
        - **(a) crown-coins 深挖** ⇒ ⭐ **淨新＝「酬賞負載軸」**：9 項機制逐一 grep 覆核，**8 項已覆蓋且吻合度高到反過來成為佐證**——日簽階梯的里程碑落 **第 8/15/22/30 天**，與 `rewards.js` **恰好逐位相同**；coinback **2–6%** 與真站返現同區間；月獎（reload 三週期）／每日任務／推薦皆在；連簽斷掉的處置由 **#84（今早才落地）** 覆蓋且更完整。**唯一未覆蓋＝登入第 2、7 天發的是「一次幸運輪盤轉動」而非幣**：`rewards.js:409-427` 的派發恆為 `amount → balance`（里程碑進獎金錢包，**兩種都仍是錢**），`grep "luckyspin" rewards.js` **0 命中** ⇒ 開卡 **#88**＝**第四條軸**（#64 遊戲軸／#83 分配軸／#85 計分軸／#88 負載軸）。**兩筆刻意的克制**：① 「商城 Gold 階才開放」**併入 #73** 並**當場收窄**——`shop.js` 已有 `VIP_DISCOUNT`（依段位打折），這是**同軸不同做法而非缺口**，若不寫進卡體，下輪會誤判成「shop 完全沒有段位概念」；② 「生日禮」`grep birthday` **0 命中＝真缺**，但需生日欄位（近 KYC）且價值低 ⇒ **據實記在 dossier 但不開卡**。
        - **(b) 1xbet 深挖** ⇒ **增量歸零、記 `saturation_watch 1/2`**：四項訊號**全數**經 grep 證實已覆蓋——Promo Code Store（點數換碼）＝ `shop.js`(#42) **＋** `redeem.js` 兩個出口且更正交／「為投注累積點數」的 opt-in 開關＝ #52 + `progress-src.js:728` `optIn`／點數也來自**非投注行為**（帳號驗證、綁手機、參賽）＝ **#65 `progressSrc.register` 容器已在**（`kind` 已含 social/purchase）／VIP 八階返現＝ `HL.vip` + #50。⇒ **「先 grep 再斷言」本輪再省下四張假卡**。非投注來源**寫進 #65 卡體**當第二平台佐證（含「真站一律 `xpPerLive:0`」的既有護欄提醒），不另開卡。tier-2，09-10 複驗仍零增量即可正式 `saturated`；⚠️ 但其 **UX 反面教材**定位不因飽和而降級（與「有無可抄機制」是兩件事）。
    - **台帳審「擴充性」5 模組全審**（08-06＝全庫最舊之一）：**五筆狀態一筆都沒硬改**，三處實質收斂：① ⭐ **修掉一個自 08-04 沿用至今的量測法錯誤**——dock 採用度一直用 `grep "HL.dock.register"`，但 `panels.js` 是**鏈式寫法**（`HL.dock` 換行後接 `.register({`）⇒ 該 grep **看不到 partner/chat 兩筆**，反而把 `dock.js` 內**兩行註解**算成命中。**naive 得 3 命中、真實註冊點也是 3 筆，但成員完全不同**（panels 2 + dock-growth 1；dock.js 0）＝**數字碰巧對、成員全錯**，比數字錯更難察覺 ⇒ 權威量測改為「去註解後掃 `.register({` 呼叫」；② 自動化測試 node fast **101→102**（本輪新增鎖），**仍維持 partial**（U34 面板鎖未清、版面回歸仍無機械閘）；③ 上架排程消費端實測仍僅 `casino.js` 3 處 ⇒ 08-06 記的缺口**原樣成立、未腐爛**（本輪罕見地是「複驗證明沒腐爛」而非又抓一筆腐爛）。
    - **實作 #86 負責任博弈閘覆蓋**（詳見卡片落地段）：⭐ **本輪最重要的一件事是卡上的範圍被實測推翻並擴大近 3 倍（6 檔 → 17 檔）**。卡的推論前提是「其餘遊戲都掛 `betPanel` ⇒ 已受保護」，**實測兩處都錯**：(i) 四款保真 slot **確實掛 betPanel、正常旋轉本來就有閘**（卡上把它們列為未閘是**過度指控**），真正沒閘的是**買入鈕**；(ii) 反過來 `keno/towers/hilo/pump/picks/duel/crash/mines` **根本沒用 betPanel**（自己 `amt.get()` + `setBal`），**外加 bounty/liveroom/chicken/arena** 全未閘 ⇒ **實際受影響面比卡上大得多**。**選擇補完全部 17 個**：同一行修法、同一風險剖面，且**若只做 6 個，那條反向 grep 鎖就得被改鬆到能通過＝為了讓測項變綠而弱化測項**。
        - **新增鎖＝全站第一條「行為型反向 grep 鎖」**：不列檔名白名單，而宣告「凡 `views/*.js` 出現直接扣餘額的寫法就必須出現 `HL.rg`」⇒ **擋得住下一個還沒被寫出來的遊戲**；另附「樣本量 ≥17」自我保護防規則被改窄而假綠。**負向擾動 8/8 全被抓**（7 檔各移除閘行 + `DEDUCT_RE` 改成永不匹配），擾動後**逐位還原**。零回歸是**既有不變量而非新宣稱**（`rg/zero-regression` 已釘死未設限時恆真，`--group rg` 10/10 全綠），且 `check()` **只評估不累加** ⇒ 不會造成限額重複計。
        - **瀏覽器 e2e＝順手打通一條繞過 §9 登入 gate 的新驗證路徑**：登入 gate 仍擋 shell，但 **#80 lazy-games 使 `HL.games.byId(id).render()` 可直接取用** ⇒ 掛進 detached 容器就能點真按鈕。Keno 設 `bet-single=10`：下注 **50 被擋、餘額一分未動**；**控制組**下注 **5 正常扣款**（28560→28555）⇒ 擋的是**注額大小**而非把遊戲弄壞。Golden Toad 買入（4320 ≫ 10）**被擋**。零 console error。sw **v158→v159**。
        - ⚠️ **量測法陷阱本輪一筆（同家族連八輪），且是被自己設的控制組抓出來的**：第一版 e2e 用「取第一個非數字按鈕」當開獎鈕，實際抓到 **`½`（減半籌碼鈕）** ⇒ A/B 兩組**都沒真的開局**。**若當初只做 A 組（被擋），我會看到「餘額沒動」而誤判閘生效**——是**控制組顯示「連該過的也沒過」才揭穿量測無效**。⇒ 通則加一句：**驗「擋得住」時必須同時驗「該過的要過」**，否則「什麼都沒發生」與「閘生效」完全同形。另誠實記一筆**未能完成的驗證**：想再驗「解除限額後大注恢復可下」，但 `setLimit(id, null)` 依 #67 的**調升冷卻不對稱**會進 `pending`（24h 生效）＝**正確行為非缺陷**，已由既有測項覆蓋故不另補。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON/擾動一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；**分段提交**（調研+台帳+開卡先一筆 `085bc56`，實作再第二筆）；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**前景未提交的 `prototype/games/registry.json` 與未追蹤的 `prototype/games/slot-engine/`。**下輪首要＝台帳次舊分類（`後台`/`功能`，皆 08-06）**；再取材前仍須先汰一筆（1xbet 已 watch 1/2 但要 09-10 才複驗；bc-game/bet365 皆 tier-1 且 1/2 ⇒ 若仍汰不掉，建議評估 courtside／mega-dice／thrill 這幾張 tier-3）；實作候選 **#85**（三軸拼圖最後一塊）或 **#87**（本輪新開、成本下修方向）或 **#88**。

- **2026-08-11（平台軌 · **08:00 窗**建置輪 · 深挖 stake-us + roobet〔並列全庫最早到期 tier-2〕· 台帳審「資安」5 模組 · 開卡 #85/#86 · 實作 #84 連簽容錯）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（維護軌 00:3x 已釋放）→ claim `p-081130-c4a9`（帶心跳）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **10.6h**（08-10T21:35 → 本輪 08:11）未達 catchup；`lead_track=games` 准讓路，但**前手 20:00 窗已明文指派三項**（台帳最舊分類＝`資安`／再取材前先汰一筆／實作候選 #83 或 #84）⇒ **做而不讓路，三項全數命中**＝跨輪指派**連續第七輪**乾淨運作。
    - **調研（配額全用於「深挖 2」、刻意不新取材）**：三條可查證理由同前輪——active 仍 **33 > 「≤ 約 32」**；兩張到期票（stake-us/roobet，`next_due` 08-11 並列全庫最早）恰好用滿 `max_platforms_per_run: 2`；且**本輪仍汰不掉任何一筆**（bc-game 1/2、bet365 1/2 皆 tier-1 且要到 08-17/08-24 才複驗）。收尾 **ACTIVE overdue 回到 0/33**。
        - **(a) stake-us 深挖** ⇒ ⭐ **淨新＝「計分軸」而非又一個活動**：Weekly Wrapped 的決勝法是 **per-game 超群獎**（每款遊戲各出一名：`Big Win`＝最大贏額、`Lucky Win`＝最高倍數，平手比押注額）。grep 證實 **ApexWin 的競賽計分是寫死的單一軸**——`tournament.js:54` `score += bet`（純流水）+ 單一全站榜；`achievements.js` 的 `bestMult/bestWin` 是**個人終身門檻成就**、與競賽資料流完全分離 ⇒ 開卡 **#85**。**這是三軸拼圖的最後一塊**（#64 資格的遊戲軸 / #83 分配軸 / #85 計分軸），三軸齊備後這類活動＝填三張表。⚠️ **刻意不記為 #83 的第三平台佐證**（per-game 超群獎 ≠ 達標者均分池，只有平手尾巴才均分——同 08-10 對 bc-game Roll Competition 的處置）。另「當週 10 款**新上市**遊戲」＝**#64 第五平台佐證**，且其候選池可直接委派既有 `core/release.js`（#54）而非另建名單（已寫進 #64 卡體）。
        - **(b) roobet 深挖** ⇒ **增量歸零、據實記 `saturation_watch 1/2`**：四項訊號**三項經 grep 證實已覆蓋**——RakeBoost 實查＝「限時提高 rakeback 百分比」＝ `rakeboost.js` 既有形制（#81 容器連「驗證後開窗」都只是加一行）；$100k 每週抽獎實查＝**依押注量發券、發給 100 位、依名次分池**＝`raffle.js` 同形制且更完整（`TICKET_PER 2000`/`WINNERS`/`prizeFor(rank)`/機率面板/開獎重置）；31 級成本加權＝ #50 `HL.edge` 已落地。⇒ **「先 grep 再斷言」本輪再省下三張假卡**。**唯一未覆蓋者＝Vault「獎勵切成 7/14 天日曆、每片各自到期、未領即作廢」，且這是 07-10／07-28／08-11 三度重記**（「處置管道沒有帳可查」家族）⇒ 依 #82 先例應升格開卡，但本輪卡額已被 #85/#86 佔滿 ⇒ **寫進台帳 `stowable_note` 並明文指派下輪開卡**，避免第四次蒸發。附帶記一筆反向經濟性質：它是**成本下修**（未領作廢＝房家留下），與 #83 同屬「送幣模組成本上蓋」家族。roobet 為 **tier-2**，08-25 複驗仍零增量即可正式改 `status=saturated`（與兩張 tier-1 不同）。
    - **台帳審「資安」5 模組全審**（08-05＝本輪前全庫唯一最舊）：**五筆狀態一筆都沒硬改**（present/weak/absent/partial/partial），三處實質收斂：① ⭐ **08-05 立下的回歸基準首次發揮作用**——`isLive()` 閘數 **41→46**，而 08-05 明載「新增假活動產生器若未加閘，此數不會增加即為訊號」，期間落地的 #76/#80/#81 使閘數同步上升 ⇒ **訊號為正、無漏閘**（下次基準 46）；② `fingerprint` grep 陷阱**複驗仍成立**（`dock-growth.js` 那個是面板座標指紋、非裝置指紋），並補記一組可重現的真訊號樣式（`velocity|deviceId|antifraud|risk_score` 實測 **0**）；③ ⭐ **本輪最尖銳的查獲＝負責任博弈的閘只覆蓋部分遊戲面**：`grep -rl "HL.rg" views/` **0 命中** ⇒ 5 個 slot + vsslot 各自的 `spin()` 全繞過下注前閘，**而累積側 `live-stats.js:48` 是全通的** ⇒ 實際行為是「**slot 押注會吃掉玩家自設的每日限額，卻永遠不會被擋**；額度用盡後 instant/table 被擋而 slot 照玩」＝**已上線功能的承諾與行為不一致** ⇒ 開卡 **#86**（S，閘本身是一行，另補反向 grep 鎖防下一款 slot 再漏）。
    - **實作 #84 連簽容錯**（詳見卡片落地段）：把寫死在 `status()` 的三分支收斂為純函式 `nextStreakOf()`＋config 化 `GRACE_SPECS`；**零新 `<script>`**。⭐ **兩處我自己的判斷被量測推翻並改強**：① `maxProtectedDay` 首版 14 → 改 **8**（列舉實測最壞多發 129,950→96,100，且 8 恰為卡上承諾「走到第 8 天里程碑」的最小 scope＝**取能達成承諾的最小範圍**）；② 成本論證從「猜一個上限並當門檻」（首版猜 90,000、**列舉當場打成 FAIL**）改為**更強的天花板不變量**——`withGrace(任何漏簽樣式) < 全勤總額 260,300`，因容錯者必定少領 ≥1 天 ⇒ **每人曝險上限一分未增**，容錯只是把被罰的玩家往既有天花板挪。node fast **98→101**、**負向擾動 11/11 全被抓**、真實 e2e 餘額/獎金錢包/帳本三方對帳（帳本 `bonus` +4020 == 1020 日獎 + 3000 里程碑、各記一次）。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON/擾動一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；**分段提交**（調研+台帳+開卡先一筆，實作再第二筆）；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`。**下輪首要＝台帳次舊分類（`擴充性`/`後台`/`功能` 皆 08-06）**；**開卡指派＝roobet Vault 多日到期日曆**（三度重記已升格、本輪卡額滿）；實作候選 **#85**（三軸拼圖最後一塊）或 **#86**（S、已上線功能的承諾不一致）或 **#83**。
- **2026-08-10（平台軌 · **20:00 窗**建置輪 · 深挖 stake + bc-game〔並列全庫最早到期 tier-1〕· 台帳審「活動」6 模組 · 開卡 #83/#84 · 實作 #81 加成觸發軸）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨（前手 catchup 輪 17:0x 已釋放）→ claim `p-201030-a7f2`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **3.1h**（17:05 → 本輪 20:10）未達 catchup；`lead_track=games` 准讓路，但**前手 catchup 輪已明文指派三項工作**（台帳次舊分類＝`活動`/`資安`／再取材前先汰一筆／實作候選 #81 或 #78）⇒ **做而不讓路，三項再度全數命中前輪指派**＝跨輪指派在無記憶架構下**連續第六輪**乾淨運作。
    - **調研（ACTIVE overdue 0/33＝連五輪歸零，故配額全用於「深挖 2」、刻意不新取材）**：不新取材的理由與前兩輪相同且仍可查證——active 仍 **33 筆 > CONTROL 的「≤ 約 32」**，前輪已明載「再取材前須先汰一筆」，而本輪唯一達判準候選（bc-game）才剛累到 1/2 ⇒ 汰不了就不該再長。改把兩格配額給**並列全庫最早到期（08-11）且 priority 全庫最高（96/90）的兩張 tier-1**。
        - **(a) stake 深挖**（tier-1、priority 96）：⭐ **淨新＝「獎池分配軸」，不是又一個玩法**。Originals Challenges（2026-07-20 起把 Originals 納入 cash challenges）資格＝單局 ≥0.5 SC + 命中 ≥x110，獎金＝**固定 5,000 SC 池「均分給所有達標者」**。**關鍵在於把資格與獎金拆開看**：資格那半 `core/challenges.js`（#26）**早就有**（`m2`/`m10`/`m50` 就是「單局倍數門檻×需達成次數」）；**缺的是獎金那半**——全站只有 `tournament.js:50` 的**名次階梯**與 `challenges` 的**每人固定額**，兩者都不是均分 ⇒ 開卡 **#83**。另兩筆**併入既有卡不開新卡**：Multiplier Drops（每週輪換一組遊戲）→ **#64** 第三平台佐證；Bonus Drops 的「過去 7 天押注達標」→ **#59**（其滾動桶多了第二個消費者）。VIP 複驗 08-04 記載無變化。
        - **(b) bc-game 深挖**（tier-1、priority 90）：**淨新缺口＝0，據實記零增量、不硬湊**。四項逐一查證，**兩項看似新的實查皆已覆蓋**：① Roll Competition「top 榜分享固定池」⚠️ **不是** stake 那條均分——仍是依名次分固定池、與 `prizeFor(rank)=POOL*SPLIT[rank-1]` **同形制** ⇒ **刻意不算成 #83 的佐證**（否則會把「兩平台共識」講得比實情強）；② Lucky Spin「獎額依 VIP 段位放大至 5 BTC」實查 `luckyspin.js:27-29` `vipIdx()→VIP_MULT→prizeAt=SEG.amt*mult()` ⇒ **段位縮放本來就在**；③ Quest Hub／Weekly Lottery ＝ #6/#26/#18 已覆蓋；④ 470% 四段儲值紅利／專屬 VIP host ＝ avoid。⇒ 記 **`saturation_watch: 1/2`**（可機械追蹤的欄位，比照 bet365），但**它是 tier-1 旗艦** ⇒ 欄位 note 已明載「即使達 2/2 也建議只延長週期、不改 `status=saturated`」，且**本輪不動 `refresh_interval_days`**。
        - **新鮮度**：兩站 `last_investigated`→08-10、`next_due`→08-17；`ACTIVE overdue` 維持 **0/33**（下一個到期＝stake-us/roobet 08-11）。`platforms_researched` +2（深挖數，非取材數）。
    - **台帳審「活動」6 模組**（08-05＝全 8 分類最舊之一，且輪替順序恰好接續前輪的「金流」）：**六筆狀態全部據實維持**（present×3／partial×2／absent×1，一筆都沒硬改），三處實質收斂：
        - ⭐ **一筆「看似新」的假缺口在開卡前就被 grep 掐掉**：bc-game 的「Lucky Spin 依段位放大」若不查就會開成一張卡，實查 `luckyspin.js:27-29` 發現 `VIP_MULT` 本來就在 ⇒ **「先 grep 再斷言」本輪連續兩次直接省下假卡**（另一次是同輪 stake 的 challenges 資格那半）。這是 07-30 `wagerFree`、08-05 CoinsBack、08-10 catchup 輪 bet365 兩筆之後的同族。
        - ⭐ **據實收窄自己 08-05 記下的缺口，並改指真病灶**：08-05 記「缺有邊界 FTUE 多日軌」——本輪查 `rewards.js` 檔頭第 7 行「第 30 天後日獎 plateau、不再觸發里程碑（**旅程完成**）」+ 里程碑落第 8/15/22/30 天 ⇒ **那已經是一條有邊界、back-loaded 的多日軌**，舊記載過度悲觀。**真病灶是零容錯**：`grace|寬限|freeze|補簽|保護` 於 `rewards.js` **0 命中**、`:217` 斷簽即歸零、`:53` 里程碑只在**精確**連續第 8 天觸發 ⇒ **第 3 天漏簽的新手必須重新連滿 8 天**，而 Zonko 那條 back-loaded 8 日軌存在的理由正是第 6–8 天為流失懸崖 ⇒ 缺口重新定義並開卡 **#84**（「處置管道沒有帳可查」家族第 9 筆：07-02 首記、08-05 複記、兩次都沒開卡）。
        - **容器採用度是升的、且有機械證據**：`promoCal.register` 外部註冊者 **2→3**（新增 `core/progress-src.js:725`＝#75，其原始碼註解自己就寫著「第三個外部註冊者」）；但 `grep audience core/promo-cal.js` **仍 0 命中** ⇒ 促銷本身仍無分群/A-B、**維持 partial 不升 present**（#54 的 `AUDIENCES` 是現成零件，但接上去是另一筆工作，未做即不記已做）。
    - **step4 實作 #81 加成觸發軸**（詳見卡片落地段）：`core/rakeboost.js` 新增 `trigger(id)`／`registerTriggered()`，把「限時窗口」從三筆種子各自手刻的時間戳**收斂成一條容器化路徑**；成本安全是**恆等式而非宣稱**（新加成一律走既有 `resolve()` 取最大不相乘 + 站別 `CAP` ⇒ 真站硬不變量 `maxPct(live)×CAP.live = 0.2175 < 1` 自動續成立）。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；**分段提交**（調研+台帳+開卡先單獨一筆，實作再第二筆）；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json` 與未追蹤的 `Game assets/00*` 目錄。**下輪首要＝台帳最舊分類 `資安`（5 模組 08-05，本輪後為全庫唯一最舊）**；再取材前仍須先汰一筆（bc-game 1/2、bet365 1/2，兩張皆 08-17/08-24 才複驗，且**兩張都是 tier-1** ⇒ 若下輪仍汰不掉，建議改評估 courtside／mega-dice／thrill 這幾張 tier-3）；實作候選 **#83**（本輪新開、零回歸恆等好驗）或 **#84**（S、不新增送幣來源）。

- **2026-08-10（平台軌 · **catchup 輪**（dark 72.7h）· 深挖 rollbit + bet365 · 台帳審「金流」5 模組 · 開卡 #81/#82 · 實作 #76 簽到揭曉化）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-153610-0f0c`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**dark 72.7h（08-07T14:55 → 本輪 15:36）> 24h ⇒ catchup，禁讓路**（`lead_track=games` 本不准讓路亦不適用）。**三軌同為 08-07→08-10 群體重啟**（App 週末關閉）——**判定為非凍結**：三軌 dark 時數一致、無懸空鎖、無孤兒 WIP、無被擋窗累積跡象（與 07-31 那次 stdin heredoc 掛死的形狀完全不同）。
    - **並行安全**：遊戲軌 15:5x 同為 catchup 輪且**同時在跑**，但只寫 `intel/db/games-catalog.json`＋`providers.json`（mtime 15:41/15:42 實測）＝**與本輪零檔案重疊**（本輪寫 `platforms.json`＋`platform-modules.json`）。依 §7 逐檔 add、**未碰**其 2 檔、未碰 `prototype/games/registry.json`（他人 08-03 起未提交）與未追蹤的 `games/slot-engine/`、`Game assets/` 增刪雜訊。STATE 全程用 **targeted edit** 而非整檔重寫（避免與遊戲軌同檔並行時互相覆蓋）。
    - **調研**（`max_platforms_per_run:2`）：**刻意不新取材、配額全用於深挖**，三條可查證理由：① ACTIVE overdue **0/33**（最早到期 08-11）；② active 仍 **33 > CONTROL「≤ 約 32」**，再取材只惡化（前兩輪皆已明載「再取材須先汰一筆」）；③ 兩張到期票恰為 tier-1/2 且皆 14 天未查。
        - **(a) rollbit**（T2 到期 08-11）：⭐**淨新＝「領取」本身觸發個人限時窗口**——Rewards Calendar **每領一次 → +15% Rakeboost 持續 60 分鐘**（官方 blog + 評測雙源一致），**可重複觸發、一天多次**。關鍵在於它與 ApexWin 已有的兩種觸發源**皆非同物**：`newcomer`＝**註冊時間**驅動、`optin`＝**加入活動**驅動 ⇒ 這是第三種。更根本的 grep 發現：`core/rakeboost.js` 三筆種子**各自手刻一份時間戳存取**（newcomer 自刻 `HL_RB_NEWCOMER`、optin 借 `promoCal.joinedAt`）＝**「限時窗口」這件事本身沒有被容器化**，第三筆要加就得再刻第三份 ⇒ 開卡 **#81**（觸發軸）。VIP 段位數**兩源衝突兩記不擇一**（27 級/7 段位 vs 4 級 Silver→Diamond，沿用 legendz 08-06 處置）。「返還最高 70% 莊優」附逐筆算式（## 分析師日誌（最新 3 則；歷史見 [BACKLOG-archive.md](BACKLOG-archive.md)）

00×5% 莊優→返 $0.75）⇒ **寫進 #72 卡體**（第三平台佐證 + 具體 spec），**非新卡**。
        - **(b) bet365**（T1 降頻 14 天後首次到期）：**對 ApexWin 增量歸零**。本輪唯二看似新的訊號**經 grep 皆證實已有** ⇒ **假缺口不開卡**：「點數換 bonus funds／free bets」→ `HL.shop`+#42 已同構；「多數紅利免流水（No Wagering 行銷標語）」→ `badd(n,{wagerFree:true})` 自 **#20（`231e791`, 07-10）**即存在、2 個生產呼叫點。留存哲學第 4 次一致 reconfirm（casino 端仍無階梯 VIP、VIP 僅邀請制）。⚠️ **saturation watch 1/2**：本站 07-03/07-10/07-17 曾**連 3 輪零增量**，但 07-28 有 1 筆小增量（opt-in→#52）**打斷連續計數** ⇒ 依 08-07 判準（連續 ≥2 輪）**本輪刻意不判 saturated、守判準不便宜行事**，下輪（08-24）仍零增量即達標。已寫入 `platforms.json` 的 `saturation_watch` 欄位使其**可機械追蹤**（而非只寫在 dossier 散文裡＝08-06 記載「處置管道沒有帳可查」的修法）。
    - **台帳審「金流」5 模組全審**（`last_audited` 08-05＝與活動/資安並列最舊）：**5 項狀態全維持**（partial/present/absent/partial/partial，據實不硬改），evidence 三處實質收斂：
        - ⭐ **「出口 vs 提及」判別法首次實戰**（兌現 08-07 對 grep 硬規則提出的補強）：`提款審核佇列` 的 naive grep 現命中 **3 筆，但全部是 `core/service-level.js:35-38` 一段「明文說明自己不做這件事」的註解** ⇒ 零真實出口、維持 absent。**若不逐筆看就會反向誤判成「已經有了」而永久跳過真缺口**——方向上比漏報更糟，正是 08-07 提出此判別法的原因。
        - ⭐ **新查獲一筆「半邊接線」，但判定為正確設計、刻意不開卡**：`ledger.js:33/37` 的 `TYPES`/`INTERNAL` 都宣告了**對稱的 `p2p_in`**，實測**零生產呼叫點**（全站僅出現於 ledger.js 自身宣告/註解/自測 fixture），只有 `p2p_out` 真被寫入。**但 Demo 是單機 localStorage、根本不存在真實收款方**，客端無從代對方入帳 ⇒ 記為「**預留型別**，真正接線點在 phase7 伺服器端」。**這與 07-30 `wagerFree` 假缺口方向相反**：那次是「有能力但台帳沒查到」，這次是「台帳查到卻不該當缺口」——兩者都會誤導下一輪，故都值得留痕。
        - **缺口形狀再收斂（因 #74 落地）**：`獎金/流水引擎` 08-05 記的 partial 理由（二元開關、無逐筆自訂倍數）**已被部分兌現**——`progress.js:38-41` 現以 `LEGACY_WAGER_MULT` 為 fallback、實際倍數由 `HL.sla.bonusWagerMult(段位)` 提供 ⇒ 倍數**已可變**。但**維持 partial**，因變動軸是「**玩家段位**」而非「**這筆紅利**」（仍無法表達「這筆活動獎 3×、那筆儲值禮 8×」）⇒ 剩餘缺口精確為「逐筆紅利 spec 的 `wagerMult` 數值覆寫」，且上線時須比照 `service-level.js:380` 的護欄**禁止 0×**（零流水只該由 `wagerFree`／來源決定）。
        - `支付/收銀台` **連續第三次審計實測 0 命中**（07-30/08-05/08-10 皆同）、`FIAT_METHODS`/`CRYPTO_COINS` 形狀未變（仍為 `{ic,n}`／`{code,net,ic}` 純展示常數）⇒ 是全台帳**最穩定不動**的缺口之一、非量測誤差 ⇒ 升格開卡 **#82**。
    - **開卡 #81／#82**：**#81**（加成觸發軸·S–M）的成本安全是**恆等式而非宣稱**——新加成一律走既有 `resolve()`（**取最大不相乘**）與站別 `CAP`，故真站既有硬不變量 `maxPct(live)×CAP.live = 0.2175 < 1` **自動繼續成立**；卡上另釘死「同 id 重複觸發取較晚到期而非累加時長 + 單日次數上限」（否則領 10 次＝10 小時加成＝變相全天加成）。**#82**（支付通道註冊表·S–M）＝**三次審計未認領的 `stowable_note` 升格為卡**，是 08-06 記載「處置管道沒有帳可查」家族第 8 筆；**刻意只做 CONTROL.avoid 允許的那半**（avoid 列的是「真金流**串接**」，而「述詞層 + 預設全關的骨架」正是 SKILL 鐵律明文允許的「開發完成 + flag 停用的可收納骨架」），並要求述詞表**複用 #54 `release.js` 的 audience 形制、額度委派 #70/#63**，不造第二套。

- **2026-08-07（平台軌 · **14:00 窗**建置輪 · 消化船長 [M8] · 台帳審「資料」3 模組+新增 1 · 實作 #80 內建遊戲延遲載入）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-141030-9d4c`（帶心跳）→ 停頓做源碼調查與 harness 基線 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **5.4h**（08:45→本輪 14:10）未達 catchup；`lead_track=games` 准讓路，但**船長指令區有一條直接點名平台軌且帶時限的新項 [M8]** ⇒ **做而不讓路**。
    - **本輪定位＝服從船長指令而非跑常規調研配額**：[M8] 明文要求「**平台軌在下一個新增 `<script>` 的建置輪前**先評估 code-splitting／lazy-load」。而佇列上的實作候選（#76／#78）**都會新增 `<script>`** ⇒ 若照常規順序做，就會**正面違反該指令**並把首屏推過門檻。故本輪先解載入架構。**刻意不做新平台取材**，理由三條且皆可查證：① **ACTIVE overdue 0/33＝連四輪歸零**（最早到期 08-11、尚有 4 天）；② active 仍 **33 筆 > CONTROL 的「≤ 約 32」**，再取材只會惡化（前輪已明載「下輪再取材須先再汰一筆」）；③ 本輪工作量已由 [M8] 佔滿。⇒ 這是「有真工作可做」而非閒置，`consecutive_idle_rounds` 維持 **0**。
    - **台帳審「資料」3 模組**（`last_audited` 08-05＝與金流/活動/資安並列最舊；前端UI/UX 今早 08:00 窗才審過）：三筆**狀態全維持**（報表與匯出 partial／資料分析 partial／注單 present），但收斂出一條**量測法更正**——⭐ 08-05 記的「`betlog.js` 為全站唯一真匯出出口」**結論仍成立**，然而 naive `grep csv|CSV|Blob|download` 現命中 **4 檔**，其中 3 檔全是**註解或 i18n 譯文**（`demo-tools.js:59` 註解／`fair.js:157` 註解／`i18n.js` 的「⬇ 匯出 CSV」等）⇒ **「斷言缺 X 前先 grep」這條硬規則要再加一層：grep 命中必須分辨「出口 vs 提及」**。方向很重要——這類誤判會讓引擎誤以為「**已經有了**」而放過真缺口，**比漏報更糟**（漏報只是慢，誤判是永久跳過）。另兩筆機械複驗：`eventSchema|funnel|cohort` 全 src **仍 0 命中**（產品側分析零進展）、`betlog.js` 仍 265 行且中央寫入點 `live-stats.js:47` 未回退。
    - **實作 #80 內建遊戲延遲載入 `HL.lazyGames`**（詳見卡片落地段）：新增 `src/data/lazy-games.js`，把 19 個「自帶 render」的遊戲 view（**22 款遊戲**，instant 12＋table 6＋slot 4 家族全覆蓋）移出首屏——meta 開機註冊（大廳卡照樣可點）、程式本體**首次開啟才注入**。**首屏 1559KB/97 支 → 1338KB/78 支（−221KB／−19 支）**，門檻餘裕 41KB → **262KB**（[M8] 的「1–2 輪內觸警」風險解除）。
        - ⭐ **關鍵設計＝換手動作就是 view 檔自己那句 `HL.games.register`**（同 id 覆蓋 stub）⇒ **19 個 view 檔一行未改**。這也是為什麼本卡是 M 而非 L：不是「改 19 個檔」，是「加一個容器 + 從 `index.html` 移走 19 行」。形制刻意與同仁放置區（`registry.json` + `games-loader.js`）同構以共用心智模型。
        - ⭐ **新風險面只有一個，已做成機械鎖**：清單 meta 與 view 檔 meta 若漂移，大廳卡會在開啟瞬間改名/變色。`platform/lazy-games-meta-parity` **靜態解析** view 原始碼逐欄比對——**不能用 `require`**，因為這些檔的 `register` 呼叫在 DOM guard 之後、node require 會提早 return 拿不到那個物件（此即遊戲軌 node 契約重構留下的副作用，本輪第一次被當成約束條件處理）。
        - ⭐ **順手把 [M8] 這類故障的「無閘」根因也補了**：`platform/first-screen-budget` 把 M6 協定的「每輪手打 node 一行量首屏」變成**會 FAIL 的常駐閘**（>1600KB 或 >120 支）。M6 自 07-31 立了門檻卻**只靠人自覺量**，於是首屏一路長到剩 43KB 才被看見——現在超標會直接紅燈。
        - **驗證**：node fast **89→92 全 PASS**；**負向擾動 9/9 全被抓**（清單 title/顏色漂移、id 指錯、src 不存在、延遲檔又被靜態掛回、容器漏掛、KB 超標、script 數超標、清單違規帶 render）。preview 實測：**`loadAll()` 前後 66 張卡 × 15 欄快照逐位相同（0 差異）**＝大廳載入前後不可區分；開機**零延遲檔被抓取**且 7 個遊戲數學命名空間全部不存在＝真沒載；Keno 走真實玩家路徑「占位→換手→真棋盤」完成；baccarat/gem-storm/towers 皆正常且 gem-storm **🔒 保真旗標仍在**（G4 白名單未受影響）；**中央結算鏈完整**（延遲載入的 dice 走 `liveStats.record` 後 turnover/GGR/betCount/JP/VIP 全前進）；375px 零溢出、頁面本體不橫捲；零新 console error。sw **v152→v153**。
        - ⚠️ **一個誠實記下的取捨**：瀏覽器測項 `games/buyin-price-single-source` 在四款買入 slot 未載入時**由 PASS 轉 SKIP**（該測項原本就寫了 `if(!checked) t.skip(...)`＝作者已預期）。**數值正確性不受影響**（權威覆蓋在 node `checks-games.js`，直接 require、與載入時機無關），開過遊戲即恢復 PASS，亦可 `HL.lazyGames.loadAll()` 先備齊。
        - ⚠️ **一個 preview 量測陷阱（新記一則，與 08-06 `HL.dock.ids()`／08-07 i18n MutationObserver 同族）**：`navigate` 到 `/_baseline_ab.html?demo=1` 時**路徑被吃掉、實際停在 `/`**，若不查 `location.href` 就會把「改版後的頁面」誤當成 HEAD 基線來對比（我一開始就這樣誤判了一次，是 `lazyPresent:true` 出現在「基線」上才發現）。⇒ **A/B 基線比對不要靠另開一頁，改用同頁前後對比**（本輪最終用 `loadAll()` 前後快照，反而是更強的證明：後者狀態**定義上**等同 HEAD 的全靜態載入）。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON／擾動腳本一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；臨時基線檔 `prototype/_baseline_ab.html` **已刪除**；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json` 與未追蹤的 `games/slot-engine/poc-spin/`。**剩餘平台軌待處理**：**P3**（維護軌 U33 已於本日 12:00 收官，可考慮結案）、**P6**（`saturated` 逐站解法已落地，全域週期調整仍待船長裁決）；佇列 #57/#58/#59/#61/#62/#64/#68/#69/#71/#72/#73/#76/#77/#78/#79 待做。**下輪首要＝台帳次舊分類＝`金流`/`活動`/`資安`（皆 08-05）**；調研若要再取材**須先汰除一筆**（評估 courtside／mega-dice／thrill 是否達 saturated 判準）；實作候選 **#76**（S、期望值恆等＝零經濟風險）或 **#78**。**⚠️ 交給下一輪的具體機會**：#80 的 `stowable_note` 已寫明第二批可移出首屏的候選——`core/i18n.js` **151KB＝全站最大單檔**，按語言拆檔可再省約 140KB 且「加語言＝加檔」才是正確 i18n 形制（動它須防 §4 SW 快取陷阱）。

- **2026-08-07（平台軌 · **08:00 窗**建置輪 · 深挖 card-crush + 新取材 cybet + 汰除 deal-or-no-deal-win〔新增 `saturated` 退場機制〕+ 台帳審「前端UI/UX」4 模組+新增 1 + 開卡 #78/#79 + 實作 #75 進度加速器）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-081030-4e7a`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark 11.4h（08-06 20:45→本輪 08:10）未達 catchup；`lead_track=games` 准讓路但前輪已明文指派三項工作 ⇒ **做而不讓路，三項再度全數命中前輪預測**（台帳次舊分類＝前端UI/UX／取材前先汰除一筆增量歸零的 tier-3＝deal-or-no-deal-win／實作候選 #75）＝跨輪指派**連續第五輪**乾淨運作。
    - **調研（ACTIVE overdue 連續第三輪歸零，故配額用於「深挖 1 + 新取材 1」＋一筆汰除）**：
        - **(a) card-crush 深挖刷新**（`last_investigated` 07-10＝全庫最舊、28 天）：四項招牌（卡牌對戰／稀有度四階／重複卡升階／ELO 榜）**全數複驗成立**，但**查出三筆首挖完全沒記到的機制**。⭐**本輪最強新素材＝社群彩池是 opt-in 側注**（slot 每轉多付 0.1 MC 才取得 Mini/Minor/Major/Grand 四級彩池**資格**，保證池 ≥50,000 MC、實測見 108,000+）⇒ 對照 `core/jackpot.js`：ApexWin 的 `onBet()` 是**每筆下注隱形抽成**（假站 0.5/0.3/0.2%、真站 0.15/0.1/0.05%）且**資格無條件** ⇒ 玩家不知道自己在付、不能選擇不付、也沒有對價感 ⇒ 開卡 **#78**（資格軸）。另兩筆**刻意不開卡並各附理由**：**賽季制卡組會過期**（Season 1/2 的收藏已不能用於對戰）＝**反向結論**，ApexWin #45 終身成就與 jackpotter 佐證的「XP 永不重置」是行銷賣點，讓收藏過期屬玩家敵意設計；**忠誠點來自每日 10 場自動對戰**＝自動入榜 ApexWin 已對齊（`tournament.record` 掛中央結算點），但「**進度來自勝負而非押注量**」是 #65 註冊表尚不存在的**來源類別**（現有三筆皆量型）⇒ 寫進 #65 後續候選。並**自我更正一處首挖過度推論**（「MC 一律購買取得」→ 另有 Quests/Mystery Drops 小額免費發放）。
        - **(b) cybet 新取材**（coingabbar/bitdegree/coingape 2026-08 新站榜點名，四家評測交叉）：⭐**最強新素材＝「玩家自選目標合約」**——VIP Mission Mania 讓玩家**自行挑一個 30 天 XP 里程碑**，達成付 $600–$3,000、期間另有 10–15% 淨損回饋 ⇒ ApexWin 的**每一個**進度目標都是平台出題（`tasks.DAILY`／`challenges.DAILY`／#46 賽季軌／#45 成就／#15 錦標賽），玩家從不參與「做什麼／做到多少／多久內」⇒ 開卡 **#79**。另三筆**只記不開卡**：`.b` Bonus Coins（每注累積、比率隨段位放大、過門檻可提）＝**ApexWin `HL.rakeback` 同型已具備**；期間淨損回饋＝#33/#48 已覆蓋、只差「綁在某目標期間內」（列為 #79 的選配欄位）；**VIP 轉移／他站段位匹配**＝純真金拉客、需人工審核，屬 avoid 精神。⚠️ **誠實定位**：該站評測普遍指出**遊戲化程度低**（僅一個將到期的錦標賽、無 missions/challenges）＝**可學的是那一項 VIP 設計、不是整站**；且本輪未親訪官網（url 待下次確認）。
        - **(c) ⭐ 新增 `saturated` 退場機制（本輪的引擎級產出）**：CONTROL 的「清單維持精煉（≤ 約 32 筆）」已被連兩輪新取材推破，前輪明文指派「取材前先汰除一筆增量歸零的 tier-3」。本輪**不直接刪除**（dossier 與已萃取佐證仍有稽核價值），改在 `platforms.json` 新增 **`status: "saturated"`**＝「仍在營運，但連續 ≥2 輪逐項對照後對 ApexWin 的增量歸零」，保留該筆但把週期拉到 180 天以**釋出調研配額**，並要求附 `saturated_reason` 與可查證理由、重大改版時手動改回 `done`。首例＝**deal-or-no-deal-win**（08-06 已以 `git log -S` 證實其唯一缺口早由 #42 落地）。⇒ 這是 **P6「刷新速率追不上到期速率」的逐站證據制解法**，與「全體 tier-3 週期一律拉長」（＝降低整體調研密度、仍待船長裁決）**刻意區分**。現況：總 35 筆＝**active 33**／saturated 1／defunct 1；**ACTIVE overdue 0 ⇒ 連三輪歸零**。⚠️ 誠實記一筆：本輪汰一筆又取材一筆 ⇒ active **仍是 33、未降到 32**，下輪若再取材須先再汰一筆。
    - **台帳審「前端UI/UX」**（4 模組 `last_audited` 08-04＝全 8 分類最舊，輪替與新鮮度兩準則一致）：三個 present 全部複驗維持（大廳種子卡 **51 筆 playable 2/49、分佈與 07-30/08-04 三輪逐位相同＝零漂移**；手機主導覽漢堡→抽屜三構件仍在線；game-frame 六顆工具與 `isPF` 單一真相未回退）。**兩筆實質產出**：① ⭐**把「維護軌 U33 到底有沒有效」變成數字**——逐檔覆蓋率（同 08-04 量測法）`season.js` **6%→40%**、`guild.js` **9%→47%**、`promo-cal.js` **12%→30%**、`safetynet.js` **→62%**，`dock-growth.js` 維持 94% ⇒ **U33 確實在生效**，且**當前最低者恰為 `promo-cal.js`＝U33 自己列的最後一項 #49**（兩軌各自獨立收斂到同一結論）；同時**首次量化該指標的誤差**（`selftest.js` 0/58、`rakeback-core.js` 0/51、`progress-src.js` 39/149 皆為測試訊息灌大分母）⇒ 明訂**只對面板型 UI 檔有效、全站聚合值不可當 KPI**（本輪 44.1% 與 08-04 的 38.9% **分母不同 3634 vs 2404 故不可相減**）。② **新增一個 absent 模組「大廳分群軸（節奏／波動／RTP 型）」**＝兌現 DoND 08-06 記下的 UI/UX 候補：grep 實證現有分群軸**全是目錄型 metadata**（all/hot/new/fav/community/author/cat + 四種排序），51 張卡**帶 volatility 者 0、帶 rtp 者 0**，各遊戲 RTP 只以顯示字串存在 ⇒ **但屬「有能力未接線」而非「沒能力」**（#50 `HL.edge` 已握有 22 款數值型莊家成本係數）。**另更正一筆跨輪不可比的計數**：08-04 記的「`HL.games.register` 全站 62 處」係口徑不明（用 `git grep` 回到該 commit 重算，發現含 `.md/.html/.json`）⇒ 改記自述口徑 **src 49 處/24 檔 + 放置區 6 處/4 檔**。
    - **step4 實作 #75 進度加速器**（詳見卡片落地段）：`core/progress-src.js` 加第二層＝**加速註冊表**，`grant()` 改走 `xpForBoosted(...)`、**呼叫端一行未改**。首批兩筆種子＝季票進階軌 ×1.2 與 #49 促銷日曆的 opt-in 限時加速 ×1.5 ⇒ **`promoCal.register` 外部註冊者 2 → 3**（台帳連兩輪盯的「容器採用度」指標**首次由平台軌自己推進**）。四條不變量全部機械化：**取最大不相乘**（1.4+1.2 ⇒ ×1.4，preview 面板肉眼可見）／**真站零加速＝恆等式**（純核心 `BOOST_CAP.live = 1.0` 夾死，刻意傳 ×9、×1e6 皆無效，真站 XP 流入與落地前逐位相同）／**乘數不得穿透每日上限**（先乘後夾：儲值帶 ×1.5 仍恰 20,000）／**<1 不得減損進度**。node **84→88**、瀏覽器 **65→71**（含 `panel-opens` 面板實開鎖＝主動照做維護軌 U34 的通則）、**負向擾動 7 項中 6 項被抓**，**第 7 項誠實記為「不可達」而非漏洞**（拿掉下限夾子後仍全綠，因 `var best = 1` 初值本身即保證下限 ⇒ 該行是防禦性冗餘、`rakeboost.resolve` 同形）。真實 e2e：未加速 1000→+1000、加入促銷後 1000→**+1500**，兩次的**餘額與帳本送幣成本 delta 皆恰 0**。sw **v150→v151**。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json` 與未追蹤的 `games/slot-engine/poc-spin/`（已在台帳註記為另一 session 的進行中工作）。**剩餘平台軌待處理**：**P3**（維護軌 U33 接手中、本輪已量化其進度）、**P6**（本輪以 `saturated` 機制提出逐站解法，全域週期調整仍待船長裁決）；佇列 #57/#58/#59/#61/#62/#64/#68/#69/#71/#72/#73/#76/#77/#78/#79 待做。**下輪首要＝台帳次舊分類＝`資料`（3 模組 08-05）或 `金流`/`活動`/`資安`（皆 08-05）**；調研配額若要再取材**須先再汰除一筆**（下輪請評估 courtside／mega-dice／thrill 是否已達 saturated 判準＝連 2 輪增量歸零）；實作候選 **#76**（S、純呈現改造且期望值恆等＝零經濟風險）或 **#78**（本輪新開、與 #72 互補）。
- **2026-08-06（平台軌 · **20:00 窗**建置輪 · 深挖 legendz + 新取材 gokong + 台帳審「擴充性」4 模組 + 開卡 #76/#77 + 實作 #74 紅利流水倍數成為 VIP 維度）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-201200-8c4f`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark 4.6h（15:35→本輪 20:12）未達 catchup；`lead_track=games` 准讓路但前輪已明文指派三項工作 ⇒ **做而不讓路，三項再度全數命中前輪預測**（台帳次舊分類＝擴充性／配額維持「深挖 1 + 新取材 1」／實作候選 #74）＝跨輪指派**連續第四輪**乾淨運作。**本輪是 08-06 的第三個平台輪**（08:00〔功能・#66〕→ 14:00〔後台・#65〕→ 本輪〔擴充性・#74〕），三輪工作完全不重疊。
    - **調研（overdue 已歸零，故配額用於「深挖 1 + 新取材 1」）**：
        - **(a) legendz 深挖刷新**（全庫最早到期 08-08、07-09 首挖後首次複查）：⭐**推翻自己 07-09 的一半結論**——當時寫「唯一新素材＝社交運彩，留存機制皆重疊」，本輪用深度評測逐項對照，**留存側查出兩條真缺口**：① **Daily Drop ＝ Plinko 式每日掉落**（不是固定值日獎）＋ GoKong 的爪機 Bonus Crab ⇒ 「每日獎揭曉化」成**兩平台共識** ⇒ 開卡 **#76**；② 任務**綁定一組遊戲**（「Legendz-branded slots」）⇒ **#64 第二平台佐證**，且形制與 SpinBlitz 不同（綁一組 vs 綁單款）⇒ 已寫進 #64 卡體。另**兌獎 SLA 帶自罰條款**（核准後 2h 未完成補 10 free spins）＝#63 第七平台佐證，但**刻意不開卡**（demo 提款即時到帳、無可違約的時效）。段位數兩源衝突（5 段 vs 7 段×3 stage）**兩記不擇一**。
        - **(b) gokong 新取材**（cryptonews 2026-08 榜點名其遊戲化最完整；四家評測交叉）：⭐**最強新素材＝獎勵「任務完成量」本身**（40+ 挑戰之外，**每週完成 20／40 個另有獎**）⇒ ApexWin 兩套日常系統（`HL.tasks`／`HL.challenges`）都只獎勵單一達成、**期間完成計數維度完全不存在** ⇒ 開卡 **#77**。另三筆**只寫進既有卡不另開**：**VIP 依最近 90 天重算**⇒ **#59 第三平台佐證**（與 Punkz 同派、Stake 為反例，本卡原設計恰為兩派交集 ⇒ 設計不改、只上調佐證，並新增「窗長應為 config」一句）；**cashback 僅 L3+** ⇒ **#73 由單平台升三平台**（連同 Jackpotter），卡首的「⚠️ 單平台佐證」標註**已撤回**；**Coins 來自儲值+投注** ⇒ **#65 第五平台佐證**且與上輪落地的 deposit/checkin 完全對上。
        - **新鮮度**：`LIVE overdue 0/33` **維持歸零**；platforms 33→**34 筆**（另把 mega-frenzy 的 `status` 由 `done` 據實改 **`defunct`**——它的 note 自 07-05 就寫「已關站」、`next_due` 已推到 2027，但 status 欄一直讓死站被算進逾期率分母）。⚠️ **本輪留一個規模提醒**：CONTROL 的「清單維持精煉（≤ 約 32 筆）」現已被連兩輪的新取材推到 **33 個 live**，**下一輪若要再取材，建議先汰除一個增量已歸零的 tier-3**（現成候選＝deal-or-no-deal-win，08-06 08:00 窗已證實其增量歸零）而非續長。
    - **台帳審「擴充性」4 模組**（與「前端UI/UX」並列全庫最舊 08-04）：⭐**又抓到一筆「欄位腐爛」（第 7 筆）**——`內容/遊戲上架排程 × 受眾分層` 記 **absent**、且 08-04 那次複審還附了「grep `earlyAccess|releaseAt|audience` 命中 0」的機械證據，**但它在次日就過期**：#54 已於 08-05 落地為 `core/release.js`（本輪實測 `HL.release` 12 個 API 出口 + casino 消費端 + node `release` 群測項）⇒ **absent→present**。另兩筆維持 partial 但數字大幅前進（`自動化測試`：node **32→79**、瀏覽器 **30→65**、`registerTests` 採用 **10 個 core 模組**，08-04 記的「核心經濟模組零數學測項」已推翻大半；仍缺 CI／**版面回歸**／`HL.bonus` FIFO 數學）。**`模組化/可停靠佈局` 更正一條量測法陷阱**：`HL.dock.ids()` 開站當下回 **`[]`**（三個註冊全是惰性、且 `main.js` 的觸發點在登入後才跑的 shell 路徑上）⇒ **採用度的權威量測是 grep `HL.dock.register` 的註冊點數，不是執行期 `ids()`**（08-04 記的值沒錯，但那是「觸發後」的值；任何拿 `ids()` 當選單資料源的 UI 會在未開過面板時得到空陣列）。
    - **step4 實作 #74**（詳見卡片落地段）：把「紅利流水倍數」做成 #63 `HL.sla` 維度表的**第五筆 spec**，`progress.js` 的扁平 `var WAGER_MULT` 改為求值出口、**只在 `badd()` 求值一次**。三個不變量全部機械化（凍結／真站平均恰 8.0× 成本中性／單調不變差）＋一條反向護欄（任一段位不得 0×）。node **77→79**、瀏覽器 **61→65**、**負向擾動 6/6 全被抓**、真實 e2e 證「升段後既有 entry 的 req 逐位不動」、三語零殘留、375/1280 零折行零裁切。**⚠️ 未做卡上第③項的一半**：「#72 說明中心加一欄」因 **#72 尚未實作**而無處可加，已在卡上註明等 #72 落地時補。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`。**剩餘平台軌待處理**：**P3**（維護軌 DEBT U33 接手中）、**P6**（建議降級為「觀察中」，overdue 已連兩輪歸零）；佇列 #57/#58/#59/#61/#62/#64/#68/#69/#71/#72/#73/#75/#76/#77 待做。**下輪首要＝台帳次舊分類＝`前端UI/UX`（4 模組 08-04，現為全庫最舊）**＋調研維持「深挖 1 + 新取材 1」但**取材前先汰除一筆 tier-3**（見上）；實作候選建議 **#75**（S、#65 的自然第二個消費者、乘數層）或 **#76**（S、純呈現改造且期望值恆等＝零經濟風險）。

- **2026-08-06（平台軌 · **14:00 窗**建置輪 · 補刷 capyspin〔**LIVE overdue 1→0＝首次歸零**〕+ 新平台取材 jackpotter + 台帳審「後台」7 模組 + 開卡 #74/#75 + 實作 #65 進度來源註冊表）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-141130-c7e5`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark 4.1h（10:05→本輪 14:11）未達 catchup；`lead_track=games` 准讓路但前輪已明文指派三項工作 ⇒ **做而不讓路，且三項全數命中前輪預測**（刷 capyspin／台帳次舊分類＝後台／實作候選 #65）＝跨輪指派在無記憶架構下**連續第三輪**乾淨運作。
    - **調研**（`max_platforms_per_run:2`）：**LIVE overdue 首次歸零（1→0/33）** ⇒ 依前輪指派把配額改為「**深挖 1 + 新平台取材 1**」。
        - **(a) capyspin 刷新**（全庫當時唯一逾期）：⭐**把 #65 從三平台推到四平台共識，且它是唯一多出一條來源的**——VIP 進度貨幣「Growth Points」明載 `earn Growth Points through **daily logins and purchases**`，其餘三站（Spree/BigPirate/DoND）只有「購買」⇒ **本輪 #65 首批兩個新來源 deposit/checkin 恰好對上它這兩條**。另兩筆：**Monthly/Season Pass 賣的是進度速度**（`climb the VIP ranks that much faster`）而非獎金額度 ⇒ 開卡 **#75**；「更高每日兌換上限」再佐證 #63。⚠️ **兩處誠實處理**：① **來源衝突不擇一**（sweepskings 記 6 段 Bronze→Master／stakester 記 Bronze 一週可達+15% 週返水+優先提領、Platinum 專屬客服+25% rakeback），兩記並存；② **推翻本專案 07-06 自己的判斷**——當時寫「不要依 CapySpin 開公會團戰卡（來源不足、**已證偽**）」，本輪兩份獨立評測皆明確描述 guild battles + 群體任務（pool spins 換團體乘數）⇒ 「已證偽」撤回，但**仍不開卡**、理由由「來源不足」更正為「**#47 已落地、剩下的協力目標層已在 #47 卡的下一步**」。⚠️ 反面教材一則：該站唯一一致負評＝「餘額低時彈窗沒完沒了」⇒ ApexWin faucet/促銷/揭曉提示的明確禁區（#66 佇列合併方向正確）。
        - **(b) jackpotter 新取材**（2026-08 crypto 榜與 Thrill 並列「新站標準設定者」；AskGamblers/WizardOfOdds/Trustpilot 皆有評測頁＝有實體）：⭐**最強新素材＝它把 VIP 當「條款軸」而非「金額軸」賣**——VIP 專屬 wager-free／極低流水獎勵 ⇒ 對照出 `WAGER_MULT` 是**扁平常數**（真站 8×/假站 1×、只分站別**不分段位**、且是載入期一次求值的 module 級 `var`）⇒ 開卡 **#74**。另三筆**刻意不開卡並各附理由**：獎勵節奏依段位解鎖（Daily 需 Sorcerer+/Weekly 需 Mage+/Monthly 需 Wizard+）⇒ **#73 由單平台升兩平台**、卡首「僅單平台佐證」可撤；Mystery Box 品質隨段位（13 階 Wooden→Cosmic）⇒ **#66 已做**（先 grep `core/reveal.js` 才下此結論）；明文行銷「XP 與段位永不重置」⇒ **ApexWin 已對齊**（VIP 累積本就不重置、賽季另計），只剩溝通面歸 #72。⚠️ 段位命名兩套說法（box 階 vs 法師系）**不擇一**。
    - **台帳審「後台」7 模組全審**（該分類 `last_audited` 08-03＝全 8 分類最舊）：**7 項狀態全部維持、無一升降**（據實不硬改），但 evidence 大幅更新且收斂出**一條貫穿本分類的病**：⭐**「能力已存在、只是沒接到需求端」**——① `GameList 管理` 的缺口性質由「沒有能力」**改判為「能力已存在但未接線」**（#54 `release.js` 明載可為未註冊遊戲先排程，但它與放置區 `registry.json` 是兩套獨立清單、registry 仍是裸路徑字串陣列）；② `活動/紅利設定` 實測 `promoCal` **外部註冊者仍是 2 個**（rakeboost/release）、與 08-05 相同＝**本窗零成長**，仍 partial 的唯一理由仍是「促銷無分群/A-B」（#54 述詞表可複用卻仍未接）；③ `金流模式/法域切換` 的 jurisdiction 軸仍不存在，但**已有容器承接**（#54 述詞表加一個 `region` 述詞即可）⇒ 記為低成本候選、因卡額已滿不立卡。⇒ 建議把「述詞表接 promoCal」與「registry 接 release」合成**一張接線卡**一次做完。另：`營運後台儀表板` 的「唯讀」本輪被**量化**（全檔僅 2 處互動控制項＝切換與關閉）＝它是**報表不是後台**；`會員管理` 是本分類唯一**連鄰近能力都沒有**者＝後台真正的空白核心（維持 ROADMAP LATER、不開卡）。
    - **實作 #65**（詳見卡片落地段）：`core/progress-src.js` 新檔 + 4 處接線，node 72→**77**／瀏覽器 54→**61**。**投注註冊為第一筆＝行為零變更**（真實一注 e2e：Pirots NT$10,000 → `edge.weighted`=16,500，VIP 與賽季各 +16,500 逐位相符）。⭐**達成卡上要求的經濟安全證明，且做成恆等式而非宣稱**：真站非投注來源一律 `xpPerLive:0` ⇒ 真站 XP 流入與改版前逐位相同（真站 e2e：儲值 100 萬+簽到 → XP／VIP／賽季／**帳本送幣成本四者 delta 全 0**，同時真站投注照常）；`register()` 對非 wager 類別**硬夾** live 為 0（刻意傳 `xpPerLive:9` 也夾 0）⇒ 動態註冊永不能成為真站漏口。**為何真站設 0 已量化**：demo 實測 NT$40,000 儲值 → 20,000 XP → 跨 2 子級 → **憑空產生 NT$800 升級金＝2% 儲值返利**。**負向擾動六項全被抓**。⚠️ **一處主動推翻卡上規劃**：卡列的第三個來源「商城消費」**刻意不做**——`shop.js` 點數本身來自 `record(bet)`＝投注衍生，再算一次＝同一筆投注計兩次，且與「選購幣包」不是同一件事（ApexWin 無真幣包商城、其對應物就是 deposit）。
    - **🐞 本輪最大意外收穫＝修掉一個既有 P0**：`progress.js` benefitMatrix 仍引用 **#60（`9fe925d`）更名前的 `RB_RATES`**（現名 `RB_LEGACY`）⇒ **`HL.vip.open()` 自 #60 起整個 VIP 俱樂部面板拋 `ReferenceError`、零渲染，歷時 2 天無人察覺**——根因是**過往驗證從未真的「開過」這個面板**。已修（並**刻意不用** `rakebackCore.edgePctFor`：那回的是「占莊家優勢比例」53.5%，會與同面板「返水率（本級）1.1%」自相矛盾；`RB_LEGACY` 本就是同一份陣列＝精準還原原意，實測矩陣 0.5/0.8/1.1/1.4/1.8% 與本級 1.1% 一致）+ 補**兩條迴歸鎖**（實際呼叫 `HL.vip.open()` 斷言不拋錯/矩陣 6 列/入口鈕恰 1；返水欄必 <5% 防再混入 edge 比例）。**通則已寫進台帳「經濟/RTP 設定」evidence**：三張經濟旋鈕表都是 module 級 `var`，更名/搬移時**沒有任何機械閘會叫出漏改的引用**（node 測項只驗純函式、不呼叫 DOM 面板）⇒ **凡「面板型」出口都應有一條「實際開一次」的瀏覽器測項**（本輪已為 VIP 面板補上，其餘面板仍缺＝可交維護軌）。
    - **i18n（P3 紀律連續第 6 輪成立，但本輪首版踩到 P3 陷阱本身並被抓出）**：+14 EN／+12 zh-Hans。⚠️ **首版把 `每日上限 20,000 XP（已用 0）` 寫成單一文字節點 ⇒ EN/zh-Hans 整列殘留繁中**，而同列純 key 的 `不設上限` 正常翻譯＝**診斷鐵證**；改為「每條 key 各自成元素、數字放裸文字節點」後三語重驗全通（EN 零殘留 CJK）。**grep-first 省 3 個 dup**（複用既有 `經驗倍率`→XP multiplier／`儲值`→Deposit／`每日簽到`→Daily Check-in，且逐一確認語意相符＝防「撞語意」）；`每日上限`／`已用` 簡繁同形故**刻意不列 HANS**（U31 型等值死鍵）。另修 **XP 誤用 `money()` 印成 `NT$`**（XP 不是錢）+ 三處標籤誠實化（`累積有效押注`→`累積 VIP 經驗`）。375px 零溢出、零新 console error（唯一 404 為既有 favicon）。sw v147→v148。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`。**交維護軌兩筆**（本輪未跨軌改）：① 「面板型出口缺『實際開一次』測項」＝上述 P0 的通則，其餘面板（rakeback/season/guild/shop…）仍無此鎖；② HANS 全域等值死鍵實測 **25 筆**（含 08-06 08:00 窗已交辦的 `太棒了，收下 ✓`）。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典，維護軌 DEBT U33 接手中）、**P6**（船長未裁決；**惟 LIVE overdue 本輪歸零＝P6 的急迫性實質下降**，容量問題暫不成瓶頸）；佇列 #57/#58/#59/#61/#62/#64/#68/#69/#71/#72/#73/#74/#75 待做。**下輪首要＝台帳次舊分類＝`前端UI/UX` 或 `擴充性`（並列 08-04）**＋調研因 overdue 歸零可維持「深挖 1 + 新取材 1」；實作候選建議 **#74**（本輪新開、S–M、直接擴 #63 既有維度表、不增送幣量）或 **#75**（S、#65 的自然第二個消費者、乘數層）。

- **2026-08-06（平台軌 · **08:00 窗**建置輪 · 補刷 tier-3 courtside/deal-or-no-deal-win〔LIVE overdue 3→1〕+ 台帳審「功能」13 模組 + 開卡 #72/#73 + 實作 #66 里程碑揭曉層）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-081015-7e21`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark 11.5h（08-05 20:40→本輪 08:09）未達 catchup；`lead_track=games` 准讓路但前輪已明文指派三項工作 ⇒ **做而不讓路，且三項全數命中前輪預測**（清 tier-3 逾期 3 筆／台帳次舊分類＝功能／實作候選 #66）＝跨輪指派在無記憶架構下連續第二輪乾淨運作。
    - ⭐ **本輪主題＝「dossier 的缺口欄位會腐爛」，一輪內連抓三筆**（全部有機械證據，非印象）：① **courtside** 的「Faucet ❌ 完全空白」——`core/faucet.js` 即 #39，**其檔頭第 3 行就寫著「對標 Courtside + CoinsBack 雙平台共識」**＝這張卡正是該 dossier 促成的，卻沒回填。② **deal-or-no-deal-win** 的「唯一增量＝#36 缺機率型兌換」——`git log -S` 證實已由 **#42（`d400894` 命運寶箱 gacha 加權抽層＋獎輪揭曉）**落地 34 天，該站現對 ApexWin **增量歸零**。③ **台帳「支援/透明度中心」記 absent（完全空白）**——實查 `layout/ai-concierge.js`（`HL.partner` AI Luna 罐頭問答 7 組 KB）早已是同型支援面，據實 **absent→weak**。⇒ 這是繼 07-30 `wagerFree`、08-05 CoinsBack 之後的**第 3–5 筆同型事故**，**「斷言 ApexWin 缺 X 前先 grep + `git log -S`」已不該只是提醒，建議升格為台帳與 dossier 的寫入前置條件**。
    - **調研**（`max_platforms_per_run:2`，取全庫最久逾期兩筆）：courtside（逾期 4 天）零新機制，但覆核出「客服＝AI chatbot Coach、數分鐘回應」被評測列為評分項＝**AI 助理已是 2026 標配支援層**（正是這條讓我回頭查出上述第 ③ 筆）；⚠️ 來源衝突兩記不擇一（CC playthrough 1× vs 2×）。deal-or-no-deal-win（逾期 3 天）⭐**最有價值＝#65 升為三平台共識**：其 7 段 Stars 階梯的 **Stars 來源＝「遊玩／任務」＋「選購 GC 幣包」兩條**，與 Spree、BigPirate 獨立收斂；另更正該 dossier 07-04「每日禮隨機」誤記（實為 500 GC 起**遞增確定值**），並記下 ApexWin 沒有的大廳排序維度 `Best for Quick Wins`（依玩法節奏分群，非熱門/最新）。**新鮮度：LIVE overdue 3→1/32**（僅剩 capyspin，逾期 1 天）。
    - **台帳審「功能」13 模組全審**（該分類 `last_audited` 07-31＝全 8 分類最舊）：除上述 absent→weak 外，覆核 sw v118→**v145**（6 天 27 次 bump＝三軌 bump 紀律運作中）、`HL.fair` 引用 20→**23** 個 views 檔、放置區仍 registry **1 筆**（HEAD 實測；工作區另 1 筆屬他軌未提交、不計入）。**一筆反向佐證**：DoND 500+ slots 但**無桌台無真人**⇒ ApexWin 的 TABLE 5 款是相對優勢、非缺口。
    - **開卡 #72**（支援與透明度中心·可註冊說明條目表；BC.GAME 官方稿 + Courtside Coach 兩源）／**#73**（進度解鎖「內容」而非只解鎖「錢」；**誠實標註僅單平台佐證**故排 #72 之後，且卡上要求先評估複用 #54 既有受眾述詞表）。
    - **實作 #66**（詳見卡片落地段）：**採用率 3→8 個呼叫點**。這張卡的價值同樣不在新功能，而在**接線＋把散寫的樣式決策收斂成一張表**——是引擎第五次撞見「元件做好但沒人用」（P4 `HL.dock`、`promoCal` 外部註冊者為零、#67 死巷入口＋死欄位、本卡 `HL.reveal` 採用率）。⭐**過程中查出並修掉三個既有缺陷**：① `HL.ui.modal` 缺 `onClose` ⇒ 玩家按 × 時佇列會**永久卡死**（新增 onClose + close 冪等）；② `enterView` 只清遮罩不清佇列 ⇒ **下一則會蓋在新頁面上**（新增 `drain()`）；③ **`reveal.js` 載入序在 `selftest.js` 之前 ⇒ 新測項在瀏覽器端整組註冊不到**（node 有、瀏覽器 `revealTests: []`）＝**「兩端不同步」型的隱形缺陷**，已重排並讓「4 項兩端皆在」本身成為迴歸鎖。node 68→72／瀏覽器 50→54／**負向六擾動全被抓**／真實 e2e（3 里程碑同時觸發但恆 1 modal、locked 逐位不動）／既有 3 個 `show()` 呼叫點零回歸（真實買寶箱走完全程）／三語零殘留且 HANS 五條皆非等值死鍵／375px 三舞台零溢出。sw v145→v146。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON 一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`。**交維護軌一筆**（i18n 維度、本輪未跨軌改）：`i18n.js` HANS 既有 `"太棒了，收下 ✓": "太棒了，收下 ✓"` 為 U31 型等值死鍵。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋，維護軌 08-06 已開 DEBT U33 接手）、**P6**（船長未裁決）；佇列 #57/#58/#59/#61/#62/#64/#65/#68/#69/#71/#72/#73 待做。**下輪首要＝刷 capyspin**（唯一逾期，屆時 LIVE overdue 歸零，可改為「深挖 1 + 新平台取材 1」）＋台帳次舊分類＝**後台**（7 模組 08-03）；實作候選建議 **#65**（本輪剛升為三平台共識、進度來源註冊表）或 **#72**（本輪新開、absent→weak 後範圍已收斂）。
- **2026-08-05（平台軌 · **20:00 窗**建置輪 · 補刷 tier-3 betpanda/zonko〔全庫最久逾期〕+ 台帳審「活動」6 模組 + 開卡 #70/#71 + 實作 #70 儲值側限額閘）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-201115-3c8f`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**本輪是 08-05 的第四個平台輪**（08-04 20:00 甦醒輪〔資料・#54〕→ 08:00 窗〔資安・#67〕→ 14:00 窗〔金流・#63〕→ 本輪〔活動・#70〕），四輪工作**完全不重疊**；留此對照是因每個 firing 都是無記憶新 session，同日多筆相鄰收尾極易被誤判為重複輪。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 僅 4.1h（16:05→20:11）遠未達 catchup，但前輪已明文指派「續清 tier-3 逾期 5 筆 + 台帳次舊分類＝活動 + 實作候選＝儲值上限 spec」⇒ **本輪三項全數命中前輪的預測**，讓路只會白費一個窗。
    - **step1 調研**（`max_platforms_per_run:2`）：取 **betpanda**（逾期 4 天＝**全庫最久** ∧ priority 65＝**逾期群最高**，一筆同時滿足兩種排序準則）+ **zonko**（同為最久逾期）。**兩張都帶回結構性事實，且各含一個「改變既有判斷」的產出**：
        - **(a) BetPanda** ⭐**本輪最有價值的新事實＝紅利的壽命被明碼寫出**：歡迎禮 **80× 流水 / 7 天未達標即失效**，另一側是**公告的固定派發時鐘**（每日 rakeback 08:00 UTC、每週淨損 cashback 週三 13:00 UTC，皆自動入帳、免 opt-in、cashback 無流水可即提）。⇒ 對照出 ApexWin 一個真缺口：`HL.bonus` 的 `unlocked` 與 `entries` **兩側都永不到期**（已 grep 複核 `badd/bstate/bclaim` 零時間欄位＝刻意複核，避免重演 07-30 的假缺口事故），而**同站內早有兩個逾期作廢先例**（rakeback 日桶、#33 cashback）⇒ **不是路線爭議、是同一房規在主軸上漏了一處** ⇒ 開卡 **#71**。另兩筆：忠誠方案更名 `XP Club→Path of the Panda` 但**六段位名稱一字未改**＝只換包裝（外部佐證「命名不該寫死在邏輯裡」）；rakeback 5%→10% + Gold 起配客戶經理 + 高段位放寬限額 ⇒ **#63 `HL.sla` 第六個平台佐證，且維度形制又與前五站不同**（比率 × 限額）＝註冊表抽象再獲驗證。
        - **(b) Zonko** ⭐**一次自我更正**：07-02 本專案把其 8-Day Reward Track 記為 **front-loaded**，2026-08 評測明確寫出獎勵權重**刻意壓在第 6–8 天**（那正是玩家原本流失的時點）＝**back-loaded、用後段大獎拉過流失懸崖**，據實更正。**但更重要的是反向結論：這一項 ApexWin 已更強、刻意不開卡**——`rewards.js` `LADDER` 是 30 天單調遞增（100→17,500）+ 里程碑大禮壓在第 **8**/15/22/30 天，**首個尖峰恰在第 8 天＝與 Zonko 的懸崖判斷一致**。真缺口只剩「**有邊界的 FTUE 多日軌**」（`onboarding.js` 僅 6 小時啟用窗口），07-02 即列為最強新缺口、**至今 34 天未認領**；本輪卡額已被 #70/#71 佔滿 ⇒ **維持記錄、不硬塞第三張**（另兩筆候選同樣只記不開卡：簽到日獎改隨機揭曉〔零件 luckyspin+#38 reveal 皆已在位，但會動送幣期望值與帳本成本歸屬〕、多來源可領物同次登入聚合）。**新鮮度**：LIVE 逾期 **5→3/32**（剩 courtside/capyspin/deal-or-no-deal-win，全 tier-3）。
    - **step2 台帳審「活動」6 模組全審**（該分類 `last_audited` 07-31＝全 8 分類最舊，輪替與新鮮度兩準則一致、無需偏離）。**兩筆實質變動**：① **促銷/活動框架**維持 partial，但 07-31 記載的三個阻礙項**已清掉兩個**——opt-in 狀態機已在位（#52），且**「容器採用度缺口」已清**：`promoCal.register` 的外部註冊者由 **0 → 2**（`rakeboost.js`／`release.js` 皆以軟依賴自我上架）⇒ 這條與船長 P4 對 `HL.dock` 的發現同形的病灶**首次被兌現**。仍不判 present 的唯一理由收斂為「**促銷本身無分群/A-B**」——#54 帶來的 `audience` 述詞表作用在**遊戲上架**、不是促銷，**已可複用但接上去是另一筆工作，未做即不記已做**。② **新手引導 present → partial（據實下修）**：三者是不同東西（6h 轉化窗口／永久 streak／有邊界 FTUE 軌），第三項為空。其餘四模組維持原狀並各補一筆新證據（referral 機械複核仍 **0 命中**＝absent 為現況非陳舊記載，#58 已在佇列故不重複開卡）。
    - **step4 實作 #70**（詳見卡片落地段）：把 #67 的限額型別註冊表**橫向擴出一個 `axis` 維度**（bet／deposit）+ period 由 `day|none` 擴為 `day|week|month|none`（**刻意沿用 #63 `service-level.js` 的同一期間口徑**，兩模組語意不分岔）+ 儲值三桶累計器，並在**全站唯一儲值咽喉點** `doDeposit` 接一行閘（商城買幣包與法幣儲值兩條路徑一次通吃；加密儲值只給地址無金額入口故無閘＝刻意）。**這張卡是 #67「容器先於內容」的第一次外部兌現**：預期成本「加三筆 spec + 一處閘」——**實測抽象成立，但需一次誠實的擴充**（axis 與長週期兩者原本不存在，若不加就會出現「單注上限 500 擋掉儲值 1000」的災難級誤擋）⇒ 結論是「註冊表方向正確，但首版把 axis 想成不必要」。另**刻意改變一項既有行為**：冷靜期原僅擋下注，本輪起**一併擋儲值**（暫停賭博期間還能儲值＝工具漏一半），已寫成測項。
    - **驗證**：node **63→68**、瀏覽器 **45→50**、**負向五擾動全被抓**（含「跨日連週/月桶一起清」這種可讓玩家繞過週限的真缺陷）、**真實錢包 e2e**（走商城按鈕：放行 +1,000 且帳本 deposit 0→1,000；被擋時餘額/txns/帳本/額度**四項逐位不變**）、**真實 Dice 局證下注側零回歸**（儲值額度用滿 2,000/2,000 下擲骰仍正常扣 −50，且儲值 used 不動、RG 下注累計器 wagered=50＝兩軸各自累積互不汙染）、EN/zh-Hans 零繁中殘留、375px 面板零元素超出右緣、7 顆「套用」鈕皆 60px 未成長條、零新 console error（唯一錯誤為既存 favicon 404）。sw v143→v144。
    - **本輪自身教訓兩條**：① ⭐**我自己犯了一個 DEBT U31 同型錯並在三語驗證時抓到**：HANS 字典寫了 `"本月已用": "本月已用"`＝**簡繁完全相同的等值死鍵**（`本月已用` 四字無簡化差異），已刪並在該處註明「只列相異者」。**這件事同時暴露我的驗證器有偽陽性**——我用「面板文字是否仍含繁體鍵」判殘留，對簡繁同形的鍵必然誤報，若不追查就會去「修」一個不存在的 bug（追查後確認是量測法問題，未誤改 code；同型陷阱與 08-04 那輪「同 tick 讀 innerText」互為家族）。順帶記下本檔既有的 `"今日已用": "今日已用"` 是同型冗餘，屬 i18n 維度既存項、**不跨軌改**。② **分段提交紀律首次實際執行**：`_platform_run_note_20260805` 記的教訓（研究做完就先落地、勿等實作完一次寫檔）本輪照做——調研+台帳+開卡先單獨提交 `7e9129f`，實作再第二筆 ⇒ 若本輪在實作階段凍結，兩張 tier-3 逾期票與台帳審計不會白費。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON／含錢字號的長中文一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋）、**P6**（船長未裁決；本軌 08-04 已算出容量為到期率 5.8 倍、建議維持 30 天）；佇列 #57/#58/#59/#61/#62/#64/#65/#66/#68/#69/#71 待做。**下輪首要＝清完 tier-3 逾期 3 筆**（courtside/capyspin/deal-or-no-deal-win）＋台帳次舊分類＝**功能**（13 模組 07-31）；實作候選建議 **#66**（S 級接線卡，`HL.reveal` 採用率）或 **#71**（本輪新開、有明確風險清單）。
- **2026-08-05（平台軌 · **14:00 窗**建置輪〔15:28 進場＝App 延後觸發〕· 補刷 tier-3 kaasino/coinsback + 台帳審「金流」5 模組〔更正一處 07-30 的假缺口〕+ 開卡 #68/#69 + 實作 #63 VIP 服務水準軸 `HL.sla`）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-152830-a7d4`（帶心跳、逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 僅 3.7h（11:45→15:28）遠未達 catchup，但 CONTROL/前輪明載「tier-3 逾期 7 筆為續清目標」＝有指派工作，且遊戲軌下一窗（16:00）在本輪之後，讓路只會白費一個窗。
    - **step1 調研**（`max_platforms_per_run:2`，延續「最久 × 最高價值各取一筆」紀律）：① **kaasino**（`next_due` 07-29＝**全庫逾期最久**、且其 dossier 自 06-29 起就掛著一個沒人開的招牌缺口）② **coinsback**（逾期 4 天但 **priority 67＝逾期群最高**）。**兩張都帶回結構性事實而非只刷日期，且兩張都直接改變了既有卡的形狀**：
        - **(a) Kaasino** 規模收斂為 **8,000+ slots / 73 供應商**（舊記「3,000–10,000+」為區間）；split-screen 招牌 reconfirm ⇒ **開卡 #69**（37 天沉在 dossier 裡的缺口終於立卡）。⭐**最有價值的是新維度**：其標準提領上限是**三週期制**（£10,000/日・£20,000/週・£60,000/月）+ VIP 分階解鎖更高門檻 + Prime 頂階專屬客戶經理。**⚠️ 來源衝突已據實兩記不擇一**（另有評測稱 Cheese Club 不含更高提領上限/客戶經理）——但**衝突本身就是證據**：即使一個留存系統相對薄弱、以 cashback 為核心的站，評測仍會逐項檢查「VIP 有沒有給更高提領上限／客戶經理」＝**該軸已是業界橫評標準項**。**兩項刻意不學**：cashback「最低淨損 €30 門檻 + 5× 流水」＝對房家有利的收緊旋鈕，與使用者優先序（體驗完整度優先）相反，只記為 §11 真金收斂時的備選旋鈕。
        - **(b) CoinsBack** ⭐**修正了本專案自己的過期判斷**：dossier 07-02 記「ApexWin 缺逐注 RTP 返還」**已不成立**——#60（08-01 落地）已把返水改為 `bet × 該遊戲莊優 × VIP 段位返還比例`＝與 CoinsBack **同一計價基準**且更細 ⇒ 殘存缺口只剩**可視化與即時性**，故 **#68** 開的是「把已算好的金額演出來」而非「補一個機制」（**零經濟變動**）。另補完 CoinsClub 14 級 Bronze→Royalty，高階解鎖的是**加速兌獎 + 優先客服**。
        - ⇒ 兩站合計把 **#63 從三平台共識推到五平台共識**，且**五站貢獻的維度互不相同**（時效／額度／週期數／客服）——這正是本輪把 #63 實作成「**可註冊維度表**」而非原卡文所寫的固定三欄位 config 的決定性依據（Kaasino 的「週上限」當場就是固定欄位容不下的第一個例子）。**新鮮度**：LIVE 逾期 **7→5/32**（剩餘全 tier-3、無 tier-1/2）。
    - **step2 台帳審「金流」5 模組全審**（該分類 `last_audited` 07-30＝全 8 分類最舊，輪替與新鮮度兩準則一致、無需偏離）。**最重要的產出是一次自我更正**：**07-30 記載的「獎金/流水引擎缺 no-wager 零流水紅利」是假缺口**——`progress.js:39` 的 `if (opts && opts.wagerFree) o.unlocked += n` 自 **#20（`231e791`, 2026-07-10）即存在**（`git log -S` 實證），且已有 2 個生產呼叫點在用（零流水 cashback、新手安全網）⇒ 那條「建議增 `wagerMult` 覆寫」的建議中「0＝零流水」那一半**當時已實作 20 天**。維持 partial 的**正確**理由改為「`wagerFree` 是二元開關、缺逐筆自訂倍數」。**教訓＝台帳條目未經 grep 就斷言「缺某能力」會製造假缺口**，這與引擎反覆遇到的「元件做好但接線沒補完」互為鏡像（那是有東西沒接、這是有能力沒查到）。其餘：收銀台維持 partial（PSP grep 再測 0 命中）並**新查獲儲值側零限額閘**——#67 `HL.rg` 的 4 個限額型別全在下注側，**業界最標準的「儲值上限」缺席**，而它天生就是 #67 註冊表的一筆 spec（僅需在 `doDeposit` 補一處閘呼叫）⇒ **記為下輪低成本高價值候選**（本輪 2 張卡額度已用盡，寫進台帳避免遺失）；錢包/帳本維持 present 並記 **#56 已修完**（`p2p_out` 型別 + UI 誠實揭露）；提款審核佇列維持 absent 且不開卡，但缺口形狀更精確＝**不是缺 SLA 顯示（本輪已有），是缺狀態機 + 伺服器權威**；P2P 交易 partial 理由收斂為單一項「單向無對手方」。
    - **step4 實作 #63**（詳見卡片落地段）：`core/service-level.js`＝`HL.sla` 服務水準維度註冊表（5 維度 · `kind:"cap"` 帶週期會被閘求值、`kind:"info"` 只呈現 · `better` 讓單調性測項自動逐向驗證）+ 提款頁三期剩餘額度與預計到帳時間 + VIP 面板唯讀說明表。**三個設計決策寫成機械化不變量**：日限刻意全段位一致、日 ≤ 週 ≤ 月、真站在任一維度都不比假站寬鬆。
    - **驗證**：node **56→63**、瀏覽器 **37→45**、**負向五擾動全被抓**、真金提款 **e2e 六步**（含「恰好等於剩餘」邊界放行、被擋嘗試餘額逐位不變且**零記帳**）、VIP 拉到 💎 後時效 48h→6h 但**日限仍 25,000**（不變量①的執行期實證）、EN/zh-Hans 零繁中殘留、375px 零溢出、入口按鈕 235px 未成長條、零 console error。sw v142→v143。
    - **本輪自身教訓兩條**：① ⭐**負向驗證抓到的是我自己寫的空殼測項**——`sla/period-rollover` 首版用 `t.ok(A || weekOf(base)!==weekOf(base+DAY))` 兜底，而該起點恰好跨週 ⇒ 斷言永遠被 `||` 右半救活，**植入「跨日誤清週累計」的真缺陷竟仍 PASS**（該缺陷可讓玩家用跨日繞過週限）。已改為對齊「週的第 3 天」+ 前提斷言，無退路。**教訓：測項裡的 `||` 兜底是空殼的溫床**——這也證明「負向驗證」這道工序本身有價值，不是形式。② **連兩輪撞同型 i18n 撞鍵**：`本期剩餘` 早已存在且 EN 作 `"Time left"`（錦標賽倒數），沿用會讓金額欄顯示「Time left」⇒ 改用專屬鍵 `本期剩餘額度`（前輪是「進行中，剩餘」撞 Happy Hour）。**建議：新增 i18n 鍵前一律先 grep 既有鍵與其 EN 譯文**，撞語意比缺鍵更難發現。
    - **寫檔紀律**：全程零 stdin heredoc（遵 §6 踩雷 #5），複雜 JSON／含錢字號的長中文一律 Write 暫存 `.js` 再 `node 檔.js` + `timeout`；只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**他軌未提交的 `prototype/games/registry.json`（mtime 08-03、社群放置殘留）。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋）、**P6**（船長未裁決；本軌 08-04 已算出容量為到期率 5.8 倍、建議維持 30 天）；佇列 #57/#58/#59/#61/#62/#64/#65/#66/#68/#69 待做。下輪首要＝**續清 tier-3 逾期 5 筆**（betpanda/courtside/capyspin/zonko/deal-or-no-deal-win）＋台帳次舊分類＝**活動**（6 模組 07-31）；實作候選建議 **儲值上限 spec**（本輪台帳新查獲、S 級、接 #67 既有註冊表）或 **#66**（S 級接線卡）。

- **2026-08-05（平台軌 · **08-05 08:00 窗**建置輪 · 補刷 tier-3 chancer/bigpirate + 台帳審「資安」5 模組〔負責任博弈 absent→partial〕+ 實作 #67 負責任博弈 `HL.rg`）** — **本輪身分先講清楚**：這是 **08-05 08:00 那個窗**，它被前一輪（08-04 20:00 firing 的甦醒者 `p-094500-b1d7`）長時間佔用的 task 實例擋住，於前輪 10:2x 釋放後**即刻放行**，故本輪起跑時間 10:20 與上一輪收尾僅差數分鐘——**不是新窗、也不是重複輪**（已在鎖行與 STATE 註明，避免下一個無記憶 session 誤判成「同一小時跑了兩輪＝空轉」）。閘門全過，進場 `build_lock=false` 乾淨 → claim `p-102030-9f2c` → 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 僅 0h 遠未達 catchup，但 CONTROL 明載「剩餘 9 筆 tier-3 已列下輪首要」＝有指派工作；且**讓路救不了遊戲軌**（其 10:00 窗已因撞前輪鎖而退出、下一窗 16:00 時本輪早已結束）⇒ 讓路只會白費一個窗，不成立。
    - **step1 調研**（`max_platforms_per_run:2`，延續「最久 × 最高價值各取一筆」紀律）：① **chancer**（逾期 7 天＝本批最久）② **bigpirate**（priority 69＝本批最高）。**兩張都帶回結構性事實、不是只刷日期**，且**兩張各自把一張既有卡從單點觀察升級為多平台共識**：(a) **Chancer** 校正為 **10 段 VIP、XP 同時吃 casino+sports、頂階回收淨損 25%**，並首次查到**與 VIP 平行的 comp point 制**（每 €10 押注 1 點）通往 Bonus Shop「**The Armoury**」（可換 free spins／現金／**實體商品** TV/PS5/iPhone）；⭐關鍵新發現＝**VIP 放大的不只是「給多少錢」，還有 shop discounts 與 increased withdrawal limits**。(b) **BigPirate** 首次觸及其忠誠層：**五段 VIP 的升段來源＝「遊玩」＋「選購 GC 幣包」兩條**（⇒ 與 08-05 前輪剛開的 **#65 進度來源註冊表**〔源自 Spree〕**獨立收斂，#65 升級為兩平台共識**）；且其 **VIP 給的多半是服務而非錢**（生日禮、24/7 真人客服、Lv3 起專屬 VIP 客戶經理）。⇒ **Dorados（兌獎 SLA/額度）＋ Chancer（提領上限/商城折扣）＋ BigPirate（客服/客戶經理）＝「VIP＝服務水準軸」三平台共識**，已回寫 **#63** 並上調優先序。另記 BigPirate 的 **Claw Machine credits ＝一種揭曉型去化口**，為 **#66** 添第三筆佐證。**新鮮度**：LIVE 逾期 **9→7/32**（剩餘全 tier-3）。
    - **step2 台帳審「資安」5 模組全審**——**刻意偏離嚴格輪替**：輪替下一棒是「擴充性」，但它昨天（08-04）才審過，而**資安是全 8 分類最舊者（07-23/07-28，逾期 8–13 天）**⇒ 依 P1 建立 `last_audited` 的本意（讓新鮮度可機械查詢並據以排序）選擇按新鮮度走，並在此註明偏離理由。結果：**負責任博弈 absent→partial（本輪實作 #67）**；site-mode 維持 present 並**首次量化** `isLive()` 閘 **41 處**（可作下次複查的回歸基準）；風控/反詐欺、KYC、RBAC 三筆維持原狀並補上可重現證據。**順帶記一個易誤判的 grep 陷阱**：`fingerprint` 現在會命中 `dock-growth.js` 的**佈局指紋**（判斷面板版位變動），與反詐欺裝置指紋毫無關係——已寫進台帳，避免下輪據此把 weak 誤升為 partial。
    - **step4 實作 #67**（見卡片落地段）：**這張卡的價值不在「新功能」，而在把三個懸空 8 天的零件接起來**——07-28 台帳早已判定它「已對外宣告但點進去是空的」並列為「下一輪平台軌首選候選」，卻連續 8 天約 15 個平台窗無人認領。**這是引擎第四次遇到同型病**（元件/欄位做好但接線沒補完：P4 的 `HL.dock`、`promoCal` 外部註冊者為零、#66 的 `HL.reveal`，加上本輪的死巷入口 + 死欄位 `lossLimitRemaining`）⇒ 建議把「**接線缺口**」當成一個獨立的缺口類別去掃，而不是每次偶然撞到。
    - **驗證**：node harness **51→56**、瀏覽器 selftest **32→37**；**負向驗證四種擾動全部被抓**（拿掉 24h 調升冷卻／限額比較 off-by-one／冷靜期不擋注／跨日不歸零）＝測項具鑑別力；**真實 Dice 局 e2e** 三態（限額擋下餘額逐位不變 + toast、冷靜期擋下、清空後同一顆按鈕正常扣 −50 且累積 50）；死欄位接線實證 `lossLimitRemaining`＝300；三語零繁中殘留、375px 零溢出、零 console error。sw v141→v142。
    - **本輪自身教訓三條**：① **第四次踩 P3 混合文字節點雷**（首版把「目前 未設定 · 今日已用 NT$0」寫成單一節點 ⇒ 永遠翻不到；preview 抓到後改為中文片語各自成節點）——這個雷已連四輪出現，**建議維護軌考慮加一個自動檢查**（掃 `el(...{text: "中文" + 變數})` 形態）而不是靠每輪自律。② **首版讓 `.ax-btn-ghost` 撐成 265px 長條**（該類別預設 `display:block`＋滿寬），正是 UI 品質清單既載的反例，preview 量測後改為內容寬 chip。③ **i18n 新增鍵務必先查重**：本輪首版誤把既有的「目前」「取消」「進行中，剩餘」重複宣告（重複＝靜默覆蓋，正是維護軌 DEBT U31 剛清過的病），已寫一支計數腳本驗到零重複才收尾；其中「進行中，剩餘」更是**語意衝突**（他處為 Happy Hour 專用、EN 作 "Live — ends in"）⇒ 改用專屬鍵「冷靜期剩餘」而非硬套。

- **2026-08-05（平台軌 · 08-04 20:00 firing 的【甦醒者】· 補刷 tier-3 spree/punkz + 台帳審「資料」3 模組〔注單 absent→present、報表 weak→partial〕+ 開卡 #65/#66 + 實作 #54 上架排程 × 受眾分層）** — **⚠️ 本輪最該被記住的是存活事件**：這個 session 於 **08-04 20:10 claim `p-201015-4c2a`**，隨即因 **App 關閉而懸置約 13.5 小時**（心跳停在 20:10:15），**08-04 22:00 遭遊戲軌依 stale-heal 正當奪鎖**（`stalled_rounds` 2→3），維護軌 00:00 亦正常交付（DEBT T32）。本 session 於 **08-05 09:42 甦醒**＝CONTROL 第 97 行「凍結者醒來帶舊記憶覆寫」情境**再次本人上演**（前例：07-30 14:10 那輪）。已按鐵律**捨棄凍結前的全部檔案狀態記憶**、完整重讀 CONTROL/STATE/db/git log/git status（確認期間有 4 個新 commit：`3b7f229`/`4963f1c` 等），確認鎖已回 `false` 後**以新 token `p-094500-b1d7` 重新進場**，並在鎖行標明甦醒身分。**與前例的差異＝這次不是 claim 後立即死，而是研究做完但一字未落地**（WebSearch 結果全在記憶體）⇒ 若直接放棄，兩個 tier-3 逾期票與整輪火力都白費，故選擇補完並定位為 **08-05 catchup 輪**（`last_platform_run_at` 落後 17.9h，未達 24h 門檻，屬常規補課而非強制 catchup）。**教訓（供三軌共用）：長輪應分段提交**——STATE `_platform_run_note_20260803` 早已記過同一句，本輪再次證實：研究階段結束就該先落地 dossier，不要等到實作完才一次寫檔。
  **step1 調研（`max_platforms_per_run:2`＝全給船長 08-04 點名的 tier-3 逾期群）**：① **spree**（逾期 4 天，但為該群 priority 最高 70） ② **punkz**（逾期 7 天＝當時最久）——延續上輪「最久 × 最高價值各取一筆」的紀律。**兩張都不只是刷日期，各帶回結構性新事實**：(a) **Spree 首次查到忠誠結構的完整形狀**——前兩輪（07-02/07-09）都記「無 VIP/忠誠方案」，本輪確認其實有 **XP Rewards Loyalty Program**：**需 enroll**、**XP 來自「遊玩 + 選購 GC 幣包」兩條來源**、**100 級 + Emerald/Ruby/Diamond 三層**、**每 10 級的禮物本身是一種開獎玩法**（Gift Spins/Scratch-Offs/Bubble Bursts/Wonder Wheels）。⚠️ **來源分歧已據實記錄**：sportsgambler 8 月評測稱其「無 XP、無錦標賽」，與官方 loyalty 頁及多家相左 ⇒ 依 sourcing-methods「只記共識」採多家一致版本並註明分歧（合理推測評測者未 enroll＝**這本身就是「需 opt-in 的方案對未加入者等於不存在」的實例**）。(b) **Punkz 首次取得完整 VIP 數值表**（Rookie 0／Riser 12k／Outlaw 24k／Hotshot 1M／Maverick 4M／**Apex** 8M，cashback 0→15%）＋兩條機制：**XP 依區塊分級（slots $1=10 XP／live+table+sports $1=1／provably-fair 一律 0）**、**XP 6 週過期 + 每週重算段位 ⇒ 段位會下降**。
  **開卡與去重（`max_cards_per_run:2`）**：**#65 進度來源註冊表**（Spree「XP 也來自購買」⇒ 本輪 grep 實證 ApexWin 全站進度**只認投注**，`liveStats.record` 下游 18 條訂閱者全由 `bet` 驅動，儲值/商城消費貢獻恰為零；卡內已寫明**必須只發進度不發錢 + 上限 + 站別感知**，否則「儲值換段位」會讓 VIP 升級金變成可套利的儲值返利）；**#66 里程碑改以既有 `HL.reveal` 揭曉發放**（Spree 每 10 級開獎玩法 + Punkz 分階 loot box 兩家共識）。**⭐ 這張卡的立卡過程修正了我自己的誤判**：原本要開「揭曉型領獎元件」大卡，**實查發現 #38 `core/reveal.js` 早已存在**且三樣式齊備、鐵律正確 ⇒ 真缺口是**採用率**（只接了 meta/onboarding/shop 三處，而 VIP 升級金／季票階梯／成就／任務四大里程碑仍直接入帳零儀式）⇒ 卡從 M 縮成 **S**、性質從「新元件」改為「接線」。**這是「元件做好但接線沒補完」的第三次同型發現**（前有船長 P4 的 `HL.dock`、07-31 台帳的 `promoCal.register` 外部註冊者為零）。**兩條不開卡的觀察據實歸併**：① Punkz 的 XP 分區塊加權＝**#50 `HL.edge` 的外部佐證**（我們逐遊戲係數更細），無淨新缺口 ② Punkz「**XP 6 週過期 + 每週重算 → 掉階**」＝**#59 的第三家共識**（前有 WOW Vegas 星星 30 天過期）⇒ 不開重複卡，改**寫進 #59 並上調優先序**；同時記入一條**反向佐證**：Stake（08-04 刷新）明確終身累計永不重置 ⇒ 頂級站在此分歧，而 **#59 原本採納的「核心等級不倒退 + 只讓額外光環層衰減」正好是兩派交集**，該取捨現有外部依據。③ Punkz「cashback 固定每週四 13:00 UTC、boost 每月 1 日發放」＝**固定發放時刻本身就是回訪錨點**，而 ApexWin `HL.cashback` 是週桶+隨時領+跨週作廢（有截止壓力、無發放時刻）⇒ 兩者各有優劣、**本輪不開卡**，記入台帳「活動」分類候補待該分類輪替時複審。**新鮮度 LIVE overdue 10→9/32**（node 一行可重算；spree/punkz 清掉、capyspin 今日新到期），**剩餘 9 筆全 tier-3**。
  **step2 台帳審「資料」分類（3 模組全審）**——取 `last_audited` **最舊者**（該分類含全台帳唯一仍停在 07-23 種子日的模組），延續 08-04 已建立的「新鮮度優先於嚴格輪替」判準。**查獲一處帳目落差並更正**：**玩家注單/投注歷史 absent → present**——**#51 早在 07-31 就落地了，台帳卻仍記 absent**（evidence 甚至自己寫著「已開 #51 卡」）＝典型的「做完沒回頭更新台帳」；本輪補上可重算證據（`betlog.js` 265 行、寫入點掛 `live-stats.js:44` 中央點＝全遊戲+主播跟注通吃且零遊戲檔改動、9 欄 COLS 描述子驅動表頭/明細/CSV 三處、CAP=500 環形、站別隔離、單筆深連結 `HL.fair.verifyModal(nonce-1)`、自帶三項 selftest＝永久迴歸鎖）。**報表與匯出 weak → partial**：前輪 evidence 記「全 src grep csv|Blob|download **零命中**」**已不成立**——node 實測命中僅 `betlog.js` 一檔，含 RFC4180 跳脫 + Blob 下載 + 欄名由 `COLS.csv` 單一真相生成，**正是前輪 stowable_note 預言的「第一個真出口可搭 #51 一併落地」已實現**；仍非 present 之因＝匯出僅限注單一種資料、且**無可註冊的報表/查詢定義清單**（加報表仍需改檔）。**資料/分析維持 partial**（營運側 ledger+opsBoard+phase6 已成形；產品側 node 實測 grep `eventSchema|funnel|cohort|retentionCohort` **零命中**＝無事件 schema 註冊表/漏斗/世代留存，加一個新事件仍等於改程式）。
  **step4 實作 #54**（詳見卡片落地段）：`core/release.js`＝三階段 + **受眾述詞表**（加一種受眾＝加一筆）+ **`game` 可為尚未註冊的 id**（先排程後實作）+ 每筆自動 register 進 #49 日曆（順帶成為 promoCal 的**第二、三個外部註冊者**，補上 07-31 台帳記的「外部註冊者為零」）。**零回歸做成常駐測項而非口頭宣稱**：未宣告排程 → `playable()` 逐位退化回 `g.playable`、`stateOf()` 回 null、卡片零角標；閘**只收緊不放寬**；未知受眾 kind **保守不放行**。**驗證**：node **49→51**、**負向三擾動皆被判 FAIL 且還原全綠**、瀏覽器 **30→32**（browser==node 同一份測項）、preview 逐項驗合格/不合格/upcoming 三路徑 + 零回歸 + 日曆兩列 + **三語 round-trip** + 零 console error；i18n 新增 EN 23／HANS 20 鍵並**以實際載入的 DICT 物件驗證**（EN 缺鍵 0、無死鍵）。sw v140→v141。**一處刻意偏離卡片原文並記錄理由**：原文寫「`HL.games.all()` 加過濾閘」，改為**「可見但鎖住 + 說明」而非隱藏**（隱藏會砍掉發現性，與 Early Access 的行銷目的相反）；種子排程也**刻意不鎖任何現有玩家**（gem-storm 門檻設 Lv 1＝人人達標，只多一枚角標），「被擋住」的行為交由測項與 runtime 驗證覆蓋＝比在 demo 製造一個鎖更有證據力。
  **已知限制（誠實記錄）**：沙箱 preview **viewport=0**（`innerWidth`/`clientWidth` 皆 0、reload+resize 皆無效）⇒ **角標的實際幾何完全未量測**——computed `position/背景/字級/圓角` 已驗但「是否壓到縮圖或收藏鈕」需線上肉眼確認；此外 preview 過不了登入 gate，casino 視圖是以 `HL.views.casino.render()` 直接取節點驗證（§9 既有手法）。**寫檔紀律**：全程未用任何 stdin heredoc（遵 §6 踩雷 #5），長腳本一律 Write 暫存 `.js` 再 `node 檔.js` 並加 `timeout`；含錢字號/反引號的中文長文一律走 Write/Edit。只逐檔 add 自己的檔，**未碰** `Game assets/` 增刪雜訊、**未碰**遊戲軌未提交的 `prototype/games/registry.json` 與 `prototype/games/slot-engine/`。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋）、**P6**（船長尚未裁決，本軌 08-04 已複核建議「維持 30 天」）；佇列 #57/#58/#59/#61/#62/#63/#64/#65/#66 待做，下輪首要＝**續清 tier-3 逾期 9 筆**（bigpirate/coinsback/betpanda/courtside/capyspin/zonko/chancer/kaasino/deal-or-no-deal-win）＋台帳次舊分類＝**資安**（4 筆 07-28 + 1 筆 07-23）。


- **2026-08-04（平台軌 · 14:00 建置輪 · 補刷 tier-3 dorados/spinblitz + 台帳審「前端UI/UX」4 模組〔i18n present→partial〕+ 開卡 #63/#64 + 實作 #52 促銷 opt-in ＆ 返水加成排程表）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-140930-9b3e`（帶心跳，長驗證階段照樣逐步更新＝消化上輪自記的教訓①）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 僅 3.8h 未達 catchup，但船長 08-04 於 CONTROL 明文點名本軌「剩餘 12 筆 tier-3 為下輪首要」＝有指派工作，讓路不成立（`yield_rounds` 已 9，不宜再累加無謂讓路）。**順手治 M4 行 bloat**：`build_lock` 那一行原已被 20+ 輪釋放註記堆到 **9,298 字元**（M4 早在 07-30 就指出「已被灌到單行破 1500 字」、後續各輪仍持續堆疊），本輪收斂為 **445 字元**（只留「當前鎖狀態 + 一句摘要」，逐輪內容本來就在本日誌與 git log 有完整可追溯紀錄）。**step1 調研**（`max_platforms_per_run:2`，取船長點名的 tier-3 逾期群）：① **spinblitz**（逾期 6 天＝最久逾期群之首）② **dorados**（逾期 3 天但 priority 71＝逾期群中價值最高）——刻意各取「最久」與「最高價值」一筆，兩種排序準則都不落空。**兩份 dossier 都停在首次調研（06-29／07-02），本輪各帶回一條淨新結構軸線**：(a) Dorados **VIP 階級決定的不是給多少錢，而是「兌獎處理速度 SLA（72h→42h→24h）+ 每月兌獎上限（30k→60k）」**⇒ 開卡 **#63**（ApexWin 的 VIP 三條延伸線 #1/#50/#60 **全是送錢軸**，提款體驗則與段位完全無關）。(b) SpinBlitz **明確無 VIP/忠誠度方案**，唯一留存結構＝**Starlight Missions＝任務綁定特定遊戲標題、獎勵發回該款 free spins** ⇒ 開卡 **#64**（ApexWin #33 是遊戲無關的通用計數、#26 只認倍數達標 ⇒ **全站沒有任何「把玩家導去某一款」的工具**，這對同仁自製遊戲放置區〔目標 2〕的新上架導流特別可惜）。(c) SpinBlitz **referral 分階段釋放**（達 $20 放第一段、累計 $500 才放剩餘）⇒ **不開第三卡**（`max_cards_per_run:2` 已用盡），改**寫進 #58 的範圍並標「實作時務必採此形狀」**——因為假站 mock 好友可任意生成，一次性紅包等於無限印幣；同時使 referral 成為**第三平台共識**（WOW Vegas／BC.GAME affiliate／SpinBlitz）。**新鮮度**：LIVE 逾期 **12→10/31**（全數 tier-3、P6 結構債）。**順帶對 P6 的結構診斷做一次算術複核**（不改設定、僅提供裁決依據）：穩態到期率 ＝ 31 站 ÷ 30 天 ≈ **1.03 站/日**，而容量 ＝ `max_platforms_per_run:2` × 每日 3 窗 ＝ **6 站/日** ⇒ **容量約為到期率的 5.8 倍、追得上**；P6 觀察到的積壓真正來自「平台軌 07-31→08-03 失聯 69 小時（M7）＋ 部分輪次把 2 個名額用於新平台發現而非刷新」，**不是週期設定太密** ⇒ 建議船長**先不要**把 tier-3 週期放寬到 45~60 天（那會實質降低調研密度），而是維持 30 天、由本軌持續把名額優先給逾期刷新即可清空（現存 10 筆 ≈ 2 天可清）。**step2 台帳審「前端UI/UX」分類（輪替接續 08:00 的擴充性；該分類同時是 last_audited 最舊者之一 07-23，輪替與新鮮度兩準則一致，不需偏離）：4 模組全審**。三筆複審維持 present 並附可重算證據（大廳 node 實跑 vm 得 `casinoGames` **51 筆／playable 2**＝與 07-30 更正值逐位相同、無漂移；導覽殼層漢堡→抽屜路徑仍在線；遊戲客戶端框架工具列實測 6 顆、🔒 僅在 `HL.fair.isPF` 為真時生成＝T28 收斂後的單一真相）。**一筆據實下修：多語系前端 i18n present → partial**——引擎面無虞（walker + observer + PREFIX/SUFFIX、EN **874** 鍵／zh-Hans **738** 鍵，vm 實跑 `dict()` 取得），但**覆蓋率可重現量測**（對 70 檔抽含 CJK 的字面量比對 EN 鍵集）＝**全站 936/2404 ≈ 38.9%**；分佈**證實並量化船長 P3**：`season.js` 6%／`guild.js` 9%／`promo-cal.js` 12%（＝#46/#47/#49 面板），而 07-30 起「落地時同步補」紀律下產出的 `dock-growth.js` 為 **94%** ⇒ 紀律有效、缺的是回頭補既有面板。量測法已註明已知誤差（拼接字串低估、內部測試字串高估）故只採相對排序。**歸屬**：覆蓋補齊屬維護軌 i18n 維度，本軌只負責「新落地面板自己補齊」（本輪 #52 已自補、三語 preview 逐項驗）。**step4 實作 #52**（見卡片落地段）：`core/rakeboost.js` 加成排程註冊表把 boost 從「硬編 happyhour」變成一張可註冊的表、`promo-cal.js` 加 opt-in 狀態層 + 「我的優惠」第三分頁；**解析取最高不相乘**（相乘會讓送幣成本無上界、疊穿 §11 剛轉正的真站 NGR），**副產品＝把 #60 只在 boost=1 成立的不變量補成「含加成也成立」**（真站 0.145×1.5=0.2175<1 硬測項逐遊戲逐段位實算；假站明載刻意超發但斷言有界）。**驗證**：node 39→**42**、**負向驗證三種擾動皆被判 FAIL 且還原全綠**、瀏覽器 harness 26→**30**、真結算 e2e ×1.5 精確且未加成值與上輪逐位相同、繁簡英三語 + 零 console error + 375px 零水平溢出。**本輪自身教訓兩條**：① **第三次踩 P3 混合文字節點雷**（首版三處「中文＋動態值」單節點全翻不到；已修兩處，第三處受限 #49 `note` 為單一字串→已在檔頭寫明並移交維護軌）② **新記一條 preview 驗證陷阱**：MutationObserver 回呼非同步，「開 modal → 同 tick 讀 innerText」會讀到未翻譯的中間狀態、連既有鍵都像壞掉 ⇒ 驗 i18n 必須延遲後再讀（本輪先誤判成回歸、追查後確認是量測法錯誤，未誤改 code）。**寫檔紀律**：含錢字號/反引號的中文長文一律走 Write/Edit 或 node 腳本（未用裸 shell heredoc＝遵 §6 踩雷 #5）。

- **2026-08-04（平台軌 · 08:00 建置輪 · 清 tier-1 逾期 stake/bc-game + 台帳審「擴充性」4 模組 + 新增台帳模組「支援/透明度中心」+ 開卡 #62 + 實作 #60 返水改以莊家優勢計價）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-080920-7c3d`（帶心跳，全程逐步更新）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功（TOCTOU 未觸發）→ 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 僅 15.3h 未達 catchup 24h，但**船長 08-04 於 CONTROL 明文點名本軌 08:00 窗優先清 tier-1 兩張**＝有明確指派工作，讓路不成立。
  **step1 調研（`max_platforms_per_run:2`＝全部給船長點名的兩張 tier-1）**：① **Stake** ② **BC.Game**（兩張 `next_due` 08-03 已過期，維護軌 00:00 記為「首見 2 tier-1 逾期」）。**Stake 淨新一條＝VIP 頂階改版**：**Obsidian 取代 Diamond 成為最高階**（thespike／igamingfuture／coinstats／spindex／sportsgambler 交叉，門檻約 10 億累積 wager）。⚠️ **階數各家記載不一（15 vs 16）⇒ 依 sourcing-methods「只記共識、不臆造絕對數」原則不寫確數**，只記共識（Bronze 起 $10k、頂階已換 Obsidian、增益為「同 Diamond 權益但由專屬 host 客製放大」）。**最可學的一點＝VIP 進度終身累計永不重置**——ApexWin `HL.vip.addWager` 亦為累計制、**方向已對齊、無缺口**（順帶提供 #59 滾動光環層的設計佐證：頂級站的核心等級同樣不倒退）。其 Pragmatic Drops and Wins／Level Up 分獎池皆為第三方供應商聯賽＝需真接入，落 `avoid`，不開卡。**BC.Game 兩筆舊缺口本輪確認皆已補上**（成本加權 XP＝#50 已落地／限時新手損失保險＝#48 已在線）⇒ 舊記可關。**淨新一條＝「公開透明度／支援溝通」軸**（prnewswire／streetinsider 官方稿三項：付款處理說明更清楚、支援資訊更結構化、帳戶審查過程溝通改善）⇒ 開卡 **#62**。其 $BC 白皮書代幣效用/銷毀＝真金代幣範疇，落 `avoid` 僅記錄。**逾期 14→12、tier-1 歸零**（船長點名事項結清）。
  **step2 台帳審「擴充性」4 模組全審**——⚠️ **本輪刻意偏離嚴格輪替並記錄理由**：SKILL 的順序在「後台」之後是「金流」，但 08-03 那輪已自行繞回「後台」（打破原序），且**金流 07-30 才審過＝第二新**，而「擴充性/前端UI/UX/資料/資安」仍卡在 07-23 種子日（12 天）⇒ 改採**新鮮度優先**取最舊者，並選 4 模組的「擴充性」（本軌核心哲學、直接餵開卡判斷）。**查獲一處明確過期記載並更正**：Dockable Layout 的 evidence 仍寫「唯一註冊者是 partner+chat」，實際 **#55 已於 07-30 加入第三個註冊者**（grep 實證 2 個註冊點、`HL.dock.ids()`=[partner,chat,growth]）＝**底座已過第二代驗證**；仍維持 partial（主內容區 grid slot／吸附／多佈局 preset 未做）。另兩筆據實維持：自動化測試 partial——採用度確實成長（games fast harness 20→34、#56 補 ledger 3 項、node 32/32），但**「核心經濟模組測項」缺口只補了 ledger 一角**，本輪 grep 實證 `selftest.js` 對 vip/rakeback/bonus **僅驗掛鉤存在性、零行為/數學測項**（返水率算錯不會被叫出來）⇒ **本輪實作卡 #60 正好補上第一批 rakeback 數學測項**；Release Scheduling 維持 absent（grep `earlyAccess|releaseAt|audience` 命中 0，#54 未動）。**新增一筆模組「支援/透明度中心 Support & Transparency Center」absent**（分類：功能）＝本輪 BC.Game 研究的落點，grep 實證全站僅 1 處註解提及「客服」、零 FAQ/說明/支援入口。台帳 46→47 筆。
  **step4 實作 #60**（詳見卡片落地段）：新增 `core/rakeback-core.js` 純數學核心（雙環境契約，比照 #50/#51/#56），公式改為 `bet × 該遊戲莊家優勢 × 段位返還比例 × boost`；中央點傳 `accrue(bet, game)`（**傳的仍是真實 bet**＝與 #50 契約不牴觸，兩檔檔頭皆寫明防日後誤讀）。**量化本卡真正修掉的東西**：舊制假站頂階 1.8%×押注，在 1% edge 的 dice ＝**吐回莊家理論收入的 180%**（每注淨虧）、cases 120%，money-wheel 只吐 40%⇒**同制度慷慨度差 3.75 倍**；改制後一律 87.5%、且 `pct<1` 使「返水 < 該注理論莊家收入」**成為數學恆真**——把 `progress.js:233` 只存在於註解的紀律變成型別安全。**校準成本中性**（基準＝EDGE 表 22 款平均莊優 2.0613%，demo 向上取整、live 向下取整）實測 demo **+0.12%～+1.00%（零退步）**／live **−0.23%～−1.06%（不增加）**。**驗證**：node **39/39**；**負向驗證證明測項非空殼**——四種擾動（頂階 120% edge 印鈔／demo 調降／live 調升／段位非單調）**皆被對應守衛判 FAIL**、還原後回綠；瀏覽器全 harness **26/26**（edge 群 7/7＝#50 契約未破）、browser==node 逐項相符、**真中央結算點 e2e**（同 NT$10,000：dice 68／pirots 262.14／未登記 slot 140＝legacy，三者與預測精準相等）、三語逐項、零 console error、零水平溢出。sw v136→v137。
  **preview 抓到並修掉一處自身缺陷**：首版單位寫成 `(值)+"% 莊優"`＝**數字＋中文的混合文字節點，i18n 永遠翻不到**（船長 P3 那條雷，本軌**再次**踩到）→ 改為「值只放純數字、單位/語意進可翻譯整句 label」；順帶把三條因改寫而不再被引用的舊字典鍵**原地換成新鍵不留死鍵**（避免累積 U31 型 i18n 債）。**另記一個本輪踩到的驗證坑**：i18n 的 API 是 `HL.i18n.setLang()`**不是** `set()`，首次三語驗證因呼叫不存在的方法而**靜默什麼都沒切**、三語輸出完全相同卻看似「翻譯全壞」——差點誤判為回歸。**驗證腳本呼叫不存在的方法時不會報錯、只會回傳假陰性**，後續三語驗證應先斷言 `HL.i18n.current()` 真的變了。
  **寫檔紀律**：全程未用任何 stdin heredoc（遵 §6 踩雷 #5，即 07-31 本軌掛死 73 小時的事故根因），長腳本一律 Write 暫存 `.js` 再 `node 檔.js` 並加 `timeout`；含錢字號/反引號的中文長文一律走 Write/Edit。只逐檔 add 自己的檔，未碰 `Game assets/` 增刪雜訊、未碰遊戲軌未提交的 `prototype/games/registry.json` 與 `slot-engine/`。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋）、**P6**（結構性未解，tier-3 逾期 12 筆仍在，設定取捨續待船長裁決）；佇列 #52/#54/#57/#58/#59/#61/#62 待做，下輪首要＝**追刷 tier-3 逾期**（dorados/spree/bigpirate/coinsback/betpanda/spinblitz/courtside/punkz/zonko/chancer/kaasino/deal-or-no-deal-win）。

> 追加規則（E3）：新則寫最上方；超過 3 則時把最舊一則移到 BACKLOG-archive.md 最上方。

- **2026-08-03（平台軌 · 20:00 firing 起跑但實際跨到 08-03 收尾 · 刷新 thrill/mega-dice + 台帳審「後台」7 模組全審 + 開卡 #60/#61 + #56 收官）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-200945-5d3b`（帶心跳）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功。**選路＝做而不讓路**：`lead_track=games` 准讓路，但 20:00 為本軌自有窗、當日 14:00 已交付且遊戲軌 16:35 亦已交付 ⇒ 比照 07-30 09:10 先例交付。
  **⚠️ 本輪最該被記住的事＝本 session 就是 M7 所指的「平台軌失聯」本體**：這個 firing 從 07-31 20:09 起跑，但**實際 wall-clock 一路漂到 08-03 12:1x 才收尾**（跨 ~64h）。期間維護軌 08-03 00:00／12:00 兩輪把「platform dark ~57.3h→69.4h、被擋窗累計 6→7」記為 **M7 第 4、5 次坐實**並連續籲請船長介入；遊戲軌 08-03 10:00 更因**我未提交的 `sw.js` WIP** 而讓路（`yield_rounds` 7→8）。⇒ 「一個 session 卡很久」的代價不只自己沒產出，而是**未提交的 WIP 會實質餓死其他軌**。**兩件事讓損害可控**：① 遊戲軌依協定對我的 WIP 做了**原樣 rescue 提交** `6d6d39e`（正是 CONTROL 第 76–80 行建議的「WIP 含控制檔時的安全奪鎖程序」首次實戰）② 維護軌 12:00 明確辨識出我 4 檔 WIP 並**刻意不碰**。醒來後我按鐵律**捨棄凍結前的檔案狀態記憶**、重讀 CONTROL/STATE/git log/git status 才動筆——實測我原本記憶中的 `build_lock` 值與 `sw.js` 版號（v132）**都已過期**（鎖已被清為 false、他軌另有提交），若憑記憶寫入必然覆寫他人工作。
  **step1 調研**（`max_platforms_per_run:2`，取逾期最久的高優先者）：**Thrill**（tier-3、priority 66）+ **Mega Dice**（tier-3、63）。兩份 dossier 都停在 06-29。**淨新增量只有一條，但價值高**＝**回饋率的「計價基準」應是 house edge 而非押注額**：Thrill 的 Instant Rakeback 明載 `up to 70% rakeback on the house edge of every bet`（且刻意不做註冊配對金、全員導入 8 階/16 級 Rewards Program）；Mega Dice 的 Throw 積分率**依遊戲類別分級**（運彩 6／slots 3／其他 casino 2 點每 USDT）＝手法不同但**獨立收斂到同一原理** ⇒ 連同 #50 檔頭已記的 BC.Game／Roobet／Duelbits，構成**五平台共識** → 開卡 **#60**。**ApexWin 對照為機械實測非臆測**：`progress.js:246 rbAccrue` ＝ `bet × VIP率 × happyhour`，**與 edge 完全無關**；配 #50 的 EDGE 表實算＝同一個率在 1.00% edge 的 originals 吐回莊家收入 **30%**、在 3.7% edge 的 slot 只吐 **8%**（差 3.75 倍，純屬基準選錯的副作用）。**更關鍵**：`progress.js:233` 既有註解自己寫著「返水率 ≥ 莊優＝結構性虧損」，但現行架構**無法機械保證**——改用 edge 基準後該不變量成為**數學恆真**（`edge × pct`，`pct<1` 恆不超過 edge）＝把口頭紀律變成型別安全。**依去重紀律未開卡**：Thrill lossback 10%（#48 已覆蓋）、referral 10%（#58 已覆蓋）、Mega Dice 個人化促銷（#52/#54 已覆蓋）、Telegram 通道與 $DICE 代幣（需後端／屬 CONTROL.avoid）。
  **新鮮度**：刷完 LIVE 逾期 **13→11/31＝35%**（全數 tier-3、皆 P6 結構債）。**順帶對帳維護軌 12:00 的 52%(16/31)**：差異源於**判準邊界**——維護軌計 `next_due <= today`、我計嚴格 `<`（過期才算）；stake／bc-game 兩張 **tier-1 的 `next_due` 恰為 2026-08-03（今天到期、非過期）**，故其報告的「首破 tier-1」精確說是「tier-1 今日到期」。⇒ **下一輪平台軌應優先刷這兩張 tier-1**（最高價值且已到期）。
  **step2 台帳審「後台」分類（輪替接續 07-31 活動）：7 模組全審**，seed-date 積壓 **13→6**。實質更動一筆：**活動/紅利設定 Bonus & Campaign Builder absent→partial**——原述「促銷=硬寫陣列、加活動須改碼」**已部分過期**，#49 `HL.promoCal` 已提供資料驅動排程容器（三種排程語意、即時求值故載入序無關、`enabled:false` 即下架）；**仍 absent 的另一半＝builder 本身**（無規則引擎/分群/模板/編輯 UI）故判 partial 而非 present。另**具體化了 CMS 缺口形狀**（`mock-data.js` 20+ 個硬寫常數、每則無 enabled/排程/地區/語言欄）→ 開卡 **#61**（容器優先：只做內容資料層，編輯器留後台）；並**點出一處長期記載不精確**：模組名含 Jurisdiction，但全站**並無任何法域軸**（CLAUDE.md 目標 4 要的 money-mode + jurisdiction 只做了前者），缺口描述已改為「第三軸完全空白」。
  **step4 實作＝#56 收官**（該卡本輪由 🟦 完成為 ✅，詳見卡片落地段）：`p2p_out`/`p2p_in` 對稱型別 + 現金流三張分類表（`CASH_IN`/`CASH_OUT`/`INTERNAL`）＝唯一真相、退役死型別 `trade`、錢包紀錄列重構為描述子 `TXN_KINDS`、儀表板加「站內轉贈」tile。**雙環境契約**接進 `tests/run.js`（含 #56 迴歸鎖），node **32/32**、瀏覽器 **3/3**。**preview 抓到並修掉一處自身缺陷（可重複踩、已寫入檔頭）**：`ledger.js` 在 `index.html` 載入序（第 52 行）**早於** `selftest.js`（第 84 行）⇒ 收尾直接 `registerTests(HL.selftest)` 時該物件尚不存在，**瀏覽器三個測項全部靜默未註冊**（`run({group:"ledger"})` 回 total=0，而 node 端正常故差點漏掉）→ 改延後至 `DOMContentLoaded`；**此坑對任何載於 selftest.js 之前的核心檔都成立**。**真實 UI e2e**（非只測純函式）：錢包→交易→送出 3000 ⇒ 餘額 −3000、`p2pOut` +3000、`withdraw`/`cashNet`/`NGR` 三者皆不動、`walletTxns.kind="p2p_out"`；**對照組**真實提款 1000 仍正常推動 cashNet（證明不是把記帳整條拔掉）；儀表板 cashNet 顯示 **−NT$1,000** 而非舊碼的 −9,000。三語逐項驗證，並修掉一處自己寫出的 P3 雷（tile sub 原為「中文＋動態值」串接＝永遠翻不到，已改純片語）。**副產品**：儲值/提款/轉贈原本標籤與時間融成單一文字節點＝**從來翻不到**（P3 雷的既存實例），拆開後**首次可譯**。sw v132→v133。
  **寫檔紀律**：含錢字號/反引號的中文長文一律走 Write/Edit 或 node（未用裸 shell heredoc，遵 07-31 自我複製事故教訓）；只逐檔 add 自己的檔，未碰 `Game assets/` 增刪雜訊與他軌檔。**剩餘平台軌待處理**：**P3**（#45–#49 五面板字典仍零覆蓋）、**P6**（結構性未解，設定取捨續待船長裁決；本輪逾期全為 tier-3 正是其症狀）；佇列 #52/#54/#57/#58/#59/#60/#61 待做，**下輪首要＝刷 stake+bc-game 兩張今日到期 tier-1**。

- **2026-07-31（平台軌 · 14:00 建置輪 · 實作 #50 成本加權進度 `HL.edge` + 台帳審「活動」6 模組 + 刷新 wow-vegas/duelbits + 開卡 #58/#59）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-141020-6b7e`（帶心跳）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功（TOCTOU 未觸發）→ 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、且 dark 僅 3.1h 未達 catchup 24h，但 **14:00 是本軌自有窗**、遊戲軌今日 10:30 已交付且下一窗 16:00（我在此前收尾即不搶火力）＝比照 07-30 09:10 先例選擇交付而非讓路（`yield_rounds` 已達 7，不宜再累加無謂讓路）。**step1 調研**（`max_platforms_per_run:2`，取 tier 最高 + 逾期最久）：① **WOW Vegas**（tier-2、唯一逾期的 tier-2）② **Duelbits**（tier-3、逾期 5 天最久）。兩份 dossier 停在 06-26/06-29，**舊缺口 grep 逐項複核：多數已關閉**——duelbits 四個點子（Hi-Lo/Keno/PvP Duel/Towers）全數落地、其「週/月桶未規劃」**已過期**（週桶＝`core/cashback.js` 已在位，僅剩月桶）；wow-vegas 的 Happy Hour（#35）與大方註冊禮（onboarding+#48）亦已關閉。**淨新三條、依去重紀律只開兩卡**：(a) Duelbits **Ace Lounge 明載為「基於 house edge 的永久 cashback，而非傳統 VIP 階梯」** ⇒ **不開新卡，歸併為 #50 的第三個平台共識**（BC.Game／Roobet／Duelbits 三家獨立收斂）——而 #50 正是本輪實作卡；其「30 階」亦**不開卡**（ApexWin 已有 5 段×5 子級＝**21 級**，granularity 同量級＝調參非結構缺口）。(b) wow-vegas **referral 雙向獎勵** ⇒ 開卡 **#58**。(c) wow-vegas **滾動 30 天視窗決定當前段位** ⇒ 開卡 **#59**（本檔 06-29 即列「最關鍵缺口 ①」卻逾月未立卡）。(d) wow-vegas **每日 prize drops 限定六個指定 slot 標題**＝「遊戲指定型隨機掉落」（Duelbits Drops & Wins 同軸＝兩平台共識）⇒ 因 `max_cards_per_run:2` 已用盡，**寫入 dossier 列為下輪首要候選、不硬塞第三卡**。逾期率 **26%→19%**，且**已無任何 tier-1/2 逾期**（wow-vegas 為唯一一筆，本輪清掉＝順手解掉維護軌 M1 的點名）。**step2 台帳審「活動」分類（輪替接續 07-31 上一輪的「功能」）：6 模組全審**，seed-date 積壓 **19→13**。實質更動一筆：**促銷/活動框架 weak→partial**——① 排程層已在位（#49 `HL.promoCal` 三種排程語意 window/recurring/always + 7 日時間軸）② 限時加成窗口已在位（#35 happyhour 走真 `rbAccrue`）③ **本欄前次自記的 ★ 缺口「限時損失保險/新手安全網」已由 #48 `HL.safetynet` 落地 ⇒ 該 ★ 撤除**；**仍不判 present 的三點**：無 opt-in 狀態機（#52）、無受眾分層/A-B（#54）、以及 ⚠️ **容器採用度缺口（與船長 P4 對 `HL.dock` 的發現完全同形）**——promoCal 的 7 個活動定義**全部集中寫在 `promo-cal.js` 檔內**，`HL.promoCal.register` 的**外部註冊者為零**（全 repo grep 僅該檔自身），「各模組自我上架」的設計意圖尚未真正被使用；已把「補上第一個外部註冊者」寫進 #58 的範圍。**step4 實作 #50**（見卡片落地段）：`core/edge.js` 22 款 edge 係數 config 表（值全取自程式內實際 `EDGE` 常數或已過閘實測 RTP，**不用行銷值**）；加權只在中央點一處發生、且**只餵 VIP 與賽季經驗**，`bet` 本身與帳本/返水/彩金/抽獎券/任務目標/公會貢獻**全部維持真實金額**。**設計＝一條曲線兩種縮放（站別感知，比照 §11 既有範式）**：假站 SCALE=1 ⇒ 最低倍率恰 **1.00×**（沒有任何遊戲比改版前更慢＝避免懲罰感，正是 wow-vegas 檔自記的設計建議）；真站 SCALE=1/mean(SHAPE) ⇒ 全表**平均恰 1.00×**（純重分配、不加發經驗＝不鬆動剛轉正的真站 NGR）。**兩性質皆為常駐測項**而非口頭宣稱。**附帶修掉一個既存缺陷（本卡前置條件）**：`views/instant-games.js` 的 **Dice／Limbo／Plinko 三款完全沒傳 `game:`**，於 `instant.js:76` 落入 `opts.game || "instant"` ⇒ 三款旗艦 originals 長期以**同一個假 key `"instant"`** 回報中央結算點；**受害者不只本卡**——#51 注單中心分不出這三款、`HL.heat` 替不存在的遊戲加溫、`HL.achievements` 逐遊戲統計失準（其餘 13 個 betPanel/betArea 呼叫本來都有帶；grep 確認無任何程式消費 `"instant"` 這個 key ⇒ 修正零破壞）。**驗證**（node + preview `?demo=1`）：node harness **16/16**、瀏覽器 **17/17**；**走真中央結算點實測** money-wheel 押 10000 → VIP **+18000**（1.80×）而 turnover／公會貢獻 **+10000**、抽獎券 **+5**＝進度與金錢的分離**機械證實**；pirots 10000 → VIP/賽季 **+16500**（恰 1.65×）；**真打一局 Dice**（繞過登入 gate 直接 render 該 view 並 click 真按鈕）→ 中央點收到 `record("dice", 50, 99)`＝key 修正在真 UI 路徑生效；倍率表 23 列排序/gold token `rgb(255,181,36)`/`overflow-y:auto`/零水平溢出；VIP 面板 CTA 寬 386px（未撐成長條）；**三語逐項**。**preview 抓到並當場修掉自身兩處缺陷**：① `HL.ui.modal` 是**位置引數** `modal(title, bodyNodes, opts)`，首版誤用物件形式 ⇒ 面板渲染出**零列**（已改正並在檔內留註）② **「莊家優勢」誤判為簡繁同形而漏列 HANS**，三語驗證抓到簡中殘留繁體（正解 `庄家优势`，已補）。i18n 新增鍵**經 node 括號配對實測無重複鍵**（EN 835→843、HANS 718→726），不重蹈維護軌 12:00 才剛清掉的 U31 靜默覆蓋債。sw v120→v121。**⚠️ 誠實限制**（已同步寫入卡片）：真站中性模式未在真站環境實跑（切站需 reload、headless 輪未做，但 `meanWeight("live")=0.9982` 與測項兩環境皆過）；「平均 1.00×」是對係數表均勻抽樣而非真實玩家分佈（若玩家集中低 edge originals，真站實際平均低於 1.00×＝偏保守不會超發）；沙箱 viewport=0 故窄螢幕幾何未量測；`slot`(Shadow Ritual)/`chicken` 無 RTP 模型故刻意未列＝維持 1.00×。**寫檔紀律**：本輪所有含錢字號/反引號的中文長文一律用 Write/Edit 工具（遵 07-31 上一輪 BACKLOG 自我複製事故的教訓），未用 shell heredoc；只逐檔 add 自己的檔，未碰 `Game assets/` 增刪雜訊。**剩餘平台軌待處理**：**P3**（#45–#49 五個既有面板字典仍零覆蓋）、**P6**（結構性未解，設定取捨續待船長裁決）；佇列 #52／#54／#56／#57／#58／#59 待做，下輪首要候選＝**遊戲指定型隨機掉落 Drops & Wins**（兩平台共識、可掛 promoCal + 本輪剛修好的逐款 game key）。

- **2026-07-31（平台軌 · **catchup 建置輪**（dark 24.85h>24＝禁讓路）· 本 session 即 07-30 14:00 那輪凍結後甦醒者 · 刷新逾期 shuffle/gamdom + 台帳審「功能」12 模組全審 + 開卡 #57 + 實作 #51 注單中心 + 消化船長 P7 + 修 BACKLOG 自我複製）** — 閘門全過（loop/platform/auto_implement on）。**存活事件（本輪最該被記住的事）**：這個 session 於 07-30 14:10 claim `p-141010-7a4d` 後**當場凍結**（起始==最後心跳），16:04 遭遊戲軌依 stale-heal **正當奪鎖**（`stalled_rounds` 0→1）；07-31 10:21 甦醒＝CONTROL 第 87 行預警的「凍結者醒來帶舊記憶覆寫」情境**本人**。已按鐵律**捨棄全部凍結前檔案狀態記憶**、完整重讀 CONTROL/STATE/db/git log/git status 後以新 token `p-102130-c4e1` 重新進場——奪鎖協定與甦醒協定兩端至此皆已實戰驗證。**調研**（`max_platforms_per_run:2`）shuffle+gamdom 兩張 tier-3 逾期票（各逾 5 天，維護軌 M1 連兩輪點名）：兩份 dossier 都停在 06-26，**舊記缺口 grep 實證已全數關閉**（reload.js/challenges.js/rain.js/instant-hilo.js/onboarding.js 皆已落地）。淨新只有一條＝**Shuffle「首位完成者獨得」限量挑戰**（獎品單一名額、先搶先贏、被領走即消失）⇒ ApexWin 現有任務/挑戰**全部**是純個人日常（各自累進各自 claim、無名額概念）、tournament 是排名分潤（人人有份），**「稀缺性：競逐同一份限量獎」軸線完全空白** → 開卡 **#57**。Gamdom 的「依 rank+playstyle 個人化促銷」則**依去重紀律歸併**為 bet365 targeted offers 的第二平台共識（#52/#54 已覆蓋相鄰面），**不重複開卡**；日/月雙節奏排行榜判為參數差非機制差，不開卡。逾期率 **32%→26%**（回到 30% 門檻下）。**台帳審「功能」分類（輪替接續 07-30 金流）：12 模組全部重驗**，seed-date 積壓 31→19。查獲**一處過期記載已更正**：可驗證公平 evidence 舊述「baccarat/roulette 仍 Math.random」早已於 07-30 G5 失效（實測 20 個 views 引用 HL.fair）——真實剩餘缺口改記為 ① 旗艦 `slot.js`(Shadow Ritual)+`chicken.js` 仍未接 ② 無逐局注單/nonce 對應表（＝本輪實作的 #51 正是此出口）；另把中央掛鉤下游訂閱者由舊記 5 條擴為**實測 18 條**。**實作 #51 `HL.betlog`**（見卡片落地段：9 欄描述子單一真相、環形 500、站別隔離、`HL.fair.isPF` 收斂、雙環境 module.exports 讓測項每次重跑、node 10/10 + 瀏覽器 3/3 + preview 零 error）。**消化船長 P7**：`providers.json` 20/27 筆無 `last_verified` 已回填種子日 2026-07-23（依 P1「寧可低估新鮮度」紀律）→ 27/27 齊備，逃生閥②重驗佇列終於碰得到它們。**順修一處自家造成的檔案損毀**：`BACKLOG.md` 竟為 515 行的**自我複製**——07-30 平台輪（我這條軌）以未加引號的 heredoc 寫卡 #55 時，文中的 `NT` 加錢字號被反引號包住而觸發 shell 指令替換，把整份檔案灌回自己內部並吃掉一段文字；已還原為單一份 274 行（卡號 1–57 連續無重複）並補回被吃掉的文句。**教訓（供三軌共用）：凡以 bash heredoc 寫含錢字號／反引號的中文卡片文字，一律用引號式 heredoc（分隔符加單引號），或改用 Write/Edit 工具——本輪寫這行日誌時又被同一個坑絆了一次（字串內的引號式 heredoc 範例把 JS 字串提前關閉），足見其反覆性。**另記一筆維護軌可撿的債：`i18n.js` 有 5 個**既存**重複鍵（兌換／收合／領取／🎁 領取中心 · 獎金錢包／本期剩餘，HEAD 即有、非本輪引入），物件字面量後者靜默覆蓋前者。收尾清 `build_lock=false`。

- **2026-07-30（平台軌 · 建置輪 · 消化船長 P1/P2/P4/P5 四項＋P3/P6 部分 · 實作 #55 成長進度可停靠面板 · 開卡 #55/#56）** — 閘門全過（loop/platform/auto_implement on），進場 `build_lock=false` 乾淨 → claim `p-091004-b3e7` → 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功（TOCTOU 未觸發）→ 收尾清回 false。**選路＝做而不讓路**：`lead_track=games` 准讓路、dark 19.9h 亦未達 catchup 24h，但 08:00 窗已到且 games 軌 07-29 兩個建置窗（16:00 Plinko 過閘、22:00 G2+G4）皆已交付，無需再讓火力。⚠️ **本 session 自身兩度長時間凍結**（07-29 13:16 進場 → 凍 11h → 00:2x 醒 → 再凍 9h → 07-30 09:08 醒）：期間 **07-29 20:00 平台窗未曾送達**，首段醒來寫下的讓路痕跡被 games 軌 `a0c29c1` 連帶提交（含我校正的 `yield_rounds` 4→5、以及上一輪誤填成**未來時間** 14:05 的 `last_platform_run_at`——未來時戳會讓 dark 判定整體晚 ~50 分，已據 git log 更正為 13:14:50）⇒ 凍結後任何「上一輪剛跑完」的記憶都會過期，醒來全部重讀 git log/status/mtime 才動筆（§7 第 1 條親身驗證）。**step1 調研**（`max_platforms_per_run=2`，取 tier-2 逾期票）：① **LeoVegas**（逾期 6 天＝維護軌 M1 點名的次逾期最久）——定位 reconfirm；**淨新＝no-wager free spins（零流水免費旋轉、贏分直接可提）** ⇒ 揭露 `HL.bonus` **只支援「帶流水」單一紅利型別**的引擎缺口：要送零流水獎只能繞過 bonus 引擎直接改餘額，而那會連帶繞過 `source` 標記與 `HL.ledger` 記帳；建議紅利 spec 增 `wagerMult` 覆寫欄位（未宣告＝沿用全域、宣告 0＝直入可提但照走 `bonus.add`→`ledger.record`），台帳「獎金/流水引擎」**present→partial**，列候選未開卡（`max_cards_per_run=2` 已滿）。② **Toshi.bet**（逾期 1 天）——**零新開卡**：其「rakeback 自註冊即生效無門檻 + 需啟用的 50% Rakeback Boost」依去重紀律**歸併為既有 #52 的第三個獨立平台共識**（原 bet365 opt-in + Rollbit 時間窗）而非重複開卡，僅提升其排序權重；另記三段式歡迎序列（200/150/100%）待「活動」分類輪替時審。兩檔 next_due→08-13/08-29，dossier 各補 07-30 附錄。**step2 台帳審（金流 分類＝船長 P1 指名的未審分類，種子日 07-23 後首次複查）**：5 模組 evidence 全部重寫——① `支付/收銀台` 維持 partial，但 grep `psp|PSP|通道|channel|對帳|reconcil` **零命中**（連 PSP 抽象介面的殼都沒有，付款方式僅 `FIAT_METHODS`/`CRYPTO_COINS` 兩個純展示常數陣列，無 enabled/地區/幣別/限額欄位）② `提款審核佇列` 維持 absent 但**語意更正**：現行提款是**前端即時扣款結算**（`app-shell.js:170` 直接 `state.set`）⇒ 不是「還沒接審核 UI」而是缺「提款→待審」整條狀態機，真金前補此項時收銀台流程需一併改寫（非加法）③ `玩家間交易/商城` 舊述「無玩家間交易」**已過期**（`app-shell.js:158` 早有休閒模式轉贈 UI + `money.canTrade()` 閘），partial 的真實理由改為「單向且無對手方」④ `錢包/帳本` 維持 present 但**查獲記帳失真**→ 開卡 **#56**：轉贈被記成 `pushDemoTxn("withdraw")`＝玩家互贈被計為**營運提款**，汙染 `ledger.derived()` 淨現金流/NGR（且 Demo 下收款方從未入帳＝幣是淨銷毀非移轉）⑤ `獎金/流水引擎` present→partial（見 LeoVegas 訊號）。**P1 根因修掉**：台帳原本只有**檔案級 `updated`** 一個時戳（改任一模組就整檔看似新鮮、個別模組多久沒審機械上不可查）→ **全 46 模組加 `last_audited`**，逾期可 node 一行算出；實測 07-30×7／07-29×2／07-28×6／**07-23×31**（＝從未複查待輪替，與 P1 指出的 31 筆吻合）。**P2 兩處據實更正並更正 P2 自身一處不準**：`導覽殼層` partial→**present**——手機主導覽缺口早由 commit `8981686`（2026-07-10「DEBT R1」）以**左側抽屜 `ax-drawer`**（含 Escape/focus/aria-expanded）修掉，**不是**船長寫的「底部列」（底部列承載任務/簽到/錦標賽/福利/VIP 五個獎勵入口＋FAB，**不含主分頁切換**）；大廳佔位卡由「~140／可玩 ~18」改為 node 實跑值 **51 筆（playable 2／非 playable 49，slots16/live8/table8/originals7/jackpot6/gameshow6）**、可玩約 20 款登錄（views 自我註冊 18 + 種子 2）⇒ 佔位卡仍是真缺口但規模僅舊述 1/3、優先度下調。**P5 選「廢除」**：`high_water_dossier_date` 係**舊四軌流水線的跨軌消費游標**（radar/investigate 產 dossier、evolve 另一軌才消化，故需水位），三軌重構後平台軌同輪自研自用、**全 repo 無任何邏輯讀它**——這正是它能死鎖 19 天而毫無影響的原因；已從 STATE 與 SKILL 第 6 步移除，完整理由留於 STATE `_abolished_high_water_doc`，並順手在 SKILL 補上「`last_platform_run_at` 務必填實際時間」的警語。**step3+4 開卡 2 張、實作 1 張＝#55**（消化 P4）：`layout/dock-growth.js` 成為 `HL.dock` 首個**第二代註冊者**——一行 `HL.dock.register` 即取得 開/關/**收合(可收納)**/桌機拖曳自由擺放/跨站持久座標/手機互斥，**面板本身零浮窗程式**（若底座設計有問題這張卡會當場暴露）；內容＝#46 季票階級與經驗、#45 成就點數與解鎖率、#47 公會週貢獻與排名三條摘要 + 各自 CTA 開回完整 modal。**刻意不把 #45–#49 直接改掛 dock**：徽章牆/季票階梯/公會週榜都是「進去專心看一次」的全幅內容，塞進 360px 側浮窗會讓資訊密度崩掉（違反 `/apexwin-ui-quality`「元件情境自適配」）⇒ **dock 管「邊玩邊看的摘要」、modal 管「完整檢視」**。同時**消化 P3 的一半**：新面板 32 條 key 同步補 `en`/`zh-Hans`（船長給的第二條路「平台軌自己在落地時同步補」）。**驗證**（preview `?demo=1`，零 console error；§9 截圖逾時→DOM eval 逐項）：`HL.dock.ids()` `["partner","chat"]`→`["partner","chat","growth"]`；收合 body `display` none↔flex；三面板桌機自動堆疊 right **16/388/760px**（STACK_GAP 372 算術正確）；`ax:dock:v1` 座標持久；手機路徑（`clientWidth` 覆寫 400）`mobileExclusive` 只留 growth；**走真中央結算點** `liveStats.record("dice",5000,0)` → 季票階 4/30・成就 60 點・公會 NT$7,000 三區同步跳動；FAB toggle 三態；繁/簡/EN 三語逐字（動態賽季名/公會名正確保持原文＝資料非 UI 文案）；token 全解析為具體值（gold `#ffb524`／radius 8px／13px·11px）；CTA `display:block` 但寬 78px **不撐成長條**；零水平溢出；progressbar `role`+`aria-valuenow`；CTA 可聚焦。**首驗抓到並當場修掉自身兩處缺陷**：① 經驗值誤用 `HL.dom.money` 而冠上 `NT$`（XP 不是金額）② 無進度條的區塊（未入會的公會區）CTA 因 button 為 inline-block 而貼在 inline `<small>` 後 → 補 `display:block`。改 `dock-growth.js`(新)／`app-shell.js`(FAB 一項)／`components.css`(`.ax-growth*`)／`i18n.js`(EN+HANS 各 32 條)／`index.html`(dock.js 後一行)／`sw.js`(v112→v114)。**引擎改進點（後續類似面板比照）**：定時重繪的面板必須加**資料指紋**（本輪為三模組狀態 hash，5s tick 僅在真變化時重繪）——除省 DOM churn，關鍵是避免「每次重繪先產繁中節點再被 i18n walker 翻回」導致**非繁中語系每 5 秒閃一次原文**；另記 `HL.i18n.t` 是 **passthrough**（`function t(k,def){return def}`）、翻譯只發生在 DOM walker 且**要求整個文字節點等於一條 key**，故「中文＋動態值」串接（如 `解鎖 1 / 19 枚徽章`）永遠翻不到——本輪首版即犯此錯、preview 抓到後改為「中文全片語 + 值純數字」並寫成 `dock-growth.js` 檔頭契約。只逐檔 add 自己的檔，未碰 `Game assets/` 增刪雜訊與他軌未提交檔。**剩餘平台軌待處理**：**P3**（#45–#49 五個既有面板字典仍零覆蓋）、**P6**（**結構性未解**：`max_platforms_per_run=2` × 每日 3 窗 vs 32 筆多為 30 天週期＝刷新速率長期追不上到期速率；本輪只還 2 筆使逾期 12→10，建議 tier-3 週期 30→45~60d 或改「1 深挖+2 淺刷」——因涉調研密度取捨故**留船長裁決、未自行改設定**）、**P7**（`providers.json` 20/27 無 `last_verified` ⇒ 逃生閥②的重驗佇列永遠碰不到它們）。

- **2026-07-29（平台軌 · catchup 建置輪 · 凍結鎖死結解除後接手 · 實作 #53 自我檢測底座 `HL.selftest` + 刷新 crown-coins/1xbet + 台帳審「擴充性」分類 + 開卡 #53/#54）** — 閘門全過（loop/platform/auto_implement on）。**開場即遇死結**：`build_lock` 自 07-28 19:13 由 **h- 前景會話**持有、心跳零前進 **17.6 小時**（≫ `lock_heartbeat_stale_min` 45 分），其未提交 WIP 含 `CONTROL.md`/`STATE.json` 本身 → 前四輪 firing（games 22:00、maintain 00:08、games 10:00、maintain 12:00）皆正確留痕讓路，但**連五輪呼籲人工介入無人回應**，三軌全暗（platform dark 22h40m、逼近 `catchup_if_dark_hours` 24h）。本輪 12:49 進場先做**完整重讀**（git log/status/逐檔 mtime/diff/`node --check` 語法檢查）確認該批 WIP 為**連貫且完整**的 07-28 健檢批（買入 RTP 修正 + 引擎存活監測），正依 stale-heal 準備奪鎖時 **12:51 該前景會話甦醒**、12:53 自行 commit `242987b` 全批並釋放鎖 → **改為讓其自然收尾、不奪鎖**（不與活躍 session 競寫；已備妥的救援暫存全數撤回）。3 分鐘後乾淨接手：claim-token `p-125630-4c7e`（帶心跳格式）→ 停頓做調研與源碼讀取 → 重讀確認 token 仍在＝claim 成功（E7 TOCTOU 未觸發）→ 收尾清回 false。**step1 調研**（`max_platforms_per_run=2`，取最高優先逾期者）：① **Crown Coins**（T2/P75，逾期 2 天）——reconfirm 續居 US sweeps 榜首、遊戲庫 450+、Crown VIP Club 6 階 coinback 2%→5% + 生日禮；**淨新①＝Early Bird 新遊戲搶先體驗/獨家標題**（新遊戲依排程分批開放而非全站同時上架）→ 本輪開卡 #54；**淨新②＝Crown Bingo Live 每日固定時段場次**（同源排程訊號；Bingo 本體屬遊戲軌內容、續低優先記錄）。② **1xBet**（T2/P74，逾期 5 天）——**零淨新缺口**：8 階忠誠、Promo Code Store（積點兌換促銷碼）、Drops & Wins 聯網積分賽、provider/熱度/類型三軸篩選，經 grep 對照 ApexWin `core/shop.js`+`core/redeem.js`（積分商城骨架）／#15 錦標賽／資料驅動 GameList **皆已同構覆蓋**；VIP 5 階 vs 8 階屬資料非架構，且 #50 成本加權 VIP 才是同區更高價值的改動。兩檔 next_due→08-12，dossier 各補 07-29 附錄。**step2 台帳審（擴充性 分類，輪替接續 07-28 資料）**：`Dockable Layout` partial／`Design Tokens & Components` present 維持；**`自動化測試 Automated Tests` absent→partial**（本輪落地 #53）；**新增模組「內容/遊戲上架排程 × 受眾分層」absent**（對應 #54）。擴充性分類 3→4 模組。**step3+4 開卡 2 張、實作 1 張**：**#53 `HL.selftest` 自我檢測底座**（✅ 本輪完成）——選它而非佇列既有的 #51 betlog／負責任博弈，理由是**本輪審的正是擴充性分類，而「自動化測試」是該分類唯一 absent，且 07-28 健檢剛以真實代價證明其必要**（`Dead By Noon` 買入價誤設 80×、E[買入]≈41.7× ⇒ **買入 RTP 僅 52%、玩家暗虧 44pp**，卻被記「13/13 PASS」上線——一次性 `node -e` 驗證做完就消失、下一輪無人重跑）。新增 `core/selftest.js`（資料驅動測項註冊表 `register({id,group,title,tier,env,run})` + 執行器 + 斷言 `ok/equal/close/finite/isFn/skip` + 唯讀面板，比照 `HL.games`/`HL.dock`/`HL.promoCal` 註冊表家族）＋ `tests/run.js` ＋ `tests/checks-games.js`；**雙環境共用同一份引擎與同一批測項**（瀏覽器 7 項不變量／node 8 fast + 4 deep，**失敗 exit 1 ＝可直接當遊戲軌保真閘的一道自動檢查**）；分層 fast（秒級）／deep（蒙地卡羅，`--deep` + `AX_DEEP_SIMS` 可調樣本）。**驗證**：node fast 8/8 PASS、deep 4/4 PASS（200k 樣本，四款買入 RTP 皆帶內＝**獨立佐證 07-28 重新校準正確**）；preview 零 console error、面板 7/7 PASS 且**連跑兩輪後餘額不變、零殘留暫存 key**（唯讀非破壞鐵律）、⚙ 工具面板入口就位。**負向驗證（證明測項非空殼）**：回放 07-28 兩處實際缺陷價 → `dead-by-noon` 80× 測得 **53.4%**、`pirots` 100× 測得 **99.7%（可套利）**，harness 皆判 FAIL。**另修自身兩處缺陷**：① 首版把 `simSpin` 的 `force` 當物件傳，而 `dead-by-noon` 判 `force===1` ⇒ 該款會**靜默量到 base RTP 而非買入 RTP**（已改標量）；② `games/registry` 首版斷言「可玩必有 render」→ 旗艦 `shadow-ritual` 判 FAIL，追查 `games.js` 的 `launch()` 實有 **render 函式／route 字串兩條合法路徑**（shadow-ritual 走 `route:"slot"`）＝**測項假設錯、非產品缺陷**（已改為兩者其一即通過；**未動任何遊戲檔**）。順帶為 `HL.i18n` 補唯讀 `dict()` 出口供字典檢測。**#54 遊戲上架排程 × 受眾分層**（M，🟦已批准待做，接 #49 promoCal 排程底座；`release:{startAt,audience}` 未宣告者零回歸）。改 `core/selftest.js`(新)／`tests/run.js`(新)／`tests/checks-games.js`(新)／`core/i18n.js`(+dict 出口)／`core/demo-tools.js`(入口一項)／`index.html`(i18n.js 後一行)／`components.css`(`.ax-self*`)／`sw.js`(v109→v110)。只逐檔 add 自己的檔，未碰 `Game assets/` 增刪雜訊與他軌未提交檔。**下一步**：**#51 betlog**（前輪指定首選、與 #16 fair 互補、零經濟回歸）與**負責任博弈工具組**（前輪已釘為首選候選；入口/狀態欄位/翻譯三者就位只缺引擎，且已釘清與 `avoid` 的界線）**兩者續為最高優先、且皆已逾一輪未開工**；#50 成本加權 VIP／#52 promo opt-in／#54 續待。tier-2 leovegas + tier-3 shuffle/gamdom/duelbits + 07-29 到期批（wow-vegas/toshi-bet/thrill/mega-dice…）待刷。**⚠️ 引擎健檢**：`yield_rounds` 本輪補記前四輪讓路；`stalled_rounds` 未計（最終由前景會話自行收尾、未真奪鎖），但**該死結已暴露一個結構性風險**——當持鎖者的未提交 WIP 內含 `CONTROL.md`/`STATE.json` 本身時，後續軌依現行保守判準**永遠無法自行解鎖**，只能等人工；建議下一輪 maintain 軌評估「WIP 含控制檔時的安全奪鎖程序」（例：先把該批 WIP 原樣 commit 為 rescue 提交再接手）。
- **2026-07-28（平台軌 · 研究輪 · 刷新逾期 bet365/rollbit + 台帳審「資料」分類 + 開卡 #51/#52）** — 閘門全過（loop/platform/auto_implement on）。**選路＝研究輪不建置**（與同日 13:57 前一 firing 的建置輪區隔）：本 firing 為 14:00，`last_platform_run` 已是 07-28（#49 `HL.promoCal` 於 **20 分鐘前**才 commit `378d603`），且 `lead_track: games` 明示平台軌可讓路 → **同一小時內連推第 2 個 heavy build 不符「質>量」**，改把火力放在真正累積的**研究債**：tier-1 bet365 逾期 4 天、tier-2 rollbit 逾期 4 天，另有 1xbet/leovegas/crown-coins/shuffle/gamdom/duelbits 續逾期＝非空心跳。進場 build_lock=false→claim-token `p-141744-7c3e`→停頓做 WebSearch 取材與 prototype grep（＝pause）→重讀確認 token 仍在且 CONTROL mtime 14:17:55 持住＝claim 成功無並行 firing（E7 TOCTOU 未觸發）→收尾清回 false。**step1 調研**（`max_platforms_per_run=2`，取最高優先逾期者）：① **bet365**（T1/P85）——**第 3 次一致 reconfirm**「casino 刻意不設點數/階梯 VIP（評測列為其最大弱點），改以輪替 + targeted offers」，且**同集團 poker/bingo 與多數非美地區反而有 loyalty 階梯**＝同品牌內按產品/地區切換留存哲學（記錄、非缺口）；**淨新＝每個促銷需玩家主動 opt-in 才生效**。連 4 輪無機制淨新 → **T1 刷新週期由 7 天降頻為 14 天**（省成本，next_due 08-11）。② **Rollbit**（T2/P76）——**淨新＝返水是時間窗階梯**（首 24h 每注 15%、之後常態 5%），先前記的「10–20%」為描述模糊，本輪收斂；對外「最高 70% rakeback」實為 rakeback+cashback+掉落+races 多桶疊加行銷值（ApexWin 已同構多桶、只差沒合成一個總回饋率呈現）；NFT Lootboxes 骨架已被 `HL.luckyspin`+`HL.shop` 覆蓋、RLB 質押續 avoid。next_due 08-11。兩檔 dossier 各補 07-28 附錄 + 來源。**step2 台帳審（資料 分類，輪替接續 07-28 早輪的 資安）**：`報表與匯出` 維持 **weak**——全 `prototype/src` grep `csv|CSV|Blob|download` **零命中**（玩家端與營運端皆無任何匯出出口）；`資料/分析` 維持 **partial**（營運側 ledger+opsBoard+phase6 已成形，產品側無事件 schema/漏斗/世代留存）；**新增 ★ 缺口模組「玩家注單/投注歷史 Bet History & Round Log」absent**——grep `投注紀錄|注單|betHistory|roundHistory` 零命中，`HL.liveStats` 為工作階段記憶體統計（`fresh()` 每次歸零）、`HL.fair` 只留最近 12 筆（`fair.js:101`）、交易紀錄只有 deposit/withdraw ⇒ **#16 可驗證公平實質半殘**（種子輪換後無注單可對應 nonce）。資料分類 2→3 模組。**step3 開卡 2 張**（`max_cards_per_run=2`）：**#51 注單/投注歷史中心 `HL.betlog`**（M，🟦已批准待做，**建議為下一張實作卡**——2026 業界基準線「bet history 含 seed/nonce/timestamp + 可匯出」；寫入點只掛既有中央點 `liveStats.record` 尾端＝不動任何遊戲檔即全遊戲通吃，環形緩衝 + 站別命名空間 + 單筆深連結 `HL.fair.verifyModal` + CSV 匯出順帶補「報表與匯出」weak）；**#52 促銷 opt-in「我的優惠」+ 限時返水加成 rakeboost**（S–M，🟦已批准待做，bet365 opt-in × Rollbit 時間窗返水**兩平台共識**；② 掛 #49 promoCal spec 加選用 `optIn` 欄位＝未宣告者零回歸、`HL.rakeback.rate()` 加 boost 乘數層並 register 進日曆＝活動排程與回饋率首次接線；真站須另設保守 boost 值並經 `HL.ledger` 確認 NGR 不轉負、與 #48 safetynet 勿雙重補貼）。**step4 實作＝略過（研究輪 · 讓路 games）**，**未寫任何 `prototype/` code**。只逐檔 add 自己的檔（`intel/db/platforms.json`·`platform-modules.json`、`intel/platforms/bet365.md`·`rollbit.md`、`intel/STATE.json`、`intel/CONTROL.md` 鎖、`intel/loop-journal.md`、`BACKLOG.md`·`BACKLOG-archive.md`），未碰 `Game assets/` 增刪雜訊與他軌未提交檔。**下一步**：下一個平台建置輪實作 **#51 betlog**（首選，與 #16 fair 互補、零經濟回歸）或 #52；#50 成本加權 XP 仍待；tier-2 1xbet/leovegas/crown-coins + tier-3 shuffle/gamdom/duelbits 續逾期待刷。**⚠️ 未因本輪開卡而失效的前輪指定項**：07-28 早輪（#49）已把 **負責任博弈工具組**列為「下一輪平台軌首選候選」（`app-shell.js:521` hub 入口只 `ui.comingSoon()`＝死巷、`app-state.js:30` `lossLimitRemaining` 為死欄位、i18n key 已備＝入口/狀態/翻譯三者就位只缺中間引擎；且已釘清與 CONTROL.avoid 的界線——avoid 列的是「法定合規」，玩家自願的自我約束工具屬純前端容器型 UX），因本輪 `max_cards_per_run=2` 已用於 #51/#52 而**尚未開卡**；它不需要新調研即可開卡，下一輪平台軌應優先補開。
- **2026-07-27（平台軌 · 研究輪·讓路 games 領跑 · 刷新 tier-1 stake/bc-game + 開卡 #48）** — 閘門：loop/platform/auto_implement on，但 `lead_track: games`→依 SKILL §0.5 平台軌**讓路 heavy build**、僅推進「調研+資料庫」（比照 games 軌在 `lead_track: platform` 時的讓路慣例）。進場 build_lock=false→claim-token `p-094607-a3f1`→停頓做 WebSearch 多訊號取材（＝pause）→重讀確認 token 仍在＝claim 成功無並行 firing（E7 未觸發）→收尾清回 false。`last_platform_run` 停在 07-24＝平台軌連讓 3 日、tier-1 平台次逾 07-24 到期＝**累積真研究債，refresh 非空心跳**。**step1 調研**（次逾期補跑 max_platforms_per_run=2 深挖 stake+bc-game）：① **Stake 續居第一無新霸主**（cryptonews/coinspeaker/esportsinsider/tokenist/99bitcoins/gamingamerica「alternatives 2026」榜基準地位不變、替代者皆既有/avoid 成員）；VIP 校正為 14 階（Bronze→Diamond 5，非舊記 5 級）、gamification 現況 ApexWin 皆覆蓋＝無淨新缺口。② **BC.Game 2026 重大改版『BC Engine + Revenue Sharing』**（btcgambling/tech-insider/covers 交叉）：**XP 改依每局實際成本(house edge)計權**（vs ApexWin `HL.vip.addWager(bet)` 平權流水）+ **72h 新手安全網（前 3 日淨損 20% cashback 無流水）** = 兩個淨新缺口。兩平台 last_investigated→07-27、next_due→08-03，dossier 各補 reconfirm 段。**step2 台帳審（活動 分類，輪替接續 07-23 功能 之後）**：6 送幣模組 grep 全在位→促銷/活動框架維持 weak；VIP 模組交叉記「成本加權 XP」精進缺口。**step3 開卡**：把 BC.Game 較自足的缺口開為 **#48 限時損失保險/新手安全網 `HL.safetynet`**（🟦已批准待做，首週淨損自動退還、掛 HL.ledger 算窗口淨損+HL.bonus 退還、做成促銷框架 campaign 模板，S–M）；成本加權 XP 屬打磨向、記台帳 VIP 模組待後續。**step4 實作＝略過（讓路 games）**。**未寫任何 prototype/ code**。只動自己檔（intel/db/platforms.json·platform-modules.json、intel/platforms/stake.md·bc-game.md、STATE.json、CONTROL.md 鎖、loop-journal.md、BACKLOG.md·BACKLOG-archive.md），未碰他軌未提交檔與 Game assets 雜訊。**下一步**：非讓路的平台建置輪實作 #48；或深化 Guild（好友/聊天頻道/raid）；tier-2 roobet/rollbit/1xbet/leovegas + tier-3 shuffle/gamdom/duelbits 仍逾期待刷。
- **2026-07-24（平台軌 · 實作 #47 團隊/公會 meta `HL.guild` · 服從船長次優先候選最後一項＝三候選全數消化）** — 閘門全過（loop/platform/auto_implement on、`lead_track: platform`）；進場 build_lock=false→claim-token `p-guild-4f7a`→停頓做實作前源碼讀取（season.js/achievements.js/live-stats.js 中央點/app-shell hub/tournament.js isLive bot 閘/dom weekNum·rint·hash·pressable/components.css spass·badge 樣式）→重讀確認 token 仍在＝claim 成功無並行 firing→收尾清回 false（E7 TOCTOU 未觸發）。**服從船長 07-23 待處理次優先三候選之最後一項（Guild meta）**——radar 連 7 輪「業界最強空缺、仍空白」、platform-modules ★ absent；依船長建議「先做純前端骨架＝好友/隊伍容器 + team-vs-team 掛中央結算點」。實作 **#47 `HL.guild`**（`core/guild.js`）＝**資料驅動公會『註冊表』**（擴充性優先＝容器先於內容：`register(spec)` 比照 `HL.games`/`HL.achievements` 自我上架、種子 6 公會），玩家加入一公會→有效押注成該公會週貢獻。掛中央結算點 `HL.liveStats.record` 尾端 `HL.guild.record(bet)`；**兩獎勵路徑**入 `HL.bonus`（帶 source 入帳本）：① 個人貢獻任務里程碑（4 階，玩家本人貢獻跨門檻可領，冪等 per week）② 週榜結算（跨 `weekNum()` 依所屬公會 vs 對手名次派團隊獎金，冪等 per week，比照 tournament.settle）。**純前端骨架 + isLive() 閘**：對手隊/隊友為確定性種子模擬（同週穩定），真站(live)一律歸零＝只留玩家自身貢獻（比照 tournament/arena bot 閘）。面板＝`HL.ui.modal` wide（未入會→6 公會瀏覽卡；已入會→儀表板：header/名次/貢獻 + team-vs-team 週榜 + 隊內貢獻榜 + 4 階任務 chip + 換公會/退出），入口掛福利中心 hub「成長·商城」（⚔️ 公會·團隊戰）。站別命名空間隔離。改 `guild.js`(新)/`live-stats.js`(掛鉤一行)/`app-shell.js`(hub 一項)/`index.html`(season.js 後)/`components.css`(`.ax-guild*`)/`sw.js`(v86)。**驗證**：preview（`http://localhost:3000/?demo=1`；先清 SW `apexwin-v85` 舊快取避免 404）零 console error；JS eval——6 註冊、未入會 record 不累積、join 好壞值、經真中央點 `liveStats.record("dice",3000,0)` 精準 +3000、貢獻 6k→1 可領/claim 冪等/未達擋/26k→milestone 1 可領、名次 6 隊中第 2~3、**isLive 閘 totalGuilds 6→1/rank=1 切回 demo 復原 6**、leave→joined=false；面板 DOM 瀏覽 6 卡/週榜 6 列(1 is-mine)/4 任務列/1 可領 button/隊友 4 列含「你」/header 正確。**對船長**：**待處理次優先三候選至此全數消化**（#45 徽章牆 + #46 季票 + #47 公會）；本輪只動自己檔（上列 6 檔 + intel/db/platform-modules.json/STATE.json/CONTROL.md/BACKLOG.md/BACKLOG-archive.md），未碰他人未提交變更與 Game assets 雜訊。**下一步**：好友系統/邀請入會、公會聊天頻道(掛 HL.dock)、公會 raid/協力目標、後端多人真週榜(phase8 guild_econ 依站別)。
- **2026-07-24（平台軌 · 實作 #46 季票 Season Pass `HL.season` · 服從船長次優先候選最後一項）** — 閘門全過（loop/platform/auto_implement on、`lead_track: platform`）；進場 build_lock=false→claim-token `p-200952-2913`→停頓做實作前源碼讀取（achievements.js/live-stats.js/progress.js(HL.bonus)/app-shell hub/index.html 序/dom·ui API）→重讀確認 token 仍在＝claim 成功無並行 firing→收尾清回 false（E7 TOCTOU 未觸發）。**服從船長 07-23 待處理次優先三候選之最後可純前端落地者（Season Pass）**——成就徽章牆 #45 已消化、Guild meta 需社交層較重（留純前端骨架待後續），Season Pass 門檻最低且可直接疊在 #45 成就點數 XP 上。實作 **#46 `HL.season`**（`core/season.js`）＝**config 驅動雙軌季票外殼**（擴充性優先＝容器先於內容：一份 SEASON 設定就是可換的 config 排程、`setSeason` 出口、tiers 純資料 30 階、`state.sid` 不符即換季重置）；免費/進階雙軌，進階軌**花費成就點數解鎖**＝直接消耗 #45 鋪的 XP 底座（兩張成長卡串成 XP 經濟閉環）；賽季經驗由中央結算點 `HL.liveStats.record` 尾端有效押注累積、跨階即時通知；階梯獎勵入 `HL.bonus`（帶 source 入帳本、走 locked 待解鎖與 #45 一致）。面板＝`HL.ui.modal` wide 資料驅動階梯（雙欄 chip 四態、逐階+一鍵領取、進階解鎖 CTA），入口掛福利中心 hub「成長·商城」。改 `season.js`(新)/`live-stats.js`(掛鉤一行)/`app-shell.js`(hub 一項)/`index.html`(achievements.js 後)/`components.css`(`.ax-spass*`)/`sw.js`(v85)。**驗證**：preview 零 console error；JS eval——中央點 4500 wager→Tier 3 精準、claim 三閘（未達/已領/進階未解鎖）皆擋、unlockPrem 恰扣 60 成就點數(135→75)且回溯開放、claimAll 清空、六階獎金精準(180/1200/2500·480/3600/30000)落地 locked 錢包、面板 30 階/60 chip/7 可領 button/is-now 渲染正確。**對船長**：待處理次優先三候選已消化 2（成就徽章牆 #45 + Season Pass #46），**僅剩 Guild/公會 meta**（需社交層、部分需後端，建議下輪先做純前端骨架＝好友/隊伍容器+team-vs-team 掛中央點）。本輪只動自己檔（上列 6 檔 + intel/db/platform-modules.json/STATE.json/CONTROL.md/BACKLOG.md/BACKLOG-archive.md），未碰他人未提交變更與 Game assets 雜訊。**下一步**：季票任務給額外賽季 XP／賽季輪替排程／進階獎勵擴充／或補上 Guild meta 骨架。

- **2026-07-23（平台軌 · 實作 #45 成就徽章牆 `HL.achievements` · 服從船長次優先候選）** — 閘門全過（loop/platform/auto_implement on、`lead_track: platform`）；進場 build_lock=false→claim-token `p-142727-5464`→停頓做平台調研（WebSearch Stake/BC.Game/gamification 2026）+ 讀 progress.js/live-stats.js/dock.js/ui.js/app-shell 源碼→重讀確認 token 仍在＝claim 成功無並行 firing→收尾清回 false（E7 TOCTOU 未觸發）。**平台調研**（step 1）：多訊號確認 **成就徽章/XP 是 2026 頂級 casino 留存核心共識**——Stake「達成新目標即獲成就徽章」、BC.Game「成就+任務→紅利+榜分」、Xtremepush 列 Badges/Points/Tiers 為 7 大 gamification 機制、業界 22% 留存增益、即時發獎才有多巴胺。**台帳審**（step 2，category=功能）：把「任務/成就/簽到」evidence 標記的 ★ 缺口（成就徽章牆）實作落地→新增獨立模組「成就徽章牆 Achievement Badge Wall」status=present。**服從船長 07-23 待處理次優先三候選之一（成就徽章牆）**：實作 **#45 `HL.achievements`**（`core/achievements.js`）＝**資料驅動成就『註冊表』**（擴充性優先＝容器先於內容：`register(spec)` 比照 `HL.games.register` 自我上架，種子 19 枚 ×6 分類 ×4 分層，未來模組/同仁遊戲一行掛新成就不動引擎）；掛中央結算點 `HL.liveStats.record` 尾端累積終身統計、每次結算即時解鎖→發**成就點數（XP 底座，供未來 Season Pass 消耗）**+獎金入 `HL.bonus`（帶 source 入帳本）+通知+toast；忠誠類即時讀 `HL.vip`/`HL.rewards`、`rewards.claim` 加一行 `sync()`。牆＝`HL.ui.modal` wide 資料驅動徽章網格（分層邊條配色/進度條/成就點數/完成度），入口掛福利中心 hub「成長·商城」。**擴充性雙落地**：extensibility_patterns「訂閱者模式中央掛鉤」+「插件註冊表」。改 `achievements.js`(新)/`live-stats.js`(掛鉤一行)/`rewards.js`(sync 一行)/`app-shell.js`(hub 一項)/`index.html`(progress.js 後)/`components.css`(`.ax-badge*`/`.ax-badgewall*`)。**驗證**：preview（`http://localhost:3000/?demo=1`）零 console error；JS eval——19 註冊、record(dice,100,1500) 解鎖 first-bet+mult-10 pts=20/獎金 delta=700、mult-100 進度 15%、冪等不重發、經真 `liveStats.record` 驅動有效、`test()` 型 vip-gold 於 VIP 升黃金後 `sync()` 即時解鎖、牆 19 卡/2 金邊/17 進度條。**對船長**：待處理次優先三候選已消化 1（成就徽章牆），Guild meta / Season Pass 仍待（見 CONTROL「已回應」）；本輪只動自己檔（上列 6 檔 + intel/db/platform-modules.json/STATE.json/CONTROL.md/loop-journal.md/BACKLOG.md），未碰他人未提交變更與 Game assets 雜訊。**下一步**：Season Pass 可消耗成就點數作 XP／徽章掛個人檔+排行榜／同仁遊戲 register 專屬成就。

- **2026-07-23（平台軌 · 三軌重構後首張平台實作卡 #44 模組化/可停靠佈局底座 `HL.dock`）** — 引擎 07-23 已從舊四軌（radar/investigate/evolve/consolidate）重構為**三軌雙線**（platform/games/maintain），本 firing（舊排程名 apexwin-evolve＝成長/加新相位）依 `lead_track: platform` + 船長 07-23 待處理指令映射到**平台軌**執行。閘門全過（loop/platform/auto_implement on），進場 build_lock=false→claim-token `p-125233-2237`→停頓讀取（CONTROL/platform-modules/shell 源碼）→重讀確認 token 仍在＝claim 成功無並行 firing→收尾清回 false。**服從船長「起步優先＝平台功能先，先把可收納/自由擺放的容器底座做起來」**：實作 platform-modules ★ 最高價值缺口 **Dockable Layout**＝新增 `HL.dock`（`layout/dock.js`）資料驅動可停靠面板註冊表，提供 開/關/收合(可收納)/桌機拖曳自由擺放/自動堆疊/跨站持久佈局/手機互斥，把既有夥伴+聊天兩浮層改掛上去（`panels.js` 保留 `HL.panels` 舊 API＝app-shell/main.js 零改動）。**擴充性優先**：容器先於內容，未來任何功能面板一行 `HL.dock.register(spec)` 即獲全套收納/擺放。改 dock.js(新)/panels.js/index.html/components.css/i18n.js；platform-modules 台帳 Dockable Layout：absent→partial（回填 evidence + 下一步：主內容區 grid slot 引擎/吸附/多 preset）。**驗證**：preview 零 console error；headless 視埠寬度回報 0（§9 已知）→ `Object.defineProperty` 覆寫 clientWidth=1280 驗桌機路徑：堆疊 16/388px 精準、收合三態同步、拖曳持久 pos 寫入、relayout 保留自訂座標其餘 reflow、跨站原生 key(ax:dock:v1)正確。**對船長**：待處理指令已服從（見 CONTROL「已回應」）；本輪只動自己檔（dock.js/panels.js/index.html/components.css/i18n.js + intel/db/platform-modules.json/STATE.json/CONTROL.md/loop-journal.md/BACKLOG.md），未碰他人未提交變更與 Game assets 雜訊。下一步：平台軌次優先候選（Guild meta / 成就徽章牆 / Season Pass ★）或把主內容區納入 slot 引擎。

- **2026-07-10（進化 · 消化 legendz 07-09 調研 · 開卡+實作 #43 社交運彩 Picks · 開出第三內容軸線）** — 缺口進化。**消化新調研**：high_water 07-06→2026-07-09，唯一新 dossier＝**legendz.md（07-09 首次深挖）**。逐條對照後其留存機制（VIP 5 段位/簽到/每日轉盤/races/任務/Bonus Shop/referral）**全部與既有 #29/#34/#17/#15/#6/#36 重疊、零淨新缺口**；**唯一真新增量＝「社交運彩 / pick'em 預測」作為 ApexWin 完全空白的第三內容軸線**（Legendz Social Sportsbook + Courtside sports-first＝**雙平台共識**）。核實 ROADMAP：「運動博彩骨架 + bet slip + 串關 Parlay」在 🔵LATER（**非** DEFER 需牌照）＝純前端具現合法；真實賠率數據源/結算引擎/即時 feed 屬 avoid，僅取前端預測 UI。故**只開 1 卡**（idea 2 bet slip、idea 3 計分掛鉤皆為 idea 1 子範圍，不灌水湊 3 張）。**實作 #43 ApexWin Picks v1**：新 `views/instant-picks.js`＝register originals `picks`，本地模擬賽程（隊名池＋⚽🏀🏈🎮🏒）3 場 × 獨贏/大小分兩盤口單注、賠率 `EDGE/p`、一單一 `HL.fair` nonce 決勝、`floor` 派彩防 edge 反轉（承 #27/#32）、派主餘額、**中央掛鉤 `HL.liveStats.record("picks",…)` 補上調研點名缺的「運彩計分來源」**。PF 表加 `picks:1` 亮 🔒；改 index.html/game-frame/components.css(ax-picks*)/i18n（繁簡英）。**驗證**：本輪 preview 8200 埠被另一 chat 的 dev server 佔用、`serve.ps1` 自動改埠不被 preview 工具追蹤 → 改用**對金流更嚴謹的 headless DOM/HL stub harness 載入真檔跑 settle()**：10/10 過（WIN payout 104@2.08 delta 精準、LOSE 恰扣 50、**60k 注 RTP 98.45%＝edge≥1% 守住**、record 恰一次 game=picks、floor 不高報）；`node -c` 全過。**⚠️ 多 firing 並行（延續歷輪根因）**：本輪執行期間偵測到並行 firing 已實作提交 **#20 流水引擎 + #35 Happy Hour** 並把 #1–#42 全標 ✅、且 `sw.js` 被一路 bump 到 **v22**（我原設 v21 被覆蓋＝無害、v22 更新）；`BACKLOG.md`「modified since read」一次、`game-frame.js`/`sw.js` 需重讀。為避免污染他人工作，本次只 stage 我自己的檔（`instant-picks.js`＋index.html/game-frame.js/components.css/i18n.js 的 picks 區塊＋`BACKLOG.md`＋`intel/STATE.json`）。counters 於並行下可能輕微漂移（屬並行固有、會鬆散自癒）。**對船長指令**：CONTROL 待處理區為空，無插隊。**強烈重申建議**（歷輪反覆記載）：排程明顯重疊觸發多個寫入型 routine 並行寫同一 `prototype/`（本輪 evolve 與另兩個 firing 同時動工、sw/BACKLOG 交錯）——建議加**單一執行鎖**（一次只跑一個寫入型 routine）或在 CONTROL 待處理區留「施工中」旗標。**注意**：#43 派主餘額（運彩贏分＝可玩幣）未新增 bonus 來源，不加劇任何流水缺口（且 #20 流水引擎本輪已由並行 firing 落地）。下一步：BACKLOG 編號卡 #1–#43 全清，引擎續掃 07-10 大批到期調研（stake/bc-game/bet365 等）找新缺口開卡。

- **2026-07-10（實作 #20 紅利/流水引擎 · 船長批准 · 全佇列清空 🏁）** — 船長批准後實作歷輪唯一 ⬜ 的 **L 級金流底層**：`HL.bonus` 分離記帳（unlocked vs 待解鎖 ledger）、流水 FIFO 解鎖、優雅遷移＋防毀損、`canWithdraw()` 閘控、領取中心雙池 UI——**API 相容＝12+ 既有 bonus 來源零改裝**，#33 cashback 以 `wagerFree` 保住零流水賣點。兩個關鍵設計：①**掛鉤順序**：`onWager` 放中央掛鉤最前，同一注不解鎖自己鑄出的紅利（否則 VIP 升級獎金會被大注當場自解鎖＝流水形同虛設）②**金額不可銷毀**：遷移分支同時保留 unlocked 欄位（異常態不歸零）。驗證走 node 模擬（9 案＋1000 輪守恆 fuzz）＋preview 實環境（跨 VIP 門檻一注鑄 740 鎖定不自解鎖等）零 error——18-agent 審查 workflow 因 session 上限 17 agent 陣亡，findings 改由 node 逐項驗證處置（2 修、3 駁回並記錄）。**重要提醒（真金前必辦）**：demo 經濟為展示刻意慷慨（每 1000 流水的返水+商城點+金磚等回饋約 80–100 > 期望損失 ~10＝刷流水 EV 正），真金模式上線前**所有回饋率需整體重調**——這是既有設計非 #20 缺陷，#20 反而首次加上了流水摩擦。**至此 BACKLOG 所有編號卡 #1–#42 全數完成**，引擎繼續自走開新卡（07-10 大批到期調研進行中、Card Crush 剛收編）。

- **2026-07-10（實作 #35 Happy Hour · 原始佇列 #17–#35 全數完成 🎉）** — 收尾原始 evolve 佇列最後一張 **#35**（WOW Vegas+Toshi 共識）。新 `core/happyhour.js`：每日 12/18/22 點三個 1 小時窗口、**窗內返水 ×2**（乘進 `rbAccrue`＝真加成走 #22 日桶、恰乘一次）；底部列 ⚡ 入口＋時段表 modal＋開窗通知（每日每窗冪等）。14-agent 審查 3 confirmed（皆 low）全修：閉包鏡像防 storage-blocked 洗版、三處返水率顯示面補 ⚡×2、底部列副標跨窗界過期→id+30s tick 同步。**里程碑：自我進化引擎開出的 #17–#35 十九張卡至此全數實作完畢**（含 4 張由背景 evolve 自動完成），僅 #20 流水引擎（⬜L 級）仍待船長批准。**並行協調**：本輪背景又在飛 #43 Picks（instant-picks.js/components.css/sw v21 未 commit）——依安全收尾流程用「重建法」只提交我的 index.html 行、components.css/sw.js 全留、i18n 混入的 10 行 Picks 無害字典鍵照 #33 前例納入並註記。下一步：#20 待批准；或 evolve 從 07-10 大批到期調研開新卡。

- **2026-07-09（實作 #34 遞增連登階梯 + 里程碑 · Opus 4.8 · ultracode）** — 續跑 **#34**（S–M）。重寫簽到 `core/rewards.js`：平 7 天循環 → 30 天單調遞增階梯 + 第 8/15/22/30 天里程碑大禮（日獎沿用主餘額、里程碑進獎金錢包）。關鍵正確性：里程碑鍵於實際 `nextStreak`（非 capped index），day30 後不重觸發；斷簽歸零；保留 `claimedToday`（#28 依賴）。8-agent 對抗性審查 1 confirmed（斷簽後表頭顯示過期 streak）已修＝`status()` 顯示 streak 斷簽歸零。preview 全測（day8/15/30/31 邊界、E2E 雙錢包派彩、冪等、三語含新「天→days」鍵）零 error。**並行**：背景引擎同期又跑（cards_implemented→21、evolve 07-09），本輪只提交 #34 相關檔（rewards/css/i18n/sw/BACKLOG），STATE/CONTROL 交背景引擎管理避免 race。下一步：佇列 🟦＝#35 Happy Hour（S–M）＋背景新開卡。

- **2026-07-09（進化 · 消化 07-04/05/06 三份調研 · 開卡+實作 #42 商城機率型兌換 · 多 firing 並行協調）** — 缺口進化。**消化新調研**：high_water 07-03→2026-07-06，三份 dossier（deal-or-no-deal-win 07-04 / mega-frenzy 07-05 / capyspin 07-06）。三份的共同結論＝**淨新缺口極稀**：mega-frenzy（已停運）與 capyspin 對既有佇列 **0 淨新缺口**，且各自**證偽一項 radar 假設**（Hype Club 被誤當可兌換點數商城／CapySpin「Guild Battles」團隊競賽層來源不足）——依 dossier 明確訊號**不開任何重複/無實證卡**（尤其不開 Guild Battles）。唯一真新增量來自 DoND：**#42 點數商城「機率型兌換」品項 命運寶箱**（gacha 加權抽層＋大獎尾，走 #38 獎輪揭曉），對標 DoND Star Shop「花固定點→up to X SC」＋姊妹站 Zonko。**實作 #42**：擴 `core/shop.js`（CATALOG 加 `kind:"gacha"`＋`pickTier` 加權抽層＋三處分支），複用既有折扣/冷卻/冪等骨架與 `HL.bonus`、`HL.reveal`，零新架構、零新 bonus 管道。preview 實測：單抽回合精準、**4000 抽分佈 56.1/27.5/11.6/4.9 對齊設計 55/28/12/5、0 非法層**、UI→獎輪 reveal、三語全譯、零 console error。**⚠️ 多 firing 並行（本輪最重要觀察）**：本輪執行期間偵測到**至少兩個並行 firing**——①一個 evolve firing 已實作並提交 **#33 Cashback**（`3aacc91`，cards_implemented 19→20），且因我的 i18n 編輯已在磁碟上，該 commit **順手把我的 #42「命運寶箱/Fortune Chest」i18n 鍵一併提交進 HEAD**（＝我的翻譯已在 HEAD、shop.js 實作卻還在工作區＝split-brain）；②另一個 firing 正 **mid-flight 實作 #34 遞增連登階梯**（工作區 `rewards.js`/`components.css`/i18n #34 鍵/`sw.js`→v19 皆未提交）。為避免污染他人未完成工作，本次 commit **只 stage 我自己的檔**（`shop.js`＋`BACKLOG.md`＋`intel/STATE.json`），把 #34 firing 的 `rewards.js`/`components.css`/`i18n.js`(#34 鍵)/`sw.js` 留給該 firing 自行提交。counters 於並行下可能輕微漂移（cards_implemented 我設 21、若 #34 firing 也從 20 起算會覆蓋成 21＝低估 1，屬並行固有、會隨時間鬆散自癒）。**對船長指令**：CONTROL 待處理區為空，無插隊。**強烈建議船長**：排程明顯**重疊觸發多個 evolve/investigate firing 並行寫同一 `prototype/`**（本輪至少 3 個 routine 同時動工、i18n/sw 交錯、#33 commit 誤帶我的鍵）——這正是歷輪日誌反覆記載的「觸發卻未收尾／並行交錯」根因；建議加**單一執行鎖**（一次只跑一個寫入型 routine）或在 CONTROL 待處理區留「施工中」旗標。**注意**：#20 流水引擎仍 ⬜待批准，#42 走既有商城 bonus 管道未新增獨立來源、未加劇 #20。下一步：佇列頂端 🟦＝#34（並行 firing 正在做）／#35 Happy Hour／#40 VIP 升級贈轉（S，最輕）／#41 `HL.engine`（建議待 #20）。

- **2026-07-07（實作 #33 淨損 Cashback 引擎 · Opus 4.8）** — 續跑佇列頂端 **#33**（M）。新 `core/cashback.js`＝`HL.cashback`：補上 ApexWin 缺的「淨輸返現」維度，與 #22 rakeback（turnover）互補。關鍵設計：**每週桶 + 真淨輸 max(0,Σbet−Σwin)**（贏局自然抵銷，非逐局 Σmax(0,bet−win) 高估）、`pot=max(0,accrued−claimed)`（大贏歸零時不追回已領、不倒扣）、中央掛鉤無條件 record(bet,win) 以吃 win-only 結算。複用 #22 桶骨架 + 底部列 💸 入口。**23-agent 對抗性審查 2 confirmed 皆 i18n**：底部列「淨輸返現」補字典（已修）、toast/notify 串接不譯屬全站慣例（保留一致）；另清死變數 rateRows。審查駁回：VIP 中途升級率追溯（符合模型、有上限非重複領）、可領副標串接（既有慣例）。preview 淨輸帳全對＋領取 E2E＋三語＋零 error。**注意**：#20 流水引擎仍 ⬜待批准，#33 又新增一 bonus 來源。**並行觀察**：背景引擎持續自走（雷達已到 07-09、開了 #39–#41），本輪工作區乾淨無孤兒衝突。下一步：佇列 🟦＝#34 遞增連登階梯（S–M）、#35 Happy Hour（S–M）、#40/#41。

- **2026-07-07（#32 Keno 審查修復 · 前景/背景並行工作協調記錄）** — 前景 session 實作 #32 Keno（超幾何精算賠付表、一球一 nonce、同步結算）並開 20-agent 對抗性審查；**同時**背景 evolve 自動實作 #39 Faucet，且把前景進行中的 Keno 誤判為「孤兒 stall」提前收編進其 commit（`9c4222c`）——結果無害（基底先落地），但**出處已在 #32 卡更正**（非 stall，勿污染引擎故障樣本）。本 commit 補上審查修復 4 件：賠付表顯示改 floor 2 位小數不高報實付（Hilo 同類一併修）、手機 <423px 網格溢出修正、zh-Hans「倍数」、倍數格初始「—」；另離線驗證數學 EV=0.9900000000 精確、加背景分頁瞬間揭曉 fast-path。**流程教訓（給後續輪次）**：前景與背景同時動 `prototype/` 時共用檔（i18n/sw/css/index.html）會交錯——建議前景開工前先在 CONTROL 待處理區留「前景施工中」一行，或 evolve 收編「孤兒」前先檢查檔案 mtime 是否在數分鐘內（活躍工作 vs 真 stall）。下一步：佇列 🟦＝#33 Cashback、#34 連登階梯、#35 Happy Hour、#40/#41（本輪新開）。

- **2026-07-07（進化 · 消化 07-03 三份刷新調研開卡 #39–#41 · 實作 #39 Faucet · 收編孤兒 #32 Keno）** — 缺口進化。**消化新調研**：high_water 07-02→2026-07-03，三份 07-03 刷新 dossier（stake / bc-game / courtside；bet365 為純 UX nuance 無新缺口卡）。**依共識×影響×低工作量開 3 卡**：**#39 餘額歸零救濟金 Faucet（Courtside+CoinsBack 雙平台共識、船長日誌點名優先、防流失鉤子 ApexWin 全空白，S）**、#40 VIP 升級贈免費 Lucky Spin（Stake+BC 共識，#17↔#29/#31 掛鉤，S）、#41 押注質押被動收入引擎 `HL.engine`（BC Engine 對標、全新留存軸線、⚠️相依 #20，M）。**實作 #39**：新 `core/faucet.js`，餘額 ≤NT$100 時右下浮現藥丸、每 8h 領 NT$1,000 續命金直入主餘額（Courtside 語意＝馬上能玩，非入 bonus）；沿用 #28 self-managed boot interval + body-mounted 藥丸 + gated 登入閘 + 語言切換重置模式，位置左移避開 #28 藥丸。preview 實測：50→+1000→1050、8h 冷卻、二領冪等回 0、餘額足時不 eligible、零 console error。**同時收編兩張孤兒卡**——本輪一啟動即發現 **#31 VIP 微等級進度條**（已由前一輪 firing 完整實作＋自行 commit `ec292c7`、SW v15，preview 驗證 Lv 1→21/subPct 數學精準、refreshChrome 同步 widget＝正常收尾，無需再處理）與 **#32 Keno 賓果彩**（完整寫在 `instant-keno.js`+`game-frame.js` PF+i18n/CSS/SW v16 但**未 commit、BACKLOG/STATE 未收尾**＝典型「觸發卻未收尾」孤兒，同 #26/#31 現象）。#32 與本輪 #39 共用 i18n/CSS/index.html/sw.js（無法按檔拆分），且兩者皆完整＋`node -c` 過＋preview 全綠（Keno：80 格盤、選號、開 20 球、bet 扣款、命中派彩、8 抽中央掛鉤 record 恰 8 次、0 console error）→ **一併驗證後同 commit 收編，避免 index.html 掛了 keno script 卻缺檔的破版部署**。bump SW **v16**（v15→v16 由 #32 帶入、#39 沿用）。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：①**「evolve 觸發卻未收尾」stall 現象本輪再度出現（#32 未 commit）**——且本輪明顯有並行 firing（我在改 faucet 時 i18n/css/sw 被另一 firing 同步寫入 Keno 內容、檔案多次「modified since read」）＝疑似兩個 evolve firing 並行；建議船長排查排程是否重疊觸發 / evolve routine 收尾 commit 環節。②#20 流水引擎（⬜待批准 L）仍未解；#39 直入主餘額（非 bonus）未加劇 #20，但新開的 #41 `HL.engine` 高頻 drip 會顯著複利放大 #20，實作前宜先批准 #20。③既存未提交的 `Game assets/` 增刪與 untracked `prototype/.claude/` 非本輪所碰、未納 commit，請自行檢視。下一步：佇列頂端 🟦＝#33 淨損 Cashback（M）/ #34 遞增連登階梯（S–M）/ #35 Happy Hour（S–M），或新開的 #40 VIP 升級贈轉（S，最輕）；#41 建議待 #20 批准後再做。

- **2026-07-06（實作 #31 VIP 微等級進度條 · 引擎自走確認：#30 已由背景 evolve 自動完成）** — 應船長「繼續」續跑。**先確認引擎狀態**：#30 PvP Dice Duel 已由背景 evolve 於 07-03 15:19 自動實作＋正常 commit（`7ad2c15`）——且**應用了 #27 審查寫進日誌的教訓**（floor 派彩防 edge 反轉、同步結算防漏帳）＝引擎不只自走、還在從日誌學習；07-04–06 radar/investigate 亦持續自跑（watchlist 達 30 上限、新收編 CapySpin）。本輪實作 **#31**（S–M，#29 已鋪好子級資料、最順）：`vstatus()` 增 `level`（全域 Lv 1–21）/`maxLevel`/`subPct`，header 玩家 widget 加「段位 · Lv N」＋ 3px 金色迷你進度條、`refreshChrome` 即時更新；`addWager` 改每注刷新（原僅升級時觸發＝條不會連續推進，preview 抓到後修正）。純顯示零派發變更。preview 全測（連續推進 0→50→75%、跨級、鑽石封頂）零 error；**審查 0 findings 全綠**。bump SW v15。**注意**：#20 流水引擎仍 ⬜待批准。下一步：佇列頂端 🟦＝#32 Keno（M）。

- **2026-07-03（實作 #30 PvP Dice Duel · ApexWin 首個玩家對戰維度 · 修 rAF 停擺+尾空格漏譯兩真 bug）** — 本輪**無新調研檔可消化**（high_water 已達 07-02；07-03 雷達新收編的 Courtside「Card Rips」經判定併入 #36/#38、且 investigate 尚未寫成 dossier ⇒ 無新缺口可開卡），依劇本「無新調研則從佇列頂端挑 🟦 純前端可落地卡」實作。#20 為 ⬜待批准（跳過），佇列頂端 🟦＝**#30 PvP Dice Duel**。新增 `views/instant-duel.js`＝register originals 可玩卡 `dice-duel`：ApexWin **第一個 PvP 對戰維度**（此前全是單人對莊）。玩家 vs bot（沿用 #15 命名/頭像池）各擲 0–99 比大小、贏家通吃扣 1% 抽水＝`floor(bet×1.98)`、平手重擲；一擲一 `HL.fair.float("dice-duel")` nonce＝逐擲可驗證，PF 表加 `dice-duel:1` 亮 🔒。複用 `HL.instant.amountField`＋公版返回鈕＋中央掛鉤 `HL.liveStats.record`。**preview 實測揪出並修兩個真問題**：①**金流綁在 rAF 動畫回呼上**——初版用 `HL.instant.animate`（requestAnimationFrame）在背景/失焦分頁會暫停，payout 卡在 `.then()` 永不入帳；且玩家中途離場＝扣了注不派彩（與 #27 審查點名的互動回合「中途離場沒收注」同族）→ 改為**決勝後立即同步 `setBal`+`record`、`setTimeout` 收尾純視覺揭曉**（setTimeout 背景也觸發），先入帳再演出＝robust 且不漏帳。②**兩個動態勝負字串 i18n 鍵帶尾空格**——DOM walker 以 `raw.trim()` 查表（沿用 Chat Rain #25 教訓），故鍵須為 trimmed 形式（`raw.replace` 會保留原尾空格）→ 修掉尾空格後 EN/zh-Hans 動態字串正確翻譯、零 leak。實測全綠：WIN +49/LOSS -50/bet3 floor-edge +2（非 round 的 +3＝零 edge）/平手重擲 90:10/餘額不足不扣/雙擊 busy 守門/VIP wager +20 中央掛鉤/bot 對手 GoldRush69🦅/🔒 modal/三語全譯/零 console error。bump SW v13→v14。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：①#20 流水引擎仍 ⬜待批准（#30 為 PvP 玩法、派彩入主餘額非灌 bonus，未加劇 #20 缺口）；②`prototype/.claude/launch.json`（非本輪產物、指向已失效暫存路徑的 untracked 檔）仍在，未納 commit，可自行刪除。下一步：佇列頂端 🟦＝#31 VIP 微等級進度條（S–M，#29 已把 `vstatus()` 資料備妥、順手）或 #32 Keno（M）。

- **2026-07-03（實作 #29 VIP 子級+大階雙層獎金 · 首次審查零確認全綠）** — 續跑佇列頂端 **#29**（S）。關鍵設計判斷：卡片原文「跨大階額外發一筆」按字面做＝同事件發兩筆（無意義的數字膨脹）；對照來源 Shuffle 的 **level-up + tier-up 雙層模型**，ApexWin 現況只有大階單層（5 段位跨段發獎），**真正缺的是微級層**——故每段位切 5 子級、升子級發小獎（60/150/400/1000）、跨段維持既有大獎＝雙層完整，且 `vstatus()` 新增 sub/toNextSub 等欄位**直接為 #31（微等級進度條）鋪好資料結構**。段位邊界由大階路徑發、子級迴圈跳過＝不重複派彩。preview 五案數學全精準（含一注混跨子級+段位、恰落邊界、20k→200k 直上鑽石 25,600 分毫不差）。**8-agent 對抗性審查 2 findings 全數駁回（0 confirmed）＝本輪系列首次零修正過關**。經濟影響：全程 150k 押注多發 6,440（+4.3%），與既有 VIP 14.7% 相加仍在 demo 慷慨帶。**注意**：#20 流水引擎仍 ⬜待批准。下一步：佇列頂端 🟦＝#30 PvP Dice Duel（M）或 #31 VIP 微等級進度條（S–M，資料已就緒、順手）。

- **2026-07-03（實作 #28 新手限時啟用窗口 · 審查揪出 HIGH 級跨日撤銷任務 bug）** — 續跑佇列頂端 **#28**（S）。新 `core/onboarding.js`＝`HL.onboard`：6h 啟用窗口、雙任務（首注＋簽到）、NT$500 大禮包（#38 揭曉儀式領取）、右下倒數藥丸＋即時任務 modal。**23-agent 對抗性審查（2 維度×3 票）發現 4 真問題全修**，最有價值的是 **(HIGH) 簽到任務未閂鎖**：初版即時讀 `HL.rewards.status().claimedToday`，而日序是 UTC 制（台北 08:00 翻日）——窗口跨翻日時**已完成的簽到任務會被靜默撤銷、領取鈕變啞彈、獎勵懸空到過期**→改成與首注一致的本地閂鎖。另修：登入頁閘（會員模式未登入不起算不掛藥丸——原版會把 6h 窗口燒在無法完成任務的登入畫面）、modal 快照→即時刷新、藥丸語言切換標籤重置（`#app` 外元素不受 `HL.app.refresh` 重繪的通用陷阱，同類 body-mounted 元件都要注意）。preview 四修復全數現場驗證＋領取 E2E＋零 error。bump SW v12。**注意**：#20 流水引擎仍 ⬜待批准，#28 再加一 bonus 來源（+500 一次性）。下一步：佇列頂端 🟦＝#29 tier-up 大階獎金（S）。

- **2026-07-03（實作 #27 Hilo 猜高低 · 審查揪出並修掉全家共通的派彩 rounding 漏洞）** — 應船長「繼續下一個任務」，**先勘誤**：上輪日誌「佇列 🟦 全清」為誤記，實際 #27–#35 仍有九張——本輪依慣例（#20 待批准跳過）取頂端 **#27 Hilo**。新增 `views/instant-hilo.js`：嚴格比較猜高低、同點算輸、機率定價單步倍數 EDGE/p（精確 1% edge）、連對累乘隨時兌現、K/A 單向鎖鈕、一牌一 nonce 可驗證（PF 表+驗證器擴充 hilo 牌面解讀）。**本輪最大收穫＝27-agent 對抗性審查（3 維度×3 票）揪出真 bug**：`Math.round(bet×mult)` 在小注會**反轉 1% edge**（bet 3 × 1.17 → round 3.51＝4 ⇒ 玩家 EV+12.8%、平均可刷 102.2% RTP；且此漏洞 **Towers 同款**、餵進 VIP/任務/錦標賽整條經濟鏈）→ Hilo+Towers 雙雙改 `Math.floor`（edge 恆 ≥1%）；另修「餘額不足（Demo）」toast 缺字典、補 Towers/Mines 共用 stat 標籤既有缺口；「中途離場沒收注、零結算」確認為 Crash/Mines/Towers/Hilo **全家既有設計**（liveroom 有退款 watchdog 前例）→ 開候補卡不在本輪修。**注意**：`core/instant.js` 的 `settle()` 也用 round（dice/limbo/plinko 同理論風險，但屬共用核心、未在本輪動）——若要根治建議單獨開卡處理。preview 實測：定價/派彩/floor 修復/守門/冪等/中央掛鉤/三語零 leak/零 console error 全過。下一步：佇列頂端 🟦 為 #28 新手限時啟用窗口（S）。

- **2026-07-03（實作 #37 黃金之城 meta 層 + #38 揭曉型領獎元件 · 雙卡合圍 · ultracode 工作流審查）** — 應船長「兩個任務一起完成」指示，同輪實作佇列僅剩的兩張 🟦 卡且**互相成就**：#38 揭曉元件的兩個首發消費點就是 #37 完成階儀式與 #36 商城神秘包。**#37**：新 `core/meta.js`＝`HL.base`，賭場之上第一個跨場養成 meta 層——押注賺金磚（NT$200=1）→ 五階建城（營地→王城）→ 每階同步派里程碑獎（總 NT$37,300）＋儀式慶祝；底部列 🏰 入口；PvP raid 留候補。**#38**：新 `core/reveal.js`＝`HL.reveal`，scratch/bubble/wheel 三種互動揭曉、**純呈現不派彩**（呼叫端先同步入帳＝中途關 modal 不漏帳）。改 `live-stats.js`（+HL.base.record 掛鉤）、`shop.js`（神秘包→刮刮卡儀式）、`app-shell.js`（底部列）、`index.html`、`components.css`（ax-reveal*/ax-base*）、`i18n.js`（繁/簡/英＋SUFFIX 金磚/點）、bump SW v9→v10。**品質流程（ultracode）**：實作後開 58-agent 對抗性審查工作流（4 維度平行審查 × 每 finding 3 獨立懷疑者、≥2 票才算真）——18 findings 中 11 個誤報被正確擊落（含「off-by-one」「軟鎖」「float 漂移」等聽起來嚇人但實際不成立的），7 confirmed 去重＝4 個真問題（全是 i18n 組字串/缺鍵類）已全修：儀式標題補 6 個組合全鍵、zh-Hans SUFFIX 補「 點」、HANS 補「已入獎金錢包」、清死鍵。preview 雙功能全流程實測（狀態機邊界/同步記帳/冪等/離線持久/三樣式互動/兩整合點/三語/regression：非神秘品項 toast 路徑不變+底部列 12 項水平捲動不破版）零 console error。**注意**：①#20 流水引擎（⬜待批准 L）仍未解，#37 又加一個高額 bonus 來源（里程碑最高 25k）；②通知中心（HL.notify）文案全站皆為組字串不翻譯＝既有慣例，未在本輪擴大處理；③part of 審查 agents 撞 session 上限（regression 維度死亡）→ 已手動補跑該維度檢查。下一步：~~佇列 🟦 全清~~（**07-03 勘誤：此句誤記**——#27–#35 仍有九張 🟦：Hilo/新手窗口/tier-up/PvP Dice Duel/VIP 微等級/Keno/Cashback/連登階梯/Happy Hour；#20 另為 ⬜待批准），evolve 下輪依序取佇列頂端 🟦 實作。

- **2026-07-02（實作 #36 點數商城 + 補收尾 #26 多倍數挑戰）** — 應船長「繼續」指示，實作佇列最高優先、三方共識（Dorados+Chancer+BigPirate）的 **#36 點數商城/Reward Market**：新增 `core/shop.js`＝`HL.shop`，補齊 ApexWin 一直缺的「點數消耗端 + 商品目錄」＝「賺→逛→換」經濟閉環。有效押注經中央掛鉤累點（NT$100=1 點），4 品項（3 獎金券固定額 + 神秘包區間隨機）兌換入 `HL.bonus`，VIP 折扣 0→20%、週期冷卻冪等；底部列 🛍️ 入口。改 `live-stats.js`（累點掛鉤）、`index.html`（掛載）、`app-shell.js`（底部列）、`components.css`（ax-shop*）、`i18n.js`（繁簡英，「點/折扣」拆節點）。**同時發現背景排程 evolve 已於稍早自動實作 #26 多倍數挑戰（`core/challenges.js`）卻未 commit、BACKLOG 未更新（又一次「觸發卻未收尾」）——本輪一併補驗證並提交**（50× 一局→claimableCount 0→2、API 齊備）。兩者皆 preview 全測、零 console error、bump SW（v8→v9 由 #26 帶入、shop 沿用）。**注意**：①自我進化引擎持續自走（07-02 排程 investigate 兩度自動甦醒：BigPirate+CoinsBack、BetPanda+Zonko）；②但 evolve 的「實作後未 commit」stall 現象重演（#26）——若持續，建議排查 evolve routine 收尾/commit 環節；③#20 流水引擎仍 ⬜待批准，#36 又新增一 bonus 出口/入口（點數→bonus）。下一步：佇列頂端 🟦 卡 #37 持久養成 meta 層（M–L，範圍較大、實作前宜與船長確認）或 #38 揭曉型領獎元件（M）。

- **2026-07-02（進化 · 實作 #25 Chat Rain · 開卡 #36–#38 · 手動補跑接回空轉引擎）** — 缺口進化第八輪。**背景**：自我進化引擎自 06-29 起雖排程持續觸發、卻空轉未產出（radar 07-02 動了 watchlist 卻沒收尾、investigate/evolve 未推進 STATE）。應船長指示手動補跑一整輪：①補齊 07-02 雷達收尾（Dorados+Zonko 收編、寫報告、commit）；②手動 investigate Dorados+Spree（其後**排程 investigate 於 12:08 自動甦醒**、自行深挖 BigPirate+CoinsBack 並 commit＝引擎已恢復自走）；③本輪 evolve。**消化新調研**：dorados/spree/bigpirate/coinsback（07-02）＋ CONTROL 明載上輪未及處理的 chancer/punkz（06-29），high_water 06-29→2026-07-02。**開 3 卡**（依三方共識×影響×低工作量）：**#36 點數商城/Reward Market（Dorados+Chancer+BigPirate 三方共識、消耗端經濟閉環、本輪最高優先）**、#37 賭場之上持久養成 meta 層（Dorados Lost City+BigPirate Adventure Mode 雙共識、ApexWin 全新軸線、v1 先做基地養成、PvP raid 另拆）、#38 通用「揭曉型領獎」元件（Spree 刮刮/戳泡/獎輪＋Punkz 開箱、可複用在所有領獎點）。其餘（Loot Box／provably-fair 揭露動畫／Split-Screen 多開／非對稱 raid／月度積分賽／Battle Pass XP 軌／Faucet 續命幣／RTP 返還中心）記入候補區待下輪。**實作佇列頂端 🟦 卡 #25 Chat Rain**（#20 流水引擎仍 ⬜待批准 L、跳過）：新增 `core/rain.js`＝`HL.rain`，通電原本聊天室的假 RainBot「雨露」訊息。平台聊天室每隔 3–5 分鐘（首次 25s）下 45s 紅包雨窗口，「近 10 分鐘發過言」＝資格→領 NT$30–149 入 `HL.bonus`、每場冪等限領一次。sticky 不透明橫幅三態（可領／先發言／已領取✓）+ RainBot 真訊息。**僅平台單例 `createRoom(true)` 掛鉤**，主播獨立聊天不掛；用面板自有 1s interval（不靠切頁清空的 `HL.ticker`）。改 `chat.js`（掛鉤/資格/裁切保護橫幅）、`index.html`（掛載）、`components.css`（ax-rain*）、`i18n.js`（繁/簡/英，鍵取 trimmed 形式）、bump SW v7→v8。**i18n 注意**：DOM walker 以 `raw.trim()` 查表，故字典鍵須為 trimmed 形式（初版誤帶尾空格＝未命中，已修）；"已領取 ✓" 沿用 Reload 既有鍵。preview 實測：未發言領取被擋（bonus 不變）、發言後領 +34 精準、二領冪等回 0、到期自動排下場並轉「下一場倒數」、RainBot 真訊息貼入、主播房不掛雨、EN/zh-Hans/zh-Hant 三態全譯、opaque 橫幅無訊息透出（截圖存證）、零 console error。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#20 紅利/流水引擎（⬜待批准、L）仍未解，#25 又新增一個直灌 `HL.bonus` 來源（複利放大 #20 缺口）；#37 meta 層 v1 屬 M–L，若骨架涉跨場資源架構，實作前建議船長確認範圍。下一步：佇列頂端待做為 #20（待批准）/ #26 多倍數挑戰（已批准 S–M），evolve 下輪續挑頂端 🟦 卡（#36 點數商城為三方共識、最高優先）。

- **2026-06-29（進化 · 實作 #24 VIP 週期 Reload · 開卡 #33–#35）** — 缺口進化第七輪。**消化新調研**：本輪 high_water 由 2026-06-27 推進至 2026-06-29，七份新 dossier（thrill / mega-dice / spinblitz / stake-us / crown-coins / toshi-bet / wow-vegas）入消化。**實作佇列頂端純前端 🟦 卡 #24 VIP 週期 Reload 領取中心**（#20 紅利/流水引擎仍為 ⬜待批准 L 級架構改動、歷輪皆留給使用者批准、全自動下跳過）：新增 `core/reload.js`＝`HL.reload`，三檔週期固定紅利（日/週/月），金額依 VIP 段位放大（青銅→鑽石：日 120→2000 / 週 600→11000 / 月 2500→50000），各檔獨立週期閘（`dayNum`/7 日 `weekNum`/30 日 `monthNum`，懶判定跨期自動可領、逾期不累積），到期領入 `HL.bonus`、已領顯示倒數至下次。**只讀 `HL.vip.status().index` 放大金額、不改 VIP 派發邏輯**；沿用 #17 daily-gate + #18 ticker 倒數。底部列 🔄 入口（可領檔數 badge）+ VIP 面板「🔄 領週期紅利」連結。改 `index.html`（掛載）、`app-shell.js`（底部列）、`progress.js`（VIP 面板連結）、`components.css`（ax-reload__*）、`i18n.js`（繁/簡/英）、bump SW v6→v7。**i18n 注意**：本輪確認 `core/i18n.js` 的 `t(k,def)` 是 **passthrough（永遠回傳 def）**，翻譯全由 DOM walker 對「畫面中文」精確/前後綴比對完成——故「標籤＋動態值」必須拆成獨立節點（標籤 span 供精確命中、數值/倒數另置 textNode 採語言中性 d/h/m），否則混排字串譯不到。preview 實測：bronze 120/600/2500、diamond 2000/11000/50000（VIP 放大精準）、claim 入獎金錢包增量精準（日 +120／週+月 +61000）、重領回 0（冪等週期閘）、claimableCount 3→2→1、modal 三卡、領取後關舊開新不堆疊（修掉初版 open() 重入會堆 modal 的 bug）、已領卡顯示「已領取 ✓ + 下次可領倒數 17h 30m」、EN/zh-Hans/zh-Hant 全譯、底部列 + VIP 連結可開、零 console error（screenshot 因頁面 ambient interval 忙碌逾時，改以 eval/console 驗證＝更強證據，同 #23）。**開卡（來源＝七份新調研，依共識度×影響×低工作量挑前 3）**：#33 淨損 Cashback/Lossback 引擎（Thrill 10%+Mega Dice 15% 共識、**全新留存維度**、與 rakeback 互補、複用 #22 桶骨架，M）、#34 遞增連登階梯+里程碑日（Crown Coins+Stake.us+SpinBlitz **三家共識**、純擴簽到派發表，S–M）、#35 時間窗口型限時 Boost/Happy Hour（WOW Vegas+Toshi **兩家共識**，催特定時段回訪，S–M）；皆純前端、複用既有引擎、已去重。候補（受 3 張上限暫緩，下輪優先）：Throw the Dice 擲骰揭曉、Game of the Week、Prize Drops 隨機掉落、社群共享彩池、referral、滾動視窗 VIP status、禮物信箱 Inbox、VIP 生日禮、Rewards Calendar、限時掉碼、Coin Flip、VIP 功能解鎖門禁（皆已記入 BACKLOG 候補區）。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#20 紅利/流水引擎（⬜待批准、L）缺口仍未解，#24 又是直接灌 `HL.bonus` 的新來源（每日/週/月三檔），複利放大 #20 缺口；#33 Cashback 若實作會再加一個 bonus 來源——**#20 仍待使用者批准**（L 級架構改動，全自動下不自行動工）。下一步：佇列頂端待做為 #20（待批准）/ #25 Chat Rain（已批准 M），evolve 下輪續挑頂端 🟦 純前端卡實作。

- **2026-06-29（進化 · 實作 #23 Towers 爬塔 · 本輪無新調研故未開卡）** — 缺口進化第六輪。**無新調研檔**（10 份 dossier 皆 ≤2026-06-26、high_water 已涵蓋 2026-06-27），故本輪不開新卡，依劇本「從佇列頂端挑 🟦 純前端可落地卡」實作。佇列頂端 #20 紅利/流水引擎為 `⬜待批准`（L 級架構改動，歷輪皆留給使用者批准、全自動下跳過），故挑頂端 🟦 卡 **#23 Towers 爬塔**。新增 `views/instant-towers.js`：8 層爬塔 Original，逐層選格、安全累乘、踩陷阱歸零、隨時兌現；難度三檔（簡單/普通/困難 = 每層 4/3/2 格、各 1 陷阱），倍數 `EDGE×(tiles/safe)^層`（1% 莊家優勢）。**開局即用 `HL.fair.float("towers")` 一層一 nonce 定陷阱＝逐層可驗證**，game-frame `PF` 表加 `towers:1` 亮 🔒；複用 `HL.instant` 餘額/金額欄 + 公版返回鈕（`view:"game"` via `gameFrame.wrap key:"towers"`）+ 中央掛鉤 `HL.liveStats.record`（餵 VIP/任務/返水/彩金/熱度通吃）。改 `index.html`（掛載）、`game-frame.js`（PF）、`styles/components.css`（ax-tower__*）、`core/i18n.js`（繁/簡預設原文 + 英譯靜態字串）、bump SW v5→v6。preview 實測（stub fair 控陷阱位）：medium 爬 3 層 mult=3.34× 精準＝EDGE×1.5³、payout 167 精準（bet 50 開局即扣、淨 +117）、踩陷阱不退款＋💥 揭露＋狀態紅＋兌現鈕鎖＋開始鈕復原、cur=0 兌現守門（餘額不變、回合續行）、🔒 公平鈕出現、8 列×24 格渲染、零 console error（screenshot 工具因頁面 ambient interval 忙碌逾時，改以 eval/console 驗證＝更強證據）。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#20 紅利/流水引擎（⬜待批准、L）缺口仍未解，bonus/cash 未分離記帳；#23 為 Original 不灌 bonus、未加劇。下一步：佇列頂端待做為 #20（待批准）/ #24 VIP 週期 Reload（已批准 M），evolve 下輪續挑頂端 🟦 純前端卡實作。

- **2026-06-27（進化 · 實作 #22 Rakeback 每日領桶 + 開卡 #30–#32）** — 缺口進化第五輪。**消化新調研**：leovegas.md / duelbits.md（上次 evolve 後 watchlist 新掃入、high_water 推進至 2026-06-27）。**實作佇列頂端純前端 🟦 卡 #22**（#20 流水引擎仍為 ⬜待批准 L 級架構改動、全自動下跳過）：`HL.rakeback` 由「永久累積 pot」升級為「每日返水桶」——`rbState` 改存 `{pot,lifetime,day}`，讀取時懶判定跨日：跨日未領的桶 forfeit pot（保留 lifetime），**舊資料無 `day` 欄位優雅遷移**（既有 pot 併為今日桶、不誤殺使用者既有返水）；新增 `rbMsToReset()`。**header 新增 💧 返水快領下拉**（可領額 + 每秒即時倒數 + 一鍵領入主餘額 + 返水明細入口 + 自管 outside-click/interval 清理），可領 ≥1 亮紅點 badge（接 `refreshChrome`，與 🔔 通知 badge 並列）。VIP 面板 Rakeback modal 同步改「今日可領 + 本桶倒數 + 逾期作廢」文案。改 `core/progress.js`（rakeback 區塊）、`layout/app-shell.js`（header widget + 下拉 + badge）、`styles/components.css`（下拉樣式）、i18n（繁/簡/英）、bump SW v4→v5。preview 實測：accrue 入今日桶（VIP×rate 精準）、claim 入主餘額(+80/+700 兩例)、day-roll pot 123.4→0 且 lifetime 500 保留、legacy 無 day 欄位 pot 77 不作廢且 day 補蓋、下拉開關/即時倒數(3h 8m)/badge/UI 領取/空狀態(暫無可領返水)全正確、EN 三條靜態字串（💧 Daily Rakeback / Expires in / Rakeback details）經 MutationObserver 翻譯到位、零 console error。**開卡（來源＝新調研 leovegas/duelbits）**：#30 PvP Dice Duel vs Bot（Duelbits 招牌、ApexWin 完全空白的 PvP 維度、差異化最大，複用 HL.instant+HL.fair+#15 bot）、#31 VIP 微等級進度條（LeoVegas 99-tier + Duelbits 多階共識，純加值 HL.vip 高頻推進感）、#32 新 Original Keno（Duelbits + 多平台共識，選號開獎類補可玩數）；皆純前端、複用既有引擎、已去重（Duelbits Hi-Lo 點子已被 #27 Hilo 涵蓋故未重開）。候補（受 3 張上限暫緩）：The Club VIP 門禁區、必須掉落限時 Jackpot、rakeback 週/月桶。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#20 紅利/流水引擎（⬜待批准、L）缺口仍未解；#22 雖是領「返水」而非灌 bonus，但派彩同樣直入主餘額，#20 仍待使用者批准。下一步：佇列頂端待做為 #20（待批准）/ #23 Towers（已批准 M），evolve 下輪將挑頂端 🟦 純前端卡實作。

- **2026-06-26（進化 · 實作 #21 遊戲熱度模組 + 開卡 #27–#29）** — 缺口進化第四輪。**無新調研檔**（8 份 dossier 皆 2026-06-26、high_water 已涵蓋），故本輪開卡來源為 shuffle/gamdom 上輪受 3 張上限暫緩、標「下輪優先」的候補項。**實作佇列頂端純前端 🟦 卡 #21 遊戲熱度模組 On Fire/Ice Cold + 當下最熱牆**（#20 流水引擎為 ⬜待批准 L 級架構改動、分析師已留給使用者批准，全自動下跳過）：新增 `core/heat.js`＝`HL.heat`，對遊戲登錄表 ambient 模擬（玩家數隨機漫步＋近期 RTP 漂移、自帶 4s interval 跨頁持續），由全站中央掛鉤 `HL.liveStats.record` 對真被玩的遊戲 fuzzy 比對加溫；卡片角標 🔥(RTP≥118)/🧊(RTP≤82)、娛樂城頂部「🔥 現在最多人玩」即時牆（依玩家數排序、本頁每 3s 刷新、換頁 ticker 自停）。**改 `live-stats.js`** central hook 加掛 record、**`casino.js`** 加角標＋頂部牆、i18n（繁/簡/英）、CSS、bump SW v3→v4。**抓到並修一個撞名 bug**：arena.js 既有 `.ax-heat`/`.ax-heat__bar`（房間挑戰者熱度條）會與新牆 class 衝突 → 全改用 `ax-trend*` 隔離。preview 實測：牆 8 格＋ 9🔥/10🧊 角標、大贏一局後該遊戲 RTP 93→130 轉火熱、ticker 數字每 3s 漂移、離開頁牆自清且 arena 5 條熱度條不受影響、三語標題正確、零 console error。**開卡（來源＝候補項）**：#27 新 Original Hilo 猜高低（Gamdom+Stake 共識，複用 HL.instant+HL.fair）、#28 新手限時啟用窗口（Gamdom 6h 倒數，沿用 #17 daily-gate）、#29 tier-up 大階獎金（Shuffle 雙層獎金，擴 HL.vip）；皆純前端、複用既有引擎、已去重。候補（受 3 張上限暫緩）：週賽距前名差距提示。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#20 流水引擎缺口仍未解（#21 不灌 bonus 故未加劇），但 #19 兌換碼/#24 Reload/#25 Rain 等 bonus 來源持續累積，#20 仍待使用者批准。下一步：佇列頂端待做為 #20（待批准）/ #22 Rakeback 每日領桶（已批准 S）；evolve 下輪將挑頂端 🟦 純前端卡（預計 #22）實作。

- **2026-06-26（進化 · 實作 #19 兌換碼 + 開卡 #24–#26）** — 缺口進化第三輪。**消化新調研**：shuffle.md / gamdom.md（上次 evolve 後新增的兩份 dossier，high_water 推進至 2026-06-26）。**實作佇列頂端 🟦 卡 #19 兌換碼 Redeem Code**：新增 `core/redeem.js`＝`HL.redeem`，內嵌碼表（5 組，可設到期日）、大小寫正規化、每碼每裝置限領一次（localStorage 冪等防雙倍）、派彩入獎金錢包 `HL.bonus`、modal 輸入框＋我的兌換紀錄、底部列新增 🎫 入口（共 8 顆）、CSS、i18n（繁/簡/英）；bump SW 快取 v2→v3。preview 實測：lowercase 命中、重領擋下、無效/空白/過期各回正確 reason、餘額增量精準(+100/+888/+1000)、UI 按鈕 handler 入帳＋顯示紀錄、EN 全譯、零 console error。**開卡（來源＝新調研 shuffle/gamdom）**：#24 VIP 週期 Reload 領取中心（Shuffle+Gamdom+Roobet/BC.Game 三方共識、ApexWin 缺週期 reload，最高 ROI）、#25 Chat Rain 聊天灑幣（Gamdom 招牌、把既有聊天死水通電）、#26 多倍數目標型挑戰（Shuffle、補任務「技巧型目標」維度）；皆純前端、複用既有引擎、已去重。候補（受 3 張上限暫緩）：Hilo Original、tier-up 獎金、新手限時窗口、週賽距前名差距提示。**對船長指令**：CONTROL 待處理區為空，無插隊。**注意**：#19 又是一個直接灌 `HL.bonus` 的來源 → #20 紅利/流水引擎（⬜待批准、L）缺口持續複利放大；#20 仍待使用者批准（L 級架構改動，全自動下未自行動工）。下一步：佇列頂端待做為 #20（待批准）/ #24（已批准），evolve 下輪將挑頂端 🟦 純前端卡實作。

- **2026-06-26（進化 · 收尾 #18 Raffle + 開卡 #21–#23）** — 缺口進化第二輪。**收尾 #18 每週抽獎**：新增 `core/raffle.js`＝`HL.raffle`，押注經中央掛鉤 `HL.liveStats.record` 每滿 NT$2,000 累積 1 張抽獎券，每週倒數＋逾期懶觸發自動開獎（沿用 #15 錦標賽冪等旗標＋單一 cycleEvent 路徑杜絕雙倍派彩，20 名階梯權重），中獎入獎金錢包 `HL.bonus`；底部列新增 🎟️ 入口（共 7 顆）、modal 顯示彩池/倒數/我的券數/預估機率/獎級/開獎紀錄、CSS、i18n（繁/簡/英）。bump SW 快取 v1→v2。preview 實測：5000 押注→2 券（carry 1000）、再 +1000→3 券；強制必中 payout=prize（30,000）精準入獎金錢包；重入/逾期多次觸發只派一次（冪等）；新期券數歸零；底部列點擊開 modal、倒數即時、en 顯示「🎟️ Weekly Raffle」；零 console error。**開卡（來源＝investigate 新增的 bet365/roobet/rollbit/1xbet 調研）**：#21 遊戲熱度模組 🔥/🧊+當下最熱（bet365+roobet 共識）、#22 Rakeback 每日領桶+快速領（rollbit+roobet 共識）、#23 新 Original Towers 爬塔（roobet+Stake 共識）；皆純前端、複用既有引擎、已去重。對船長指令：CONTROL 待處理區為空，無插隊。下一步：循環續跑；佇列頂端待做為 #19 兌換碼／#20 流水引擎（分析師建議 #20 先於 #19），evolve 下輪自動挑頂端純前端卡實作。

- **2026-06-26（每日分析 · 自我進化引擎高速產出巡檢 + #18 WIP 警示）** — 自當日 09:57 上一輪分析後大幅推進並巡檢確認：#15 錦標賽（`b0cccc8`/`977a663`）、#16 Provably Fair（`9708249`/`217df81`）、#17 Lucky Spin（`99d0463`）皆**已完成並提交**，core/ 已含 `tournament.js`/`fair.js`/`luckyspin.js` 且 index.html 皆掛載，BACKLOG/ROADMAP 勾選與程式**一致、無需修文件**。自我進化引擎（`intel/` + CONTROL 總開關 + 3 Skill + watchlist，Stake/BC.Game 已寫 dossier、next_due=2026-07-03）已上線跑首輪閉環。⚠️ **發現 `prototype/` 有未提交的 #18 Raffle 實作 WIP**：`core/raffle.js`（未追蹤 `??`）＋ `index.html`／`live-stats.js`／`app-shell.js`／`components.css`（已改未提交 `M`），另有未追蹤 `intel/platforms/bet365.md`（雷達剛掃 bet365）。依規範本日誌**只動文件、未碰亦未提交這些程式變更**——請使用者確認後由實作 Claude 收尾提交；**#18 在提交＋驗證前先維持 🟦，勿標 ✅**。**今日建議下一步：新增 #20 紅利/流水（wagering/rollover）引擎（L，純前端可做、使用者明確要求「引擎」），並建議排在 #19 之前先做**。理由：自我進化引擎正高速量產「派彩入獎金錢包 `HL.bonus`」的留存功能（#15 錦標賽／#17 Lucky Spin／#18 Raffle 實作中／#19 兌換碼待做），全部往 `HL.bonus` 灌錢卻**零流水控制**，bonus 來源每多一個缺口就複利放大；先做此共用引擎＝「共用引擎先於個別功能」加速器原則，之後所有 bonus 來源（含未來自動開卡）直接受流水規則約束、零逐功能改裝。替代快速項：🟢NOW 唯一剩的「分享單局戰績（Web Share API，S）」。

- **2026-06-26（🤖 自我進化引擎上線 · 首輪閉環 · #17 Lucky Spin）** — 建立並啟用自我進化引擎（`intel/` + 3 Skill + 3 本機 Routine + CONTROL 總開關，詳見 `intel/README.md`），完成第一個完整閉環：①**調研** Stake + BC.Game（寫 `intel/platforms/`、回填 watchlist 週期 next_due=2026-07-03）②**開卡** #17 每日 Lucky Spin、#18 週期抽獎 Raffle/Lottery、#19 兌換碼（皆為兩大頂級平台共識、ApexWin 皆缺、純前端可做）③**全自動實作 #17**：新增 `core/luckyspin.js`（`HL.luckyspin`，每 24h 免費轉、獎品依 VIP ×1~×3、中獎入獎金錢包 `HL.bonus`）+ 底部列入口 + 轉盤 CSS + i18n（繁/簡/英）。preview 實測：派彩=base×VIP 精準、每日閘鎖定、二轉擋下、轉盤落點與圖例高亮一致、零 console error。**順手補提交** #15 錦標賽遺漏的 `tournament.js`（core+view）script 掛載——功能已於 `b0cccc8` 完成，但 index.html 的註冊未提交（線上版實際載不到錦標賽），本次一併補上。下一步：循環自動續跑（investigate 每小時、evolve 每 2h），#18/#19 待後續自動實作；市場雷達每日 08:15 刷新清單。

- **2026-06-26（#15 錦標賽 / Slot Race）** — 完成分析師當日推薦的 #15。新增 `core/tournament.js`＝`HL.tournament`：限時積分賽（賽期內有效押注即積分，掛 `HL.liveStats.record` 中央點＝全遊戲＋跟注通吃）＋即時排行榜（mock bot＋真玩家）＋賽末依名次階梯（40/24/14/9/6/4/2/1%）自動派彩到獎金錢包 `HL.bonus`。新增 `views/tournament.js` 賽事頁。**電亮**寫死的「Slots 競賽 100 萬獎池」促銷卡（改 go:"tournament"）＋大廳橫幅入口；新增 `view:"tournament"` 路由。跑對標審查工作流（3 維度×驗證＝24 agents、18 findings）並修真問題：settle 加冪等旗標＋單一 `cycleEvent` 路徑（杜絕雙倍派彩）；移除「跨頁 4s setInterval」改觀看時 `viewTick` 推進 bot＋懶觸發逾期結算（免暴衝/空轉）；排行榜同分玩家優先；浮層暫停刷新；賽事頁全文進 i18n（繁/簡/英）。實測：押 30 萬→第 1 名、Demo 結算 40 萬恰一次入獎金錢包、非前 8 名派彩 0、跨 reload 持久、促銷卡/橫幅進場、en 翻譯、無 console error。**ROADMAP 🔵LATER 剩**：紅利/流水引擎、運動博彩、Crazy Time、營運後台。建議下一步：Crazy Time 類 Game Show（可掛主播主持、體驗強）或紅利/流水引擎（使用者明確要求）。

- **2026-06-26（#16 Provably Fair · 從 LATER 升級）** — 應使用者「看分析師任務接著執行」，從 🔵LATER 取 Provably Fair 實作（建構當下尚未見到當日 routine 已把 #15 錦標賽排入，故此項編 #16）。新增 `core/fair.js`＝`HL.fair`：同步 SHA-256+HMAC-SHA256（對標 WebCrypto 逐位元一致，含多 block／>64byte key／SHA256("")標準向量），種子模型 serverSeed 承諾雜湊→clientSeed→nonce，每注=HMAC(serverSeed, clientSeed:nonce) 前 4byte→[0,1)。Dice/Limbo/Plinko 改用 `HL.fair.float(game)`（Plinko 一注一 nonce、單 float 取 16 位元定各排）。通電 GameFrame 🔒／PiP ✓／底部「可驗證公平」。**跑對標審查工作流（4 維度×獨立驗證＝30 agents，23 findings）並修**：Plinko 補可驗證公平（原漏）、🔒/✓ 僅 PF 遊戲顯示、揭露「純前端 Demo 伺服器種子存本機」誠實聲明、setClientSeed 回傳成功、revealModal 改用 modal API、新增近期下注紀錄、驗證器加 Plinko 解讀+nonce 上限+i18n 標籤。實測：HMAC 對標一致、Dice 41.79／Limbo 7.28× 由種子完整重算、Plinko 一注一 nonce、🔒 僅 PF 遊戲、無 console error。建議下一步：回到分析師當日推薦 **#15 錦標賽 / Slot Race（M）**。

- **2026-06-26** — 巡檢：原始佇列 #1–14 與程式完全一致（core/ 18 檔含 instant/table/jackpot/notify/i18n 俱在；rakeback 內嵌於 live-stats.js+progress.js，非獨立檔但功能在；views/ 含 table-baccarat/table-roulette；prototype/ 有 manifest+sw.js+icon.svg）。`prototype/` 工作樹乾淨，無未提交程式。順手修正**文件與程式不一致**：ROADMAP 🟢NOW 的試玩/真錢·搜尋·i18n·通知中心仍標 `[ ]`、🟡NEXT 的 Rakeback·百家樂輪盤·Jackpot·PWA 仍標 `[ ]`，實際皆已完成 → 全部勾起並附 #編號/日期。**建議下一步：新任務 #15 錦標賽 / Slot Race（M）**——從 🔵LATER 升級。理由：原始佇列清空後，限時競賽是頂級平台（Stake 週賽/Roobet/BC.Game）的招牌留存引擎，是 LATER 中體驗完整度 ROI 最高者；且**雙重加速器命中**——(1) 已有裝飾用側欄排行榜＋一張寫死的「Slots 競賽 100 萬獎池」促銷卡承諾賽事卻無實作（電死招牌）；(2) 中央掛鉤 `HL.liveStats.record` 可直接餵積分、獎金錢包 `HL.rewards` 可直接派彩，零逐遊戲改裝。純前端零牌照。會動到：新增 `core/tournament.js`＋`views/tournament.js`、接促銷卡/大廳入口。替代快速項：🟢NOW 唯一剩的「分享單局戰績（Web Share API，S）」。

- **2026-06-23（補強·i18n 全站覆蓋）** — 使用者回報「換語系幾乎沒變化」。原 #13 只翻被 `t()` 包過的少數字串。改為**片語字典引擎**：以「畫面中文」為 key，DOM 自動翻譯層 walk 文字節點＋title/placeholder，`MutationObserver` 接住 Modal/Toast/換頁/聊天，`renderApp` 末同步 `apply()`（換頁即時翻不閃爍）；另加 prefix/suffix 比對處理「標籤＋動態值」串接（房主 X／加入 NT$X／3/4 玩家／% 挑戰者）。**擴充覆蓋＝字典加一條**。字典涵蓋側欄/header/底部列/錢包/帳號/大廳/競技場(房間卡·熱度條·賞金)/直播間/娛樂城/遊戲共用鈕/彩金·VIP·返水·通知標題；en 全譯、zh-Hans 補差異。預設 zh-Hant 零成本(observer 關)。preview 驗證四大頁全轉換、繁體可還原、简体正常、無 console error。殘留深層 Modal/遊戲內文/登入頁可續補(加 key 即可)。⚠️ 開發注意：PWA SW + 瀏覽器 HTTP 快取會讓改 i18n.js 後驗證拿到舊檔，需清 SW/caches 或 no-store 重載。

- **2026-06-23（#14 PWA · 佇列收尾）** — 完成 #14，**原始 14 項佇列全清**。新增 `manifest.webmanifest`（standalone、start_url `./?demo=1`、theme #131312、`icon.svg` any+maskable）、`icon.svg`（品牌 A 漸層）、`sw.js`（network-first + cache 後備：線上最新、離線回快取、導航離線退回 index.html；activate 清舊版、改版 bump CACHE）。index.html 補 manifest/theme-color/apple-touch-icon + 註冊 SW（僅 http(s)，file:// 略過維持可直接開啟）。驗證：manifest 解析、SW activated 並控制頁面、二次載入快取 56 檔＝離線可開、head 標籤齊全、無 console error。下一步建議：從 ROADMAP 🔵LATER 挑（紅利/流水引擎、運動博彩、Crazy Time、錦標賽、Provably Fair、營運後台），或回頭把 i18n 在地化覆蓋擴到大廳/競技場/直播間等其餘畫面（字典加 key 即可）。

- **2026-06-23（#12 + #13）** — 一次做兩項。**#12 搜尋排序+最近遊玩**：`games.js` launch 中央點記錄最近遊玩（localStorage，`HL.games.recent`），娛樂城預設頂部加 🕘 最近遊玩區；搜尋加 220ms 防抖；分類/搜尋結果牆加排序控制（推薦/熱門/最新/A-Z）。**#13 i18n 輕量引擎+語言切換器**：新增 `core/i18n.js`（`HL.i18n`，`t(key,def)→DICT[lang]||def`，預設 zh-Hant 免建字典），header 🌐 死按鈕通電＝切換器（繁中/簡中/English，寫 localStorage+設 `HL.lang`+`HL.app.refresh`）。已在地化 header/底部列/娛樂城 chrome；其餘畫面逐步擴充（字典加 key 即可）。驗證：#12 最近遊玩最新在前、A-Z 排序正確；#13 三語切換 chrome 文案正確、persist；無 console error。建議下一步：#14 PWA（manifest + Service Worker，M）＝佇列最後一項；或回頭把 i18n 覆蓋擴到大廳/競技場等其餘畫面。

- **2026-06-23（#10 + #11）** — 一次做兩項。**#10 通知中心**：新增 `core/notify.js`（`HL.notify`，localStorage 佇列），header 🔔 死按鈕通電——未讀紅點（取代寫死的 3）、點開為通知中心 Modal、開啟即標已讀清紅點、`renderApp` 後同步；由真實事件餵入（VIP 升級、累積彩金中獎）＋首載三則種子通知。**#11 遊戲卡雙鈕**：可玩卡新增「▶ 試玩／💵 真錢」（`casino.js`）——試玩直接啟動；真錢已核照直接玩，否則彈真金模式說明（提款待牌照、`canWithdraw()` 閘控），可切換真金模式或改用試玩。驗證：#10 紅點 3→清→+1、Modal 列表正確；#11 26 張可玩卡皆雙鈕、即將推出卡 0、真錢閘控 Modal（休閒/真金兩變體）、試玩啟動且公版返回鈕在；皆無 console error。建議下一步：#12 搜尋排序+最近遊玩(S)，或 #13 i18n 輕量引擎+語言切換器(接 🌐，目標3, S)。

- **2026-06-23（#9·累積彩金 Jackpot）** — 新增 `core/jackpot.js`＝三級漸進式彩池 `HL.jackpot`（MEGA 8M／MAJOR 80K／MINI 3K 起跳）。**遞增**：每秒 ambient 成長（`setInterval` 跨頁持續）＋每筆下注貢獻比例（mega 0.5%／major 0.3%／mini 0.2%）。**命中**：每筆下注各級依機率（mini 1/120、major 1/3000、mega 1/80000）觸發，命中即把當前池額真派到 `HL.money`、重置該池、播全螢幕中獎演出（彩帶＋金額 count-up）。掛在 `HL.liveStats.record` 中央點（全遊戲＋跟注通吃，與 VIP/任務/返水並列）。娛樂城頁頂新增即時跳動彩金橫幅、ℹ 規則/近期中獎/預覽演出。驗證：橫幅渲染、池額即時遞增、onBet 與中央點貢獻精確、forceHit 派彩=當前池額並重置 seed、演出浮層各元素正確、無 console error。可玩體驗再升一級。建議下一步：#10 通知中心（接 header 🔔 badge），或 #11 遊戲卡「試玩/真錢」雙鈕(S)。

- **2026-06-23（7d + #8）** — 一次做兩項。**7d**：全頁直播間 `liveroom.js` 跟注補齊到與 7c 一致（先前只有子母畫面接真桌，全頁仍假）——確認跟注真扣 `HL.money`、本局倒數結束以 `HL.baccarat.deal()` 真桌結算真派彩、掛 `HL.liveStats.record`、重複跟注被擋、開獎前離開退回本金。**#8 Rakeback 返水**：新增 `HL.rakeback`（綁 VIP 等級係數 0.5%→1.8%），每筆下注即時累積，掛在 `HL.liveStats.record` 中央點＝全遊戲+跟注通吃；VIP 俱樂部面板可查返水率/可領額並領到主餘額（取整數、餘數續留）。驗證：7d 強制莊勝+手動驅動 ticker 繞過分頁節流→扣100退195(淨+95)、真點數聊天、離開退回；#8 accrue 數學精確、經中央點累積、領取入餘額；皆無 console error。建議下一步：#9 真實累積彩金 Jackpot（demo 獎池遞增+命中演出），或 #11 遊戲卡「試玩/真錢」雙鈕(S)。

- **2026-06-23（7c·主播跟注接真開獎）** — 完成 #7 的 7c，#7 整段收尾。`streamer.js`（子母畫面 PiP 主播）的 `Math.random()<0.5` 假勝負→改用 **`HL.baccarat.deal()` 真桌真開牌**（7a 暴露 `HL.baccarat={deal,returnsOf}`），依主播押注方向(莊/閒)判勝負；跟注改為「**真扣**」`HL.money`、下一局以真桌結果「**真派**」（莊1.95×/閒2×/和退本/輸0），並掛 `HL.liveStats.record("跟注·百家樂")`（餵 VIP/任務）；取消/關閉/切大畫面/換主播皆退回未結算本金。200 局自動測：每局精準扣注、淨值僅{+19/0/-20}、零漏帳、分布 47/9/44% 符合百家樂、傭金正確。**發現同類缺口**：全頁直播間 `liveroom.js` 的跟注仍是假結算（toast + 結果硬寫「pickSide 勝」），已列為 **7d**（表面一致性、S）。建議下一步：7d 把全頁直播間也接真桌（與 7c 一致），或跳 #8 Rakeback 返水。

- **2026-06-23（7b·輪盤）** — 完成 #7 的 7b。新增 `views/table-roulette.js`＝歐式單零輪盤（0–36、莊家優勢 2.7%），注區**直接複用 7a 的 `HL.table.betArea`**（驗證了桌引擎的可複用性）：直注 35:1(36×)、紅黑/單雙/大小 1:1(2×)、打(12)/列 2:1(3×)、開 0 場外全輸。賭桌佈局（0+12×3 號碼+列注+打/場外）、CSS 旋轉輪盤+中心開號+近況路紙。覆蓋 mock 占位卡 id:"european-roulette"。**可玩 8 → 9 款**。3 局自動測 0 對帳誤差（涵蓋全輸/紅單列/直注 36× 命中）、中獎高亮與開號顏色正確、無 console error。建議下一步：**7c 主播跟注接真開獎**——把 `streamer.js` 的 `Math.random` 勝負改成「真桌結果」、跟注真扣/派 `HL.money`，完成 #7 整段（直播間最大死路通電）。

- **2026-06-23（7a·RNG 桌引擎+百家樂）** — 完成 #7 的 7a。新增 `core/table.js`＝共用桌遊引擎 `HL.table.betArea`（多注區：籌碼列、place/undo/clear/rebet 受餘額閘控、commit 扣注+快照、settle 依各注區總賠付倍數派彩+同步餘額+掛 `HL.liveStats.record` 餵 VIP/任務）——7b 輪盤可直接複用。新增 `views/table-baccarat.js`＝真開牌百家樂（標準補牌：閒0–5補、莊依閒第三張補牌表、天牌8/9停；閒1:1·莊1:1扣5%傭金=1.95×·和8:1=9×·閒對/莊對11:1=12×、和局退本），覆蓋 mock 占位卡 id:"baccarat"。**可玩 7 → 8 款**。實測抓到並修掉一個跨局 bug（每局結算後未清籌碼→累積）；15 局自動測 0 對帳誤差、莊家傭金/和局退本正確、undo/clear/重押皆正常、無 console error。建議下一步：**7b 輪盤**（歐式單零，複用 `HL.table`，工作量 S）。

- **2026-06-23（修補·公版返回鈕）** — 使用者回報：5 款 instant(Dice/Limbo/Crash/Mines/Plinko)無「返回娛樂城」鈕，只有暗影儀式/小雞有。根因：返回鈕原寫死在各遊戲自身 DOM。改為**公版**：shell 層 `mountView` 統一注入返回列，掛在遊戲節點「之外、之上」(不進 GameFrame)；`main.js` 以 `GAME_BACK={slot,chicken,game}` 宣告套用頁，instant/同仁自製遊戲走 `view:"game"` 自動繼承；拔掉 slot.js/chicken.js 舊手刻鈕。preview 實測 7 款各恰好 1 顆、為 main 首子元素、點擊回 casino、無 console error。`(b20de37)` 接著進 7a。

- **2026-06-23（優化）** — 五款 instant 由 MVP 重做到 Stake 類標配水準（對標規格後）：引擎 betPanel 支援「動畫結束才結算(res.done)」+ onBetChange + 共用 animate；Dice(紅綠軌道/可拖曳握把/大於小於/指針落點/三欄資訊/最近結果)、Limbo(滾動上升+脈動/抖動+三欄+歷史)、Crash(SVG 火箭曲線/爆炸/歷史/動態兌現/補接 liveStats)、Mines(資訊條/翻格/踩雷震盤/兌現金光/隨機鈕/補接 liveStats)、Plinko(逐排彈跳/8·12·16 排可選/程式生成倍數表)。動畫一律「單一 setTimeout 閘門保證結算 + 視覺盡力」以防背景分頁節流卡死。各款 demo 實測通過、無 console error。

- **2026-06-23** — 巡檢：佇列 #1–6 與程式一致（core/ 已有 instant/live-stats/rewards/progress/money，views/ 五天王俱在）。確認 #7 缺口屬實——`streamer.js:130` 仍用 `Math.random()<0.5` 演主播勝負，跟注只發 toast/聊天、**不動 HL.money 餘額**；`liveroom.js:123` 的 Math.random 僅作假聊天。建議下一步：**#7，但拆三段做**——先 7a「共用 RNG 桌引擎＋百家樂」(真開牌、HL.money 結算)，再 7b 輪盤複用結算，最後 7c 把主播跟注接真桌(取代 Math.random、真扣真派)。理由：先做共用桌引擎再做個別桌＝加速器原則，且把直播間最大死路(跟注不結算)通電，體驗完整度躍升、純前端零牌照依賴。工作量原 M、拆後每段 S。會動到：新增 views/table-baccarat.js·table-roulette.js(或合一 instant-tables.js)、改 streamer.js。⚠️ 另發現 `prototype/src/views/instant-crash-mines.js` 有未提交程式變更(+47/-11)，非本日誌任務、未碰未提交，請使用者自行確認。

- **2026-06-22（深夜）** — 完成 #6 留存三件套：VIP 等級(以 HL.liveStats.record 為中央點累積押注→5 段位、升級發獎金)、每日任務(下注/贏/押注/簽到，事件自動推進)、獎金錢包/領取中心(任務與升級獎金入此、可領到主餘額)。玩家頭像「Unranked」已換真段位；底部「每日任務/獎勵中心/VIP 俱樂部」三顆死按鈕通電。建議下一步：**#7 百家樂/輪盤 RNG 桌**，並把虛擬主播跟注接到真開獎(取代 liveroom 的 Math.random)。

- **2026-06-22（晚上）** — 完成 #5 Plinko，**originals 五天王(Dice/Limbo/Crash/Mines/Plinko)補滿**，可玩遊戲 6 → 7。遊戲擴充告一段落；建議轉做 **#6 留存三件套**（VIP 等級 + 任務/成就 + 獎金錢包），把玩家 widget「Unranked」換真段位、點亮升級動機，與每日簽到相乘。

- **2026-06-22（傍晚）** — 完成 #4 Crash + Mines（可玩遊戲 4 → 6 款）。Crash：倍數爬升+手動/自動兌現；Mines：5×5 翻格累乘+隨時兌現，皆 1% 莊家優勢、掛 HL.instant。建議下一步：**#5 Plinko**（落球動畫 + Low/Med/High 風險 + 倍數槽），補滿 originals 五天王（可玩 6 → 7）。

- **2026-06-22（下午）** — 完成 #2 統一 instant 引擎 `HL.instant` + #3 Dice/Limbo（可玩遊戲 2 → 4 款，含手動/自動下注、1% 莊家優勢）。建議下一步：**#4 Crash + Mines**——複用 chicken.js（Crash 家族範本：倍數/cashout）與 bounty.js（Mines 後端雛形）狀態機，掛上 HL.instant，可玩數可再 4 → 6。

- **2026-06-22** — 完成 Wave 1（收藏 + 每日簽到 + 死按鈕通電）。建議下一步：**統一 instant 引擎 `HL.instant`**（佇列 #2）。理由：先磨刀，之後 Dice/Limbo/Crash/Mines/Plinko 五款的工作量從 M 降到 S，是所有 originals 的加速器；且 autobet 是 instant 類標準配備（目前僅 slot 固定 auto=10）。

- **2026-07-27（平台軌 · 建置輪 · 實作 #48 限時損失保險/新手安全網 `HL.safetynet`）** — 閘門全過（loop/platform/auto_implement on）。**選路**：同日 09:46 firing 已做研究輪（讓路 games、開 #48）、10:00 games 已出第 8 款保真遊戲 Gem Storm、12:xx maintain 已補 U29 i18n——`lead_track: games` 的讓路目的（讓 games 取得當日建置）**已達成**；platform 自 07-24 起連 3 日未實作 code，#48 已 🟦已批准待做（container-first 純前端），故本輪不再做第 2 個研究輪（tier-1 今晨剛刷、next_due 08-03＝再刷屬空心跳），改**實作已批准的 #48**（SKILL 線 17「可讓路」為軟提示、非硬禁；符合拒絕閒置逃生閥）。進場 build_lock=false→claim-token `p-142618-b8a9`→停頓做實作前源碼讀取（ledger.js/progress.js HL.bonus/cashback.js #33/season.js 容器範式/live-stats 中央點/app-shell hub/index.html 序/site-mode ns）→重讀確認 token 仍在＝claim 成功無並行 firing（E7 TOCTOU 未觸發）→收尾清回 false。**實作 `HL.safetynet`**（`core/safetynet.js`）＝**config 驅動促銷 campaign 模板**（擴充性優先＝容器先於內容：一份 `CAMPAIGN` 設定＝可換排程、`windowDays/rate/cap/enabled` 皆 config、`setCampaign` 出口、`cid` 不符即重置窗口；未來「週末損失保險」等變體換一份 CAMPAIGN 不動引擎）。**淨損源＝營運帳本 `HL.ledger`**（`liveStats.record` 為唯一 bet/win 入口＝記的即玩家真實下注）：enrollment snapshot ledger totals，窗口淨損＝max(0, Δbet−Δwin)（贏局自然抵銷）；窗口內每日冪等退還 `min(cap, 淨損×rate)−已退`、逾窗最後結清，一律**零流水**入 `HL.bonus`（`{wagerFree:true, source:"新手安全網"}`＝比照 #33、授予當下自動入帳本）。**站別感知（§11）**：假站 rate 20%/cap 5000（慷慨展示）、真站 rate 10%/cap 500（保守，須小於莊優不侵蝕利潤）；站別命名空間隔離（demo/live 平行宇宙，比照 cashback/ledger）。**懶觸發**（比照 raffle/guild 週期）：boot/status()/open() 各跑 tick() 結算，無常駐計時器。入口掛福利中心 hub「獎金回饋」（🛡️ 新手安全網，sub 顯示待退/保障剩餘天數）。改 `safetynet.js`(新)/`index.html`(cashback.js 後一行)/`app-shell.js`(hub 一項)/`sw.js`(v101→v102)；**未改 live-stats.js**（讀 ledger 快照 delta＝零 hot-path 侵入，降低跨軌檔衝突）。**驗證**：node e2e 20/20 PASS（demo/live config、enrollment snapshot、day0 同日退、每日冪等不重付、跨日退累積 pending、贏局抵銷且已退不追回、cap 5000 封頂、逾窗結清+done+不超付）；in-app（自起 :8449、清 SW v101+caches；§9 截圖逾時→JS eval）零 console error＋API 齊全＋boot tick 不 throw＋config 正確＋**e2e 實測** `liveStats.record('dice',4000,1000)`→窗口淨損 3000→自動退 600(20%)、ledger bySource「新手安全網」=600 精準、pending 0、面板 5 kv 正常建構。**擴充性**：`extensibility_patterns`「config 驅動 campaign 模板」+「命名空間隔離持久化」+「站別感知閘門 isLive()」+「訂閱者模式（讀中央帳本）」四落地；與 #33 cashback（每週桶/VIP 分級/按鈕領）互補不重疊（本作限時窗口/每日自動退/零流水）。**只 add 自己檔**（safetynet.js/index.html/app-shell.js/sw.js + intel/db/platform-modules.json/STATE.json/CONTROL.md/loop-journal.md/BACKLOG.md/BACKLOG-archive.md），未碰 Game assets 雜訊/他軌未提交檔。**下一步**：i18n 覆蓋（maintain 軌後續，比照 guild/season）、成本加權 XP（BC.Game『BC Engine』缺口，台帳已記）、Guild 深化、tier-2/3 平台逾期刷新。

- **2026-08-20（平台軌 · **08:00 窗**建置輪 · 實作 #111 首屏依賴分析器〔**首版跑得出漂亮報表，但答案是錯的——而兩個錯都不是筆誤，是模型錯**〕· 台帳審「後台」7 模組〔**更正台帳自己的採用度指標**〕· 開卡 #112 · 不取材〔ACTIVE overdue 0/35、最近到期 08-24〕）**
    - **實作 #111**（S–M·選它而非 #109 的理由＝前手交棒明列「純 node 工具·headless 最佳」，而 #109 要新增產品面出口、在無 preview 的輪不宜）：新增 `intel/tools/first-screen-deps.js`（分析器＋CLI `--scope/--file/--json/--verify`）＋常駐鎖 `platform/lazy-list-not-first-screen-bound`。**一支 `prototype/` 產品檔未動、零 `<script>` 變動、sw 不 bump**（`sw.js` 對 `tests/` 零命中，`intel/` 本就不被服務）。
    - ⭐ **本輪最重要的一條＝新量測工具的第一版必須先對「已知答案的樣本」校準，而那個樣本是免費的**。首版把 9 支中 **7 支**判成 `first-screen-bound`（含 casino/slot/chicken/tournament）——若當成判準就等於宣告首屏再也省不了。兩個錯都是模型錯：① 把 router 登錄表 `VIEWS = { slot: { render: function(){ return HL.views.slot.render(); } } }` 的**路由閉包**當成首屏引用；② 裸括號計數被註解與字串裡的大括號騙到，`main.js:16` 那個頂層 ticker 回呼「永遠沒閉合」⇒ **整個 main.js 的引用都被標成「setInterval 回呼內」**。**決定性反證就在手邊**：#110 已成功延遲的 globe/liveroom/bounty/vsslot **引用點正是那張登錄表**，它們在線上活著沒白屏 ⇒ 判它們 bound 的是工具不是它們。⇒ 修法＝新增 R 類（非預設路由的閉包不算首屏，預設路由自 `VIEWS[view] || VIEWS.lobby` 讀出）＋ `maskSrc()` 遮罩字串/註解（長度不變故索引仍對得回原檔），並把「已延遲 24 支必須全非 bound」做成 `--verify` 與常駐鎖第一段。**副效果**：本工具**結構上不可能**把註解算成引用——正好解掉台帳連四輪記載的「提及 vs 出口」陷阱。
    - **校準與對帳**：`--verify` 對延遲清單 **24/24 皆非 bound**（21 safe + 3 needs-methods）且 24/24 真的被分析到（非 null-skip 假綠）；對 arena 的三筆判定與 #110 的人工結論**逐位相符**（A `lobby.js:110` 無守衛／B `main.js:131` 開機 setInterval **有守衛也算**／C `main.js:66` 事件驅動不阻擋）。**產出的答案**：⛔ arena 41.8／lobby 10.5／auth-view 4.2 必須留；✅ slot 42.1／chicken 21.9／casino 13.1／tournament 8.9 safe；🟡 game-frame 11.2 `[wrap,resumeFrame]`／fgboard 6.2 `[create]` ⇒ **可回收 103.4KB**，已開 **#112** 承接（並更正模組 46 `stowable_note` 08-19 的候選清單：fgboard 需 methods stub、且漏了 casino）。
    - **台帳審「後台」7 模組**（`last_audited` 08-17＝與「金流」並列最舊，依 journal 接續取「後台」）→ 全數回填 08-20、**7 個 status 全維持**（partial 5／absent 2）。零漂移四筆：儀表板唯讀性 `onClick|onInput|onChange` 恰 **2 處**（連五輪逐位相同＝它仍是報表不是後台）、`HL.econCfg` 在該檔 **13** 次（08-17 釘死口徑後首次可跨輪比較、零漂移）、`mock-data.js` 模組級 `var` **26**（連五輪）、admin/cms/backoffice view **0 命中**（連五輪）、`jurisdiction` 外部讀者 **0**（連四輪·依 08-17 自訂紀律改一行帶過不再重述全文）。
    - ⭐ **台帳更正一筆，而且錯的是台帳自己的採用度指標**：08-17 記 promoCal「4 個外部註冊者」含 `responsible.js`——但 `grep -c 'HL.promoCal.register(' src/core/responsible.js` ＝ **0**，該檔兩處命中（:12／:20）**都在檔頭註解裡**（正是把 promoCal 當成「建了容器沒人用」的舉例）。⇒ 真實歷史值 08-17＝**3**（progress-src 08-07／rakeboost 08-04／release 08-05，`git log -S` 可查），今天＝**5**（新增 challenges 08-18 #57／referral 08-18 #58）＝**真實成長 3→5**。**這是「註解提及被當成出口」家族的第五筆，而這次發生在台帳用來證明容器非孤兒的那個數字上** ⇒ 口徑自本輪釘死＝`grep -rl 'HL.promoCal.register(' src`（排除定義處、只認呼叫）。同法複驗經濟旋鈕表 **12→14**（referral 08-18／activity 08-19），兩筆皆落地當輪自己註冊、無人回頭補＝#90 反向覆蓋鎖的預期效果連三輪自動生效。
    - **驗證**：`node prototype/tests/run.js` **191 → 192 全綠**；**負向擾動 2/2 全被抓**（① arena 塞進 lazy 清單 → 3 項紅、訊息直指 `A lobby.js:110`；② 把分析器符號正則改成永不命中＝模擬「壞掉但沒人發現」→ arena 反向錨紅、訊息明說「要嘛依賴真被拆了請一併改寫本測項，要嘛分析器壞了正在把所有檔都判 safe」），各跑完立即還原、還原後全綠。`ledger-card-sweep` 進場與收尾各跑一次皆 **⚠️ 待人工確認 0 筆**。
    - ⚠️ **驗證誠實聲明（連續第十六輪）**：排程輪不得啟動 dev server ⇒ 無 preview。**但本輪與前十五輪不同**：改動全在 `intel/tools/` 與 `prototype/tests/`（皆非被服務的資產）、零產品檔、零 `<script>`、sw 不 bump ⇒ **本輪沒有瀏覽器可觀測面**，屬「無可驗之物」而非「該驗沒驗」。#110 的 5 個入口目視仍是欠款，且 #112 會再加 6 支 ⇒ 已在 #112 卡上寫明「建議排在有可靠 preview 的輪，不宜再累積未目視的遷移」。
    - **不取材**：`db/platforms.json` ACTIVE overdue **0/35**、最近到期 bet365／rollbit **08-24** ⇒ 本輪不深挖（非閒置：有真實作＋真台帳審計＋一筆台帳更正＋兩條鎖，`consecutive_idle_rounds` 維持 0）。
    - **下輪（08-20 14:00）**：台帳依輪替序＝**`金流`（5 模組·08-17＝現存最舊）**；實作候選 **#109**（報表/匯出註冊表·headless 可落地）；**#112**／**#107**／**#93** 建議排 preview 輪（#112 含「遊戲→返回→遊戲牆」這條大量玩家會踩的返回路徑）。


- **2026-08-23（平台軌 · **08:00 窗**建置輪 · 實作 **#119** i18n 覆蓋棘輪閘〔**一條鎖第一次對「還沒寫的表面」生效——代價是先把三個會讓它『修一半而看起來正常』的陷阱拆掉**〕· 台帳審「前端UI/UX」4 模組 ＋ **sweep 回填「風控/反詐欺」** · 開卡 **#120** · **不取材**〔platforms.json 逾期 0/36、最近到期 08-24〕）** — 閘門全過（loop／platform／auto_implement 皆 on），進場 `build_lock` **false 乾淨**（維護軌 08-23 00:00 窗已釋放、未奪鎖）→ claim `p-081215-5a4c`（帶心跳 08:12→08:58→收尾，**claim 當下就 commit** `d09b4c9`）→ 停頓做卡片/源碼閱讀 → 重讀確認 token 仍在＝claim 成功 → 收尾清回 false。dark **11.0h**（08-22T21:10 → 本輪 08:12）未達 `catchup_if_dark_hours`；`lead_track=games` 准讓路，但**前手 20:00 窗已把「台帳輪替＝`前端UI/UX`」與「實作候選 #119（推薦）」指好、且本輪有真產出** ⇒ **做而不讓路（跨輪指派連續第十八輪）**。照 ENG-1 處方**分段提交**（補譯 `22f2a5e`／掃描器與鎖 `bc3dabc`／intel）。
    - **實作 #119（三件套，且刻意只有一把尺）**：`prototype/tests/i18n-key-scan.js`（狀態機掃原始碼，**只認呼叫、不認提及**）＋常駐鎖 `platform/i18n-key-ratchet`（逐檔 + 總量棘輪，基線 **0＝全站零容忍**、不在基線表的檔一律 0 ⇒ **新檔天生在射程內**）＋`intel/tools/i18n-key-gaps.js`（逐檔缺漏排行）。鎖與報告 **require 同一支掃描器**——本專案被「同一把尺抄成兩份然後 drift」咬過太多次（T26／T28／#94 側表 rtp）。
    - **首測 15 條真缺漏、當輪補完 ⇒ 基線壓到 0**：EN 2 + zh-Hans 13，其中 **11 條是兌換碼面板 `core/redeem.js` 整片 zh-Hans 零覆蓋**（切簡中整個面板露繁中，而 node 全綠、畫面在繁中下完全正常＝#109 報表中心那個病灶的同型第 8 例）。
    - ⭐ **本輪最重要的一條：這條鎖有三個『修一半而看起來正常』的入口，三個都踩到才拆掉**（全寫進掃描器檔頭，見 CLAUDE.md §4 該家族）：① **key 沒 trim** ⇒ `t("已領取 ✓ ")` 這種帶尾空白的鍵補進字典**永遠查不到**（真正查表的是 DOM walker 的 `nodeValue.trim()`，`HL.i18n.t` 只是 passthrough），首版因此多報 4 條「補了也不生效」的假缺漏；② **串接判定只看左右緊鄰的 `+`** ⇒ 漏掉 `x + (cond ? t("…") : "")` 這種**藏在三元裡的串接**，改成「逐層往外走、每層先用該層的逗號切段」後 N/A 從 106 升到 146（那 40 條否則會永遠卡在基線裡）；③ **zh-Hans 需求用天真逐字對齊反推** ⇒ 「評估視窗內累積經驗→评估窗口内累积经验」等長但換的是**詞序**，學到 `窗→口` 假映射、把「保障窗口」誤判成缺漏，改成**只採一致映射**後 27 條假缺漏收斂成 13 條真缺漏。**三條的共同形狀：功能看起來正常，只有寫測項去打自己才會發現。**
    - **負向擾動 7/7 CAUGHT**（每條先 assert mutated≠orig 排除 no-op 假綠）：M1 未翻譯整節點鍵／M2 刪必要 zh-Hans／M2b 刪必要 EN／M3 掃描器抽不到呼叫點／M4 語言包讀不到內容／M5 串接判定失效／M6 基線表殘骸。⚠️ **M1 首版是打錯目標的擾動**：用 `+ t("新片語")` 注入時鎖沒紅——**因為串接規則正確地判它 N/A**。改成「把既有非串接鍵換成字典裡沒有的片語」才真的在打棘輪。**擾動打錯目標時，紅不紅都證明不了鎖。**
    - **台帳審「前端UI/UX」4 模組**（08-19 起最舊）：大廳/遊戲牆 **present**（`casinoGames` 51 筆〔playable 2〕、分佈六類逐位同 08-19＝**連五輪零漂移**；唯一變動＝`lazyGames` MANIFEST **19→20**，是遊戲軌 08-21 上架 Moles 進了延遲清單＝容器**該有**的行為，記下以免下輪誤判漂移）／導覽殼層 **present**（`SIDE` **5** 筆無 `enabled`/`order`、抽屜四構件 2/2/9/6、`registerNav`/`navRegistry`/`HL.nav` 全站 **0/0/0** ⇒ **#93 結論連四輪未腐爛**）／遊戲客戶端框架 **present**（六顆工具全在、`isPF` 去註解 2 處＝**連六輪**）／多語系 i18n **partial**（見下）。
    - ⭐ **i18n 模組的 partial「理由被換掉一半」**（同 08-22「資安/權限角色」那種回填形狀）：`t()` **宣告面**從今天起有機械保證（棘輪 0），但**沒走 `t()`、直接把中文餵進 DOM 的那一大面仍無任何閘**——順手量了 `text:`／`textContent=`／`placeholder:` 三種形狀去註解後共 **615 條**，其中 **225 條（36.6%）在 en.js 沒有條目**（`views/bounty.js`／`global-prize.js`／`vsslot.js` 各 18 最多）⇒ **開卡 #120**（把射程從宣告面擴到 DOM 面），並把兩個會灌大分母的陷阱（`title:` 混進測項標題、PREFIX/SUFFIX 表也算覆蓋）**先抄進卡**。舊粗尺 `intel/tools/i18n-coverage.js` 的定位同時收斂：只排序、**不當閘**。
    - **sweep 回填「風控/反詐欺」**（`ledger-card-sweep.js` 本輪告警 1 筆、第 3 次真陽性）：evidence 末尾的「已據此開卡 #117」在 #117 落地後就到期 ⇒ 回填「**status 維持 weak，一個位元組都不改**」並寫清理由——#117 給的是**述詞容器**，本項要的是**伺服器權威 + 異常偵測**，兩者不重疊；反詐欺訊號全 src 實測仍 **0 命中**（連七輪）。回填後 sweep 告警歸 **0**。
    - **驗證**：node **258→259 全綠**（+1 常駐鎖）；sw v212→v213。⚠️ **驗證誠實聲明（連續第二十一輪）**：排程輪不得啟動 dev server ⇒ **UNVERIFIED**＝兌換碼面板切簡中後的**視覺**（是否有斷行/擠壓）；字典鍵覆蓋與棘輪行為已由常駐鎖 + 7 條擾動機械證明。
    - **下輪（平台 08-23 20:00）**：台帳輪替＝**`後台`**（依 8 分類序，`前端UI/UX` 已審完）。實作候選 **#120**（本輪新開·M·純工具+棘輪·**headless 完全可驗**·推薦，且阻塞事實已抄齊）或 **#93**（仍需 preview 逐一目視三個導覽表面 ⇒ **仍不宜排程輪吞**）；**#118 必須排可靠 preview 輪**。**#116 仍不建議**（票權尺無權威來源、需設計決策）。
    - **未碰**：`prototype/games/registry.json`（M）與 `prototype/games/slot-engine/`（untracked）——mtime 皆 08-03，非本輪工作，依 §7 一個位元組沒碰。

<!-- 2026-08-27 平台軌·08:00 窗自 BACKLOG.md 分析師日誌輪替下來 -->
- **2026-08-26（平台軌 · **08:00 窗** · 建置輪＝實作 **#129**〔i18n 第五面：fallback 面〕· 台帳審「**後台**」7 模組＋**新增第 8 個模組** · 取材＝**換取材對象（B2B 平台供應商）** · 開卡 **#131** · commit `144c7c5` 等）**
    - **① 實作 #129（第五面棘輪）**：`t("nav.menu", "主選單")` 這種寫法**同時逃出前面四面**——呼叫面（#119）要第一引數含 CJK，而 `nav.menu` 純 ASCII；
      DOM 面（#120）／資料面（#121/#126）／屬性面（#122）都要值是**引號字面量**，而這裡 `title: t(...)` 是**呼叫**。⇒ 同一件事的第五種寫法、P3 紀律第 9 例。
      實測 **42 呼叫點／34 鍵／10 個掛在屬性鍵上**，**缺漏 0**（35 條 fallback 逐條回查字典全數命中）⇒ 立鎖成本恰為零。新鎖 `platform/i18n-fallback-ratchet`，node **278 → 279 全綠**。
    - **② 這一輪最值得記的不是那條鎖，是「量錯對象就會量到一個永遠補不完的空集合」**：直覺會去量那 37 個點分 key 的字典覆蓋，
      但 `core/i18n.js` 的 `t(key, def)` 是 **passthrough**（回傳 `def`），真正查表的是 DOM walker／`tAttrs` ⇒ 需要字典條目的是**第二引數**。
      那 37 個 key 在 `en.js`／`zh-Hans.js` **零命中**、本來就不是字典鍵——量它會得到「37 條全缺」這個看起來很嚴重、實際上完全錯位的數字。
    - **③ 負向擾動 13 例、12 CAUGHT，其中一例打空且據實記載為失敗**：`P3 右邊界失效` 拿掉後**沒有任何鎖轉紅**，
      因為它與 `src[p] === "("` 重複；真正擋住 `tt(` 的是**左**邊界（改打那裡即 CAUGHT）。⇒ 斷言訊息已改寫指名左邊界。
      另一條差點打空的是**位置分流**（屬性走 `coversExact`／文字節點走 `covers`）——真實語料 `strictDelta = 0` 使它成為 no-op ⇒ 抽成具名 `fbCovers()` 外露、由探針直接站崗，雙向擾動才打得到。
    - **④ 台帳審「後台」7 模組：全數零漂移，而那正是問題本身**。七項釘死的口徑逐位相同（唯讀性 2 處〔連七輪〕、`HL.econCfg` 13、`mock-data` var 24、
      MANIFEST 20、registry.json HEAD 1 筆、admin view 0〔連七輪〕、promoCal 註冊者 7、RTP ids 18、`jurisdiction` 外部讀者 0〔連六輪〕）。
      ⇒ **連續三輪只能複述同一組數字**，不是因為沒事做，是因為**既有七個取材維度沒有任何一條會問後台的事**（它在任何 casino 的前台都看不到）。
    - **⑤ 因此本輪換的是「取材對象」而不是「取材來源」**（08-24 那條教訓的延伸）：玩家面維度問「營運商做了什麼給玩家看」⇒ 一手來源是官方 Help Center；
      **營運面分類（後台／資料／擴充性）問「營運商自己用什麼」⇒ 一手來源是 B2B 平台供應商的產品頁與版本發布**。
      這一步立刻命中：SOFTSWISS **Motion**（2026-04，官方發布把 trigger–condition–action 講到欄位級）＋ GR8 **Journey Builder**（同型第二訊號）
      ⇒ 台帳新增全庫首見模組「**營運自動化規則 Ops Automation Rules**」（absent）＋開卡 **#131**。
    - **⑥ #131 為何是 M 不是 L**：三段裡有兩段**已經就位**——`when` 有 `HL.release.AUDIENCES`（#107 單一真相＋常駐鎖）、`then` 有 `bonus.add`／`notify.add`／`ledger.record`。
      缺的只有把三段接起來的規則物件。但它**卡在 #118**（新 core 檔＝新首屏 script，餘裕 3 bytes），故標 ⬜待批准並在卡上寫明「#118 前只能推 `tests/` 側的形狀盤點」。
    - **⑦ leovegas 到期票淨新 0 條，據實記載不硬湊**：兩次搜尋的可用訊號只有 app UI 改版（無可對照形制）與 2026-04 體育投注擴充（落在 `avoid`）；
      責任博弈細節逐項比對後**全數已由 #67/#70/#86/#96 覆蓋**（第三次確認）。連兩輪邊際產出下降 ⇒ dossier 上建議下輪仍為 0 時把 `refresh_interval_days` 由 14 拉到 21。
    - **[P-FS] 逐位未動**：1638397／1638400 bytes、91 支 script，與進場時**逐位相同**（本輪只碰 `tests/` 與 `intel/`，一支首屏 src 檔都沒碰）。**sw 刻意不 bump**（`tests/` 不在 PRECACHE、永不送到使用者機器）。


<!-- 由平台軌 2026-08-29 08:00 窗自 BACKLOG.md 輪出 -->
- **2026-08-27（平台軌 · **20:00 窗** · 建置輪＝立**常駐鎖 `platform/central-hook-fanout-roster`** · 台帳審「**資料**」3 模組＋**新增全庫首見模組「本機存檔台帳與保留策略」** · 開卡 **#138** · claim `p-201230-0713` · **首屏零位元組** · `sw.js` 不 bump）**
    - **① 本輪最重要的一件事：全庫最吃重的一條資料流，headless 覆蓋率是 0——而且「有一個測項在守」與「沒有任何東西在守」在 CI 輸出上完全同形。** CLAUDE.md §4 開宗明義那句「`HL.liveStats.record` ＝全遊戲結算的中央點，任何依玩家行為觸發的留存/任務/成就/返水掛這裡即全遊戲通吃」，兌現方式是 `core/live-stats.js` 的 `record()` 裡**一行一個下游、20 個消費者 22 次呼叫**；本輪實測其中 **14 支 API 全庫（去註解去字串）只有這一個呼叫點**。⇒ **注單、成就、返水、JP、活躍度、負責任博弈…每個子系統與全部 25 款遊戲＋主播跟注之間，只隔著一行。**
      而唯一自稱在守它的 `core/selftest.js:212 core/central-hook` 有**兩個獨立的失效**：① 它是 **`env: "browser"`** ⇒ `node prototype/tests/run.js`（引擎每輪唯一跑得動的閘）**從來沒有執行過它**——env 過濾把它整個排除，連「略過 N 項」都不顯示（實測 `共 288 項｜略過 0`）；② 就算跑了也擋不住要擋的事——它只斷言「`HL.betlog.record` 是個 function」，**沒有斷言 `record()` 真的去呼叫它**，刪掉那一行它照樣全綠。`checks-platform.js` 既有的涵蓋是 **3/20**（#114 徽章輪的副產品）。
      ⇒ **§4「修一半而看不出來」的教科書形狀**：刪掉任一行，注單中心照開、報表照匯出、徽章牆照顯示（都是舊資料），**畫面上沒有任何一處會變**。
    - **② 立鎖（八條紀律·以 stub global 實跑真檔）**：`platform/central-hook-fanout-roster`（`env: node`）把下游全換成間諜函式後**真的呼叫一次 `record()`**，看誰真的被叫到、拿到什麼參數（掃字串擋不住 ①② 那種形狀）。(a) 實跑真檔反向錨／(b) 被驗的檔＝被出貨的檔／(c) **名冊雙向等式**（少一支＝靜默斷線、多一支＝名冊過期）／(d) 集合大小自檢 ≥18／(e) **押注側零白送**（`bet<=0` 時 14 支押注側一支都不許動，反向錨＝但 win 任務仍須 bump、純派彩仍須進注單與帳本）／(f) **兩把尺不互相冒充**（進度吃 #50 邊際加權值 50、任務吃原始流水 100、活躍度兩把尺同時收到）／(g) **退化路徑不得偷改經濟**（`progressSrc` 缺席時 vip/season 須收到同一個加權值）／(h) **record-once**（14 支呼叫點恰 1）。
      **node 288→289 全綠·負向擾動 7/7 CAUGHT**，每例**恰 1 支測項轉紅且皆為本鎖**（P7 首版誤判：注入點選在 eager 的 `rewards.js` 連帶撞破 63 bytes 首屏預算 ⇒ 改注入 lazy view 後乾淨單紅）。
    - **③ 一個新的 grep 陷阱變體（值得後手知道）**：`HL.betlog.record(` 的 naive grep 會多命中 `core/reports.js:593` 的 `src: "HL.betlog.record(game, bet, win)"`——那是 #109 事件 schema 的**字串描述子**、不是呼叫點。本庫「grep 命中 ≠ 功能/呼叫點」前四例（可驗證公平的「驗證器」／`title` 一詞兩義／`role` 全是 aria／`fingerprint()`）都是**註解或同詞不同義**，**這是第一例落在字串值裡的** ⇒ 本鎖的比對式因此連 `""`／`''`／`` `` `` 一併剝除，不只剝註解。
    - **④ 台帳（資料 3 模組全審＋1 新增）**：報表與匯出 present（零漂移·`reports.js` 737 行·四鎖全綠；**補記一件相鄰事實**——那四把鎖驗的全是「拿到 rows 之後」的形狀，若中央結算點停止餵資料，它們會拿著一份空的/凍結的 rows **全綠通過**）／資料分析 partial（缺口敘述不變＝仍缺跨裝置事件流·需後端·依 08-16 判定刻意不做不開卡）／注單 present（`betlog.js` 265→277 行＝#109 遷移時的註解、契約零變動；`CAP=500` 與單一寫入點仍在）。**新增「本機存檔台帳與保留策略」absent**。
    - **⑤ 開卡 #138（⬜待批准·卡在 #118）**：`prototype/src` 有 **42 個 storage key 常數**（散在 25+ 個 core 檔），而 `HL.storage|清除資料|resetAll|localStorage.clear` **0 命中** ⇒ 玩家看不到也清不掉自己這台裝置上的東西，且**只有 2 個 key 有上界**（betlog CAP=500／activity 90 天），其餘 **40 個無限成長**。**盲點成因與 14:00 窗「出金安全鎖」同型（分類交界·第 5 例）**、且很可能一直被 **`CONTROL.avoid` 外溢**蓋住（資料可攜聽起來像 GDPR·第三例）——本卡刻意不做合規承諾。**能問出這一格是 14:00 窗那把 `site-ns-storage-allowlist` 的直接紅利**：在「存檔出口唯一」有鎖之前，任何清冊都不可能宣稱完整。
    - **⑥ [P-FS]／sw／counters**：本輪只動 `prototype/tests/checks-platform.js`（不在 PRECACHE、不出貨），`prototype/index.html`／首屏 `src/`／`sw.js` **三者逐位未動** ⇒ 首屏**結構上不可能**改變（**第十一個「逐位未動」數據點**·餘裕維持 **63 bytes**）。`sw.js` **刻意不 bump**（無任何會送到瀏覽器的檔改動）。`platform_cards_opened` +1（#138）；`platforms_researched` **+0**（本輪無深挖對象——`platforms.json` 逾期 **0/36**，最早 next_due 08-29 toshi-bet ⇒ 不虛報）；`platform_cards_implemented` **+0**（產出是常駐鎖與情報，非卡落地）；`consecutive_idle_rounds` 維持 **0**。#118 閘上積壓 **15→16 件**。
